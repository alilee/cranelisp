# Sprint 102: Increment I (read path) opens + the S101 defect/T1 wave

**Status**: PHASE 5 LANGUAGE (ACTIVE) — **PUSH-THROUGH decided (user, 2026-07-04: "continue with the original scope aspiration, no reason to stop")**. Close-short seam NOT taken; full increment-I read path delivered this sprint. Remaining ladder: Wave 10 (display tail) → 0512 soundness fixes + 0497 (typecheck, pre-Wave-11) → 0495 harness (Wave-11 prep) → Wave 11 (mechanisms) → Wave 12 (0459) → Wave 13 (full I-G1..I-G7 gates + wrap) → tail drains. Phase-7 close still needs explicit user sign-off.

**Goal**: Land ownership-inference increment I's read path (golden-CLIF baseline first, then `pass5_ownership` → `ModeSummary` → the four backend mechanisms) while draining the S101 defect wave with the T1 silent split-world at the top — the two are deliberately interleaved because the backend defect cluster (0474/0483/0476) rides increment I's own seam rework.

## Scope

Three blocks, priority-ordered. Block A and Block B are peers with an explicit coupling (A's backend cluster rides B's seams); Block C rides wherever its crate is already open.

### Block A — the defect/T1 wave (risk-register order, `tests/plan/coverage-audit-s101.md` axis 4)

1. **T1 silent split-world — ranked #1** (/port + audit: the at-scale DEFAULT path, not an edge). Two legs:
   - **Interim cure ships regardless**: make the T1 downgrade PRINT (transaction report names the downgrade + affected callers instead of silence). Owner /int (src/), small.
   - **Full cure assessed in Phase 3**: end-of-turn-sequenced module reload (`session-transaction.md` §10 named residue, "increment-I-or-later"). /design (int) sizes it; lands in-sprint only if it fits without displacing block B's baseline change-sets, else explicitly deferred to S103 with the print as shipped mitigation.
   - /qa lane **L-U1** (unannotated-default siblings) lands FIRST in the QA-first stage — the register's own ordering.
2. **Persistence-integrity cluster** — D1 (def-poisons-directory), D2 (cwd `user.cl` adoption/data-loss), 0489 (restart lockout). Owner /int; FIXME 0496 (`lifecycle.rs` 1,918 LOC / 0 tests — sits exactly under this cluster) is its mandatory unit half. /qa lanes L-S2 (session-lifecycle grid) + L-S3 (file-backed dev-loop).
3. **0488 generic-fn missing monomorphisation (3 signatures)** — cross-skill **isolation owed BEFORE fix dispatch** (CLAUDE.md minimal-repro rule; audit register #3). /qa isolates; owner then assigned (/typecheck vs /backend seam).
4. **File-backed dev-loop** — D3 (dependent recompilation false-BROKEN, restart-only recovery) + 0487 (`/mod` scope gaps). Owner /int.
5. **Display/diagnostic batch** (register #6–8, capacity-gated tail): 0486 (bare-lookup corrupts /info//source — /int), 0484 (import-shadow order dependence — /int, /spec precedence pin first), 0491 (`__expr` cascade leak — /int), 0492 (`/sig` FQ — /repl arbitrates spec-vs-impl first, then fix), 0493 (nested parameterized-ADT display), trap-message §18.5 format (/int), 0490 (phantom-member misleading error — /int), 0485 (macro-clause diagnostic span — /frontend crate via /dev). /qa lanes L-N1 (display-exact) + L-N2 (no-internal-artifacts) back this batch.

### Block B — increment I read path (the Phase-H spine; `design/arch/ownership-inference.md` §7, backend §0, qa plan §6)

0. **Golden CLIF capture is the FIRST change-set** — pre-mechanism baseline for the differential oracle (hard sequencing pin from S100/S101). Phase-2 question for /arch: which Block-A backend-adjacent fixes must land before capture vs force re-baseline.
1. **0476 phantom-NULL-slot representation cure** in the first change-sets — /arch-ruled IN; rides the `ModeSummary` types-surface schema cascade (one `CACHE_SCHEMA_VERSION` bump, Principle 8).
2. **Typecheck + types**: `pass5_ownership` reverse-topo fixpoint, `ModeSummary` carrier (`cranelisp-types` change), primitive fact table on `DefKind::Primitive` (REQUIRED in increment I per the S100 spine amendment).
3. **Backend mechanisms**: borrow-elision (`borrowed_vars`), stack slots (immortal-RC sentinels), per-site non-atomic Confined RC, `str-len$borrowed` sibling template. **Riding this seam rework**: 0474 COW copy-branch leak (3 guards, user-approved carry) + 0483 vec-op-as-value SIGBUS (3 guards) — same `fn_as_value` seam; 0482 `#[non_exhaustive]` attribute; 0495 backend `tests.rs` split + thin-submodule drain (§2.2 discipline applies to every new mechanism's scenario matrix).
4. **0459 remainder** — floor-restoring gate + `lenient-eval.md` §2.6.2/§3.6.3 scope alignment (ROADMAP pins the remainder to increment I).

**Acceptance**: qa gates **I-G1–I-G7** (`tests/plan/s100-ownership-verification.md`): F1 rc_inc ≥99% drop; F2–F4 rc_inc within 1% attribution; parallel ≤+5%; serial ≤+3%; compile ≤+10%; REPL body-only turn ≤1.10×. Differential oracle (`CRANELISP_NO_OWNERSHIP`, landed S101) byte-identical off.

### Block C — methodology + enabler drains

- **0494** (/arch): facade test-organization / module-composition convention — the architecture half of METHOD §2.2; /arch's first S102 touch, together with the P22 ratification-blockquote update (S101 close residual).
- **0499** (/qa): e2e lane refactor — 7 lanes + 4 standing drafting rules, ordered L-U1 → L-S2+L-S3 → L-N1/L-N2 → L-S1 → L-M1-with-increment-I; includes registering `coverage-audit-s101.md` in `tests/CLAUDE.md` §Plan documents.
- **0497/0498/0500/0501/0502** unit-tier drains (/dev typecheck, types, frontend, intrinsics, platform): ride wherever the crate is already open this sprint; otherwise capacity-gated tail, explicitly re-deferred with rationale if untouched.

### Sizing + close-short seam (named up front)

This is a large sprint — the S100 design docs themselves note increment I "may split". The coupling (0474/0483/0476 ride B's seams; T1's full cure is increment-I-or-later) makes one interleaved sprint MORE coherent than a split, but Phase 5 holds the close-short authority: if capacity runs out, the **clean seam is after B2** — golden capture + 0476 + `ModeSummary`/`pass5_ownership` land; the four backend mechanisms + their riding defect cluster carry to S103 as increment I's second half. Block A items 1–4 and Block C item 0494/0499-head do NOT slip past this seam.

### Out of scope (deferred, with rationale)

| Item | Rationale | Target |
|---|---|---|
| Increment II (write path: reuse tokens, R5 flattening, region arena) | Sequenced after increment I settles | S103+ |
| `--release` tier + polish pins 0050/0052/0365/0416 | Gated behind the settled memory model (ROADMAP Phase-H table) | after increment II |
| 0408 perf half (Sudoku parallel-search showcase) | Depends on increment II's write-path wins (vec unlock noted S101) | increment II |
| 0463 network poll-shape example | Unmet trigger (blockers re-verified S101) | trigger-based |
| 0466 GOT slot-hole reclamation | User-directed indefinite deferral, trigger-based | trigger-based |

## FIXME debt

Full open set at Phase 1 (31 files). Dispositions:

| FIXME | Target | Status | S102 disposition |
|---|---|---|---|
| 0474 | /qa→/backend | open | Block B3 — rides `fn_as_value` seam rework (user-approved S101 carry) |
| 0476 | /arch→/dev (backend) | types-half LANDED P3 | CS-A delivered the `PrimitiveBody::{Extern,Inline}` reshape (behaviour-identical); consumption half = Wave 6 B1-be (trio→Inline, stop-predicate flip, name-list retirement, golden-diff-EMPTY witness) — FIXME deletes there |
| 0482 | /dev (backend) | open | Block B3 — next backend change-set (/arch-timed) |
| 0483 | /qa→/backend | open | Block B3 — same seam as 0474 |
| 0484 | /qa→/int (+/spec) | open | Block A5 |
| 0485 | /frontend | open | Block A5 tail |
| 0486 | /qa→/int | open | Block A5 |
| 0487 | /int | open | Block A4 |
| 0488 | /qa | open | Block A3 — isolation before fix dispatch |
| 0489 | /int | open | Block A2 |
| 0490 | /int | open | Block A5 tail |
| 0491 | /qa→/int | open | Block A5 |
| 0492 | /repl→/int | open (retargeted P3) | Block A5 — arbitration CLOSED (spec stands, impl defective; `handle_sig` repl.rs:711); fix rides Wave 10 |
| 0493 | /qa | open | Block A5 |
| 0494 | /arch | **RESOLVED P2** | Authored as Principle 23 + command-file imports; FIXME deleted by /arch |
| 0495 | /dev (backend) | open | Block B3 rider |
| 0496 | /dev (src/) | open | Block A2 unit half |
| 0497 | /dev (typecheck) | open | Block C — rides B2 typecheck work |
| 0498 | /dev (types) | open | Block C — rides B2 types change |
| 0499 | /qa | open | Block C — QA-first stage head |
| 0500 | /dev (frontend) | open | Block C tail — capacity-gated (frontend only open if A5/0485 dispatched) |
| 0501 | /dev (intrinsics) | open | Block C tail — capacity-gated |
| 0502 | /dev (platform) | open | Block C tail — capacity-gated |
| 0459 | /backend | **RESOLVED P3** | Doc half landed by /design(backend); gate design = lenient-eval §2.7; impl rides ladder B4 |
| 0503 | /qa | **RESOLVED P3** | Filed by /arch P2 (golden corpus pin); actioned + deleted by /qa P3 |
| 0504 | /design (backend) | **RESOLVED P3** | Filed by /design(typecheck) P3 (`neq-string` audit row); drained in-flight by /design(backend) |
| 0408 | /port | open | Deferred — increment II (unmet dependency) |
| 0463 | /examples | open | Deferred — unmet trigger |
| 0050 | /int | deferred | Pin — `--release` polish (display protocol) |
| 0052 | /repl | open | Pin — `--release` polish |
| 0365 | /spec | open | Pin — `--release` polish |
| 0416 | /arch | deferred | Pin — `--release` polish (trigger-based) |
| 0466 | /design | deferred | Pin — indefinite, trigger-based (user-directed S100) |
| 0505 | /repl | open (filed W1) | Unpinned `/mod` env-parity invariant (audit P5 rule 4 — neither /spec nor /repl pinned it in Phase 3); spec-half pin needed before the L-S3 guards' flip criteria are fully bound; slot with A-3/Wave-7 prep or Phase 6a |

**Escalation check (METHOD §2.4)**: no item hits the 2×-deferral gate this sprint without an existing user sign-off or a standing trigger-based pin (0050/0416/0466 pins are user-ratified; 0430's third deferral was signed off S100 and it has since been drained).

## Architecture review (Phase 2)

**Verdict: COHERENT — approved as drafted, with the rulings and pins below. No scope moves in or out.** Reviewed 2026-07-03 (/arch) against technical coherence, interim-architecture risk (Principle 8), and public-API impact per METHOD §2 Phase 2. Sign-off gates Phase 3: **given**.

### Q1 — Golden-CLIF capture sequencing (RULED; canonical home = spine §6.2, `design/arch/ownership-inference.md`)

**Capture-then-scoped-rebaseline. The capture is the first Block-B change-set and is NOT gated on any Block-A fix.** The corpus is **green-only by construction** — shapes under open failing-not-ignored guards (0483's two-instantiation HOF, 0488's FQ-call/imported-value-use, 0484's shadow-order shapes) are excluded, which is exactly what makes 0488 isolation and the /int wave non-blocking for capture.

- **Classifier** (for /sprint's Phase-4 wave ordering): a fix is *emission-affecting* iff it changes backend emission, primitives entry shapes, monomorphisation derivation, or name-resolution precedence **for green programs**. By this test: Block-A items 1 (T1 print), 2 (D1/D2/0489 persistence), 4 (D3/0487), and the whole item-5 display/diagnostic batch (0486/0491/0492/0493/0490/0485/trap-format) have **no capture interaction** — schedule freely. 0484 and 0488 are potentially emission-affecting but their shapes are corpus-excluded, so they don't block capture either.
- **Before vs after**: an emission-affecting fix that happens to be ready before Block B opens lands before capture (the capture parent floats forward for free). After capture, an emission-affecting fix carries a **scoped re-baseline in its own change-set** — re-dump only the corpus entries whose CLIF changed, golden diff in the same commit, delta attributed to the fix's seam (the L-B1 / `public-api.txt` discipline). Wholesale re-capture without attribution is forbidden.
- **Extension ≠ re-baseline**: when a fix makes a previously-excluded shape green (0483/0488/0484 post-fix), /qa *extends* the corpus with the newly-green shape in the fix change-set; existing golden entries are untouched. 0474/0483/0476 land at B3/B1 *after* capture **by design** — their emission deltas are precisely what the oracle should see attributed; their scoped re-baselines ride those change-sets.
- **Recorded**: spine §6.2 (the ruling's manifestation site); corpus-construction pin for the L-B1 lane spec filed as **FIXME 0503** (`target: /qa` — the verification plan is /qa-owned; natural slot is the 0499 head or L-B1 drafting, whichever comes first).

### Q2 — FIXME 0494 (RESOLVED — numbered principle, not a facade-convention item; FIXME deleted)

**Authored as Principle 23 — "Unit tests mirror module composition (submodule × scenario class)"** (`design/arch/principles/23-tests-mirror-module-composition.md`), index updated, and the `@import` line added to all four command-file import blocks (`arch`/`design`/`dev`/`review`) per the `principles/CLAUDE.md` filer convention — that import mechanism is exactly the "Principle-22 precedent propagation" 0494 asked for, and it is the only /arch-owned channel that reaches the triad. Rationale for principle-over-facade-convention: the facade conventions govern the *public* surface (`lib.rs` mechanics); this convention governs **crate interiors** (module composition + per-submodule test accounting) and carries role bindings the triad must see — filing it under facade conventions would misfile it and hide it from the triad. The principle carries the architectural axiom + role bindings and **cites METHOD §2.2 for the methodology half (maintained there, not duplicated — Principle 7)**. Mid-sprint authoring follows the P21/P22 user-directed precedent: provisional blockquote now, **formal ratification at S102 Phase-7 close**.

### Q3 — Close-short seam after B2 (CONFIRMED CLEAN, with three pins)

The seam splits cleanly. Post-seam state = golden capture + 0476 reshape + `ModeSummary`/fact-table types change + `pass5_ownership` landed, **zero backend mechanisms consuming summaries**. This state is sound by the spine's monotone-soundness contract (§6.1): summaries emitted but unconsumed ⇒ codegen remains strictly Decision-24; the §5.7 precondition is already satisfied (R3 machinery landed S101). Pins:

1. **ABI-diff widening at the seam**: once summaries exist, the R3 transaction's `AbiSurface` comparison gains mode vectors — mode-changing redefinitions classify ABI-changing and take fresh slots/recompiles that are semantically unnecessary but conservative-correct. Acceptable across one sprint boundary; /qa's L-R lanes should expect the stage-M "type-scheme-only" degeneration to end at the seam.
2. **Gates at the seam**: if closed short, perf gates I-G1–I-G4 + I-G7 defer wholesale to S103 (they grade mechanisms). But **I-G5 (compile ≤ +10%) and I-G6 (REPL turn ≤ 1.10×) MUST run at the seam** — pass5's cost is live the moment it runs, consumers or not. L-B1/L-B2 protocol lanes are live from capture (trivially green, which is their install value). The differential oracle stays byte-identical-off throughout.
3. **Guard bookkeeping**: 0474 (3) + 0483 (3) guards stay RED across a short close — already itemized intentional; no CLAUDE.md §Testing count change until B3 lands.

### Q4 — P22 ratification blockquote: DONE

`principles/22-*.md` blockquote now records the 2026-07-03 user ratification at S101 Phase-7 close (audit trail: `sprints/archive/sprint-101.md` §Outcome); index entry updated to match.

### Public-API impact statement (increment I)

**One `cranelisp-types` change-set, one `CACHE_SCHEMA_VERSION` bump (v11→v12) — confirmed.** Phase 3 (/arch authors; it is the types crate) must define, in that single change-set: (i) `Mode`/`ModeSummary`/`ResultMode`/`ParamFlow` + `mode_summary` on the four callable `DefKind` variants + advisory site-fact fields on `MonoExpr` nodes (spine §3.3 shapes, `#[serde(default)]` = Decision-24 conservative point); (ii) the per-entry value-use mark; (iii) the declared-fact payload on `DefKind::Primitive` (+ optional `borrowed_sibling_slot`); (iv) the **0476 `PrimitiveBody::{Extern, Inline}` discriminator + `is_callable_target()` accessor** (the ruled representation cure — same crate, same bump, one cascade per Principle 8). Cascade in the same change-set: `public-api.txt` regen for `cranelisp-types` + consumer baselines where accessor signatures surface (backend, primitives, typecheck), `interfaces.md`, BC §7.

Other public-API edges this sprint: **0482** (`CacheInvalidReason` `#[non_exhaustive]`, backend — additive attribute, baseline regen). **Expected new public functions: ZERO** — increment I's mechanisms are crate-interior (borrow-elision, stack slots, non-atomic gating, R2 wrappers all sit behind existing entry points; S101's `compile_trap_stub` remains the last public addition); pass5 is internal to the `check_forms` seam; the T1 print is `src/` binary-side. Any /dev deviation files FIXME `target: /arch` before publishing.

### Interim-architecture check (Principle 8)

The **T1 interim print** passes: it is additive diagnostic honesty (a transaction-report line naming the downgrade + affected callers), not throwaway infrastructure — the full end-of-turn-reload cure subsumes its trigger without rework. Pin: word the print as a transaction-report line the full cure keeps. The interleave itself (A's backend cluster riding B's seams) is Principle-8-positive: one cascade, one seam visit, no interim duplication.

### Scope adjustments (in-scope timing only; nothing moves in/out)

1. **0482 re-timed to B1**, not B3: it rides the *first* backend-crate change-set of the sprint (the 0476 consumer-side edit), so it lands even under a short close instead of stranding a third sprint.
2. Golden capture is **Wave 1 of Block B and may run before/parallel to Block-A waves** (per Q1's classifier — nothing in Block A blocks it). /sprint should not serialize A-then-B.
3. If close-short triggers: run I-G5/I-G6 at the seam (Q3 pin 2) — add to the Phase-5 close-short checklist.

### Doc-set triage (standing Phase-2 duty)

No archive moves: the ownership four-doc set is live (this sprint's spine), no working doc is past its trigger; Decisions drain stays opportunistic (no S102 fire touches a Decision-grounded section yet). FIXMEs against /arch at review close: **none open** (0476 is ruled + pinned to B1, /arch authors its types half in Phase 3; 0494 resolved + deleted this review; 0503 newly filed against /qa).

## Skill plans (Phase 3)

### /spec — DONE Phase 3

- **Task**: 0484 precedence ruling + /port D4 designed-vs-gap verdicts.
- **Ruling (0484)**: **Definition-over-explicit-import is a compile-time ERROR — order-independent, mode-uniform** (spec §8.6.4 already ruled it; §8.6.1 layer 2 clarified — no def-over-import tier; new pinned subsection "Definition-Over-Import: Order-Independent, All Modes `[S102]`"). Rejected form has no effect; import stays the binding; introspection agrees; symmetric for import-over-def; prelude-provided names stay shadowable (0475 cells unaffected, but their comments mis-cite the anchor — /qa opportunistic fix).
- **Consequences**: BOTH 0484 guards re-anchor (the RED guard AND the currently-green control — its shadow-wins pass is itself the violation). /int's A5 fix = reject the later-arriving conflicting form, not resolution-caching. Diagnostic SHOULD name import source + remediations.
- **D4 verdicts**: (a) arity-error shape DESIGNED (auto-curry §4.6.3; wording quality → /frontend, rides 0485/0500 — /sprint note, no FIXME); (b) `match` Int-literal patterns DESIGNED (§6.6.2 explicit exclusion). No FIXMEs filed.
- **Next skills**: /qa (re-anchor both guards to §8.6.4), /int (rejection fix), /frontend (arity wording rides 0485/0500).

### /repl (spec half) — DONE Phase 3

- **Task**: §18 T1-downgrade-report wording; 0492 arbitration; §18.5 trap-format check.
- **T1 report contract**: new `repl/spec.md` §18.1.1 `[S102]` — one comment-line section after the §1.3 confirmation, exact header `; stale: compiled callers keep the previous definition of {cause}`, exact caller set both ways (every stale caller named; no late-bound/never-compiled names), omitted entirely when nothing stale, informational-only. Display-exact worked example included. WHAT-only — mechanism left to /design(int); Principle-8 pin recorded (section survives the full cure, rendered empty post-reload).
- **0492 arbitration**: spec stands, **implementation is the defect** (FQ `:Type value` principle held consistently across §1.1/§1.4/§4.1/§18.4). New §3.8 makes bare-lookup/`/sig` primary-line byte-identity an explicit MUST. **Fix owner /int** (`handle_sig`, repl.rs:711); FIXME 0492 retargeted /repl→/int; guard polarity CORRECT, no re-anchor.
- **§18.5 verdict**: exact as written; one ambiguity amended (`[S102]` bullet: no own span, no wrapper chain — §5.1 satisfied by the span inside the original error). Fix is mechanical, /int.
- **Next skills**: /int (A5 batch against pinned bytes: §18.1.1 + §3.8 + §18.5 — three guards flip), /qa (L-N1 display-exact tests for §18.1.1 both polarities), /design(int) (consume §18.1.1 as fixed output contract).

### /qa — DONE Phase 3

- **Task**: S102 test plan. Durable doc: `tests/plan/s102-test-plan.md`. Housekeeping landed: `tests/CLAUDE.md` §Plan documents registers both audit + plan (0499 item 3); **FIXME 0503 actioned + DELETED** (corpus pins in `s100-ownership-verification.md` §3.1 L-B1: green-only corpus, committed EXCLUSIONS.md keyed to guard flips, extension-≠-rebaseline).
- **Stage-1 drafting order**: L-U1 (~8–12; incl. RED interim-print acceptance pair for A1) → L-S2 (new `repl_lifecycle_matrix.rs`, ~15–20) → L-S3 (new `repl_mod_devloop.rs`, ~10–14) → L-N1 (new `display_exact.rs`, ~12–18, first `assert_stdout_eq` adoption) + L-N2 (harness helper + ~15–25 retrofits) → increment-I set (L-B1 golden capture corpus ~10–12 modules + MANIFEST/EXCLUSIONS/script; S1–S6 fences; fact-table rows; H2/H5 hook smokes RED-at-draft; `ig_gates` runner). L-S1 capacity-gated; L-M1 rides B3.
- **0488 isolation = its own EARLY wave** (recommended, accepted): per-signature seam attribution (typecheck-side vs src/ batch-derivation vs backend resolution — the two error classes suggest the 3 signatures may not share one home); deliverable = attribution notes + unit-test shapes in the handoff brief. Doesn't block/isn't blocked by capture (shapes corpus-excluded).
- **Guard-flip map (22 REDs)**: 0474×3+0483×3 → B3 (stay RED at close-short); 0488×3 → post-isolation wave; 0489+D1+D2 → A2; D3 → A4; the 9 display/misc → A5. Root-CLAUDE.md count update = Phase 7 user-flagged item.
- **I-G readiness gaps**: H2 per-mechanism counters (/backend, B3, gate-blocking I-G3/I-G7); H5 ownership-trace (/typecheck, B2); `ig_gates` differential runner + I-G5 compile probe = /qa stage 1 EARLY (I-G5/I-G6 run even at close-short); fresh toggle-off baseline on S102 HEAD. I-G6 ready today.
- **/sprint reconciliations (post-ruling, carry into stage-1 brief)**: (a) BOTH 0484 guards re-anchor to §8.6.4 error polarity (/spec ruling postdates this plan) — fold into L-S2/A5 acceptance; 0475 pin comment anchors fixed opportunistically; (b) 0492 guard confirmed correct — no re-anchor (arbitration closed).
- **Next skills**: /sprint (fold + schedule 0488 isolation early, capture as B Wave 1), /dev per crate (fixes against lane acceptance surfaces), hook obligations H2→/backend, H5→/typecheck.

### /design (cranelisp-typecheck) — DONE Phase 3

- **Task**: pass5_ownership change-set staging. Design recorded: `design/typecheck/ownership-inference.md` §13 (staging/feed-verification/fact-verdict/toggle-pin/refinements/P23 matrices).
- **Staging**: **CS-A** (/arch types v11→v12, hard dep) → {**CS-B** (/dev primitives: fact table on the same `mode_summary` slot — no separate type, Principle 19), **CS-1** (classifier + 0476 `PrimitiveBody` consumption)} → **CS-2** (pure transfer fn) → **CS-3** (fixpoint+SCC+confinement+memo; toggle gate at driver entry) → **CS-4** (publication + **H5 `CRANELISP_OWNERSHIP_TRACE`** — qa I-G3/L-D3f dep). New `ownership/` module cluster, per-submodule sibling tests (P23 from birth). Out-of-crate rider: **/int `abi_eq` summary-diff-gate widening rides with/after CS-4**.
- **Graph-feed verdict**: S101 v11 widening sufficient at template grain; **fixpoint re-entry rides transfer-harvested `DepSet` edges (exact grain); `call_graph_edges` demoted to seeding-order hint** (bad order = revisits, never wrong). Accepted risk: mutual-import cycle edges go Decision-24 (monotone-sound). Coordination: 0497 rider vs /qa's 0488 isolation on `monomorphise.rs`.
- **Fact-table verdict**: ring2-rc §3.3 sufficient seed; **gap: `neq-string` (no audit row) → FIXME 0504 filed** (target /design backend — blocks CS-B transcription + L-D3e); named scope cut: `PrimitiveExtern` (sconcat/bind/…) = pinned Decision-24 boundary, watch item.
- **Toggle-off pinned**: emit NOTHING (return at pass entry; `.meta.json` field-identical; emit-but-ignored rejected on oracle-honesty/behavioral/persistence grounds). Needs-list item 12: /arch rules on relocating the toggle read-once gate to cranelisp-types.
- **Needs list (12 items)**: Mode/ModeSummary (full Eq)/ResultMode/ParamFlow; conservative-read accessors (⊤-on-absence single home); `abi_eq` shared view; `mode_summary` on callable DefKinds + MonoDefnVariant; MonoExpr site facts incl. symbol-keyed `provenance` w/ shadowing⇒None; value-use mark; 0476 reshape; toggle relocation. /arch verifies at exit gate.
- **Next skills**: /arch (CS-A + needs-list verification + item-12 ruling), /dev typecheck (CS-1–4 + 0497 stages), /dev primitives (CS-B after 0504), /design backend (consume §13.6 pins), /qa (L-D3e gated on 0504; I-G5/I-G6 at B2 seam).

### /design (cranelisp-backend) — DONE Phase 3

- **Task**: Block B backend half. Design recorded: `ownership-codegen.md` §13 (capture §13.1 / ladder §13.2 / seam rework §13.3 / 0459 §13.4 / matrices §13.5); `lenient-eval.md` §2.7 + §9 scoping. **FIXME 0459 RESOLVED+deleted** (doc half landed; gate design = §2.7; impl = ladder B4). **FIXME 0504 RESOLVED+deleted** (`neq-string` audit row added, body-verified).
- **Change-set ladder** (each independently landable; else-arm discipline = pre-change helper verbatim): **B0-be** capture substrate + Hook H1 frame-atomic dumps (Wave 1, NOT gated on /arch types CS) → **B1-be** 0476 consumption + 0482 (golden diff expected EMPTY — the CS's own witness) → **B3.0** 0495 tests.rs relocation (pre-seam) → *B2 = typecheck; I-G5/I-G6 at seam* → **CLOSE-SHORT SEAM** → **B3.1** fn_as_value defect half (0483 root-cause pin: collision is a HYPOTHESIS — investigate vs the instantiation matrix first; 0474 RC-polarity contract `SourceOwnership::{Owned,Borrowed}`, static polarity NOT uniformly Borrowed; 0488 conditional rider iff isolation attributes here; corpus EXTENSION) → **B3.2** borrow-elision core, one atomic CS (I-G1/I-G2; H2 counters; biggest re-baseline) → **B3.3** confined non-atomic RC (I-G3) → **B3.4** stack slots (I-G7; L-C2 ASan) → **B3.5** `str-len$borrowed` sibling (S5) → **B4** 0459 density gate on `is_worth_sparking` (facts-absent ⇒ axis inert; I-G4 progress). B3.3/B3.4 individually droppable at squeeze.
- **Capture spec**: `CRANELISP_CODEGEN_DUMP=*` cold-cache `--run`; frames sorted by module::symbol; content **byte-verbatim, NO canonicalization** (wrapper/slot identity is load-bearing — masking blinds the oracle to 0483's class); config pins in MANIFEST (all perf toggles unset); determinism self-test (double capture) before golden commits; binding 5-step scoped-rebaseline procedure; 0474's leaking shapes may join the corpus (green programs).
- **Wrapper-identity ruling**: (dispatch identity × concrete signature), never (span × discriminator) — `__d24wrap_{fq}_{slot}__` / `__inlwrap_{bare}_{monosig}__`; dedup via declare-idempotency.
- **Typecheck consumption contracts folded** (`ownership-codegen.md` §13.6): (b) site facts arrive in ONE post-convergence walk — fact-absent always means "concluded conservative", never "not yet written" (no backend staleness handling); (d) provenance `Symbol`-keyed matching `borrowed_vars` as-built; shadowing⇒None⇒D24 materialization — pinned scenario row in both matrices.
- **Next skills**: /arch (types CS = B1-be dep; B0-be independent), /dev backend (ladder in order; B0-be starts with capture wave), /qa (corpus per §13.1 contract), /sprint (wave per dependency column).

### /design (src/) — DONE Phase 3

- **Task**: Block A defect-wave designs + T1 sizing. Recorded: `session-transaction.md` §9.1.1 + §10 amendment; new `design/int/s102-defect-wave.md` (§1–§9, root causes source-verified, five P23 matrices A–E); 0496 annotated (unit briefs derive from the matrices).
- **T1 print (A1)**: one small CS, wave-ready — `TransactionReport.stale` + pure `stale_callers()` over on-demand ReverseIndex + **load-bearing gate widening** (`RedefinitionOutcome.prior_was_def`; both T1 shapes emit NO outcome today — trigger is the route, not the surface diff). Binds exactly to /repl's §18.1.1. L-D1 untouched.
- **T1 full cure — VERDICT: OUT, defer S103.** Both original blockers dissolve at end-of-turn (~3 CSes), BUT this sprint's A2/A4 fixes are hard preconditions (regen fidelity, 0489 floor, D3/0487 env). S102 leaves in place exactly the in-scope wave + the print (Principle-8 kept-report pin). S103 = one wave incl. flipping the two coherent-stale pins.
- **Persistence cluster**: D1 = two recording sites disagree (original outer form vs expanded defmacro) → origin-uniform recording + regen dedup (CS-D1). D2 = regen renders from desugared Sexp + rehydration overwrites source → source-text-first emission + verbatim slicing (CS-D2; hybrid-meta arm unreduced, re-probe post-cluster). 0489 = startup exit at `main.rs:146` → REPL-mode catch + degraded form-by-form entry load (green forms commit; failed forms kept as (symbol, error, verbatim text), re-emitted by regen until repaired) (CS-0489).
- **File-backed dev-loop**: unified root cause — session-env companions populated only on fresh-typecheck path; `TypecheckProduct.file_path` never `Some` on ANY path (rehydration/T2 reload dead as wired). Cure: `install_module_session_env` at every install route + FQ `resolve_symbol_arg` shared across introspection commands + `/refs` fed by `redefine::ReverseIndex` (CS-D3a→D3b→CS-0487).
- **Display batch**: 0491/0486/trap-format trivially small (0491 = `__expr` exclusion at ReverseIndex::build; 0486 = display-only Def writing `.source` at eval.rs:130–146; trap = int-side `program_outcome_to_result` only). 0490 one confirmation step; 0484 commit-side verified CLEAN, best-evidenced pin typecheck-side overload rehydration — **reconcile with /spec's landed ruling** (rejection polarity, below).
- **Wave order (input to Phase 4)**: A-1 (print+0491+0486, shared seams) → A-2 (D1→D2→0489 + 0496 drain) → A-3 (D3a→D3b→0487) → A-4 tail (trap, 0490, 0484-post-spec). All capture-neutral per /arch Q1. Waits on /arch types CS: nothing.
- **/sprint reconciliation**: /spec's 0484 ruling (landed concurrently) = definition-over-import is a compile-time ERROR, order-independent — the A-4 fix is *rejection of the later-arriving form*, and BOTH guards re-anchor; the typecheck-rehydration hypothesis is re-evaluated under that polarity at fix time (order-dependence itself should vanish with rejection).
- **Next skills**: /qa (L-U1 before A-1 close; L-S2/L-S3 from matrices A/D before A-2/A-3 close), /dev src/ (Wave A-1 first), /sprint (Phase 4 from §8 wave table).

### /arch CS-A + exit gate — Phase 3 (final invocation, 2026-07-03)

- **CS-A LANDED** — the single `cranelisp-types` interface change-set, exactly one `CACHE_SCHEMA_VERSION` bump (**v11→v12**, `cranelisp-backend/src/cache/mod.rs`, S102 entry documented). New `crates/cranelisp-types/src/ownership.rs`: `Mode`/`ResultMode`/`ParamFlow`/`ModeSummary` (full `Eq`; all fields `#[serde(default)]`; ⊤-on-absence accessors `param_mode`/`param_flow`/`spark_op` as the ONE conservative-read home; `abi_eq` + `abi_eq_opt`/`is_abi_conservative` over `(param_modes, result)` only) + the relocated read-once `ownership_analysis_off()` toggle. `module.rs`: `mode_summary` on all four callable kind carriers (`UserFnState::Concrete`, `Primitive`, `Constructor`, `PlatformEffect`) + uniform `ModuleEntry::mode_summary()`/`set_mode_summary()` (did-write bool) + per-entry `value_use` mark (`value_use()`/`set_value_use()`; builder-defaulted, no setter) + the **0476 reshape**: `DefKind::Primitive { body: PrimitiveBody::{Extern{got_slot, borrowed_sibling_slot}, Inline}, mode_summary }` + `is_callable_target()` + `DefKind::primitive(slot)` convenience ctor. `mono_expr.rs`: `MonoDefnVariant.mode_summary`; `escapes`/`confined`/`unique_static` on `StringLit`/`Lambda`/`Apply`/`VecLit`/`ConstrADT`; `provenance: Option<Symbol>` on `Apply` + `MonoMatchArm`. 16 new unit tests (11 ownership + 5 module-accessor incl. the v11-shape-must-not-deserialize serde pin).
- **Needs-list verification (typecheck §13.1, item-by-item): items 1–11 delivered as specified; item 12 RULED YES + implemented** — the `CRANELISP_NO_OWNERSHIP` gate lives in `cranelisp-types` (`ownership_analysis_off()`); `cranelisp-backend::cache::manifest::no_ownership_enabled()` is now a thin delegation (manifest key untouched, call sites unchanged). Backend §13 hard-dependency rider delivered: `borrowed_sibling_slot` rides `PrimitiveBody::Extern` only (inline-with-sibling unrepresentable, Principle 20).
- **Behaviour-identical by construction**: every primitive still constructs `Extern` (vec trio keeps its NULL slot), the S101 name-list + wrapper exemption arms stay live, `resolve_driven` still stops on `callable_got_slot()`. **FIXME 0476 ANNOTATED, not deleted** — types-half landed; the consumption half (trio→`Inline`, stop-predicate flip to `is_callable_target()`, name-list retirement, golden-diff-EMPTY witness) is B1-be (/dev backend+primitives); the FIXME deletes there.
- **Cascade landed**: `crates/cranelisp-types/public-api.txt` regenerated (canonical command); the six consumer baselines verified byte-identical (no other crate's surface moved); `interfaces.md` §"Ownership-inference carriers"; BC §7 "S102 — Principle 20 applied one level down"; spine §3.3 blockquote records the landing. Sequence-diagram sweep: no facade signature changed shape (`callable_got_slot` contract intact; new accessors are additive) — no diagram edits required.
- **Verification**: `cargo check --workspace --all-targets` clean, zero warnings. Canonical run: **3496 tests — 3474 passed / 22 failed / 1 skipped**; the 22 are exactly the itemized intentional guards (root CLAUDE.md §Testing), fail-set unchanged. Two transient extras surfaced and fixed in-set: the stale types `public-api.txt` (regenerated) and `s68_primitives_uniform`'s source-grep (primitives constructions restored to the explicit `DefKind::Primitive { .. }` struct form — also the right declaration-site shape for CS-B's fact population).
- **Out-of-types files touched (mechanical/mandated, flagged)**: `cranelisp-backend/src/cache/mod.rs` (schema bump) + `cache/manifest.rs` (toggle delegation); `cranelisp-primitives/src/lib.rs` (explicit-form construction, behaviour-identical); mechanical `mode_summary: None`/`value_use: false`/`..`-pattern updates across ~40 files in `cranelisp-typecheck` (`adt.rs`, `builtins.rs`, `program.rs`, `traits/{impl_check,monomorphise}.rs` + tests), `cranelisp-backend` (`lib.rs`, `compiler/{apply,fn_compiler,rc_emission,control_flow/let_if}.rs` + tests), `src/` (`bootstrap.rs`, `code.rs`, `display.rs`, `exe.rs`, `expander.rs`, `platform.rs`, `repl.rs`, `save.rs`, `worker.rs`, `redefine.rs` tests + other test files), and one line in `tests/platform_schema_roundtrip.rs` (E0063 default — flagged: `tests/` is /qa-owned; /qa may re-express).
- **EXIT-GATE VERDICT: PASS — the interface set is complete for Phase 5.** (i) All 12 needs-list items + the backend `borrowed_sibling_slot` rider are landed; (ii) no other public-API edges remain pending for the sprint: 0482 (`CacheInvalidReason` `#[non_exhaustive]`) is B1-be's additive attribute per the Phase-2 re-timing, and expected new public functions outside CS-A remain ZERO (consumer baselines verified unchanged; `compile_trap_stub` stays the last backend addition); (iii) the /design(src/) claim that Block A needs NOTHING from `cranelisp-types` is verified — `TransactionReport`/`RedefinitionOutcome`/`AbiSurface` are `src/redefine.rs` pub(crate) types, the §18.5 trap-format fix is `src/pipeline.rs::program_outcome_to_result` int-side, and the future R3 `AbiSurface` mode-widening consumes `ModeSummary::abi_eq_opt` already provided. One watch item (not a gap): /dev deviations from this surface file FIXME `target: /arch` before publishing, per the Phase-2 statement.

## Waves (Phase 4)

Organized 2026-07-03 from the six Phase-3 plans + /arch's exit-gate verdict. CS-A is already landed (Phase 3), so the B-ladder's hard dependency is satisfied from Wave 1.

**Standing execution constraints** (every wave, every brief):
- **All source-touching agents run strictly serially** (worktree isolation broken). Waves are dependency groupings and dispatch order, NOT parallelism.
- One agent, one test run; canonical `cargo nextest run --no-fail-fast`; **post-Wave-10a baseline: 3719 run — 3704 passed / 15 failed / 1 skipped** (15 = 0484×2 [reverted, /spec-gated] + 0485×2 [frontend W10b] + 10 backend/ownership [0474×3, 0483×3, curried-partial, h2/h3, vec-returned-from-generic — W11] + 1 /qa test-authoring bug `display_exact_option_in_option_value_line` [uses repl() w/o prelude, bare Some unresolvable — /qa fix owed, NOT a defect]) (25 REDs = 27 − 2 H5/l_d3f flips; remaining: 0474×3, 0483×3, display batch 0484/0490/0492/0493/trap/macro-arity (W10), W11 #25/#26/H2/H3 + vec_query; suite still ~60s with pass5 running) (32 intentional REDs = exactly the itemized guards for Waves 8/10/11: 0474×3, 0483×3, 0488×5, H5/l_d3f (W8), display batch 0492/0493/0484/trap/0485/0490 (W10), W11 #25/#26/H2/H3; ledger-itemized in `tests/plan/ledger.md`; /qa owes cumulative flip annotations W2–W7). Golden `clif_golden.sh diff` EMPTY is a separate per-emission-CS gate (first passed W6). Any extra RED is a regression the owning agent fixes before returning. Watch item: suite wall time ~110s (subprocess-heavy new lanes) vs the 3-min kill threshold.
- **Wave gate** (METHOD §2.5): before dispatching wave N+1, scan `design/arch/fixmes/` for `target:` matching wave-N's skill AND `status: open`.
- Each source wave closes with **/review (narrow, same crate)** before the next wave dispatches; Blocker/Important findings resolve in-wave.
- Emission-affecting change-sets after Wave 3 carry their **scoped re-baseline in the same change-set** (Q1 discipline); Block-A waves are capture-neutral by the Q1 classifier.
- Forbidden-git list verbatim in every brief; commit per change-set on `main`, no branches, no push.

### Stage 1 — QA-first (sprint-wide)

| Wave | Skill | Crate | Task | Status |
|---|---|---|---|---|
| 1 | /qa | tests/ | Stage-1 failing tests per `tests/plan/s102-test-plan.md` drafting order: **L-U1 first** (incl. RED interim-print acceptance pair) → L-S2 (`repl_lifecycle_matrix.rs`) → L-S3 (`repl_mod_devloop.rs`) → L-N1 (`display_exact.rs`, first `assert_stdout_eq`) + L-N2 retrofits → increment-I set (L-B1 corpus modules + MANIFEST/EXCLUSIONS + capture-script contract, S1–S6 fences, fact-table rows, H2/H5 hook smokes RED-at-draft, `ig_gates` runner + I-G5 probe). 0499 head drains here. **Reconciliations carried**: both 0484 guards re-anchor to §8.6.4 error polarity; 0475 pin-comment anchors fixed opportunistically; 0492 guard stands (no re-anchor). | **done** (2026-07-03; 8 commits; ledger §"S102 Phase-5 Stage-1 QA-first RED set") |
| 2 | /qa | tests/ | **0488 isolation** (before any fix dispatch, CLAUDE.md minimal-repro rule): per-signature seam attribution — typecheck-side vs src/ batch-derivation vs backend resolution. Deliverable: attribution notes + unit-test shapes in a handoff brief; /sprint assigns the fix owner at wave close (conditional rider on Wave 11 B3.1 iff backend-attributed; else its own src//typecheck slot). | **done** (2026-07-03; all 3 sigs → /dev(typecheck), two mechanisms — (a)+(b) mono-derivation reference-shape misses, (c) fold-shape scheme over-generalization; B3.1 rider OFF; fix slots added to Wave 8; `tests/plan/0488-isolation.md`) |

### Stage 2 — per-crate D/D/R, serial ladder

| Wave | Skill | Crate | Task | Status |
|---|---|---|---|---|
| 3 | /dev → /review | cranelisp-backend | **B0-be golden CLIF capture** (Block B Wave 1; `ownership-codegen.md` §13.1): dump substrate + Hook H1 frame-atomic writes, `CRANELISP_CODEGEN_DUMP=*` cold-cache `--run`, frames sorted module::symbol, byte-verbatim (NO canonicalization), determinism self-test before golden commit; corpus per /qa L-B1 contract (green-only + EXCLUSIONS.md). | **done** (review verdict: first-occurrence pin SOUND, goldens byte-identical under `--no-cache` 3/3; findings F1–F6 routed — F1/F5 → FIXME 0506, F2/F3/F4/F6 + ratification → Wave 3R) |
| 3R | /qa | tests/ | **Oracle hardening + cross-ownership ratification** (review F2/F3/F4/F6, must land BEFORE Wave 6 B1-be): enforce config pins in `clif_golden.sh` + smoke (`CRANELISP_CAPTURE_BORROW` live hole, `CRANELISP_NO_LENIENT`, `CRANELISP_NONATOMIC_RC`); non-empty-capture guard in `dump()` (the false-green class); adopt `--no-cache` capture (verified zero golden churn) making duplicate frames a hard error; ratify /dev(backend)'s edits to `clif_golden.sh`/smoke/MANIFEST. | **done** (`b82ebf1`; zero golden churn 13/13; duplicate=hard-error + non-empty guard demonstrated; RATIFIED; +2 unpinned emission vars found → 0506 addendum) |
| 4 | /dev → /review | src/ | **A-1** (risk-register #1): T1 interim print — `TransactionReport.stale` + `stale_callers()` + `RedefinitionOutcome.prior_was_def` gate widening, bytes bound to `repl/spec.md` §18.1.1 — plus 0491 (`__expr` exclusion at ReverseIndex::build) + 0486 (display-only Def `.source` write, eval.rs). Guards flip: 0491×2, 0486×2 + L-U1 print pair. | **done** (review: approve-with-findings, nothing blocks W5; F1/F4/F6 → Wave-5 brief; F2/F3/F5a → FIXME 0507; F5b–d → /qa later wave; S103 boundary respected) |
| 5 | /dev → /review | src/ | **A-2** (persistence-integrity cluster): CS-D1 (origin-uniform recording + regen dedup) → CS-D2 (source-text-first emission + verbatim slicing) → CS-0489 (REPL-mode degraded form-by-form entry load) + **0496 unit drain** (lifecycle.rs briefs from matrices A–E). Guards flip: D1, D2, 0489 + 3 L-S2 REDs (ledger-authoritative). Riders: review F1 (broken-clear predicate), F4 (dead mirror), F6 (regen gating, both sites). | /dev + /review done (approve ×3, approve-with-findings ×2, **1 Blocker B-1** → Wave 5R) |
| 5R | /dev src/ | src/ | **Wave-5 review fix pass** (in-wave Blocker resolution): **B-1** watcher-reload success must clear `failed_forms` for the reloaded module (external repair currently re-corrupted by next regen — data-loss loop) + unit cell; **M-1** `/reset` same clear; **M-4** eprint swallowed IO error on `recover_startup_failure` None-path; **M-5** extract shared verbatim-slice gate helper; **M-2** pull.rs `Ok(None)` regen arm if trivial (else report). I-1/I-3/I-4/M-3 → 0507 addendum 2 (design taxonomy); I-2 repro + B-1 e2e + I-5 pin + ledger annotations → next /qa pass. | **done** (`25e1e42`, `084091d`; B-1 fixed at `reload_module` general seam — all reload paths; M-1/M-4/M-5 done; M-2 skipped design-gated; 3615/3576/39/1, fail set unchanged, +3 green cells) |
| 6 | /dev → /review | cranelisp-backend (+primitives) | **B1-be**: 0476 consumption (vec trio→`Inline`, stop-predicate→`is_callable_target()`, name-list retirement) — **golden diff EMPTY is the change-set's own witness; FIXME 0476 deletes here** — + 0482 `#[non_exhaustive]` + baseline regen; then **B3.0** 0495 tests.rs relocation (pre-seam, mechanical). | **done** (review: all 3 CS CLEAN, zero findings; empty diff = "no corpus regression" NOT value-use proof — real witness = unit tests now + B3.1 re-baseline; `resolve_is_callable_target` VERIFIED correct for B3.1; gate was NOT pre-0476-buggy; 0495 partial deferral endorsed) |
| 7 | /dev → /review | src/ | **A-3** (file-backed dev-loop): CS-D3a (`install_module_session_env` at every install route) → CS-D3b (FQ `resolve_symbol_arg` shared across introspection) → CS-0487 (`/mod` scope + `/refs` via `redefine::ReverseIndex`). Guard flips: D3. Carries: Wave-5 (a)-(d), Wave-4/6 ReverseIndex `__expr` exclusion + 0507-Issue-2 macro-clause question, 0505 `/mod` env-parity. | /dev + /review done (approve, 1 Important: `/refs` variant-leak/double-list → Wave 7R; mirror FAITHFUL, no Blockers) |
| 7R | /dev src/ | src/ | **`/refs` fix** (Important, before W8): `collect_referers` pushes reverse-index callers verbatim → leaks `$`-mangled variant names (`m/g$Int`) + double-lists (mono variant + base token-scan). Apply `base_fq()` before push (mirror `stale_callers` which already does; do NOT copy its `code: Some` filter — /refs wants all referers) → existing sort+dedup merges legs. One-line + unit cell (mono-variant caller reports at base grain). | **done** (`2b57415`; `base_fq` applied, promoted pub(crate); dedup merges legs; `code: Some` filter NOT copied (intentional); failing-first confirmed; 3634/3602/32/1 fail set unchanged) |
| 8 | /dev → /review | cranelisp-typecheck (first), then cranelisp-primitives, then cranelisp-typecheck | **0488 fixes open the typecheck visit** (Wave-2 attribution: all typecheck-side; shapes + regression scope in `tests/plan/0488-isolation.md`): **CS-488ab** (mono-derivation reference-shape misses — FQ-callee collection incl. same-module + cross-module raw-key probe, imported fn-value gate at program.rs:3629 + `home: None` mint; flips 3 guards; unit shapes U-a1/U-a2/U-b) → **CS-488c** (fold-shape scheme over-generalization; flips 2 guards; U-c1/U-c2; **named regression scope: `polymorphic_accumulator_fold_does_not_over_unify` stays green** — if the fix requires reshaping generalization beyond a scoped tie, STOP and escalate to /design(typecheck)). Both emission-affecting → corpus EXTENSION rides each CS. Then **B2**: CS-B (declared-fact table on the `mode_summary` slot; ring2-rc §3.3 + `neq-string` row; `PrimitiveExtern` = pinned Decision-24 boundary) → CS-1 (classifier + 0476 typecheck-side consumption) → CS-2 (pure transfer fn) → CS-3 (fixpoint+SCC+confinement+memo; toggle gate at driver entry — toggle-off emits NOTHING) → CS-4 (publication + **H5 `CRANELISP_OWNERSHIP_TRACE`**). New `ownership/` module cluster with per-submodule sibling tests (P23 from birth). **0497 rides** the typecheck visit — same-file contention with CS-488ab (`traits/monomorphise.rs`): same agent, sequenced, never parallel. **Split for execution: Wave 8a = 0488 fixes (CS-488ab + CS-488c); Wave 8b = pass5 ladder (CS-B primitives + CS-1→CS-4 typecheck + 0497).** Wave 8b further split by crate: **8b-1 = CS-B (cranelisp-primitives fact-table); 8b-2 = CS-1→CS-4 + 0497 (cranelisp-typecheck).** | 8a done; **8b-1 /dev done** (`9ca65ef`; golden EMPTY, 21 green tests, 27 unchanged; FIXME 0510 filed — `neq-string` shim-only, no entry); **8b-2 /dev done** (`04ba05b` CS-1, `7f2f4a3` CS-2, `de6c1c6` CS-3, `367361d` CS-4, `2f61aff` 0497+0511, `a582b5e`; **golden EMPTY + toggle-off field-identical** — both soundness witnesses; H5 works, 2 flips; 53 unit tests; 0497 partial; FIXME 0511 filed); **8b CLOSED by /review** (approve CS-B/CS-1/CS-4; **2 Blocker + 1 Important ADVISORY-half unsound narrowings** in CS-2/CS-3 → FIXME 0512, **pre-Wave-11 preconditions, NOT seam blockers** — ABI half is sound; toggle-off + golden witnesses hold; fixpoint modes monotone+terminating; CS-B facts correct; 0511 in-pass memo sound) |
| 9 | /qa + /dev src/ | — | **SEAM CHECKPOINT — PUSH-THROUGH taken (seam NOT a close point)**: (a) /qa runs **I-G5 (compile ≤+10%) + I-G6 (REPL turn ≤1.10×)** — folded into Wave 13's full I-G1..I-G7 gate run since pushing through; (b) /dev src/ small CS: R3 `AbiSurface` mode-vector widening via `ModeSummary::abi_eq_opt` — now a pre-Wave-11 item (rides with the 0512 typecheck pass or its own src/ CS before mechanisms make modes ABI-real). | superseded — push-through |
| 8c | /dev → /review | cranelisp-typecheck | **FIXME 0512 advisory-half soundness fixes (MANDATORY pre-Wave-11)** + **0497** (now UNBLOCKED — 0488 landed in program.rs/checker.rs, monomorphise.rs untouched) + R3 `abi_eq` widening input: fix (1) binding-mediated escape/`param_flow` narrowing, (2) confinement single-pass → worklist fixpoint (transitive `spark_ops`), (3) §13.6(d) let-shadow provenance→None, (4) cap-exhaustion widen-to-⊤; each with its failing negative cell (the untested soundness-edge shapes the review named). Then 0497 `traits/tests.rs` split. | pending |
| 10 | /dev → /review | src/ (+ cranelisp-frontend if capacity) | **A-4 display/diagnostic tail** (capacity-gated per scope item 5): trap-format §18.5 (`program_outcome_to_result`), 0490 (one confirmation step first), 0484 rejection fix per /spec §8.6.4 ruling (re-evaluate typecheck-rehydration hypothesis under rejection polarity), 0492 (`handle_sig` FQ per §3.8), 0493 (owner attributed at fix time). If frontend opens: 0485 span fix + arity-error wording (D4a) + **0500 rides**. **Split: 10a = src/-owned display batch (0492/0493/0490/trap-format + 0484 if src/-side); 10b = frontend 0485/0500 + 0484 per /spec ruling.** | 10a /dev done (`1fcb896`…`1e9639e`; 0492/0493/0490/trap fixed, 10 flips; 0484 STOPPED+reverted→needs /spec glob ruling; FIXMEs 0490/0492/0493 deleted, 0513 filed); 10a /review + 0484 /spec ruling dispatched |
| 11 | /dev → /review | cranelisp-backend | **B3.1–B3.5 mechanism ladder** (post-seam; each CS independently landable, scoped re-baseline riding): B3.1 fn_as_value seam rework — 0483 root-cause FIRST (collision is a hypothesis) + 0474 `SourceOwnership::{Owned,Borrowed}` RC-polarity + the two Wave-1-discovered seam defects (span-keyed `curry_drop_glue_{span}` collision — the forbidden (span×discriminator) pattern — and the generic-fn-returned Vec temp leak) + corpus extension (guards 0474×3/0483×3 + the 2 W1 guards flip; **0488 rider OFF** — Wave-2 attribution: typecheck-side, fixes moved to Wave 8) → B3.2 borrow-elision core, one atomic CS (H2 counters; I-G1/I-G2; biggest re-baseline) → B3.3 confined non-atomic RC (I-G3) → B3.4 stack slots (I-G7; L-C2 ASan) → B3.5 `str-len$borrowed` sibling (S5). B3.3/B3.4 individually droppable at squeeze. **0495 remainder** (thin-submodule drain) rides. | pending |
| 12 | /dev → /review | cranelisp-backend | **B4**: 0459 remainder — lenient-eval §2.7 floor-restoring density gate on `is_worth_sparking` (facts-absent ⇒ axis inert); I-G4 progress measured. | pending |
| 13 | /qa | tests/ | **Gate + wrap** (push-through — full increment I): full `ig_gates` **I-G1–I-G7** run (I-G5/I-G6 fold in here since not close-short); L-M1 (rides B3 landing); guard-flip verification against the Wave-map; corpus EXTENSION for newly-green shapes (0483/0488/0484 + the Wave-8a 0488 shapes owed); the module_grain_reload degrade + file-deleted repros (Wave-7 /qa carries); B-1 external-repair e2e; I-2 macro-redefinition-restart repro; 0508 collision repro; intentional-count reconciliation drafted for Phase 7 (root-CLAUDE.md §Testing update is user-flagged). | pending |
| tail | /dev | types/intrinsics/platform | **Capacity-gated drains**: 0498 (types was open at CS-A), 0501, 0502 — dispatched only if capacity remains post-Wave-12; else re-deferred with rationale at Phase 7. | pending |
| tail-test-splits | /dev | typecheck, backend | **Deferred test-hygiene splits** (Principle 23): **0497** typecheck `traits/tests.rs` split — deferral rationale (monomorphise.rs contention w/ 0488) **EXPIRED** (Wave 8a fixed 0488 in program.rs/checker.rs, never touched monomorphise.rs); NOW UNBLOCKED → run as a quick pass after the 8b review clears (typecheck otherwise done). **0495** backend ~600-LOC harness extraction into `test_support` — **push-through decided → Wave-11 PREP** (B3.1 needs clean submodule test homes; runs right before Wave 11). | pending |

## Notes

- 2026-07-03: Phase 1 scope drafted from S101 close record + ROADMAP §Phase-H table + `tests/plan/coverage-audit-s101.md` risk register. Presented to user for approval.
- 2026-07-03: **Scope APPROVED by user as proposed** (one interleaved sprint, close-short seam after B2 named). Phase 2 /arch review dispatched.
- Operational rule carried from S101 finding 5: defect reports from parallel 6b proxies must be validated against the landed tree before hotfix dispatch.
- 2026-07-03: **Phase 3 COMPLETE** — six design plans collected; /arch CS-A landed (v11→v12, all 12 needs-list items + backend rider; nextest 3496 run — 3474/22/1, fail set = intentional guards) with exit-gate **PASS**. FIXMEs resolved in-phase: 0459, 0494, 0503, 0504; 0476 types-half landed (deletes at B1-be); 0492 retargeted /int. **T1 full cure ruled OUT → S103** (pre-approved scope branch: A2/A4 fixes are hard preconditions; print ships as mitigation).
- 2026-07-03: **Phase 4 COMPLETE** — 13-wave serial ladder written (§Waves); Status → PHASE 5. CS-A + Phase 2–4 docs committed (`7800b9a`, `b203857`); user approved dispatch.
- 2026-07-03: **Wave 1 DONE** (/qa stage-1, 8 commits). New baseline **3578 / 3529 / 49 / 1** (27 new intentional REDs, ledger-itemized with flip waves). Deliveries: 5 of 7 lanes exist; L-B1 corpus turnkey (13 MANIFEST entries, `clif_golden.sh` selftest passes on HEAD — G-1 answered: harness-side sorting suffices, H1 likely no-op); `ig_gates.py` ready incl. seam-mandatory `--gates g5`; I-G6 ready. **Two NEW defects discovered + guarded, both B3.1 seam**: span-keyed `curry_drop_glue_{span}` identifier collision (the exact (span×discriminator) anti-pattern backend §13 forbids — direct B3.1 input) + generic-fn-returned Vec temp 1-alloc leak (0474-adjacent). FIXME 0505 filed (/repl, `/mod` env-parity pin). 0499 stays open annotated (L-S1 + L-M1 remainder). Findings: RC_STATS ±1 exit jitter (fences carry ±2 tolerance); one 1-of-3 flake of pre-existing `mixed_blocking_and_poll_par_overlaps_on_both_pools` (recorded, symptom-framed, not chased); suite ~110s. Root-CLAUDE.md count update (22→49) = Phase 7 user-flagged.
- Wave-gate scan before Wave 2: open /qa-targeted FIXMEs (0474/0483/0484/0486/0488/0491/0493/0499) all have explicit in-sprint wave dispositions in this plan — none blocks; 0505 targets /repl (not in wave).
- 2026-07-03: **Wave 2 DONE** (/qa 0488 isolation, 2 commits `389ae46`/`e1340bc`). **All 3 signatures typecheck-side** ("never minted"; the two error classes encode reference position, not seam; REPL ≡ `--run`, src/ + backend + `derive_codegen_batch` exonerated). Two root-cause mechanisms: (a)+(b) mono-derivation reference-shape misses (FQ-callee collection gates + imported-fn-value gate/`home: None` mint) → CS-488ab; (c) fold-shape scheme over-generalization (over-general template scheme → outer site fails all-args-concrete guard) → CS-488c with 0344-guard regression scope. +2 RED reductions (#28/#29) + 2 controls; guard map 0488 ×3→×5; **baseline 3582 / 3531 / 51 / 1**. Wave-11 B3.1 rider OFF; Wave 8 reshaped (0488 CSes open the typecheck visit; 0497 same-file serialization pin). Dirty-dir probe hazard noted (phantom mints from `user.cl` + cache restore — fresh-dir re-verified).
- 2026-07-03: **Wave 3 /dev DONE** (`05818e9`, `da465d6`; baseline **3582 / 3532 / 50 / 1**, exactly the one expected flip). Findings: (a) **Wave-1 selftest was a FALSE GREEN** — harness parsed stdout but `CRANELISP_CODEGEN_DUMP` frames arrive on stderr; every capture was empty (empty==empty passed). (b) Stream fixed → 6/13 entries nondeterministic under last-occurrence dedup: recompilation passes re-derive the JIT symbol set after scheduler-timing-dependent registrations, shuffling `u0:N` FuncId immediates; **first-occurrence (initial cold-cache compile) frames ARE byte-deterministic** (20/20 + 13/13×2) → dedup flipped LAST→FIRST. (c) H1 = NO-OP, evidence-based (zero mid-frame interleaving in 25 dumps), recorded at §13.1. (d) 13/13 goldens committed (292K), double-capture gated, fresh diff EMPTY. **Cross-ownership flag**: /dev(backend) edited /qa-owned `clif_golden.sh` + smoke (blocking the turnkey capture; loudly flagged) — /qa ratifies at its next wave. **Caution for all post-capture waves**: oracle pins FIRST-occurrence frames; pre-B0 conclusions from the old script are void. /review dispatched on the dedup-policy soundness question.
- 2026-07-03: **Wave 3 CLOSED by /review — pin SOUND, oracle trustworthy, nothing blocks Wave 4.** Review corrected the mechanism: duplicate frames = **nice-worker `.o` cache-write pass** (not "recompilation"); 115 dup-pairs diffed — zero diverge beyond FuncId immediates; 0483/0474 classes provably visible in first-occurrence frames (wrapper/slot/RC signal derived pre-codegen, shared emission path). Scope pin: oracle sees JIT-pass emission only; object side guarded by mode-equivalence lanes. Structural caveat: first-occurrence-=-JIT relies on nice-thread priority (TypecheckDone claim race, `scheduler.rs:1082`) — loud-flake risk, not false-green; cure = `--no-cache` capture (verified byte-identical, zero churn). Findings routed: **F1/F5 (§13.1 normative gap + mechanism correction + stale :980 claim) → FIXME 0506** (/design backend, due ≤ first B3.x re-baseline); **F2 (config-pin holes, `CAPTURE_BORROW` live) / F3 (non-empty guard) / F4 (`--no-cache` adoption) / F6 (dual-dialect extraction watch) → Wave 3R** (/qa, before B1-be — its diff-EMPTY witness is only as trustworthy as the lane's pinning); MANIFEST SHA-fill added to the ratification scope (under-reported in the W3 flag). FuncId shuffle disposition: benign now, Phase-H reproducible-builds note folded into 0506.
- 2026-07-03: **Wave 3R DONE** (`b82ebf1`; baseline identical 3582/3532/50/1, fail set name-verified). `--no-cache` capture adopted (13/13 selftest + diff EMPTY, goldens untouched); dedup deleted → duplicate frame = hard error both sides; non-empty guard demonstrated end-to-end; frame-name-mismatch assert added. Pin set enforced both sides incl. **two emission-affecting vars named nowhere in spec**: `CRANELISP_RC_DEC_CHECK` + `CRANELISP_NO_IO_SCHEDULE` (→ 0506 addendum; MANIFEST pin record updated). /dev(backend) cross-ownership edits RATIFIED (dedup halves superseded, not reverted). Operational pin for all oracle users: captures/diffs go through `clif_golden.sh` ONLY — hand-rolled dump runs will double-frame and hard-error by design.
- 2026-07-03: **Wave 4 /dev DONE** (`a605290` T1-print, `d139a83` 0491, `3583e26` 0486; baseline **3591 / 3546 / 45 / 1**, exactly the 5 named flips + 9 new green unit tests; FIXMEs 0491/0486 deleted). T1 print: `RedefinitionOutcome.prior_was_def` + `is_t1_downgrade()` + variant-aware compiled-only `stale_callers()`, base-defn-grain reporting; **commit gate now emits outcomes for every staged Def over a prior live Def** (both T1 shapes covered; Import-shadow + fresh stay false — 0484 territory untouched). 0491 cured at the feed (index build), not the rendering. 0486: `EvalResult::Def.defined` flag + extracted seam `record_defining_turn_source`. §18.1.1 bytes pinned by unit `report_render_stale_section_exact_header_and_omission`. **Carries for Wave 5 (A-2)**: (a) D2 regeneration consumes the new `record_defining_turn_source` seam; (b) 0489's degraded form-by-form re-drive could print `stale:` sections against a warm table — check when wiring suppressed-output loads; (c) `main.rs` still regens the backing file on display-only bare lookups (harmless no-op) — gate on `defined` if A-2 wants regen-silence (regen surface is A-2's).
- 2026-07-03: **Wave 4 CLOSED by /review** (approve-with-findings ×2, approve ×1; commit-gate widening verified clean — outcome consumers audited, retention byte-equivalent, ABI-epoch untouched; "display mutates state" recurrence sweep CLEAN; S103 boundary respected; unit tests confirmed genuinely seam-level). Findings routed: **F1** (broken-clear loop misses `New`-classified redefinitions with `prior_was_def` — recovery inconsistent across the three T1 shapes; one-line predicate + unit cell) + **F4** (dead belt-and-braces mirror in `stale_callers`) + **F6** (regen-on-display gating, BOTH `main.rs:441` and P8 mirror `agent/pull.rs:407`) → **Wave-5 brief**; **F2** (trigger over-fires slotted→slotted late-binding ctor shape — §18.1.1 negative-MUST risk; slot refinement proposed) + **F3** (0491 exclusion's safety argument doesn't cover persistent `__macro_*` clauses; reachability confirmation needed) + **F5a** (macro-target outcomes dropped at eval.rs:329 — S103 pinning note) → **FIXME 0507** (/design src/, gates §18.1.1 `[Tested+Neg]`, resolve in-sprint); **F5b–d** (ctor cell, cross-module mint edge pin, trapped-caller-in-stale-set corner) → /qa later wave; **F7** (per-outcome ReverseIndex rebuild in begin-clusters) noted only.
- 2026-07-03: **Wave 5 /dev DONE** (5 commits; baseline **3612 / 3573 / 39 / 1**, exactly the 6 ledger flips, two identical consecutive runs; +21 unit tests incl. lifecycle.rs's FIRST unit module; FIXME 0489 deleted, 0496 annotated-open with precise remainder). D1 needed a third leg beyond design: **defmacro name-position shield** in the expansion walk (cache-preloaded zero-arg macro re-poisons its own redefinition's name on restart recompile). D2 = consistency-gated verbatim emission (`sexp_matches_source` reader-desugar-aware + docstring §5.12 slot check; falls back to render). 0489 = REPL-only degraded form-by-form re-drive, output suppressed, **startup-print ruling: SUPPRESSED** (drains `pending_cascade_reports`; startup is a load, not a redefinition turn) — /design pin via 0507 addendum. Riders: F1 predicate + 3-shape recovery cell; F4 filter DELETED (feed = single authority); F6 both sites gated on new `is_defining()`, shape-only `is_def()` REMOVED. Hybrid-meta re-probe: NO repro (stays exemplar-only). Design corrections for /design(src/) via 0507 addendum: §5.2 "error_modules gates nothing" is wrong (gate was wired; the change was the definition carve-out). Known residual: cache-restored expansion-artifact macro w/o introspection falls back to expanded `macro_sexp` at regen — unreachable today, resolves with W7 D3 file authority. **Carries for Wave 7**: (a) `resolve_recheck_sexps`/`module_grain_reload` go LIVE once D3a makes `file_path` Some — degraded loader could converge on same authority; (b) decide whether cache-restore installs set `source_text`; (c) `failed_forms` × `/mod M` interaction untested (Matrix D cells); (d) residual in (8) resolves with introspection rehydration.
- 2026-07-03: **Wave 5 /review** — approve ×3 + approve-with-findings ×2 + **BLOCKER B-1**: `poll_and_reload` clears `error_modules` but never `failed_forms` → user's external repair (the design's own escape hatch) is silently undone by the next regen appending the stale broken form; next restart re-fails — the exact data-loss class the cluster guards. Doubly load-bearing: for symbol-less failed forms external edit is the ONLY repair path (I-1). D2's consistency gate verified structurally safe BOTH directions (false-CONSISTENT excluded at reader-equality level). Findings routed: **B-1 + M-1 + M-4 + M-5 (+M-2 if trivial) → Wave 5R** (/dev, in-wave); **I-1 (repair carve-out refuses macro-mediated/`:Type` definitions — stdlib-def users expression-locked) + I-3 (shield = spot-patch on binder-position class) + I-4 (single-authority is section-8-local) + M-3 → 0507 addendum 2** (/design src/); **I-2 (SUSPECTED macro-redefinition restart-revert — recording is only-if-absent; needs repro) + B-1 e2e cell + I-5 reachability pin + 6 flip annotations → next /qa pass**; M-6 recorded no-action. Deviations judged sound as-built (shield necessary for D1's own guard; §5.2 correction verified).
- 2026-07-03: **Wave 5R DONE** (`25e1e42`, `084091d`; baseline **3615 / 3576 / 39 / 1**, fail set unchanged, +3 green cells). B-1 fixed at `reload_module` general seam (covers watcher poll AND T2 `module_grain_reload`) — invariant "failed set non-empty ⟺ error-blocked" restored both edges; unit failing-first confirmed. M-1 (`/reset` clear), M-4 (startup IO-fail eprint), M-5 (`save::verbatim_slice` shared helper) done. **M-2 SKIPPED — design-gated**: agent Build `submit` persisting structural (non-def) forms is a /design question (should the definition-spec'd tool accept imports at all?), e2e-shaped test — NOT a surgical W5R fix. Left for /design. **Wave 5 fully closed.** /qa carry-list for its next pass: I-2 macro-redefinition-restart repro, B-1 external-repair e2e (drive real watcher path), I-5 reachability pin, 6+ flip annotations, M-2 pin iff design rules it in.
- 2026-07-03: **Wave 6 /dev DONE** (`534e95c` B1-be, `30ea2b4` 0482, `98982fd` B3.0; baseline **3618 / 3579 / 39 / 1**, +3 green units, fail set unchanged; **golden `clif_golden.sh diff` EMPTY** across all 13 — the 0476-consumption witness, re-confirmed after all 3 commits). B1-be: vec trio → `PrimitiveBody::Inline` no-slot, `resolve_vec_query_primitive` re-keyed off Inline (S101 name-list retired), stop-predicate → `is_callable_target()`. **Extra seam found+fixed**: `is_known_function` fn-as-value gate used slot-returning `resolve_got_target` (misses slot-less inline) → new `resolve_is_callable_target` resolver; byte-identical for slot-carriers. FIXMEs 0476+0482 DELETED. 0482 sibling audit: `CacheManifest`/`CachedModuleRef` safe (via `new_for_host()`, not struct-literal-external) — `#[non_exhaustive]` on all three; public-api.txt diff = 3 marker lines. **0495 PARTIAL** (kept open+annotated): trap-stub trio relocated; 73-test remainder gated on extracting the ~600-line shared harness into `test_support` — a focused import-rewrite CS deliberately not bundled with the golden-witness CS. **Carries for W8/W11**: fn-as-value gate now recognizes inline primitives via `resolve_is_callable_target` (NOT `resolve_got_target`); `PrimitiveBody::Inline` is the sole vec-trio discriminator (name-list gone) — B3.1's `__inlwrap_{bare}_{sig}__` keys off it; B3.0-continuation (harness extraction) should land before B3.1 scenario-matrix tests need submodule homes.
- 2026-07-03: **Wave 6 CLOSED by /review** — all 3 change-sets CLEAN, zero Blocker/Important findings. **Q1 sharpened**: `insert_vec_query_entries` runs LAST (lib.rs:215), so dropping the trio's slots renumbers ONLY vec-len's slot (−3); no other primitive shifts. Empty diff proves "no corpus regression + every non-vec path byte-identical" but NOT the value-use path (vec-family value-use corpus-EXCLUDED under 0483's guard) — **real witness = the seam unit tests now + the B3.1 re-baseline** when 0483/0488 flip and those shapes enter the green corpus (design's stated sequencing, not debt). The flip can only have improved the value-use path (NULL-slot→no-slot makes the single-instantiation SIGSEGV foot-gun structurally unrepresentable); residual 0483 SIGBUS is a separate defect. **Q2**: `resolve_is_callable_target` VERIFIED correct — byte-identical for slot-carriers (shares `resolve_driven`; only divergence is Inline = vec trio), no over-admission (excludes PrimitiveExtern/Macro/templates), right predicate at the gate, downstream slot-consumers safe. **Q3**: fn-as-value gate was NOT pre-0476-buggy (every callable carried a slot then; 0476 SPLIT callability from slot-presence per Principle 20, same CS closes the gap — clean root-cause fix). Four resolvers NOT a mirror (distinct questions, one `resolve_driven` walker, name-list genuinely retired — grep-confirmed). One Suggestion (pre-existing `is_vec_primitive` stringly list at apply.rs:1653 — call-position dispatch, legitimately different question, no action). 0495 partial deferral ENDORSED (bundling harness extraction would have muddied the empty-diff witness). Optional /qa carry: track the value-use e2e-witness gap explicitly ahead of B3.1 (else left to re-baseline) — folding into the /qa next pass carry-list, no separate FIXME.
- 2026-07-03: **Wave 7 /dev DONE** (`5e128e3` D3a, `ff63a02`+`83eac1b` 0487; baseline **3633 / 3601 / 32 / 1**, exactly 7 flips + 15 green units; FIXME 0487 deleted). D3a: shared `imports::install_module_session_env` at every install route (worker cache-restore, CachedLoad, blank `/mod M`) + `TypecheckProduct.file_path` authoritative on ALL routes. 0487: `resolve_symbol_arg`/`resolve_entry_arg` shared across all introspection commands + `/refs` = ReverseIndex ∪ token-scan. **CS-D3b DISSOLVED** (no commit — D3 faces resolved by D3a; guard extension is /qa's). Wave-5 carries: **(a)** `resolve_recheck_sexps`/`module_grain_reload` now LIVE + verified; **(b)** source_text at cache-restore = DECIDED NOT (rehydration reads file directly, no consumer); **(c)** `failed_forms × /mod M` = structurally unreachable (failed_forms is startup-entry-only); **(d)** residual CLOSED (backing file round-trips single-authority; the `(x)` runtime error is a PRE-EXISTING bare-Int macro-return marshalling limit, not D3). `/refs` × macro-clause gap NOTED for 0507 (not patched by weakening 0491). **Carry for Wave 10**: 0492 fix = flip `handle_sig`'s `was_qualified || is_import` guard so LOCAL renders via `format_def_entry` too (CS-0487 kept local BARE on purpose to not pre-flip the 0492 guard); all introspection commands now uniformly FQ-arg-aware so display-batch fixes can assume it.
- 2026-07-03: **Wave 7 CLOSED by /review** — approve ×3, **no Blockers**, 1 Important. Enabled-dead-code risk assessed: rehydration-SUCCESS path PROVEN by D3 guard; `module_grain_reload` T2-degrade branch + drift-mis-attribution + rehydration-failure are newly-LIVE-but-UNWITNESSED — all code-verified graceful (reload holds cloned SCC data not live refs; failure → conservative `mark_broken`, no panic/lockout) → routed to /qa as narrow repros (not blocking). **Mirror `install_module_session_env` verdict: FAITHFUL** (prelude bit + import + submodule aliases match all 3 fresh-path sites; exports correctly omitted — `install_exports` registers no module_aliases). Carries CONFIRMED: (b) source_text-not-set SAFE (sole consumer falls back to pretty_print; never regressed), (c) failed_forms×/mod unreachable CONFIRMED (only inserter is startup-entry `recover_startup_failure`). Q3 file_path false-Some CLEAN (blank `/mod M` stays None; entry fallback path identical to regen target). Q7 0492 carry ACCURATE (bare-local `/sig` untouched, W10 fix lands clean). **Important → Wave 7R**: `/refs` variant-leak/double-list (`collect_referers` repl.rs:986 — Principle-7 asymmetry vs `stale_callers` which base_fq's). **Later /qa** (non-blocking): module_grain_reload degrade repro (highest-value unwitnessed path) + file-deleted-mid-transaction cell. Suggestion: `/refs` token-scan module-blindness (pre-existing → 0507 drain).
- 2026-07-04: **Wave 7R DONE** (`2b57415`; `/refs` `base_fq` normalization, `code: Some` filter intentionally NOT copied, failing-first cell; **3634 / 3602 / 32 / 1**, fail set unchanged). Wave 7 fully closed.
- 2026-07-04: **Wave 8a /dev DONE** (`1ca0c56` CS-488ab, `1b75c90` CS-488c; baseline **3641 / 3614 / 27 / 1**, exactly 5 0488 flips + 5 green units; golden diff EMPTY; FIXME 0488 deleted). CS-488ab: new `checker.rs::resolve_terminal_fq_or_prelude`; pass-4 collectors record BARE terminal symbol + thread callee `home` (dropped `home==current` gate). CS-488c: root cause = generalization ORDERING bug (`vreduce` forward-refs later `vreduce-loop`; 0344 writeback froze under-tied scheme before helper tied acc↔result) → new `resettle_polymorphic_schemes` re-runs idempotent generalization earlier, scheme-only, constraint-free-gated — **scoped result-tie, NO escalation; `polymorphic_accumulator_fold_does_not_over_unify` stays green**. **Corpus EXTENSION owed to /qa** (a/b FQ-call + imported-value-use + c fold-composition shapes — not yet added). **Carries for 8b**: `monomorphise.rs` UNTOUCHED (0497 same-file contention clear); `resolve_terminal_fq_or_prelude` reusable by pass5 collectors; `resettle_polymorphic_schemes` precedes any pass5 pre-body pass (O(N×P)).
- 2026-07-04: **Wave 8a CLOSED by /review** — approve ×2, no Blockers, nothing blocks 8b. **CS-488c re-settle verdict: SOUND** — re-derives each scheme from fixed source vars (not accumulative), so monotone toward more-tied; a partial subst can only add ties finalize would also add, never manufacture a spurious one; idempotent; constraint-free gate correct (`Num a =>` folds are `Constrained` not `Polymorphic`, skipped); fresh-instantiate protects later siblings. No over-tie fold shape exists. `resolve_terminal_fq_or_prelude` VERIFIED a pure extraction (byte-identical for existing callers) — safe for 8b. **Findings**: **Important → FIXME 0508** (/qa repro + /arch mangling question): bare home-independent mono-mangle key collides for two same-named imported generics at one arg type → silent wrong-dispatch, NEWLY reachable by CS-488ab (traded loud "undefined function" for silent mis-mint in that narrow shape; uncommon; not a Blocker). **Debt → FIXME 0509** (/design typecheck): `resettle_polymorphic_schemes` compensates for the 0344 ordering bug rather than curing it (root cure = topo-order or defer-writeback, O(n) + complete); O(n²) cost + reverse-definition-order under-tie gap (same 0488(c) symptom, uncovered). Both non-blocking; documentation-sufficient + repro.

- 2026-07-04: **Wave 8b-1 /dev DONE** (`9ca65ef`; golden EMPTY, 21 green tests, 27 unchanged). New `ownership_facts.rs` name-keyed `declared_mode_summary` at the primitives declaration site (Principle 7/19); `insert_primitive_entry`+`insert_vec_query_entries` populate `mode_summary` at construction. Facts by group per ring2-rc §3.3. **Gap → FIXME 0510** (/design backend): `neq-string` is shim-only (Eq.!= trait-dispatch), no `ModuleEntry` to populate — classifier transcribes its Borrowed row for when an entry exists; `==`/`!=` string asymmetry until resolved. Every in-crate primitive carries `Some(ModeSummary)`; conservative-default (`None`) only for entry-less shims.
- 2026-07-04: **Wave 8b-2 /dev DONE** (6 commits; baseline **3715 / 3690 / 25 / 1**, −2 H5/l_d3f flips + 53 unit tests; suite ~60s). New `ownership/` cluster: CS-1 `classify.rs` (§2.1 pure classifier + scalars-only Copy), CS-2 `transfer.rs` (pure MonoExpr walk → summary+sitefacts+DepSet), CS-3 `fixpoint.rs`+`confinement.rs` (toggle-gated driver wired into `finalize_check_result_inner`, monotone worklist, DepSet re-entry, stratified confinement), CS-4 `sites.rs`+`trace.rs` (one-shot site-fact annotation + H5 trace). **BOTH soundness witnesses CONFIRMED**: golden diff EMPTY (13/13, after CS-3 AND CS-4) + toggle-off `.meta.json` field-identical (`mode_summary: null`×4). H5 shows the read-path win on real corpus (accessors → ProjectionOf(0)+Borrowed+provenance). **§13.6 judgment calls flagged**: (i) session memo → shipped SOUND in-pass memo instead (TypeCheckEnv is per-call borrow-only; cross-invocation memo needs int-threaded session field; determinism ⇒ absence = recompute cost never wrong) → **FIXME 0511 (/design)**; (ii) `param_flow` ambiguous stores default to conservative `Retained` (advisory-half, sound; ABI-bearing modes/result precise). 0497 PARTIAL (dispatch pool relocated; 1293-LOC traits/tests.rs split re-deferred — coordinates with 0488 isolation on monomorphise.rs shared tree; FIXME 0497 kept open + remainder trigger). **Seam-ready**: pass5 RUNS on real clusters (H5-verified), toggle gives clean on/off differential for I-G5/I-G6. **Wave-11 carries**: accessors `ModuleEntry::mode_summary()` (persisted) + `MonoDefnVariant.mode_summary` (in-hand), ⊤-on-absence; site facts on codegen_view (`unique_static` present-but-always-None in incr I); provenance shadowing⇒None⇒D24; `value_use()` for wrapper gating; confinement emits boolean `confined` + `spark_ops`; **summaries UNCONSUMED today — empty golden diff is the contract any W11 consumption must re-baseline against**.
- 2026-07-04: **Wave 8b CLOSED by /review (soundness pass)** — the load-bearing audit. Verdict: **ABI half SOUND** (`param_modes`/`result`, what R3 consumes at the seam) — every ambiguous classify/transfer/fact-table shape errs toward Owned/Crossing; fixpoint modes monotone + terminating; DepSet complete; toggle-off no-leak (gate is first statement of `run_pass5`); CS-B facts all correct vs ring2-rc §3.3 (incl. `string-identity=AliasOf(0)`, the §9.1 Borrowed/Consumed split); 0511 in-pass memo sound (nothing partial published as final). **BUT 2 Blocker + 1 Important ADVISORY-half unsound narrowings** → **FIXME 0512** (pre-Wave-11 precondition, NOT a seam/current blocker — invisible to golden diff because nothing consumes advisory facts until W11): (1) escape+`param_flow` narrow below truth for binding-mediated escaping fresh aggregates (`(let [box (Some x)] box)` → escapes=false + flow=Consumed vs IntoResult; W11 stack-alloc UAF); (2) confinement single unordered pass under-propagates transitive `spark_ops` (W11 non-atomic-RC data race; order-dependent); (3) §13.6(d) let-shadow provenance guard unimplemented (wrong alias root). Plus (4) cap-exhaustion publishes too-precise summaries (unsound failure direction). **Seam-clean**: R3 consumes only the sound ABI half; pass5 stays landed; nothing to revert. 0512 rides to S103 (close-short) or is a mandatory pre-W11 pass (push-through).

- 2026-07-04: **Wave 10a /dev DONE** (`1fcb896`+`6164dc1` 0493, `5e83a66`+`63aba28` 0492, `96767e2` trap, `b026d3d`+`1e9639e` 0490; 0484 `85a44bb`+`3ecf9e7` REVERTED by `3ba2ca8`+`a928417`; baseline **3719 / 3704 / 15 / 1**, 10 flips, golden EMPTY). 0493: `display.rs` `split_once(' ')` split inside parameterized `type_display` → space-safe `strip_type_prefix`. 0492: `handle_sig` bare-local now routes through `format_def_entry` (dead short-form renderers deleted, Principle 8). trap: int-side `ExprOutcome{Value,Trap}` + `EvalResult::RuntimeError` (no types change) → normative `runtime error:` format. 0490: int-side `phantom_member_diagnostic` names the real module (+ FIXME **0513** /typecheck for the deeper `checker::lookup` ordering cure). **0484 STOPPED+reverted** — rejection regressed ~30 prelude tests (glob-re-export-of-seeded-ADT shadow is legit + indistinguishable from specific-import collision at commit gate) → needs /spec glob-exemption ruling (addendum on FIXME 0484); guards stay RED. FIXMEs 0490/0492/0493 DELETED. **/qa test-authoring bug**: `display_exact_option_in_option_value_line` uses `repl()` w/o prelude (bare `Some` unresolvable) — /qa fix owed (0493 verified correct via qualified form); folds into W13.

- 2026-07-04: **USER-ARBITRATED LANGUAGE RULING (0484 resolution)** — the user is sole arbiter of the language (`/spec` scribes; see `memory/feedback_spec_scribe_user_arbiter.md`). Ruling, in the user's terms: **"An attempt to create a public symbol with the same name as another module's symbol is resolved not by importing the dependency, but using fully-qualified references."** Consequences: (a) creating a public symbol whose name collides with an imported name is a **compile-time error** — NOT resolved by the import system (no shadowing, no precedence, order-independent, all modes); the other module's same-name symbol is reached **fully-qualified** (`module/name`). (b) Uniform — no glob-vs-specific carve-out in the language (my earlier glob-exemption proposal is DROPPED); glob stays a convenience for non-colliding names. (c) Backdrop property the user stated: **type identity is nominal by module+name** — same-name types in different modules are DISTINCT and do not unify/match (this is what makes FQ the unambiguous resolution). (d) The stdlib's `Option` choice — reuse `primitives/Option` vs define its own `fn.option/Option` — is a **stdlib-ecosystem design tradeoff, NOT a language question**; I do not reconcile it; flag only if internally incoherent (a primitive sig referencing one Option while the prelude exposes the other). My earlier "verify seeded==defined Option" watch-item was a category error (retracted — nominal typing makes them distinct by construction). Cascade: /spec SCRIBES (a)+(c-confirm) into §8.6.4/03-types; /stdlib refactors domain modules to FQ-reference primitives across any collision (drop the colliding glob reliance); /int implements the rejection. The 2 0484 guards (re-anchored to reject-polarity) + the ~30 stdlib tests resolve together once /stdlib + /int land.
- 2026-07-04: **/spec SCRIBED the ruling** (`3784b36`, spec-only): §8.6.4 FQ-resolution framing + no-glob-exemption (earlier glob-exemption draft SUPERSEDED) + /int + /stdlib consequence prose; §3.8.4 nominal-typing property ADDED (was only ambiguously implied — same-name types distinct by FQ identity, don't unify/match). Guard polarity CONFIRMED correct; the 2 existing guards test the SPECIFIC-import collision (`import [util [measure]]` then `defn measure`); the GLOB-collision leg (the actual stdlib breakage) is an OPTIONAL new guard, not required by the ruling. FIXME 0484 left open (needs /stdlib + /int). **Order pin: /stdlib refactor MUST land before /int re-enables rejection** (else prelude self-collision re-trips). **0484-resolution wave sequence added to ladder: 10c-1 /stdlib refactor (green-preserving) → 10c-2 /int rejection (flips 2 guards).**
- 2026-07-04: **Wave 10c-1 /stdlib DONE** (`cadcf08`; suite IDENTICAL 3719/3704/15/1, green-preserving). ONLY collision found: `num/bits.cl` glob-imported primitives + defined `bit-and/or/xor/not`+`popcount` wrappers over the same-named primitives → selective import `[primitives [Int Bool shl shr eq-i64 not]]` (bodies already FQ'd primitives/bit-*, zero body change). **Option/seeded-ADT coherence: COHERENT via REUSE choice** — stdlib re-exports `primitives/Option`/`Result`/`Pair` (fn.option/fn.result/collections.pair), does NOT redefine; primitive sigs returning these return the SAME type users match. No user decision needed. **/int carry (critical)**: rejection fires on DEFINITION-over-import ONLY, NOT re-export-of-imported-name (many modules `(import [primitives [X]]) (export [primitives [X]])` legitimately — terminal-source-identical, must be allowed). Only defn/deftype/deftrait/defmacro colliding with an active import is the error.
- 2026-07-04: **USER-ARBITRATED — export semantics unified with import** (spec §8.4 was AMBIGUOUS, /sprint checked it: §8.4 described export as "public API" + "from imported modules" = downstream-only-reading; §8.3.9 gives import "bare scope"). User ruling: **"exported symbols are accessible [in the exporting module]. The difference between import and export is merely the public/private accessor on the symbol table."** Model: import and export are the SAME bring-into-scope operation; import binds PRIVATE (local use, not re-exposed), export binds PUBLIC (local use + public API / re-export). Parallels `def-`/`def`. Consequence: `(import [m [X]]) (export [m [X]])` is REDUNDANT (export subsumes import); the "re-export of imported name" carve-out DISSOLVES. 0484 collision rule now reads uniformly over "a name in scope (via import OR export)" — creating a symbol colliding with any in-scope name is the error, FQ to reach the other. Cascade: /spec scribes §8.4 (export = import-semantics + public flag; reconcile §8.6.4 to "in scope") → /int implements export-into-scope (verify current) + 0484 rejection (def-over-in-scope, uniform) → /stdlib drops the ~7 redundant import+export overlaps (hygiene). Supersedes the prior /int dispatch (stopped afe40f26) which allowed import+export as a special case.
- 2026-07-04: **/spec SCRIBED export unification** (`00fa405`, spec-only): new §8.4.0 "Import and Export Are One Operation, Differing Only in Visibility" (import→private bare scope, export→public bare scope + API; parallels §5.7 `def-`/`def`); §8.4 opening reconciled from downstream-only; §8.6.4 collision rule widened to "a name in scope via import OR export"; §8.14 summary table relabeled. /int carry: verify/implement export-brings-into-bare-scope (if currently downstream-only = impl change) + widen 0484 rejection to def-over-(import-or-export). /stdlib carry: drop redundant import+export pairs (hygiene). **Wave 10c-2 re-scoped: /int = export-into-scope (verify current) + unified 0484 rejection; 10c-3 = /stdlib redundant-import hygiene.**
- 2026-07-04: **/int Wave 10c-2 landed but on the WRONG SEAM** (`e1fe4a8`; 2 guards flipped, 3727/3714/13/1, FIXME 0484 deleted). PART A confirmed: **export was ALREADY into-scope** (`install_imports`/`install_exports` both write inner-scope `ModuleEntry::Import` differing only by Private/Public flag) — the impl already matched the user's model, spec was just behind. BUT PART B put the rejection at `worker::commit_staging_to_live` = **REPL (Additive) path ONLY**; batch/`--run` (Replace path) still lets a same-file def WIN over an import (the old 0484 behavior) — QA fixture `tests/fixtures/preludes/test-standard.cl` (`export [primitives [*]]` then `deftype Option`) relies on it. **USER RULING (2026-07-04)**: mode-divergence (REPL vs --run/--link) is a SERIOUS red flag = dual-path defect; **it is ALWAYS an error to redefine/shadow an imported name (private via import OR public via export), all modes**; make it clear in spec; **the rejection must live at the SHARED typecheck seam both modes traverse, not a REPL-only commit gate.** Cascade: /spec sharpens §8.6.4 (always-an-error, all modes, one behavior) → **/arch investigates the dual-path** (why REPL/Additive vs batch/Replace diverge; the shared check_forms seam where the collision is visible to all modes; why the impl landed REPL-only) → /dev re-implements the rejection at the shared typecheck seam (supersedes `e1fe4a8`'s commit-gate approach) → /stdlib + fixture hygiene. See `memory/feedback_investigate_suspected_dual_path.md`.
- 2026-07-04: **/spec SHARPENED §8.6.4** (`3ef1205`): "ALWAYS a compile-time error to redefine/shadow a name in scope via import (private) or export (public)" — unconditional, no same-cluster/same-file/local-def-wins escape, every order + import shape. **Mode-uniformity now NORMATIVE**: impl MUST NOT reject in one mode + accept in another — a divergence is a defect (spec-side backing for the shared-seam requirement). Prelude-shadowable contrast bounded: implicit-prelude names are outside the rule's DOMAIN (outer scope, never in inner table), not an exception. **Mode-parity test owed → /qa** (assert same rejection across REPL/--run/--link). Awaiting /arch dual-path investigation (seam) before /dev re-implementation.
- 2026-07-04: **USER SHARPENING — NO EXCEPTIONS, prelude included.** "the prelude import is not correct. they also should not be shadowed. there are no exceptions." The prelude is just an IMPLICIT IMPORT; its names are in scope like any import; redefining/shadowing a prelude name is the SAME compile-time error. **The prelude-shadowable carve-out is REMOVED** (reverses the /spec-3ef1205 contrast + prior Phase-3 "prelude names stay shadowable" + the 0475 pins). Blast radius flagged to user (broad — 0475 + examples/stdlib/user code defining prelude-named things now error; remedy = FQ or empty/reduced prelude, the Optional-prelude escape stays). Load-bearing scope detail: prelude names live in OUTER scope (§8.8.1), explicit imports in the inner table — the shared-seam rejection must check a def against BOTH. Sent as addendum to the /arch investigation. Cascade: /spec removes the prelude exception (no-exceptions, all-scopes) — **held until /arch returns to avoid concurrent-commit tree race**; /qa re-anchors 0475 (prelude/builtin shadowing → error); /dev re-impl covers inner-import + outer-prelude collisions at the shared seam. **`memory/feedback_spec_scribe_user_arbiter.md` + the prelude-shadowable assumption corrected.**
- 2026-07-04: **/arch dual-path investigation DONE** (`6388df1`, FIXME **0514** filed target /dev). **Verdict: DUAL-WRAPPER, not dual-driver** — pipeline unified (`process_cluster_once`→`check_forms`→`commit_staging_to_live` all shared); the defect is a language-semantics check hand-rolled as a mode-conditional gate keyed on `additive` (process_form.rs:172), inert for batch (Replace). Impl VIOLATES the frozen spec §8.6.4. **Shared seam = `check_forms` Def-registration site** (form.rs:249-254; staging-first `View::union(staging, live)` exposes colliding Import entries at registration — one point, all modes, order-independent). Fix: one predicate "staged Def/TypeDef colliding with an explicit `ModuleEntry::Import` → CheckError" — collapses both directions, covers export (Import+Public), preserves byte-identical-on-reject. Delete the two mode-gated gates (`reject_def_over_import` worker.rs, `reject_over_local_def` imports.rs) + `additive` flag + relocate `def_over_binding_error` message to typecheck. **GAP vs user's no-exceptions ruling**: /arch's minimal check keys on inner-table Import → keeps PRELUDE shadowing allowed (outer scope, not reached); the fix must ADD an outer-scope prelude arm. **Blast radius (large)**: batch-rejects now breaks glob-then-define fixtures (test-standard.cl, prelude, exemplar) + 2 unit tests to invert + batch e2e owed; PLUS prelude-no-shadow breaks every prelude-named definition across stdlib/examples/exemplar/tests. **SEQUENCING DECISION surfaced to user** (do-now-in-S102 vs focused follow-up; module rework is orthogonal to the ownership increment).
- 2026-07-04: **/arch REFINED report** (processed the prelude addendum; `11c59c4` FIXME 0514). **Prelude-arm gap CLOSED**: the shared seam `check_forms` Pass-1 registration already has the ONE two-scope resolver `checker.rs:961 resolve_current_or_prelude` (inner-table View + prelude outer-scope fallback + public-only filter). Single predicate: "reject a Def whose name resolves (via resolve_current_or_prelude) to anything other than this module's OWN prior Def (home==current)" — covers def-over-import + import-over-def symmetry + export (Import+Public) + prelude-outer, all order-independent, all modes, no cranelisp-types change. Provenance discrimination: resolve to own-module Def ⇒ ordinary redefinition (allowed); anything else ⇒ error. **Blast radius split**: **Tier-a** (contained) = explicit import/export glob-then-define fixtures (test-standard.cl, prelude, exemplar) + 2 unit-test inversions (`commit_allows_defn_over_import_on_replace_path`, imports/tests.rs:685); **Tier-b** (DOMINANT, from no-prelude-exception) = every module defining a prelude-provided name across stdlib/examples/exemplar/tests — /arch recommends /sprint scope tier-b as its OWN sequenced sweep (else suite goes red at once). 0475 pins re-anchor allow→error (`tests/vec_query_value_use.rs` L183/203/219 + `tests/spec_08_modules.rs:2206`). /spec-carve-out correction is a hard predecessor to the impl.
- 2026-07-04: **USER SEQUENCING DIRECTION** — "at a minimum we need updated spec and failing tests for positive and negative cases of name shadowing. If a focused wave to implement it now because it simplifies things, I'd be supportive. But I can agree to return to the main sprint thread as long as the failing tests are signalling the problem." **Plan (evidence-first)**: (1) /spec updates spec [running]; (2) /qa authors failing-not-ignored tests — positive (FQ resolution + legal cases: not-loading-prelude, reuse-by-re-export) + negative (shadow rejected: def-over-import, import-over-def, export-brought, prelude-shadow) across REPL/--run/--link (mode-parity) — AND MEASURES the real Tier-b blast radius (count of existing tests/fixtures encoding allow-shadow that would flip); (3) /sprint decides implement-now (if measured-small + net-simplifying — /arch's fix deletes 2 gates + flag) vs defer (if large: land failing tests as signal, revert e1fe4a8 wrong-seam partial so tree carries no divergence, return to ownership ladder; FIXME 0514 + failing tests track). Minimum (spec + failing tests) delivered either way. /qa held until /spec doc edit lands (one tree-toucher). Note: the fix is net-simplifying (deletes the dual-wrapper hack); the unknown is Tier-b cleanup size (prelude surface is mostly operators/traits/types — trait-IMPL method defs are NOT top-level shadows — so possibly smaller than "dominant").
- 2026-07-04: **/spec removed prelude exception DONE** (`a953de0`): §8.6.4 rule now ranges over import ∪ export ∪ implicit-prelude; prelude-shadowable paragraph DELETED → "a loaded prelude name is NOT shadowable"; not-loading (name out of scope, define freely) vs shadowing (loaded name in scope, error) distinction crisp (§8.6.4/§8.8.1/§8.8.3); outer/inner-scope demoted to resolution-mechanism impl detail; [S20] retitled "The Implicit Prelude Is an Implicit Import — No Silent Shadow"; §8.6.1 layer-2 corrected (only lexical let/fn/match still shadow). 0475 re-anchor list for /qa: whole `tests/spec_08_prelude_outer_scope.rs` suite + `vec_query_value_use.rs` L183/203/219 + `spec_08_modules.rs:2206`. Filed **FIXME 0515** (/arch — `design/int/s78-entry-module.md §2` still presents silent-shadow model). Spec correction (0514's hard predecessor) COMPLETE. Dispatching /qa: failing tests + blast-radius measure.
- 2026-07-04: **USER DECISION — DEFER enforcement.** "the risk of having a lot of test and example code being invalid programmes is a bigger risk. Those issues confound debugging and fix." Enforcing now would mass-invalidate stdlib/examples/exemplar/tests .cl code → confounds debugging. DEFER. **Key sequencing insight (addresses the risk structurally, not just by deferral): the eventual fix is CLEAN-FIRST-THEN-ENFORCE** — convert every shadowing program to FQ green-preserving FIRST, THEN turn enforcement on at the shared seam (same pattern as the stdlib explicit-import case: /stdlib FQ'd → then rejection fired). No program is ever invalid; the enforcement flip is a no-op on already-clean code. **Plan**: (1) /qa lands the failing-test matrix [running] — the durable signal, itemized as 0514, NOT invalid programs; (2) /sprint REVERTS `e1fe4a8` (wrong-seam REPL-only partial) → clean uniform baseline, no mode-divergence in the tree (2 0484 guards → RED, joining the signal); (3) return to the OWNERSHIP INCREMENT (the S102 payload: frontend 0485, Wave-8c 0512 soundness, backend harness 0495, Wave-11/12 mechanisms, Wave-13 gates); (4) module-system fix = focused FOLLOW-UP (clean-first-then-enforce), scoped by /qa's measure, tracked by FIXME 0514 (+ 0484/0515) + the failing tests. Minimum (spec [a953de0] + failing tests) delivered.
- 2026-07-04: **DECISION REVERSED — /sprint MISREAD; DRIVE OUT NOW with urgency.** User clarification: "the opposite interpretation… having a whole bunch of tests and examples incorrectly and silently passing when they are non-compliant is the bigger risk. We have to drive that situation out with urgency." The risk is NOT "enforcement invalidates code" — it is **non-compliant code (shadowing prelude/imports) SILENTLY PASSING because nothing enforces the rule.** That is a correctness hole to eliminate NOW, not defer. **Corrected plan (URGENT, ownership increment WAITS):** (1) /qa failing tests + measure [running] — makes the non-compliance LOUD + scopes it; (2) **/dev implements enforcement at the shared seam (FIXME 0514, all modes)** — the compiler drives out ALL hidden violations as loud errors; **COMPLETES e1fe4a8 (moves to shared seam), does NOT revert it**; (3) coordinated CLEANUP SWEEP — FQ/rename every non-compliant .cl across /stdlib (stdlib), /qa (test fixtures), /port (exemplar), /examples (examples) → green; (4) result: tree compliant + enforced + green, all modes; THEN resume ownership. Sequencing (enforce-first compiler-guided vs clean-first survey-guided) decided on /qa's measure. **The prior "DEFER + revert e1fe4a8" plan is CANCELLED.**
- 2026-07-04: **/qa failing-tests + measure DONE** (`9aa75db`; baseline **3744 / 3715 / 29 / 1** = 13 pre-existing + 16 new shadowing REDs). `tests/spec_08_name_shadowing.rs` (17: 9 RED signal + 8 green) covers def-over-import/export/prelude × REPL/--run/--link + mode-parity MUST + positives (FQ-reaches, suppressed/no-prelude, reuse-by-re-export, lexical let/fn). 7 pins re-anchored allow→error (`spec_08_prelude_outer_scope.rs` ×5, `vec_query_value_use.rs` ×2). **VERDICT: SMALL / NET-SIMPLIFYING.** Tier-b (prelude-name collisions in .cl) ≈ **ZERO** (stdlib suppresses prelude + is defining-source/same-terminal-dedup; examples use minimal prelude; exemplar clean; trait-IMPL methods excluded). Tier-a = **3 fixtures** (`test-standard.cl`, `prelude.cl`, `f4_sudoku.cl` — all glob-import + redefine seeded Option/Result → fix = re-export the seeded type, the stdlib/fn/option pattern). Collateral (~45 TestStandard tests + 6 CRANELISP_LIB files) ALL cleared by that same 3-file edit — not a sweep. **The 6-of-16 def-over-prelude/distinct-terminal-poison REDs need the prelude OUTER-SCOPE resolver arm (`resolve_current_or_prelude`), not just un-gating inner-table.** **EXECUTION (clean-first, urgent): Wave A /qa fixes 3 fixtures (behavior-preserving redefine→re-export); Wave B /dev implements 0514 (check_forms shared-seam enforcement + prelude arm + delete 2 gates + `additive` flag + relocate `def_over_binding_error` msg + invert `commit_allows_defn_over_import_on_replace_path` & imports/tests.rs:685) → 16 shadowing REDs flip green, all modes, compliant+enforced.** Then resume ownership.
- 2026-07-04: **Wave A /qa fixtures DONE** (`c09c0a2`; baseline IDENTICAL 3744/3715/29/1, zero consumers broke). 3 fixtures (test-standard.cl, prelude.cl, f4_sudoku.cl) redefine→re-export the bootstrap-seeded `primitives/Option`/`Result` (verified `src/bootstrap.rs` steps 4/4c seed them; re-export = nominally identical). Tree pre-cleaned; Wave B enforcement lands with zero collateral. Dispatching Wave B /dev 0514.

## Outcome (Phase 7)

{Pending.}
