# Sprint 101: R3 Machinery — Dependent Recompilation, ABI-Epoch Slot Versioning, Trap Stubs

**Status**: CLOSED — user approved 2026-07-03; Principle 22 RATIFIED at close

**Goal**: Land the R3 redefinition machinery (the dev-session dependent-recompilation transaction + ABI-epoch slot versioning + per-symbol trap stubs) so ABI-bearing ownership modes can safely reach the REPL in increment I — and fix the NULL-slot fn-as-value defect, flipping `tests/vec_query_value_use.rs` green.

## Scope

This is the **machinery sprint** in the Phase-H ownership-inference delivery sequence (ROADMAP §"Phase H — ownership-inference delivery sequence", ratified at S100 close). It is valuable standalone: it **cures the latent type-changing-redefinition hole that exists today** (a redefinition that changes a param type currently leaves compiled callers silently unsound — spine §5.2), independent of any ownership analysis. Per spine §5.7 (binding), this machinery must land **before or with** increment I's ABI-bearing modes.

### In scope

1. **NULL-slot fn-as-value defect fix** (`/dev` on `cranelisp-backend`) — `vec-get`/`vec-set`/`vec-push` used as values (passed to a HOF) SIGSEGV through NULL GOT slots via `fn_as_value.rs::emit_wrapper_call` `call_indirect`. Flips the 4 failing-not-ignored guards in `tests/vec_query_value_use.rs` green (green control `vec-len` pins the working path). Sequenced here per backend §12.7 — the fix must precede increment I's `str-len$borrowed` sibling + R2 wrapper work on the same seam.

2. **`design/int/` session-transaction design fire** (`/design` on `src/`) — the one seam the S100 exit gate deferred-with-pinned-interface. Designs the transaction interior: reverse dependency index, summary-diff gate (ABI-preserving vs ABI-changing classification), affected-set closure, reverse-topo recompile ordering, cascade error reporting, BROKEN-state bookkeeping, `/info`/`/sig` surfacing. Consuming interface is **pinned at backend §8.3's three calls** (`compile_to_module` existing; `compile_trap_stub` new; `got().store_slot`/`allocate_got_slot` existing). **Fire checklist (from `/arch` Phase 2):** (i) provenance-string lifetime — the string must live exactly as long as the returned `Code` retention handle, stored paired in the session pool (a broken symbol recovered with a *new* ABI freezes its old slot pointing permanently at the trap stub, so §8.1's "retained until the symbol recompiles" understates it); (ii) does `Def.callees` record fn-as-value references? If not, the conservative module-level-reload fallback triggers (spine §5.4 step 3) — resolve explicitly. Precedents to build on: `ModuleDependencyGraph` transitive-dependents + `recompile_module_and_dependents` (S35/S37), S45 error-cascade (`reset_module`, embedded original error), `session_v4.rs` redefinition path.

3. **Dependent-recompilation transaction implementation** (`/dev` on `src/`) — the session re-typechecks and recompiles caller functions on ABI-/type-changing redefinition; cascades (a caller that no longer typechecks) surface gracefully as BROKEN with provenance. Body-only redefinitions take the summary-diff fast path (no dependent recompiles — today's cost, pinned by L-D1).

4. **ABI-epoch slot versioning** (spine §5.6) (`/dev` on `src/` — session bookkeeping + `frozen_code` retention pool; zero backend emission change per backend §8.2, whose inherited slab-growth invariant `/dev` on `cranelisp-backend` verifies before enabling fresh-slot churn) — fresh GOT slot + frozen old slot on ABI-changing redefinition with retained old code (heap closures / suspended continuations embed direct code ptrs — recompilation alone cannot rescue them). Frozen-world semantics: pre-break closures see the old chain; recompiled callers see the new; no stop-the-world. Persistence pins (i)–(iv) honoured (slot numbers stable in `.meta.json`, holes survive, `next_got_slot` high-water respected).

5. **Per-symbol trap stubs** (`/dev` on `cranelisp-backend`, per backend §8.1–§8.2) — a BROKEN symbol's slot is patched to a stub raising a clean runtime error naming provenance ("g is broken by the redefinition of f: …") over the existing raise machinery, no new intrinsic. `compile_trap_stub` is the **one public-API addition** — backend `public-api.txt` diff flagged for `/arch`.

6. **`CRANELISP_NO_OWNERSHIP` master toggle** (`/dev` on `cranelisp-backend`, backend §2) — ships this sprint per qa plan §6 (stage M), joining the cache-manifest global keys, so the L-B2(i) suite-polarity lane starts here. (Analysis doesn't exist yet; the toggle + manifest plumbing land so increment I's oracle has its substrate.) *(Pull-forward of the manifest key from increment I to stage M ruled by `/arch` Phase 2 — inert pre-analysis, load-bearing for increment I; `/design`(backend) reconciles backend §2.3's timing sentence in its Phase-3 touch-up; `/qa` reconciles the L-B2 stage line when drafting.)*

7. **`/repl` spec half — trap-stub / broken-status wording + redefinition semantics** (`/repl`) — normative REPL wording for trap messages, `/info`/`/sig` broken-status + provenance display, and the cascade report; **plus the frozen-world vs late-binding redefinition semantics** (spine §5.6): ABI-preserving redefinitions keep today's late binding; ABI-changing redefinitions freeze the old chain for pre-break closures — L-R2's assertions need a normative anchor exactly as L-R1's wording does (`/arch` Phase 2 revision 2). L-R1's substring assertions are provisional until this lands (qa plan §5 limit 6); the exit bar requires it this sprint.

8. **QA-first (Phase 5 stage 1)** (`/qa`) — per qa plan §6 stage-M list: **L-R1…L-R5 drafted failing-first** (L-R4, the type-change-hole cure, is the sprint's own RED witness), plus the **L-D1 body-only-turn latency script** with its M-stage gate.

### Acceptance (stage M — correctness only, no performance gates)

- `tests/vec_query_value_use.rs` guards flip green (**7** — superseded 4→7 by the Wave-1 cat-3 sweep); root `CLAUDE.md` §Testing intentional-failing count updated at close (flag for user — outside `/sprint` edit boundary).
- qa lanes L-R1 (trap stubs incl. pre-break closure/curried-partial reach, recovery both directions, bounded RC-mid-panic leak), L-R2 (frozen-world + ABI-preserving late-binding fast path), L-R3 (summary-diff observability, positive + negative affected-set), L-R4 (type-change hole cured: traps-or-recompiles), L-R5 (persistence pins, two-session scripts) — all green.
- L-D1: body-only redefinition turn observably at today's cost.
- Cascade UX live: recompiled-set reporting names exactly the static callers; BROKEN status + provenance via `/info`/`/sig`.
- `/repl` spec half of trap-stub wording landed; toggle ships + suite-polarity lane starts.

### Out of scope (deferred, with rationale)

- **Increment I (read path)** — golden CLIF capture, `pass5_ownership`, `ModeSummary` carrier, primitive fact table, borrow-elision, stack slots, per-site non-atomic RC, `str-len$borrowed` sibling → **S102+** (may split per ROADMAP). Golden-capture must be increment I's *first* change-set.
- **Increment II (write path)** — reuse tokens, R5 flattening + F2v, region arena → after increment I.
- **`--release` tier + release polish** (0050 display protocol, 0052 `/learn`, 0365 `Type.member`) → after increments settle the memory model.
- **Performance gates** — stage M is correctness-only + the single L-D1 latency pin (qa plan §2.1).

## FIXME debt

Disposition of all 11 open files in `design/arch/fixmes/` (per `memory/feedback_close_fixmes_each_sprint`: drain everything actionable; defer only for Phase-H sequencing or unmet trigger):

| FIXME | Target skill | Status | S101 disposition |
|---|---|---|---|
| 0430 | /design | **RESOLVED** ✓ | Verified both halves shipped S94 (`src/save.rs:183–230` renderer + `set-doc` re-land); file deleted by `/design` in Phase 3 (`git rm` staged). |
| **0470** | /typecheck | **open (filed S101 Phase 3)** | **In-sprint, load-bearing**: widen `call_graph_edges` to every statically-resolved user-fn reference (call- + value-position); `Vec<FQSymbol>`, no types edit, `CACHE_SCHEMA_VERSION` bump same change-set. L-R3/L-R4 cannot pass without it. **Wave-ordered before the src/ transaction work.** |
| 0460 | /qa | **RESOLVED** ✓ (W5) | 2 e2e in the agent lane (never-defined/import-only → face-1 refusal; non-fn target → face-2); §17.15.4 → `[Tested+Neg]`; file deleted. |
| 0464 | /design | **RESOLVED** ✓ | Drained Wave 1: 3 platform docs refreshed to v9 (history trail through v8/v9; stale gating + tag rows fixed); file deleted. |
| **0471** | /platform → /dev(cranelisp-platform) | **RESOLVED** ✓ (W5) | `cranelisp-platform/src/lib.rs` `ABI_VERSION` rustdoc bump trail stops at v8, const is 9 — doc-comment-only, non-gating. Filed against retired skill name `/platform`; resolves via `/dev`(cranelisp-platform) — folded into Wave 5. |
| **0472** | /typecheck → /dev(cranelisp-typecheck) | **RESOLVED** ✓ (W2b) | **In-sprint, gates Wave 4**: trait-impl method bodies record no `callees` (extraction-seam gap; empirically confirmed in persisted `.meta.json`); §10 triggers don't cover caller-side. Cure (i) widen extraction per `codegen_view` precedent, or (ii) §10 conservative trigger — resolver + `/design`(int) pick. F2 prose caveats + F4 test + /qa §2.1 row ride it. Does NOT block Wave 3 (disjoint surface). |
| **0473** | /design (int) | **RESOLVED** ✓ (W3, paired) | **In-sprint, gates Wave 4**: §4.1/§10 must pin how slot-less closure members (constrained templates) classify/propagate — absorbed-change and BROKEN-template sub-cases stop the walk before the mono-minting caller. Doc-only; runs paired with Wave 3 (disjoint surface). |
| **0474** | /qa (resolver /backend) | **repro landed (W5); file now redundant** | 3 failing-not-ignored guards in `tests/vec_cow_value_use_leak.rs` (RED-first; exact signature imbalance 400 vs control 0). **Class WIDENED at drafting**: plain STATIC `vec-set` with source live also leaks (2 allocs/call) — the FIXME's "static sites are balanced" premise is empirically wrong; almost certainly pre-existing (cores line-identical to pre-S101). Per `feedback_no_fixme_with_failing_test` the tests + ledger are the record; resolver /backend deletes the file when fixing. Fix-vs-carry = Phase-5-conclusion call. |
| **0475** | /qa | **RESOLVED** ✓ (W5) | Shape constructible; 2 GREEN pins (direct call takes user body; value-use neg). File deleted. |
| **0476** | /arch | **RULED + deferred** (W5) | 3rd instance of the phantom-NULL-slot value-use class. **/arch ruled the representation cure IN** (NULL slots not load-bearing — no persistence pins): `DefKind::Primitive` slot becomes two-armed `Extern { got_slot }` vs `Inline` discriminator; `callable_got_slot()` → `None` for inline by construction; stop-predicate moves to kind-callability accessor; name-list + both exemption arms retire. `status: deferred`, pinned to increment I first change-sets (rides the ModeSummary types surface — Principle 8, one schema cascade). |
| **0477** | /design (int) | **open (filed S101 W4 review)** | Amend `session-transaction.md` to as-built: §10 T1 downgrade (+ rationale + named residue), §7.3 deleted-symbol leg unimplemented, §9.2 rendering seam (`SymbolDescription` not extended). Doc-only. **Wave 5** (paired /design(int) invocation). |
| **0478** | /qa | **RESOLVED** ✓ (W5) | /dev(src/) authored the guard RED-first (live SIGSEGV) → green over 0479; /qa CONFIRMED the green-pin-with-flip-note polarity (coherent-stale = designed stage-M behavior per amended §10; RED guards are for defects) + authored the concrete→Overloaded sibling (GREEN). File deleted. |
| **0479** | /dev (src/) | **RESOLVED** ✓ (W5) | Commit gate gained the complementary displacement arm — prior slotted Code retained in the pool when displaced by a slot-less staged Def; UAF → coherent-stale (T1 semantic cure remains 0477's named residue). F9 extraction: `RetainedCode::frozen` single constructor, all 4 sites. Unit + e2e RED-first (live SIGSEGV confirmed); file deleted. |
| **0480** | /dev (src/) | **RESOLVED** ✓ (W5) | `handle_info` renders the definition source block in both arms (healthy: sig→source→stats; broken: sig→provenance→source, no stats) via `info_definition_source` (introspection-first, backing-file rehydration fallback). 2 unit + 1 e2e RED-first; file deleted. |
| **0481** | /repl | **RESOLVED** ✓ (6a) | `repl/spec.md` §18.1 needs a stage-M scope note: §18 coherence MUSTs satisfiable only for concrete single-sig UserFn targets at stage M (T1 downgrade residue); /repl may re-route part to /spec. **Phase 6a drain** (testless doc change; /repl active in 6a anyway). |
| **0482** | /dev (cranelisp-backend) | **open (filed S101 W5 by /arch)** | `#[non_exhaustive]` on `CacheInvalidReason` (facade convention item 3; attribute free today — no in-workspace exhaustive matcher) + `public-api.txt` baseline regen + sibling-DTO audit. **Rides the next backend change-set** (increment I's golden capture at latest). |
| **0486** | /qa (likely owner /int) | **open (filed S101 6a by /docs)** | Bare lookup at the prompt corrupts introspection source (`/info`/`/source` show bare name, not the defn); deterministic repro; healthy+broken; degrades S101 `/info` source display + §18.4; restart self-heals. **6b: /qa guards.** |
| **0483** | /qa (resolver /backend) | **open (filed S101 6a by /examples)** | Vec trio op as value at ≥2 instantiations of one generic HOF → SIGBUS (exit 135), both modes; 3-line repro + full green-control matrix in the FIXME. Adjacent to carried 0474 on the same `fn_as_value.rs` seam — **disposition at Phase 7**: /qa guards next sprint; resolver /backend rides the S102 seam rework with 0474. |
| 0463 | /examples | open | **Drain (Phase 6b)**: poll-shape network example joins the learning sequence. |
| 0459 | /backend | open | **Defer → S102 (increment I)**: substance delivered S99; the remainder (floor-restoring default-on spark gate) is explicitly pinned to increment I (spine §8.3, ROADMAP). Phase-H dependency, not habit. |
| 0408 | /port | open | **Defer → increment II**: the perf half needs the write-path memory work — S99 *measured* the floor unrestorable pre-Phase-H. Unmet trigger. |
| 0050 | /int | deferred | **Defer → `--release` polish**: blocked on the display-protocol design (does not exist yet). Unmet trigger. |
| 0052 | /repl | open | **Defer → `--release` polish**: `/learn` is a pinned release-polish item (ROADMAP Phase-H table). |
| 0365 | /spec | open | **Defer → `--release` polish**: `Type.member` field accessors, pinned release-polish item. |
| 0416 | /arch | deferred ✓ | **Pinned to `--release` polish — user sign-off 2026-07-03** (scope approval). `/arch` set `status: deferred` + target on the file in Phase 2. |
| 0466 | /design | deferred | **Leave**: deferred indefinitely, user-directed, trigger-based (GOT slot-hole reclamation). |

## Architecture review (Phase 2)

**Verdict (2026-07-03, `/arch`): SIGN-OFF-WITH-REVISIONS** — scope coherent, load-bearing throughout, correctly sequenced; 3 additive revisions (all folded into §Scope above). Phase 3 cleared.

- **Coherence**: confirmed against spine §3.1/§5.4–§5.7/§9/§12, backend §8/§12-item-7, qa §2.1/§3.6/§6. Standalone value confirmed: at stage M the summary-diff gate degenerates to type-scheme-only comparison — exactly what cures the latent type-change hole (L-R4), no analysis dependency.
- **Toggle timing discrepancy RULED**: manifest-key pull-forward from increment I to stage M approved (inert pre-analysis — both polarities byte-identical; load-bearing as increment I's oracle substrate). `/design`(backend) reconciles §2.3's timing sentence; `/qa` reconciles the L-B2 stage line. (Revision 3 → item 6.)
- **Principle 8 (interim-architecture risk): NONE** — reverse index = the same edge set the ownership fixpoint walks (one graph, two consumers); trap stubs/BROKEN-state = permanent dev-session UX; slot versioning = the permanent commit mechanism (Principle 20); toggle = the permanent differential oracle.
- **Public API**: `compile_trap_stub(msg_ptr, msg_len) -> Result<(ptr, Code), CompilationError>` **APPROVED as shaped** (raw ptr/len is the honest shape — address baked as `iconst`s outlives the call). Lifetime pin routed to the `design/int/` fire checklist (item 2(i)). **No other edges verified**: no `cranelisp-types` edit (ModeSummary is increment I); slot-versioning bookkeeping session-side; `.meta.json` rides existing serde; the transaction lives in `src/` (no `public-api.txt` boundary, e2e-gated per the S81 ruling); manifest key backend-internal. `public-api.txt` diff at the implementing change-set.
- **`/spec` NOT needed in Phase 3**: no normative redefinition semantics in `spec/`; the §10.10 platform-callback forward-commitment is *satisfied* by frozen-world semantics (stale callback resolves through frozen slots to the coherent old chain). Normative home is `repl/spec.md` (item 7). Extends the S100 Gate-2 ruling.
- **Backend §8.3 interface SUFFICIENT** for the `design/int/` fire; two fire-checklist items named (folded into item 2): provenance-string/`Code`-handle lifetime pairing; `Def.callees` fn-as-value coverage vs conservative module-reload fallback.
- **Wave-carry items** (named so they don't drop): backend §8.2 GOT slab growth/pre-sizing invariant verified by `/dev`(backend) before enabling fresh-slot churn; the `Def.callees` question answered explicitly by the fire.
- **0416 actioned by `/arch`**: frontmatter set `status: deferred`, target = `--release` polish step, citing user sign-off 2026-07-03. No new FIXMEs filed (all findings have in-sprint carriers).

## Skill plans (Phase 3)

All four Phase-3 invocations complete 2026-07-03 (parallel, disjoint doc surfaces). `/spec` not invoked (confirmed unnecessary, Phase 2 question d).

### /design (session-transaction fire) — DONE Phase 3

- **Task** (design half done; `/dev` task below): Implement the session-transaction machinery per `design/int/session-transaction.md` §13 — `AbiSurface` summary-diff gate at the staging→live commit (`RedefKind` classification, slot policy, retention push), new `src/redefine.rs` (on-demand `ReverseIndex`, affected-set closure + SCC reverse-topo walk, transaction driver, BROKEN marking + trap patching), `SharedState.broken` + `SharedState.retained_code`, Replace-path integration (retire slot-zeroing, per-symbol gate, deleted-symbol freezing, `Code`→pool instead of `None`), `/info`/`/sig` broken-status + `TransactionReport` rendering (FQ-name trap-message composition per repl §18.5), broken-module cache-write poisoning.
- **Crate**: src/
- **Design refs**: `design/int/session-transaction.md` (§13 module map); spine §5.3–§5.7; backend §8.1–§8.3; FIXME 0470 (typecheck edge feed — **sequenced before or with this task**); `repl/spec.md` §18 (UX contract).
- **Acceptance**: L-R1(a–f), L-R2, L-R3 (pos+neg), L-R4, L-R5 green; L-D1 body-only turn at today's cost; unit tests on the pure seams (AbiSurface compare, reverse-index build, closure/SCC ordering, gate classification); no pool/scheduler transitions mid-transaction; `worker.rs:439` comment reconciled; stale `kept_jits` comments corrected.
- **Fire-checklist resolutions**: (i) provenance-string lifetime — structural pairing in `SharedState.retained_code` (`{code, trap_msg, fq, module, slot}`, `kept_dlls` precedent), entries never freed on recovery. Bonus finding: the as-built reload path's `*code = None` (`lifecycle.rs:1069`, `process_form.rs:702`) frees pages in-flight closures may still execute — the retention pool cures this class (in-sprint `/dev` scope). (ii) `Def.callees` gap wider than assumed — misses fn-as-value refs AND plain direct calls to user fns (`program.rs:690–752`, `infer.rs:585–604`) → **FIXME 0470 filed** (`/typecheck`: record every statically-resolved user-fn reference into `call_graph_edges`; uniform `Vec<FQSymbol>`; no types edit; `CACHE_SCHEMA_VERSION` bump same change-set) + conservative-fallback triggers T1–T3 (doc §10) for the residue.

### /design (backend touch-up) — DONE Phase 3

- **Task**: DONE — `design/backend/ownership-codegen.md`: §2.3 toggle-timing reconciled to the Phase-2 ruling (+§0/§10 cross-refs); §8.1/§8.3 pinned implementation-ready (stub sig `() -> i64` with cross-arity ABI argument; msg UTF-8 no-NUL; `ptr = *const u8`; existing facade error type; `runtime/panic` via ordinary intrinsics registration); §12.7 upgraded to a full `/dev` fix brief, verified against `fn_as_value.rs`/`primitives_inline.rs` — incl. the finding that the vec family is NOT in `primitives_inline`, so **curried `(vec-get v)` likely reaches the same NULL slot** (assess under unit-test-per-fix).
- **Crate**: cranelisp-backend
- **Design refs**: `design/backend/ownership-codegen.md` §2.3, §8.1–§8.3, §12.7; `tests/vec_query_value_use.rs`
- **Acceptance**: `/dev` can implement `compile_trap_stub`, the toggle + manifest key, and the NULL-slot fix from the doc alone; §12.7 anchor preserved.

### /qa (stage-M test plan) — DONE Phase 3

- **Task**: DONE — qa plan made sprint-ready: new §6.1 drafting spec (files, concrete `.cl` fixtures, pos+neg assertions, RED/GREEN-at-draft — **14 RED / 4 green pins / 2 scripts** across `tests/repl_redefinition.rs` (new), `tests/repl_persist_redefine.rs` (new), `cache.rs` extensions, `suite_polarity.sh`, `l_d1_turn_latency.py`); L-B2(i) **and L-B3(1)–(3)** stage lines reconciled to the toggle-at-M ruling (L-B3(1)–(3) pulled I→M as the manifest key's RED witness — doc-owner extension); §7.1 vec_query flip protocol; §4 unit-tier handoff extended for the stage-M `/dev` triads.
- **Crate**: — (tests/)
- **Design refs**: qa plan §2.1, §3.1, §6.1, §7.1; spine §5.2–§5.7; backend §2.3, §8.1–§8.3
- **Acceptance**: Phase-5 stage 1 drafts directly from §6.1 with no further design input; 14 drafted-RED flip green as `/dev` waves land; re-anchor pass to `repl/spec.md` §18 before close; flip protocol executed on the NULL-slot fix (ledger annotated; root `CLAUDE.md` count 4→0 flagged for user at close).
- **Caveat**: L-R2's fixture needs a real pre-break-closure carrier (bare `def` re-evaluates through a recompiled static caller — `/repl` finding); pick the carrier at drafting, informed by the fire.

### /repl (spec half) — DONE Phase 3

- **Task**: DONE — `repl/spec.md` **§18** (new, `[S101]`-tagged): normative trap message `{broken} is broken by the redefinition of {cause}: {original error}` with **fully-qualified names** via standard §5.1 runtime-error presentation; §18.3 cascade report (`; recompiled:` / `; broken:` sections, exact-set pos+neg, empty omitted); §18.4 broken-symbol introspection (bare lookup + `/sig` + `/info`, depth-1 provenance, self-documenting-REPL preserved); §18.7 frozen-world vs late-binding with worked session; §18.6 recovery both directions; §18.8 persistence floor (restore-as-broken SHOULD — fire to confirm feasibility). Cross-refs §3.6/§15.6; reconcile linter clean.
- **Crate**: — (repl/spec.md)
- **Design refs**: spine §5.2/§5.4–§5.7; backend §8.1–§8.3; qa plan §3.6
- **Acceptance**: every L-R1(a)–(f)/L-R2 surface has a §18 anchor (map recorded in the agent report); `/qa` re-anchors substring needles to §18 at drafting; `[S101]` → `[Tested …]` flips with `/qa` post-implementation.

## Phase 3 exit gate

**PASS-WITH-NOTES (`/arch`, 2026-07-03).** Interface set complete: backend→int = §8.3's three calls (one new, approved); typecheck = 0470 (population-side only — `callees: Vec<FQSymbol>` carrier unchanged in `cranelisp-types`, verified `module.rs:725–729`); int = all `src/`-internal (e2e-gated per S81); `cranelisp-types` = zero edit at stage M (verified); repl §18 matches the fire's §9.1 data contract. Spine §5.3/§5.5 reconciled by `/arch` to the fire's rulings (on-demand index; retention pool, never `None`d in place). Trap-stub `() -> i64` cross-arity argument verified against the uniform all-I64 value representation. **Four carried notes** (folded into the wave briefs below): (1) 0470's `CACHE_SCHEMA_VERSION` bump edits backend `cache/mod.rs` — coordinate with Wave 3's manifest-key work, one bump serves both; (2) 0470 resolver owes a consumer audit (`save.rs::dependency_sort`, `macro_resolution.rs:491` denser-edge walk); (3) repl §18.8 restore-as-broken SHOULD vs the fire's designed floor (ordinary load-time compile error) — `/repl` reconciles, `/qa` L-R5 asserts the floor; (4) `/arch` self-assigned: evaluate a `sequences/` diagram for the transaction flow when implementation lands.

## Waves (Phase 4)

Serialization per root `CLAUDE.md` §Testing: **one source-editing agent at a time**; doc-only tasks may pair with a disjoint-surface wave; only the wave's source owner runs tests. Design docs are current from Phase 3 — `/dev` proceeds directly; `/design` re-engages only on a discovered fork.

### Wave 1 — QA-first drafting + doc drains (stage 1)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /qa | tests/ | Draft the §6.1 set: `tests/repl_redefinition.rs` + `tests/repl_persist_redefine.rs` (new), `cache.rs` extensions, `suite_polarity.sh`, `l_d1_turn_latency.py` — 14 RED / 4 green pins / 2 scripts; anchored to `repl/spec.md` §18; **L-R5 asserts the fire's designed floor, not §18.8's SHOULD** (gate note 3); L-R2 carrier chosen per the fire (not bare `def`); ledger entry (S93 precedent, six fields). **PLUS the coverage post-mortem (user scope amendment 2026-07-03):** for each Phase-3 find (0470 callees gap; `*code = None` in-flight free; NULL-slot fn-as-value), a written root-cause of the coverage miss + category sweep, recorded durably in `tests/plan/`: (cat 1) audit of convention-populated symbol-table metadata fields whose consumers degrade instead of failing — completeness contract tests for `callees` specified for Wave 2; (cat 3) use-position × builtin-family matrix sweep (direct/HOF-arg/curried/stored-in-ADT) — gaps become tests or failing-not-ignored repros (ledgered) per the defect protocol; (cat 2) documented + routed to `/arch` (Wave 5) as a recurring class, NOT swept by /qa. Owns the suite run. | **done** — 21 tests drafted (15 RED / 3 green pins after L-R5(c) reclassification, +3 cat-3 guards) + 2 scripts; suite 1814 pass / 23 fail / 1 skip (22 named intentional + 1 pre-existing R18-class intermittent, ledgered); every RED hand-probed; post-mortem at `tests/plan/s101-coverage-postmortem.md`; **FINDING**: nice workers abandon pending `.o`/`.meta` persists at `/quit` (R18) → Wave-4 obligation; **cat-3**: all failures = the one NULL-slot root cause widened to curried/returned/stored-in-ADT → vec flip count 4→7 (Wave-3 obligation); no new FIXMEs |
| /design | design/platform docs (doc-only) | FIXME 0464: refresh `platform.md`/`poll-support.md` ABI stamp 7→9; resolve + delete the FIXME. | **done** — 3 docs refreshed (v8/v9 history trail added, stale gating claims fixed, tags 4/5/6 documented); 0464 deleted; deeper find → FIXME 0471 filed (lib.rs rustdoc bump trail stops at v8; doc-comment-only, non-gating) |
| /repl | repl/spec.md (doc-only) | Reconcile §18.8 to the fire's designed restore floor (or explicitly mark SHOULD as future target) — gate note 3. | **done** — §18.8 now pins the floor as MUST (load-time compile error naming the symbol; cache-write poisoning; §15.4 qualification explicit); restored-as-broken demoted to explicit non-normative MAY future target with a floor-preserving constraint; anchor stable; linter clean |

### Wave 2 — typecheck: FIXME 0470 (D/D/R)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /dev | cranelisp-typecheck | Resolve 0470: widen `call_graph_edges` to every statically-resolved user-fn reference (call- + value-position; `program.rs:690–752`, `infer.rs:585–604`/`824–856` seams); `CACHE_SCHEMA_VERSION` bump (backend `cache/mod.rs:201` — **one bump this sprint, Wave 3 does not re-bump**); **consumer audit** (gate note 2): `save.rs::dependency_sort` regen assertions + `macro_resolution.rs:491` denser-edge walk (verify no mid-cluster compile on not-yet-codegen'd same-module defn); unit tests per fix; delete the FIXME file. | **done** — single-chokepoint recorder in `infer_var` (covers call+value position uniformly); span-keyed delta extraction into `call_graph_edges`; 15 new tests (14 contract in-crate + 1 save.rs consumer guard; 5 confirmed RED pre-fix); schema 10→11 with one-bump note; `dependency_sort` SOUND (Kahn's + cycle fallback); `macro_resolution` walk deleted S76 — vacuous, no src/ fix needed; suite 1816/22/1 = exactly the intentional set, no stage-M polarity shifts; 0470 deleted |
| /review | cranelisp-typecheck | Change-set review vs 0470 + fire §3. | **done** — APPROVE-WITH-NOTES; suite + lib tier exactly at baseline (1816/22/1; 476/476); no fixes needed in-crate; 1 Important (F1: impl-method bodies record no callees — extraction seam gap, §10 doesn't cover caller-side; gates Wave 4 → **FIXME 0472** filed by /review via /sprint) + 4 Minor (F2 prose overstatement rides 0472; F3 latent slash-name divergence, deferred; F4 cross-module curried sibling test, rides next touch; F5 lib-tier runner gap — /sprint-level, flagged for user); recurring-class note: 4th instance of "per-form extraction misses non-form body seams" — cure should follow the `codegen_view` one-helper precedent |

### Wave 2b — typecheck: FIXME 0472 cure (inserted after Wave-2 review)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /dev | cranelisp-typecheck | Resolve 0472 via cure (i) (review-recommended): one shared `user_fn_refs` extraction helper applied at ALL body-check seams per the `codegen_view` precedent (impl-method call sites impl_check.rs ~:177/:191; assess default-method + mono-recheck seams); F2 prose caveats lifted; F4 cross-module curried sibling test; contract test for the cured seam (+ postmortem §2.1 impl-method-caller row noted for /qa). If cure (i) forks structurally, STOP and report for the §10 design route. No schema re-bump (same S101 window). Delete 0472. **PLUS (user-approved F5 fix): add `[workspace] default-members` to root `Cargo.toml` so bare `cargo nextest run` covers every crate's lib tier; report the new canonical suite counts.** | **done** — one shared `harvest_callee_edges` helper (ResolvedCall + recorder delta) wired at all 3 seam families incl. `finalize_impl_method_writeback` (impl/default/HKT methods); mono-recheck deliberately excluded with documented template-chain rationale; 3 new tests (2 RED-first, F4 green-at-draft pin); typecheck lib 479/479; prose caveats lifted; no re-bump (const stays 11); 0472 deleted; default-members added (root + 8 crates, DLL fixtures excluded) — **new canonical suite 3386/26/1 @ ~64s**; 26 = 22 named + **4 pre-existing intrinsics lib-tier failures newly visible** (routing below) |
| /review | cranelisp-typecheck | Delta review of the 0472 cure only. | **done** — APPROVE-WITH-NOTES; cure empirically confirmed through the real persistence path (`Sizey.bump$Int → prog2/helper` in `.meta.json`); mono-exclusion SOUND at edge-feed grain; default-method home-switch CORRECT; default-members verified exact (only the 2 DLL fixture crates excluded, nothing fell out); suite exactly 3386/26/1. **F6 Important → FIXME 0473** (transaction walk unpinned at slot-less template intermediaries — /design(int) §4.1/§10, gates Wave 4 only); F7 Minor (default-method test doesn't exercise the actual home-switch — sibling test next touch); F8 Minor (Step-5 stamp class at one more seam, subsumed by increment-I channel unification). **Wave 3 CLEAR** |

### Wave 3 — backend: defect fix + trap stub + toggle (D/D/R)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /design | design/int (doc-only, paired — disjoint surface) | Resolve FIXME 0473: pin §4.1/§10 walk semantics for slot-less closure members (option 1 always-propagate vs option 2 module-grain degrade); delete the FIXME. | **done** — **option 1 (slot-less pass-through)** ruled: skip test + BROKEN no-propagation apply only to members WITH codegen artifacts; discriminator is the existing `ModuleEntry::callable_got_slot()` accessor (S83 structural kind⇔slot pairing — no new field); worked cures for (A) absorbed-change and (B) BROKEN-template in §4.1; 9 sections amended; 0473 deleted; Wave-4 carries 4 knock-ons (see Notes) |
| /dev | cranelisp-backend | (a) NULL-slot fn-as-value fix per §12.7 brief (**flips 7 `vec_query_value_use` guards** — cat-3 sweep widened the set to curried [distinct signature: JIT panic `can't resolve symbol vec-get`, exit 101, confirming the curry path's `primitives_inline` gap], returned-from-fn, stored-in-ADT; assess each under unit-test-per-fix); (b) `compile_trap_stub` per §8.1/§8.3 (`public-api.txt` diff → flag `/arch`); (c) `CRANELISP_NO_OWNERSHIP` toggle + manifest key per §2 (no version re-bump — Wave 2 bumped); (d) verify §8.2 GOT slab-growth invariant before fresh-slot churn; (e) **fix the stale `cranelisp-intrinsics` catalog-count assertion** (`catalog::tests::name_set_is_exactly_the_expected_32` expects 32, table has 34 — newly visible via default-members; intrinsics is backend-paired; verify the 2 additions are legitimate before amending the count). Unit tests per fix. | **done** — (a) dual-seam root cause (fn-as-value GOT-indirect through NULL slot; curry path's unknown-builtin Import arm — both signatures reproduced at unit seam RED-first); shared emission cores (Principle 7) now feed static + wrapper + curry paths, elem heap category recovered per-site; **all 7 vec guards green**, control intact; (b) `compile_trap_stub` lands, approved §8.3 shape verbatim + 3 tests; (c) toggle + manifest key, no re-bump; **finding: `check_manifest` alone insufficient** — in-memory `CacheState` re-flush persists stale polarity; cured backend-side (`read_manifest` treats other-polarity as absent); both L-B3 green; (d) slab-growth HOLDS structurally (fixed 1024-slot array, never realloc; 2 pinning tests; residual: unchecked exhaustion at 1024 → Wave-4 note); (e) both additions legitimate (S99 `rc_stat_inc/dec` probes) — set amended 32→34. 13 new unit tests; **suite 3409/16/1 @ ~56s** (16 = 13 stage-M + 3 drop trio, exactly named); zero new warnings/lints |
| /review | cranelisp-backend | Change-set review; confirm `public-api.txt` diff matches the approved §8.3 shape. | **done** — APPROVE-WITH-NOTES; CLIF-eye check PASS (cores instruction-order identical to removed static bodies); public-api delta exactly 5 lines, `compile_trap_stub` = §8.3 verbatim, all 3 stub behaviours pinned; RC polarity correct on wrapper paths EXCEPT the COW rc>1 copy branch → **FIXME 0474** (leak-only, NEW on the new paths — wrapper owns the vec but has no scope machinery to balance the copy branch; curried path hits it every call); **FIXME 0475** (shadowing precedence sound by construction, unpinned by any test); **FIXME 0476** (/arch: 3rd instance of "name-resolvable entry, no slot-callable body, reached via value-use" — stringly dispatch reintroduces what S83 made unrepresentable; timed increment I); suite byte-for-byte baseline 3409/16/1; **Wave 4 CLEAR** |

### Wave 4 — src/: the session transaction (D/D/R)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /dev | src/ | The transaction per fire §13 (skill-plan block above): `AbiSurface` gate, `src/redefine.rs`, `SharedState.broken`/`retained_code`, Replace-path integration, `/info`/`/sig` + `TransactionReport` (FQ names per §18.5), cache-write poisoning, `worker.rs:439` + stale `kept_jits` comments reconciled. **PLUS (0473 ruling knock-ons, per amended §4.1/§5.1/§5.2):** (a) `affected_closure` propagation keys off `callable_got_slot().is_none()` (pure seam, unit-testable with synthetic entry kinds); (b) `mark_broken` gains a slot-less degenerate arm — registry record only, no pool push, no trap patch; (c) `TransactionReport.recompiled` includes re-typechecked-green templates per §9.1; (d) spine §5.5 reads at slotted grain (doc-recorded precision, no spine edit). **PLUS (Wave-3 backend finding): fix the manifest global-key non-convergence in `src/session_setup.rs::CacheState`** — the in-memory manifest object is re-flushed after recompiles without refreshing global keys; backend cured the ownership-polarity instance in `read_manifest`, but the same class leaves `compiler_mtime` permanently stale after a compiler rebuild → permanent cache miss (pre-existing; unit test per fix). **PLUS (Wave-1 R18 finding): make the final `.o`/`.meta` persist deterministic at `/quit`** — `nice_worker.rs` abandon-on-shutdown intermittently drops the last defining turns' meta; flips L-R5(b)/(c) AND the ledgered pre-existing `search_burndown` intermittent (same class, latent since S91). Flips L-R1–L-R5/L-D1 lanes green. | **done** — all 13 stage-M guards flipped; suite 3438/3/1 (2 consecutive runs); 16 new unit tests; obligations R18 + manifest-convergence + GOT-exhaustion + comments all landed; 2 design forks + 1 test amendment flagged (see Notes) |
| /review | src/ | Change-set review vs fire intent; Principle 18/20 structural claims verified in code. **PLUS verify the 3 /dev fork calls**: (i) T1 non-concrete-target downgrade to AbiPreserving (§10 module-grain reload not implementable mid-turn — type-change hole for macro/type/trait/overloaded targets remains open at stage M); (ii) the `redefine_broken_caller_info_and_sig_report_broken_status` assertion narrowing (whole-stdout `does_not_contain("broken")` unsatisfiable vs normative §18.3); (iii) §9.2 broken-status rendered at `handle_sig`/`handle_info`/bare-lookup directly, `SymbolDescription` NOT extended. **PLUS scrutinize the load-bearing novelties**: `preload_entry_slot_assignments` (entry-module slot pre-seed on valid cache — platform-decl interaction risk), scheduler generation-counted object staleness (lost-update race fix), per-defining-turn `.o`/`.meta` rewrite cost. | **done** — APPROVE-WITH-NOTES; 4 Important → FIXMEs 0477–0480 (crash-grade 0479 = pre-existing T1-shape UAF, fix small now the pool exists); 2 Minor fixed in place (F5 stub-handle unit pin, F6 pre-existing clippy deny); all 3 fork calls SOUND/ACCEPTABLE; all 5 novelties verified (preload probed vs platform-decl entry module — sound); suite 3438/3/1 confirmed |

### Wave 5 — verification + green-up

| Skill | Crate | Task | Status |
|---|---|---|---|
| /dev | src/ | **(Wave-5 head, serial before /qa — added at W4 review)** Drain **0479**: commit gate retains the prior slotted `Code` in the pool when replaced by a slot-less staged Def (converts UAF → coherent-stale; failing-first unit test per fix + flip the 0478 e2e guard if /qa's lands first, else author it failing-first per the FIXME's repro). Drain **0480**: `/info` definition-source line (§3.6/§18.4 MUST), healthy + broken arms, test per fix. Delete both FIXMEs. | **done** — 0479 RESOLVED (complementary displacement arm at the commit gate; UAF → coherent-stale; F9 extraction taken: `RetainedCode::frozen` now the single constructor across all 4 displace-to-pool sites); 0480 RESOLVED (`info_definition_source` helper, both arms, §3.6/§18.4 order); 5 new tests RED-first (2 unit + 3 e2e incl. the 0478-shape guard, live SIGSEGV confirmed pre-fix); suite **3443/3/1**; both FIXMEs deleted, 0478 kept for /qa |
| /design | int | **(added at W4 review)** Drain **0477**: amend `session-transaction.md` §10 (T1 downgrade as-built + rationale + named residue), §7.3 (deleted-symbol leg unimplemented), §9.2 (rendering seam) to as-built. Doc-only, disjoint — may run parallel with /dev(src/). Delete the FIXME. | **done** — §10 T1 downgrade + rationale + named residue recorded (anchors into `redefine.rs`); T3 unimplemented-because-unreachable; §7.3 deleted-symbol leg = recorded gap; §9.2 as-built (shared `broken_status_line`, `SymbolDescription` not extended); 0477 deleted; **new FIXME 0481** (/repl — §18.1 stage-M scope note) |
| /qa | tests/ | Flip protocol (§7.1, superseded 4→**7** guards) on the vec set (ledger annotated); suite polarity double-run; L-D1 script + M-stage gate; §18 re-anchor pass + reconcile linter; full-suite green check (root `CLAUDE.md` intentional count — 22 at mid-sprint — flagged for user at close); **drain FIXME 0460** (`set-doc` honest-failure e2e lane — not covered by the Wave-1 prompt, still open); **drain 0475** (shadowing pin) + **draft the 0474 repro** (COW copy-branch leak, failing-not-ignored; RC-stats probes available); **ledger the 3 `cranelisp-intrinsics` `drop::tests::*` failures per /arch's W5 diagnosis** — test-fixture artifact (untracked `alloc_zeroed`), NOT intentional guards; expected already-green if the appended /dev(intrinsics) fixture fix lands first — ledger the disposition either way; reconcile the canonical intentional count for the close-time root-`CLAUDE.md` flag; **confirm the W4 test amendment** (`redefine_broken_caller_info_and_sig_report_broken_status` narrowing vs §18.3, incl. the F8 fragile-skip nit); **drain 0478** (T1 concrete→polymorphic SIGSEGV guard — coordinate with /dev(src/)'s 0479 fix: guard lands failing-first if 0479 not yet fixed, else green with polarity note in ledger). | **done** — all 13 tasks executed; final canonical **3453 run: 3450/3/1 @ ~60s**, the 3 = new 0474 COW-leak guards (suite's only intentional REDs; **class widened — static sites leak too**); polarity lane certified identical; L-D1 PASS (median 0.0ms); §18 re-anchored (+5 honest `[Gap(S101)]` legs); 0460/0475/0478 drained + deleted; F8 + reactor-margin latent defect fixed; drop-trio + flips ledgered |
| /arch | — | Evaluate `sequences/` diagram for the transaction flow (gate note 4); confirm `public-api.txt` diffs (incl. the `CacheInvalidReason` non-exhaustive flag); **evaluate FIXME 0476** (phantom-NULL-slot 3rd instance — timed increment I; defer-with-rationale acceptable). **PLUS (user scope amendment): rule on the lifetime-across-suspension recurring class** — second-plus recurrence (S97/S98 launched-effect arg UAF → invariant 15; now the `*code = None` reload free; **and the 3 intrinsics `drop::tests` tripwires citing 0494 bug #2, newly visible via default-members**) — structural guard / standing principle vs per-instance fixes, per `memory/feedback_review_root_cause_and_duplication` escalation. | **done** — `exec-flow-redefine.mmd`+svg authored (+ 2 drifted/unrenderable diagrams reconciled); public-api CONFIRMED, `CacheInvalidReason` ruling: NOT accept-as-is → **FIXME 0482** (/dev backend, rides next backend change-set); 0476 RULED (two-armed `DefKind::Primitive` discriminator; name-list retires) + deferred to increment I first change-sets; **Principle 22 authored** (published pointers have retention owners — ratify at Phase 7 close); drop-trio = **test-fixture artifact** (untracked `alloc_zeroed` in `drop/tests.rs` helper trips the guard; not a product UAF, not an intentional guard; owner /dev intrinsics) |
| /dev | cranelisp-platform | FIXME 0471: extend the `ABI_VERSION` rustdoc bump trail through v9 (doc-comment only); delete the FIXME. | **done** — v9 entry appended (ctx-vtable handle-model cutover, S97; cross-checked against 4 sources incl. bump commit `1846f13`); intra-doc links resolve; zero new warnings; FIXME deleted; residual: stale parenthetical in `design/platform/platform.md` §3 line ~125 cites the deleted FIXME → micro-/design(platform) routed |
| /dev | cranelisp-intrinsics | **(appended at W5 per /arch diagnosis — green-up)** Fix the `drop/tests.rs::make_vec_struct` fixture: route buffer allocation through the tracked path (`alloc_data_buffer` via `pub(crate)` or `#[cfg(test)]` registration hook) so `databuf_guard` knows the test-built buffers → flips the 3 `drop::tests` REDs. The tripwire itself is correct (Principle 22) and stays. Serial after /dev(src/). | **done** — `alloc_data_buffer` → `pub(crate)` (restores the Principle-7 pair with already-`pub(crate)` `free_data_buffer`; no `pub` surface change); fixture routes through the tracked path; +1 `should_panic` pin proving the tripwire still trips on stale fixture buffers; crate 226/226; **full suite 3447/0/1 (~57s) — fully green** |

**Wave gates**: before each advance, scan `design/arch/fixmes/` for `target:` matching the wave's skill + `status: open`. Known: 0470 blocks nothing before Wave 2 and is resolved by it; 0464 resolves in Wave 1.

## Notes

- 2026-07-03: Phase 1 scope drafted from the S100 close record (`sprints/archive/sprint-100.md` §Next skills), ROADMAP Phase-H delivery table, spine §5/§5.7, backend §8/§12.7, qa plan §2.1/§3.6/§6. S100 exit-gate notes to `/sprint` all actioned in this scope: `design/int/` fire scheduled here ✓; golden-capture pinned as increment I's first change-set (out-of-scope note) ✓; F2v was ratified at S100 close ✓; root `CLAUDE.md` failing-count flagged for user at close ✓.

- 2026-07-03: **Phase 1 scope APPROVED by user.** 0416 pinned to `--release` polish (explicit sign-off). Advancing to Phase 2 (`/arch` review).
- 2026-07-03: **Phase 2 COMPLETE — `/arch` SIGN-OFF-WITH-REVISIONS** (3 additive revisions, folded into §Scope; verdict recorded in §Architecture review). 0416 marked deferred by `/arch`. Status → PHASE 3 DESIGN.
- 2026-07-03: **Phase 3 DISPATCHED** (user go): 4 parallel invocations — `/design`(src/) session-transaction fire, `/design`(cranelisp-backend) touch-up, `/qa` stage-M plan, `/repl` spec half. All doc-authoring, disjoint owned surfaces, no test runs.
- 2026-07-03: **Phase 3 COMPLETE.** All four returned; skill plans recorded. Fire resolved both checklist items; **FIXME 0470 filed** (typecheck edge widening — load-bearing, wave-ordered before src/); **0430 RESOLVED + deleted**; 0464 rescheduled to Wave 1 (platform surface uncovered by Phase-3 deployments). `repl/spec.md` §18 landed; qa plan §6.1 sprint-ready (14 RED / 4 green / 2 scripts).
- 2026-07-03: **Phase 3 exit gate PASS-WITH-NOTES** (`/arch`) — 4 notes folded into wave briefs. **Phase 4 COMPLETE** — 5 waves organized (serial source-editing per project rule). Status → PHASE 5; Wave 1 dispatch pending user go.
- 2026-07-03: **Scope amendment (user-approved):** coverage post-mortem + category sweeps added to Wave 1 `/qa` (cats 1+3); lifetime-across-suspension recurring-class ruling added to Wave 5 `/arch` (cat 2). Rationale: the three Phase-3 finds each expose a coverage category, not just an instance.
- 2026-07-03: **Wave 1 DISPATCHED** (user go): `/qa` (drafting + post-mortem, owns suite run) ∥ `/design`(platform docs, 0464) ∥ `/repl` (§18.8 reconcile) — the latter two doc-only, disjoint surfaces.
- 2026-07-03: **Wave 1 COMPLETE.** `/qa`: 21 tests (15 RED / 3 green pins) + 2 scripts, all `spec:`-cited, `spec_link_check.py` clean; suite **1814 pass / 23 fail / 1 skip** — 22 named intentional (7 vec + 15 stage-M) + 1 ledgered pre-existing intermittent. Two surprises, both honest-handled: (A) L-R5(c) green-pin was non-deterministically RED → real **R18 finding** (nice workers abandon pending `.o`/`.meta` persists at `/quit`) — reclassified RED, Wave-4 obligation added; (B) `search_burndown` intermittent surfaced — same R18 class, latent S91, rides the Wave-4 fix, excluded from intentional count. **Post-mortem** (`tests/plan/s101-coverage-postmortem.md`, registered in `tests/CLAUDE.md`): cat 1 — 12 fields audited, `callees` the sole defect-class member (+1 WATCH `trait_origin`), completeness-contract spec §2.1 ready for Wave 2; cat 3 — 25 cells probed, all failures the one NULL-slot root cause widened to 3 new positions → **vec flip count 4→7** (Wave-3 obligation); standing rule: new builtin registration kinds get a value-use row at drafting. L-R2 carrier resolved (pre-break-compiled zero-arg minting fns; §18.7 direct frozen-world assertion not e2e-assertable at stage M — structural witness is L-R5(b), documented). No new FIXMEs. **Wave-1 gate scan**: no open FIXME targets Wave-2 skills other than 0470 (resolved BY Wave 2); 0460 (/qa) not drained in Wave 1 — folded into Wave 5 `/qa` brief. **Wave 2 clear to dispatch.**
- 2026-07-03: **Wave 2 DISPATCHED** (user go): `/dev`(cranelisp-typecheck) resolving 0470, serial.
- 2026-07-03: **Wave 2 `/dev` COMPLETE — 0470 RESOLVED + deleted.** Single-chokepoint recorder in `infer_var` after all rejection gates (call- and value-position Vars uniformly); span-keyed `user_fn_refs` map with per-form delta extraction into `call_graph_edges`; self-edges structurally skipped (recursion name binds local — FIXME permitted either; test-pinned). 15 new tests (14 in-crate contract per postmortem §2.1 owner line, 1 `save.rs` consumer guard — test-only, zero production src/ change); **schema 10→11** (one-bump note in place — Wave 3 must not re-bump). Consumer audits: `dependency_sort` SOUND (Kahn's + alphabetical cycle fallback terminates on newly-representable mutual recursion; self/cross-module filtered); `macro_resolution` callee walk **deleted S76 fire B** — vacuously safe; `save.rs:816` sole live reader; stale rustdoc at `macro_resolution.rs:490–493` joins Wave 4's owed stale-comment reconciliation. Suite **1816/22/1** — exactly the named intentional set; no stage-M polarity shifts; checks/clippy zero new. Note for durability: contract tests live in the crate lib-test tier, which the default `cargo nextest run` excludes (run via `-p cranelisp-typecheck`) — flagged to `/review`. Residuals (a)–(f) handed to `/review`.
- 2026-07-03: **Wave 2 `/review` COMPLETE — APPROVE-WITH-NOTES.** Change-set verified sound (recorder/`lookup` divergence impossible in constructible cases; kind-gate holds; self-edge skip structural + test-pinned; schema bump clean; (h) no cross-turn leak — fresh `CheckState` per `check_forms`, spans can't mask). **F1 Important → FIXME 0472 filed**: impl-method bodies drop all recorder edges (Pass-1 spans precede every Pass-2 delta snapshot; empirically confirmed — `Sizey.bump$Int` persists empty `callees`); §10's target-based triggers can't catch a caller-side miss → silent stale-slot unsoundness once Wave 4 lands. 4th instance of the "non-form body seams" recurring class — cure per `codegen_view` one-helper precedent. **Wave 2b inserted** (0472 cure, D+R) before Wave 4; does not block Wave 3 (disjoint surface). Minors: F2 rides 0472; F3 latent (non-constructible name shape), deferred; F4 one sibling test rides 0472; **F5 flagged for user** — no `default-members`/CI/justfile runs the crate lib tiers, so bare `cargo nextest run` excludes ~476 typecheck unit tests; mitigation (justfile `nt-all` or `[workspace] default-members`) is a canonical-baseline change → user call, outside /sprint edit boundary. Full-picture suite runs need `--no-fail-fast` now that named guards fail early.
- 2026-07-03: **Wave 2b DISPATCHED (user go: "dispatch Wave 2b, then Wave 3 on completion")** — Wave 3 auto-follows Wave 2b's review without a further gate. **User also approved the F5 fix**: `[workspace] default-members` added to root `Cargo.toml` so the canonical suite runs all crate lib tiers — folded into Wave 2b `/dev` (source edit, sole editing agent); new canonical counts to be recorded here and flagged into the close-time root-`CLAUDE.md` update.
- 2026-07-03: **Wave 2b `/dev` COMPLETE — 0472 RESOLVED + deleted.** Cure (i) landed clean (no §10 design fork): one shared `harvest_callee_edges` helper at all body-check seam families — form defns (single/multi-sig) + `finalize_impl_method_writeback` (impl-provided, default D1 home-switched, HKT). Mono-recheck seam deliberately excluded, documented rationale: template's edge set is complete via its own defn check, recorder gives caller→template, mono re-mints on caller re-typecheck — so the reverse closure reaches the minting caller through the template chain; helper is ready to wire at `monomorphise_call` P4 if Wave 4's closure finds otherwise. 2 RED-first tests (impl-method + default-method positions, exact empirical signatures) + F4 pin (green at draft — Wave-2 recorder already covered cross-module curried; retained against drift). Prose caveats lifted to now-true statements + explicit mono residue; **no schema re-bump** (const stays 11). **default-members landed (user-approved)**: root + 8 crates (DLL fixtures excluded, comment); **new canonical suite: 3412 run — 3386 passed / 26 failed / 1 skipped @ ~64s** (~1m28 with build). The 26 = 22 named intentional (unchanged polarity) + **4 pre-existing `cranelisp-intrinsics` lib-tier failures newly visible** (verified pre-existing at HEAD via stash round-trip): 1 stale catalog-count assertion (expects 32, table has 34) → **folded into Wave 3 `/dev`(backend)** (intrinsics backend-paired); 3 `drop::tests` UAF tripwires citing 0494 bug #2 → **Wave 5**: `/qa` ledgers + reconciles the intentional count, `/arch` takes them as input to the recurring-class ruling. Root-`CLAUDE.md` close-time flag now carries: new counts, ~64s runtime (the ~9s figure was already stale — root-only ran ~71s here), intentional-count reconciliation.
- 2026-07-03: **Wave 2b `/review` COMPLETE — APPROVE-WITH-NOTES; Wave 3 CLEAR.** Cure confirmed end-to-end through the real persistence path; mono-exclusion verdict SOUND at edge-feed grain (chain `f → t → c` fully represented; mono entries never per-symbol targets); default-method D1 home-switch CORRECT (snapshot-before-switch, span-keyed delta survives restore, entry written in writer's table); default-members exclusion exact (bare 3412 vs --workspace 3432 = only the 2 DLL fixture crates; default set strictly grew 1838→3412). **F6 Important → FIXME 0473 filed** (design-grain, /design(int)): §4.1/§5.5 evaluate propagation on a member's OWN status, but slot-less templates have no codegen — absorbed-change (A) and BROKEN-template (B) sub-cases stop the walk before the mono-minting caller; the 0472 exclusion removed the accidental T2 safety valve; cure is one §4.1/§10 paragraph (option 1 always-propagate vs option 2 module-grain). Gates Wave 4 ONLY. F7 Minor (home-switch untested — sibling next touch), F8 Minor (Step-5 stamp at one more seam, subsumed by increment-I unification). **Wave 3 DISPATCHED** (per user-approved chain): `/dev`(cranelisp-backend) tasks (a)–(e) ∥ `/design`(int, doc-only, disjoint) resolving 0473.
- 2026-07-03: **0473 RESOLVED + deleted** (`/design` int, paired with Wave 3). **Option 1 ruled — slot-less pass-through**: skip test/BROKEN no-propagation gate only members with codegen artifacts; slot-less members (templates, Overloaded bases) re-typecheck in SCC position but never gate the walk. Discriminator: existing `ModuleEntry::callable_got_slot()` (module.rs:1303; S83 kind⇔slot structural pairing) — no new field. Rationale recorded: module-grain degrade (option 2) would make module grain the COMMON case in polymorphic code, failing L-R3's negative half; option 1's over-visit is rare, monotone-sound, bounded. Worked cures: (A) pass-through re-mints `t$Int` via caller re-typecheck → AbiPreserving in-place patch; (B) broken template = registry-record-only, propagation continues → caller goes BROKEN slotted → trap lands there with depth-1 provenance naming the root cause. 9 sections amended (§4.1 load-bearing); §10 T1 cross-note: slot-less MEMBERS are not a conservative trigger. Wave-4 brief updated with the 4 knock-ons. No spine edit needed.
- 2026-07-03: **Wave 3 `/dev` COMPLETE.** NULL-slot defect cured at BOTH seams (fn-as-value wrapper + curry unknown-builtin arm — vec family has allocated-but-NULL name-resolution-only slots since no monomorphic extern body can know the elem heap category; inline emission via shared cores per Principle 7, one source for static/wrapper/curry). **All 7 vec guards + 2 L-B3 cache + catalog-count flipped → suite 3409 passed / 16 failed / 1 skipped @ ~56s**; the 16 = 13 stage-M (Wave 4) + 3 drop trio (Wave 5), exactly named. `compile_trap_stub` = approved shape verbatim. **New findings routed**: (i) manifest global-key non-convergence class — ownership instance cured backend-side, `compiler_mtime` sibling is pre-existing src/ defect → **added to Wave 4 brief**; (ii) `CacheInvalidReason` not `#[non_exhaustive]` (new variant technically breaking for exhaustive external matchers) → riding the public-api diff to `/arch` Wave 5; (iii) GOT exhaustion unchecked at 1024 (release-mode UB) → Wave-4 session note, recorded in got.rs; (iv) COW copy-branch vec-release polarity — shared pre-existing, now single-sourced, flagged to `/review`. Backend `/review` DISPATCHED.
- 2026-07-03: **User go recorded: Wave 4 dispatches on backend-review approval** (no further gate). Wave-4 brief is fully assembled: fire §13 grain + the 4 §4.1 slot-less knock-ons (0473) + deterministic-final-persist (R18, Wave 1) + manifest `compiler_mtime` convergence (Wave 3 finding) + GOT-exhaustion surfacing note + stale `kept_jits`/`macro_resolution` comment reconciliation.
- 2026-07-03: **Wave 3 `/review` COMPLETE — APPROVE-WITH-NOTES; Wave 4 CLEAR (dispatched per conditional go).** CLIF-eye PASS (extraction instruction-order identical); public-api exactly the approved shape; all trap-stub behaviours pinned (one precision: message *address* baked, bytes read per invocation — the §8.1 lifetime contract Wave 4 must honour via the paired string+Code pool). Reviewer filed 3 FIXMEs: **0474** (COW copy-branch leaks the consumed vec on the NEW wrapper/curry paths — the /dev "mirrors pre-existing" claim was half-right: the core is faithful, but static sites compile the vec arg borrowing with scope machinery balancing the copy branch, while the wrapper owns it with nobody releasing; curried hits it every call; leak-only) → /qa repro Wave 5; **0475** (shadowing unpinned) → /qa Wave 5; **0476** (phantom-NULL-slot 3rd instance, stringly dispatch reintroduces what S83 made unrepresentable at kind level; timed increment I) → /arch Wave 5. `check_manifest`'s OwnershipToggle arm noted production-unreachable (read_manifest gate filters first) — harmless defense-in-depth. Gate scan: new FIXMEs target /qa + /arch (Wave 5) — Wave 4 (/dev src/) unblocked.
- 2026-07-03: **Wave 4 `/dev` COMPLETE — the session transaction landed; all 13 stage-M guards flipped.** Suite **3438 passed / 3 failed / 1 skipped** (2 consecutive runs, ~62s); the 3 = Wave 5's intrinsics drop-trio, untouched. New `src/redefine.rs` (AbiSurface alpha-canonical diff, classify, RetentionPool with paired provenance strings, BrokenRegistry, on-demand ReverseIndex, Tarjan reverse-topo, pure §4.1 pass-through seam `member_propagates`, `run_transaction`, report render, GOT-exhaustion guard) + 11 unit tests; commit gate rewritten in `worker.rs` as single slot-policy authority; §7.3 NULL-window cure (`clear_module_codegen` no longer zeroes slots, displaced Code pool-moves); §18.8 cache-write poisoning in nice_worker; §18.4 provenance on `/sig`/`/info`/bare-lookup; `SlotFreeze`/`TrapPatch` GOT-trace events live. **All 4 obligations landed**: R18 three-part (per-turn `mark_object_stale` + generation-counted completion closing a lost-update race + `flush_final_persist` at `/quit` — also roots the ledgered `search_burndown` flake); manifest global-key convergence (stale-key manifests discarded at load via `check_manifest` probe — class fix covering `compiler_mtime`); GOT exhaustion surfaces an error naming module + remedy; all stale comments reconciled. 16 new unit tests total (redefine 11, scheduler 3, session_setup 2). **Flagged for /review**: T1 fork (non-concrete targets downgrade to AbiPreserving — §10's module-grain reload not implementable mid-turn; pre-existing type-change hole for macro/type/trait/overloaded targets stays open at stage M, no new unsoundness), T3 unimplemented (unreachable given v11 bump), one e2e assertion narrowed (whole-stdout negative unsatisfiable vs normative §18.3 — /qa confirms Wave 5), §9.2 rendered without extending `SymbolDescription`, `preload_entry_slot_assignments` novelty, RC_STATS arm's cleaner home is cranelisp-intrinsics (out of crate scope). `/review`(src/) DISPATCHED.
- 2026-07-03: **Wave 4 `/review` COMPLETE — APPROVE-WITH-NOTES; Wave 4 CLOSED.** Transaction machinery verified faithful to the fire design (gate/pool/index/walk = §13 grain; frozen-world argument honoured; suite exactly 3438/3/1). **All 3 fork calls upheld**: T1 downgrade SOUND (mid-turn reload would resurrect pre-redefinition backing source; T2 stays sound by asymmetry) but needs the design amended → **0477**; test narrowing SOUND (whole-stdout negative genuinely unsatisfiable vs §18.3); §9.2 rendering ACCEPTABLE (one shared helper, three thin sites). **All 5 load-bearing novelties verified** — preload probed live against a platform-decl entry module (separate DLL-owned GOT slabs, no collision; stale-scheme seed monotone-safe); scheduler generation counting correct on all three legs; per-turn persist cost claim verified (one lock + notify, background rewrite); stub-handle pairing now unit-pinned (F5 fixed in place); §8.1 message lifetime structurally sound (append-only pool + extra stub-handle clone). **4 Important → FIXMEs**: **0479** (/dev src/ — commit gate skips retention when slotted prior replaced by slot-less staged Def → live SIGSEGV repro; pre-existing crash, fix small+local now the pool exists), **0478** (/qa — failing-first guard for that shape), **0480** (/dev src/ — `/info` missing definition-source line, §3.6/§18.4 MUST, pre-existing), **0477** (/design int — as-built amendments §10/§7.3/§9.2). Minors: F5+F6 fixed in place (re-verified green); F7 convention drift + F8 fragile skip + F9 pool-loop duplication (3rd variant — extraction at next touch) + F10 benign NULL-window noted. Also verified live: §18.8 restart floor; `run_transaction` unreachable in `--run`. **Gate scan for Wave 5**: 0478 targets /qa (in-wave drain), 0479/0480 target /dev(src/) (folded in as Wave-5 head), 0477 targets /design (paired invocation added) — no blockers outside the wave's own briefs.
- 2026-07-03: **Wave 5 `/design`(int) COMPLETE — 0477 RESOLVED + deleted.** `session-transaction.md` amended to as-built: status header indexes the 3 divergences; §10 T1 downgrade recorded with rationale (post-turn `regenerate_backing_file` + entry-module re-entrancy vs Invariant SW; T1/T2 asymmetry) + named residue (type-change hole open at stage M for T1 kinds; 0479 fix = coherent-stale not UAF; sound cure = end-of-turn-sequenced module reload, increment-I-or-later); T3 unimplemented-because-unreachable (v11 bump anchor); §7.3 deleted-symbol leg = recorded gap (ghost entries, synth-def resurrection possible — pre-existing); §9.2 as-built (shared `broken_status_line` at 3 render sites, `SymbolDescription` not extended, agent-harvest = residual nicety); consistency touch-ups §1/§2.2/§6.3/§11/§13. No fork call overruled — all 3 rationales verified against source. **New FIXME 0481** (/repl): §18.1 stage-M scope note — §18 coherence MUSTs satisfiable only for concrete single-sig UserFn targets; testless doc request, Phase-6a drain. **Polarity note for /qa**: the /design §10 text frames the 0478 guard as "stays RED by design" (pinning the full trap-or-recompile cure) while the /dev(src/) brief has the guard pinning sound post-0479-fix behavior (coherent-stale, GREEN, T1-residue comment) — /qa reconciles which polarity the guard actually pins when draining 0478 and ledgers accordingly.
- 2026-07-03: **Wave 5 `/dev`(src/) COMPLETE — 0479 + 0480 RESOLVED + deleted.** Commit gate: complementary displacement arm (slot-less staged over slotted prior → prior Code pool-retained, `trap_msg: None`, frozen supersession; no transaction — T1 semantic cure stays 0477's residue); pool-less contexts keep pre-S101 drop matching sibling sites. F9 extraction TAKEN: `RetainedCode::frozen` is now the single constructor at all 4 displace-to-pool sites (commit gate ×2, `clear_module_codegen`, `reload_module`, `mark_broken` displaced-half). `/info` source line: `info_definition_source` (introspection `source`→`sexp` precedence matching `/source`, FIXME-0220 rehydration fallback mirroring `resolve_recheck_sexps`); both arms per §3.6/§18.4. 5 tests RED-first (unit commit-gate pool pin; e2e `redefine_concrete_to_polymorphic_caller_survives_coherent_stale` — RED was the live exit-139, post-fix pins `(g 5)` → `:primitives/Int 6` through frozen old chain with T1-residue flip note; 2 `/info` unit + 1 e2e; source-line assertion also added to /qa's existing broken-status e2e — **flagged for /qa ledger**). Suite **3443/3/1** = baseline + 5; checks/clippy clean; `src/CLAUDE.md` invariant row updated. 0478 kept for /qa (confirm, ledger, consider concrete→Overloaded sibling shape, delete). **Serial queue advances: /dev(cranelisp-intrinsics) fixture fix DISPATCHED.**
- 2026-07-03: **Wave 5 `/dev`(cranelisp-intrinsics) COMPLETE — drop-trio fixture cured; SUITE FULLY GREEN: 3447 passed / 0 failed / 1 skipped (~57s).** Pre-fix panic text confirmed /arch's diagnosis exactly (`previous free site = None` = never-registered buffer, not a real UAF). Fix = visibility, not a test hook: `alloc_data_buffer` → `pub(crate)` mirroring the already-`pub(crate)` `free_data_buffer` (Principle-7 single-source pair; no `pub` facade change); `make_vec_struct` routes through it. Guard NOT neutered: register/release symmetric with production; new `#[should_panic("NOT live")]` pin (debug-gated) proves the tripwire still fires on stale fixture buffers. No product code touched; 0494 bug #2 closure undisturbed. Note: pre-existing clippy lints in cranelisp-platform/cranelisp-types surface on dependent `--all-targets` runs — backlog observation, not chased. **Serial queue advances: /dev(cranelisp-platform) 0471 DISPATCHED.**
- 2026-07-03: **Wave 5 `/dev`(cranelisp-platform) COMPLETE — 0471 RESOLVED + deleted.** v9 `ABI_VERSION` rustdoc trail entry appended (ctx-vtable handle-model cutover: HostCtx `acquire`/`retire`, `Acquire` enum, `role` byte from reserved — size unchanged, poll-node leading-pair injection deleted; layout-affecting per Principle 14 analogue); description cross-checked against FIXME text, tests.rs comment, platform.md §3 v9 arm, and bump commit `1846f13` — all four agree. Zero new check/doc warnings. **Residual routed**: `design/platform/platform.md` §3 stale parenthetical ("still stops at v8… FIXME 0471") now cites a deleted FIXME — one-line deletion, /design-owned → micro-/design(platform) invocation appended (DONE — parenthetical removed, sentence reads correctly). **/qa DISPATCHED (final Wave-5 slot, quiet tree, suite 3447/0/1).**
- 2026-07-03: **Wave 5 `/arch` COMPLETE — all 4 tasks ruled.** (1) `exec-flow-redefine.mmd`+svg authored (full redefining-turn flow, §8.3-exact backend arrows, Principle-21 lanes); lockstep sweep found `exec-flow-repl.mmd`/`exec-flow-compilation.mmd` drifted (missing `capture_clif`) AND unrenderable (pre-existing `;` breakage) — both reconciled + re-rendered. (2) Public-api CONFIRMED (exactly the approved 5 lines); `CacheInvalidReason` NOT accept-as-is per facade convention item 3 → **FIXME 0482** (/dev backend, rides next backend change-set + sibling-DTO audit; attribute free today). (3) **0476 RULED now, implementation deferred to increment I first change-sets**: representation cure IN — NULL slots verified not load-bearing; `DefKind::Primitive` two-armed `Extern{got_slot}`/`Inline` discriminator, kind-callability stop-predicate (the one careful seam — preserves shadowing precedence), stringly name-list retires; rides ModeSummary types surface (one schema cascade). (4) **Principle 22 authored** — "published pointers have retention owners": every raw pointer published across a frame-outliving boundary gets a named retention owner whose release point is the un-publish event; in-place free of published referents forbidden by construction; companion layout-neutral-tripwire rule; instance register (invariant 15, RetentionPool + 0479 as proof per-instance fixing doesn't terminate, 0494 tripwires, trap-stub pairing). **Ratify at Phase 7 close** (authored mid-sprint under user mandate). **Drop-trio diagnosis for /qa: test-fixture artifact** — `drop/tests.rs::make_vec_struct` allocates via raw `alloc_zeroed`, bypassing `databuf_guard::on_alloc`, so the (correct, Principle-22) tripwire panics on a never-tracked buffer; NOT a product UAF, NOT an intentional-defect guard; 0494 bug #2 stays closed; fix = route helper through tracked alloc (`pub(crate)` or `#[cfg(test)]` hook); failing tests are the record, no FIXME. **/sprint action: small /dev(cranelisp-intrinsics) fixture-fix invocation appended to Wave 5** (green-up intent; serial after /dev(src/)) — flips the last 3 non-guard REDs.

- 2026-07-03: **Wave 5 `/qa` COMPLETE — Phase 5 exit bar met.** Final canonical: **3453 run — 3450 passed / 3 failed / 1 skipped @ ~60s**, both toggle polarities identical; the 3 = the new 0474 COW-leak guards, the suite's ONLY intentional REDs. All 20 sprint guards (7 vec + 13 stage-M) ledger-flipped; `search_burndown` intermittent RESOLVED (R18 fix held across all Wave-5 runs). L-B2(i) lane certified — and itself surfaced 2 test-side finds, both fixed: env inheritance in the L-B3 polarity tests (harness `env_remove()`), and a LATENT margin defect in `two_real_leaves_in_par_overlap_max_not_sum_one_thread` (2/8 flake at default polarity — wall-clock bound too tight; rewritten structural `< 2×delay`, 8/8 both polarities). L-D1 PASS (30 body-only turns median 0.0ms; ABI-changing turn 1ms; per-turn persists don't regress eval). §18 fully re-anchored (612 live / 0 dead reconciler; 5 honest `[Gap(S101)]` legs recorded as next-redefinition-sprint candidates). Drains: 0460 (2 agent-lane e2e, §17.15.4 `[Tested+Neg]`), 0475 (constructible; 2 pins), 0478 (green-pin polarity CONFIRMED with ledgered reasoning + concrete→Overloaded sibling authored). W4 amendment confirmed sound; F8 fragility fixed (`skip_while` over `;`-prefixed report body). Drop-trio ledgered with post-mortem (invisible pre-default-members = no lib-tier runner; default-members is the structural cure). **0474 repro: 3 RED-first guards — and the class WIDENED: plain static `vec-set` with source live also leaks (FIXME premise "static sites are balanced" empirically wrong; near-certainly pre-existing).** Root-`CLAUDE.md` close flag (user edit): intentional count 4→**3**, total 1812→3453, runtime ~60s. Nothing blocks Phase 6.

### Phase 6a — user-proxy assessment

| Skill | Surface | Task | Status |
|---|---|---|---|
| /repl | repl/ | Assess delivered redefinition UX vs `repl/spec.md` (§18 wording landed W1; machinery W4/W5); **drain FIXME 0481** (§18.1 stage-M scope note; may re-route part to /spec); produce 6b plan (sprint demo). | **done** — surface substantially conforms (§18.2/.3/.4/.5/.6/.8 + 0480 display + vec value-use all verified live); **0481 DRAINED** (§18.1 scope note written, no /spec routing; §18 examples aligned; new §18.8 restart-MUST-reach-prompt bullet [S102]); **5 defects with repros** incl. 3 §18 MUST violations (`__expr` leaks into broken list; trap wrapper prefix; `/sig`≠bare-lookup primary line) + restart exit(1) lockout (filed as 0483 → /int, COLLIDES ×3) + phantom-module-member error (filed as 0484 → /int, COLLIDES ×2); 6b: `10-redefinition.demo` + full prior replay |
| /port | exemplar/ | Exemplar (Sudoku) still builds + runs; assess redefinition-machinery impact on at-scale dev loop; produce 6b plan. | **done** — GREEN no-regression (solver end-to-end, 40/40 both polarities, web showcase live); at-scale verdict: machinery exactly right where it fires, BUT **T1 silent split-world is the COMMON case** (unannotated fns generalize → downgrade → no report/trap) — increment-sequencing weight; **FIXME 0487** (/int, `/mod` scope gaps); **3 serious defects D1–D3** (def-poisons-directory; cwd `user.cl` adoption/data-loss class; file-backed dependent recompilation false-BROKEN unrecoverable) → /qa 6b guards; 6b = explicit no-op on exemplar source (CLAUDE.md warning landed in 6a) |
| /stdlib | stdlib/ | Stdlib loads + behaves; exercise redefinition over stdlib-backed sessions; produce 6b plan. | **done** — GREEN load + self-tests (14/14, 17/17, 27/27, 6/6); redefinition machinery "excellent" (cascade/trap/recover verified over stdlib-backed callers); **3 FIXMEs filed**: generic-fn FQ-call/imported-value-use missing mono (filed as 0483 — COLLIDES with /examples', renumber at gate; stdlib collateral: `vec-flatten` unusable from user code), 0484 import-shadow order-dependent resolution, 0485 macro-arity-exhaustion diagnostic; 6b plan: `vec-concat` simplification + `vec-flatten` self-test + explicit redefinition no-op |
| /examples | examples/ | Learning-sequence examples replay green; assess new-surface example needs; 6b plan carries the 0463 poll-shape drain. | **done** — 32/32 GREEN; **new defect found + FIXME 0483 filed** (vec-op-as-value at ≥2 instantiations of one HOF → SIGBUS, both modes, 3-line repro + green-control matrix; target /qa, resolver /backend, same `fn_as_value.rs` seam); **0463 NOT ready — stays deferred** (all 3 blockers re-verified: no socket/connect leaf, bare exit-code harness; annotated); 6b plan: `33-redefinition.cl` headline + `14-vecs.cl` single-instantiation sub-test |
| /docs | user/ | User docs vs delivered redefinition behavior (trap messages, /info source display, broken status); produce 6b plan. | **done** — WRONG: cli-reference omits `--agent`/`--no-agent`/`--yes` (stale since S88/S89; §0.6 MUST-agree); MISSING: entire redefinition/live-dev story (S101 headline); rest verified FINE against binary; §18 behavior verified live; **FIXME 0486 filed** (bare lookup corrupts introspection source → `/info`/`/source` show bare name; deterministic repro; healthy+broken; restart self-heals; likely /int); 6b plan: cli-ref fix + `guide/live-development.md` + getting-started notes |

- 2026-07-03: **User: carry approved for 0474** (backend fix rides increment I's ownership-mode rework of the same seam; 3 guards + ledger are the record). **Phases 6–7 authorized.** Phase 6a dispatched — all 5 user-proxy skills parallel (disjoint owned surfaces; no suite runs — binary/REPL exercise only).

- 2026-07-03: **Phase 6a `/examples` COMPLETE.** 32/32 replay green. Redefinition split verdict: batch-observable rebind semantics = legit 6b example; interactive cascade/trap UX = /repl + /docs territory (both have it in scope ✓). **New defect: FIXME 0483** — vec NULL-slot-trio op as a value at TWO instantiations of one generic HOF → SIGBUS exit 135 (both `--run` and REPL; also two different trio ops through one HOF; green controls: per-op HOFs, same-op-same-type ×2, vec-get+user-fn mix, `vec-len` at two types). One step past the S101 pins; no shipped example hits it. Target /qa (guards), resolver /backend (`fn_as_value.rs`). 0463 re-verified NOT ready — stays open/deferred with annotation (natural trigger: net-leaf or harness-driver sprint); 6b headline replaced by `33-redefinition.cl`.

- 2026-07-03: **Phase 6a `/stdlib` COMPLETE.** Load + curated surface + all in-language self-tests GREEN. Redefinition seam verdict: user-helper redefinition over stdlib-backed callers works exactly as specified (cascade named exact sets; trap provenance full; recovery both ways). **Finds**: (1) **generic fns unusable outside bare call position** — FQ call on a generic (`(user/iden 5)`) fails codegen `undefined function`; imported generic in value position (`vec-map identity`) fails `undefined variable`; mono instance never reaches the consuming batch; **stdlib collateral: `vec-flatten` broken from user code** (same-module fn-as-value in its body); distinct signature from 0344; filed as **0483 — NUMBER COLLIDES with /examples' 0483** (different defect, same seam family) → /sprint renumbers at 6a gate. (2) **0484**: import-shadow order-dependence — `import→call→defn→call` keeps import, `import→defn→call` takes shadow; `/info` claims `user/count` in both (introspection disagrees with resolution). (3) **0485** (/frontend, usability): macro clause-exhaustion diagnostic shows internal span `1000056..`, Debug FQSymbol dump, recursion bottom. Minor UX (triple-wrapped trap prefix, `? bytes`) left to /repl's pass. Vec value-use unlock verified incl. the cross-module stdlib shape → 6b: `vec-concat` = `(vec-reduce vec-push va vb)`, `vec-flatten` self-test (rides the generic-fn fix; polarity call at 6b dispatch), redefinition no-op explicit.

- 2026-07-03: **Phase 6a `/docs` COMPLETE.** Audit: 1 WRONG (cli-reference flag table vs §0.6 MUST-agree — S88/S89 flags missing), 1 big MISSING (redefinition/live-development guide — zero `user/` mentions of S101's headline), rest verified FINE with live transcripts. §18 behavior verified end-to-end independently (matches /stdlib's verdict). **FIXME 0486** (/qa; likely owner /int): bare lookup at the prompt corrupts the symbol's introspection source — `/info`/`/source` then show the bare name instead of the defn form; deterministic 3-step repro; hits healthy AND broken symbols → directly degrades S101's new `/info` source display + §18.4; backing file correct; restart self-heals. (/docs self-renumbered 0483→0486 on observing the sibling collision — so remaining collision is exactly the two 0483s.) Divergence notes routed to /repl's pass (trap wrapper prefix vs §18.5; `/info` stats fields). 6b: cli-ref fix, `guide/live-development.md` (transcripts avoid the 0486 shape), getting-started pointers, CLAUDE.md row.

- 2026-07-03: **Phase 6a `/repl` COMPLETE — 0481 RESOLVED + deleted.** Conformance verdict: redefinition surface substantially works — §18.2 body-only, §18.3 cascade exact-sets incl. the Gap-noted zero-dependent shape verified live, §18.4 provenance, §18.5 trap depth-1, §18.6 recovery zero-residue, §18.8 backing-file floor, 0480 source display incl. multi-line docstrings, vec value-use. §18.1 stage-M scope note landed (T1 kinds = coherent-stale, verified live; cure horizon named); §18.3/.4/.5/.7 worked examples aligned to delivered output; new §18.8 restart-reaches-prompt MUST tagged [S102]. **5 defects (repro'd, no fixes)**: (1) `__expr` leaks into `; broken:` (stale expr-wrapper joins the transaction as dependent — §18.3 exact-set MUST violation; /int); (2) trap presentation wrapper `Error: codegen error at 0..0: runtime error: runtime panic:` — §18.5 format MUST; pre-existing generic to all runtime errors; /int; (3) `/sig` primary line ≠ bare lookup (unqualified types/name — §18.4 MUST; existing test only asserted `"Fn"`); (4) = /docs' 0486 independently reproduced; (5) restart with broken backing file **exits(1) before the first prompt** — locks out the §18.6 in-REPL repair path + doesn't name the broken symbol (filed → /int). FIXMEs filed as 0483/0484 — **collisions ×3 on 0483, ×2 on 0484; /sprint renumbers at gate**. 6b: `10-redefinition.demo` (sequenced around defects 1/4) + prior replay 01–09 + re-verify.

- 2026-07-03: **Phase 6a `/port` COMPLETE — 6a EXIT GATE MET (all 5 proxies done, plans produced, FIXMEs filed).** Exemplar GREEN (identical solution, 40/40 both polarities, web showcase incl. POST /solve verified; timing = known 0408 profile). Dev-loop at scale: where the transaction fires it is exactly right (precise cascade over solver/grid, 1ms trap, clean recovery; body-only 0–1ms — L-D1 holds); **but the T1 downgrade is the at-scale default** — unannotated redefinitions generalize to polymorphic → silent split-world (REPL sees new body, compiled callers keep old; no report) — nearly all exemplar fns are unannotated. **Weight for increment sequencing: the T1 cure is the at-scale UX, not an edge.** FIXME **0487** (/int): `/mod M` env lacks prelude values + type aliases M's own file compiled with (verbatim re-paste of real fns fails); introspection rejects FQ names the cascade report itself prints; `/refs` current-module-only. **Defects D1–D3 (repro'd)**: D1 `(def x 1)` + `/quit` poisons the directory (backing file gains a broken `defmacro` expansion; startup exit 1; `--no-cache` doesn't recover); D2 REPL adopts cwd `./user.cl` as session backing file + shares the `user` cache slot with a batch module (rewrote `exemplar/user.cl`; hybrid meta broke next session; data-loss class — `exemplar/CLAUDE.md` warning landed); D3 dependent recompilation of file-backed module symbols false-BREAKS ("definition source unavailable" cross-module; prelude-less recompile env same-module `undefined variable: None`) and revert cannot heal — restart-only recovery. D4 minors flagged unfiled (ADT payload display ~0050 class; arity error shape; `match` Int-literal pattern — /spec//frontend confirm designed-vs-gap). Vec unlock noted for 0408 increment II; 0474 caveat: solver's `set-cell` is exactly the leaking shape — leak growth on deep backtracking until S102. **6b: explicit no-op** on exemplar source.
- 2026-07-03: **6a gate housekeeping: FIXME collisions resolved** (earliest filer keeps): 0483 = /examples SIGBUS (kept); /stdlib generic-mono → **0488**; /repl restart-lockout → **0489**; 0484 = /stdlib import-shadow (kept); /repl qualified-ref → **0490**; frontmatter `number:` fields updated. Current open 6a-filed set: 0483 (SIGBUS), 0484 (import-shadow), 0485 (macro diag), 0486 (bare-lookup corruption), 0487 (/mod scope), 0488 (generic-mono), 0489 (restart lockout), 0490 (phantom member) + unfiled-but-repro'd: /repl's `__expr`-in-broken / trap-wrapper / `/sig`-mismatch; /port's D1/D2/D3. **All defect-class items route to the 6b /qa guard batch** per the defect discipline (proxy work unfinished until narrow failing repros exist).

### Phase 6b — user-proxy action

| Skill | Surface | Task | Status |
|---|---|---|---|
| /docs | user/ | cli-reference flag fix; new `guide/live-development.md` (binary-verified transcripts, sequenced around 0486 + `__expr` defects, honest T1 paragraph); getting-started pointers; CLAUDE.md row. | **done** — all 4 items landed, every transcript verified live; **FIXMEs 0491** (`__expr` cascade leak, deterministic — formalizes /repl's 6a find) + **0492** (`/sig` ≠ bare-lookup FQ, → /repl arbitrate); noted unfiled: 1-of-7 spurious-broken race (unreproduced, for /qa awareness); **stdlib prelude cold-cache compile FAILURE naming /stdlib's new `test-vec-concat-empty-right` row → hotfix routed back to /stdlib** |
| /repl | repl/ | New `10-redefinition.demo` (concrete-annotated sigs, defect-sequenced) + prior-demo replay 01–09 with compliance read. | **done** — demo plays GREEN (full §18 story, ~24 turns, real spans; sharper `__expr` sequencing constraint discovered: eval-wrapper rejoins transactions on signature change of anything it called — incl. reverts); replay 01–09 GREEN (06 exposed 0486 live in a shipped demo — resequenced — plus a NEW defect: **nested parameterized-ADT display garbled**, filed 0491 → renumbered **0493** at gate; existing guard presence-only, too weak); §18.8 stale 0483→0489 ref fixed |
| /examples | examples/ | `33-redefinition.cl` headline + `14-vecs.cl` single-instantiation sub-test; report exit-code changes for /qa's `tests/examples.rs` update. | **done** — `33-redefinition.cl` (3 sub-tests: direct rebind / dependent rebind / 3-deep transitive; exit **136**); `14-vecs.cl` vec-ops-as-values section, one instantiation per HOF per 0483 (exit 29 → **81**); plan table + coverage rows updated; **full replay 33/33 green**; no new FIXMEs. **For /qa exit-table: 14-vecs 29→81; 33-redefinition NEW 136.** |
| /stdlib | stdlib/ | `vec-concat` = `(vec-reduce vec-push va vb)` (+ self-test rows); vec-flatten self-test HELD (rides 0488, note landed instead); optional fn-as-value blast-radius sweep. | **done** — simplification ATTEMPTED, REVERTED: fold body works bare but **poisons composed use at the consuming turn** (`(count (vec-concat …))` → `undefined function: count`; loop body clean — stash round-trip confirmed) = **THIRD 0488-family signature**, recorded as 0488 addendum for /qa's batch; +4 composed-shape self-tests green vs loop body (double as fold-body regression guards; collections.vec 14→18); vec-flatten note landed as directed; blast-radius sweep: broken cell bound stays `vec-flatten`-only |
| /port | exemplar/ | Explicit no-op (6a landed the CLAUDE.md warning + 0487). | **done (no-op)** |
| /qa | tests/ | **(user-mandated, after the guard batch) Special risk review + coverage audit**: (1) miss post-mortem — per 6a defect, "what lane would have caught this and why doesn't it exist?" (audits the Wave-1 post-mortem's narrowness too); (2) risk register ranking the systematically-untested surfaces (multi-session persistence; file-backed modules + `/mod`; instantiation/value-use matrix; display-conformance exactness; the unannotated path) by user impact — S102 sequencing input with /port's T1 finding; (3) coverage-model audit (substring-assertion class vs normative MUSTs; zero-negative-coverage surfaces); (4) deliverables: durable risk/coverage doc in `tests/plan/`, proposed lane families, FIXMEs where other skills own the enabler. | **done** — `tests/plan/coverage-audit-s101.md` (4 axes); 6-pattern miss taxonomy over 17 items; 7 lanes + 4 standing drafting rules; per-crate submodule thinness map; top-8 risk register (T1 #1); FIXMEs 0495–0498 filed |
| /qa | tests/ | **Guard batch (serial, after the 4 above)**: failing-not-ignored narrow repros for 0483 (SIGBUS), 0484 (import-shadow), 0486 (bare-lookup corruption), 0488 (generic-mono + vec-flatten shape), 0489 (restart lockout), /repl's `__expr`-in-broken + trap-wrapper + `/sig`-mismatch, /port's D1/D2/D3; `tests/examples.rs` exit-table update; final counts. | **done** — 19 RED guards + 9 green controls / 7 files, all RED-first-watched; 3 stdlib-free reductions (0484, 0488 all 3 signatures, D1 mechanism isolated); exit-table updated; canonical 3480/3458/22/1 @ ~60s ×2 identical; ledger entry |

- 2026-07-03: **User scope mandate: special /qa risk review + coverage audit** — triggered by the 6a miss pattern (~10 defects found by proxy exercise despite QA-first + 20 guards + §18 re-anchor + 5 D/D/R cycles). Runs as a dedicated /qa invocation after the 6b guard batch, before Phase 7. Shape: miss post-mortem per defect → risk register (ranked, S102 sequencing input) → coverage-model audit → durable `tests/plan/` doc + lane proposals + enabler FIXMEs.

- 2026-07-03: **Phase 6b `/stdlib` COMPLETE — simplification correctly reverted.** The 6a-verified `(vec-reduce vec-push va vb)` shape holds standalone but fails composed (`count`/`get` over the fold-bodied generic → `undefined function` attributed to the OUTER imported generic; inference also degrades). Loop body confirmed clean via stash round-trip; landing would have regressed working stdlib behavior. Third 0488-family signature → FIXME 0488 addendum (composition over builtin-as-value fold-bodied generic). 4 new composed-shape self-tests land green against the loop body AND double as regression guards for any future fold-body re-attempt (`NOTE(0488-family)` names the re-attempt trigger). collections.vec 18/18; neighbors unchanged. vec-flatten owed-test note landed; sweep bounds the broken generic-as-value cell to `vec-flatten` only (concrete-as-value cells all green).

- 2026-07-03: **Phase 6b `/docs` COMPLETE.** cli-reference synopsis+table now binary-exact (§0.6 MUST restored); `user/guide/live-development.md` landed — full §18 story with live-verified transcripts, faithful trap output + wrapper-defect footnote, stage-M honesty section (coherent-stale transcript anchored to §18.1), restart-floor as-is; getting-started optional-prelude note + pointer; CLAUDE.md row. FIXMEs: **0491** (/qa — `__expr` cascade leak now deterministically pinned: any expression turn before the breaking redefinition) and **0492** (/repl — `/sig` unqualified primary line, spec-vs-impl arbitration). Unfiled notes: one-off spurious-broken under fast piped input (1/7, unreproduced — /qa awareness); **prelude cold-cache regression: `CRANELISP_LIB=stdlib` in a clean dir aborts startup — `module 'prelude' failed … collections.vec.test … ambiguous type … test-vec-concat-empty-right`** — names /stdlib's OWN new 6b test row (their `--no-cache` direct-load runs were green; the prelude-compile path is stricter) → **hotfix dispatched to /stdlib** (likely S84-ruling annotation on an empty-vec literal). Breaks getting-started's stdlib instructions on cold cache until fixed.

- 2026-07-03: **Phase 6b `/repl` COMPLETE.** `10-redefinition.demo` GREEN + active set + CLAUDE.md table (09 row backfilled). Replay 01–09 all GREEN (worked around the in-flight /stdlib edit via pristine-HEAD stdlib snapshot; re-verified 06+10 against the recovered live tree). Demo work sharpened the `__expr` model: the eval wrapper rejoins any later transaction where a symbol it called changes signature — reverts included (0491's determinism confirmed from a second angle). Demo 06 exposed **0493** (renumbered from colliding 0491): nested parameterized-ADT payload display garbled — `(MkWrap (MkWrap 7))` → `(Wrap.MkWrap primitives/Int) (Wrap.MkWrap 7))`; unbalanced parens; spec's own §3.7 list example violates §1.5; concrete payloads fine; likely = /port's unfiled D4; existing display guard presence-only → joins the /qa guard batch. Collision ledger: 6-of-8 parallel-proxy filings collided this phase — max+1 scanning is not collision-safe under parallel filing; methodology note for Phase 7 findings.

- 2026-07-03: **User ruling: strategy-derived unit-scenario discipline.** /dev tiers must derive unit scenarios from the implementation STRATEGY (not just spec): complexity paths, edge cells of implied matrices (all cells, not the design-named one), negative cases — expressed through the crate facade, organized seam×scenario-class so /qa audits mechanically. Encoded binding in `METHOD.md` §2.2 ("Implementation-strategy unit scenarios", with S101 evidence: 0479/0483/0488/D1/D2); architecture half filed as **FIXME 0494** (/arch — facade test-organization convention + propagation to the 4 import blocks per the Principle-22 precedent; principle-vs-convention is /arch's call); memory written. The in-flight /qa audit's per-crate unit-tier axis now audits against this taxonomy; S102 D/D/R cycles inherit it. (Numbering note: 0494 reuses a historically-deleted number — "0494 bug #2" refs in older docs predate this file; joins the collision finding for Phase 7 methodology notes.)

- 2026-07-03: **/stdlib hotfix resolved as SNAPSHOT RACE — no live regression.** The /docs cold-start abort reproduces ONLY against the transient mid-6b fold-body stdlib state (reverted before 6b closed); the landed tree: 9/9 cold-cache + 3/3 --no-cache startups green, self-tests 18/18. Sharpened facts → 0488 addendum: fold-body failure is typecheck-time (a re-attempt aborts cold startup entirely); `:(Vec Int)` annotation does NOT cure it; the "prelude path stricter" framing was wrong (both paths fail identically on the fold-body copy — the two 6b agents saw different snapshots). Comments/records only; no row changes. **All 6b proxy work complete → /qa GUARD BATCH DISPATCHED** (12 defect items incl. the 3 unfiled-guard-is-the-record items + D1–D3 + examples exit-table; RED-first; final canonical counts for close).

- 2026-07-03: **User refinement of the strategy-derived unit-scenario ruling: the seam unit is the SUBMODULE, not the crate.** Bounded contexts (crate boundaries) are settled and not in question; the discipline governs internal module composition inside each crate + per-submodule test accounting. Named anti-pattern: monolithic crate-root `tests.rs` (exhibit: backend — flat 5.9k-line `tests.rs` over 32.5k LOC of well-composed submodules; submodule coverage unattributable). Encoded: METHOD.md §2.2 sharpened (submodule×scenario-class organization, anti-pattern named); FIXME 0494 amended (post-filing addendum for /arch — module-composition companion convention, crate splits explicitly out of scope); pending /qa risk-review + coverage-audit brief will assess the unit tier **at submodule granularity** (seam inventory = submodule inventory; map tests→submodules; flag zero-coverage + happy-path-only submodules).

- 2026-07-03: **/qa GUARD BATCH COMPLETE — Phase 6b closed.** 19 new intentional-RED guards + 9 green controls across 7 files, all RED-first-watched with expected signatures: 0483 ×3 (SIGBUS both modes; populated-slot control), 0484 ×1 (stdlib-free reduction; pins order-agreement + /info-agreement), 0486 ×2 (healthy + broken arms), 0488 ×3 (**all three signatures reduced stdlib-free**, incl. (c) via local vreduce/vconcat/vcount fixture — micro-shape-sensitive, do-not-simplify header), 0489 ×1 (§18.8 floor: prompt + repair), 0491 ×2 (break + revert-rejoin angles), trap-format ×1 (§18.5, guard-is-the-record), 0492 ×1 (spurious first draft corrected 2→3 threshold, re-watched RED), 0493 ×2 (exact nested-ADT strings; presence-only guard strengthened → intentionally RED), D1 ×1 (**stdlib-free reduction; mechanism isolated** — backing file persists both defmacro expansion artifact AND original call form, which don't co-load), D2 ×1 (§15.4.7 authorship-fidelity leg; the hybrid-meta next-session break did NOT reproduce in six reductions — exemplar-only, ledger note), D3 ×1 (cache-restored /mod redefinition dies at unknown-type wall, 0487 adjacency; fresh-session file-backed transactions all correct — control pins). Exit table: 14-vecs 29→81; 33-redefinition→136 (clean round-trip, no >127 masking). Ledger: grouped 12-item entry in `tests/plan/ledger.md`. **Canonical numbers (two consecutive runs, identical fail sets, zero flakes): 3480 run — 3458 passed / 22 failed / 1 skipped @ ~60s.** Intentional-RED itemization: 3× 0474 COW-leak + 19 this batch. **Close flag (user edit): root CLAUDE.md intentional count 4→22, total 3453→3480, runtime ~60s.** Unreduced residues (recorded in ledger): D2 hybrid-meta break (exemplar-only), D3 exact exemplar faces (blocked behind the unknown-type wall), 0488(c) flipping micro-detail.

- 2026-07-03: **/qa RISK REVIEW + COVERAGE AUDIT DISPATCHED** (user-mandated; last pre-close invocation). Four axes: (1) per-defect miss post-mortem with miss-pattern taxonomy; (2) coverage-model audit (substring/presence-only assertions, zero-negative surfaces, single-session-only REPL testing, no file-backed-module lane, cache-restored-path gap, unannotated-default-path blindness) + lane proposals; (3) per-crate unit-tier audit at **submodule granularity** (submodule inventory → test mapping → zero/happy-path-only flags per §2.2 taxonomy; backend flat tests.rs exhibit; depth on backend + src/); (4) risk register ranked by user impact for S102 sequencing, T1 weighted top. Deliverables: `tests/plan/coverage-audit-s101.md` + enabler FIXMEs (guarded defects excluded — guard is the record). Phase 7 outcome authoring follows its return.

- 2026-07-03: **/qa RISK REVIEW + COVERAGE AUDIT COMPLETE.** Durable doc `tests/plan/coverage-audit-s101.md` (standalone S102 Phase-1 scoping input); suite untouched. **Axis 1 — six miss patterns over 17 classified items, no single hole**: P1 assertion-too-weak ×3 (substring/presence where spec pins exact output — 0492/0493/trap-format; the 0493 guard *existed and passed* on garbled output); P2 canonical-script blindness ×3 (session-history preamble never a test dimension — 0486/0491/0484); P3 never-exercised state combination ×4 (D1/D2/D3/0487 — D2 structurally hidden by the fresh-tmpdir discipline); P4 strategy-matrix single cell ×3 (0483 instantiation-count axis, 0488 referent-genericity axis, 0474 rc>1 branch — METHOD §2.2's exact class); P5 design-blessed behavior ×2 (0489 lockout floor faithfully pinned as designed; T1 split-world — **not testing failures: this is why Phase 6a exists**); P6 diagnostic-surface exemption ×2 (0485/0490 — the "error tests use substring" standard institutionalizes P1 for diagnostics). P1/P2/P3/P6 = 12/17 are e2e-construction properties /qa cures with lanes; P4 is the §2.2 discipline + per-crate drains. **Axis 2 — quantified**: 1,817 presence-style assertion sites vs 13 exact-output / 0 regex / 0 golden (helpers exist, unused); 15 files with ≥10 tests carry zero negative-named tests; multi-session exists (`run_again` ×67) but only over simple healthy states; redefinition fixtures 39 concrete-annotation sites vs ~2 polymorphic — **the at-scale default path is nearly invisible**. 7 lanes proposed (L-N1 display-exact, L-N2 no-internal-artifacts, L-S1 preambles, L-S2 session-lifecycle grid, L-S3 file-backed dev-loop, L-M1 reference-shape matrix, L-U1 unannotated-default siblings) + 4 standing drafting rules. **Axis 3 — submodule thinness map**: backend THIN + unattributable (flat 5,861-line tests.rs; ~5k LOC strategy zero-dedicated — rc_emission, fn_as_value ~7 happy-only, match_codegen, let_if); src/ THIN on exactly the 6a defect surfaces (**lifecycle.rs 1,918 LOC / ZERO tests — largest untested surface in repo**; display.rs ADT render, save.rs macro round-trip, cache_restore all ZERO; new S101 seams well covered); typecheck GOOD except traits/ pooled (monomorphise 1,070/0-inline); frontend GOOD minus rendered-diagnostic tests (0485 class); types ADEQUATE with marshal drift-guard hole; intrinsics/primitives/platform ADEQUATE (io_guard, declare.rs, concurrency.rs v9 vtable ZERO-inline). **Axis 4 — risk register (user impact)**: 1. T1 silent split-world (S102 top; L-U1 first; interim cure = make the downgrade PRINT); 2. persistence-integrity cluster D1+D2+0489 (/int; data-loss class); 3. 0488 generic-fn missing mono (cross-skill isolation owed before fix dispatch); 4. fn_as_value/COW cluster 0483+0474+0476 (hold increment-I sequencing; §2.2 matrices as unit half); 5. file-backed dev-loop D3+0487; 6–8. 0486 / 0484+precedence-pin / display-diagnostics batch. **FIXMEs filed (enablers only): 0495** /dev(backend) tests.rs split + thin-submodule drain (rides increment I); **0496** /dev(src/) session-seam unit drain (= unit half of the S102 /int defect wave); **0497** /dev(typecheck) de-pool traits/; **0498** /dev(types) marshal drift-guard. Recommended /qa S102 order: L-U1 → L-S2+L-S3 → L-N1/L-N2 → L-S1 → L-M1-with-increment-I. **Housekeeping owed (outside audit's boundary): register the doc in `tests/CLAUDE.md` §Plan documents** — /qa one-liner, flagged at close.

- 2026-07-03: **Close review, first pass (user).** Three directives actioned: (1) **root `CLAUDE.md` §Testing edit delegated to /sprint and DONE** (see Close actions); (2) **audit-finding tracking completed** — the per-bounded-context unit-tier refactor + qa lane refactor are now fully enqueued as FIXMEs: audit-filed 0495–0498 (backend split/src/typecheck/types) + /sprint-filed **0499** (/qa — the 7-lane e2e refactor + 4 standing drafting rules + audit-doc registration; lanes ride the sprints touching each surface, L-U1 first), **0500** (/dev frontend — rendered-diagnostic tier, P6 cure), **0501** (/dev intrinsics — io_guard/strand), **0502** (/dev platform — declare.rs/concurrency.rs v9 vtable); primitives had no named thin spot — no FIXME. Tracking mechanism: standard FIXME lifecycle — S102 Phase 1 scope carries, wave gates block while open, 2×-deferral escalation applies per METHOD §2.4; drains ride each crate's next D/D/R touch rather than a standalone refactor sprint. (3) **FIXME-numbering ruling: keep max+1 with /sprint gate-resolution of collisions** — recorded in Findings 4; no METHOD amendment. P22 full text presented for ratification.

## Outcome (Phase 7)

**Status: CLOSED — approved by user 2026-07-03. Principle 22 RATIFIED (full text reviewed at close).** Residual for /arch's first S102 touch: update P22's provisional-ratification blockquote to record the 2026-07-03 ratification (principle file is /arch-owned; this archive + the close commit are the audit trail).

### Delivered

**The R3 machinery (sprint goal) — in full, plus the sprint's own RED witness cured:**

- **Dependent-recompilation transaction** (`src/redefine.rs` + rewritten `worker.rs` commit gate): `AbiSurface` alpha-canonical summary diff, `RedefKind` classification (body-only fast path at today's cost — L-D1 median 0.0ms), `RetentionPool` with paired provenance strings (Principle 22 shape), `BrokenRegistry`, on-demand `ReverseIndex` over the widened `callees` feed, Tarjan reverse-topo SCC walk with slot-less pass-through (0473 ruling), `TransactionReport` + §18.3 cascade UX, GOT-exhaustion surfacing. All 13 stage-M guards + L-R1–L-R5/L-D1 lanes flipped green.
- **ABI-epoch slot versioning**: fresh slot + frozen old slot on ABI-changing redefinition; frozen-world semantics for pre-break closures; persistence pins (i)–(iv) honoured; §8.2 slab-growth invariant verified.
- **Per-symbol trap stubs**: `compile_trap_stub` — the sprint's one public-API addition, landed exactly as the approved §8.3 shape; BROKEN provenance via `/info`/`/sig`/bare-lookup; cache-write poisoning per §18.8.
- **The latent type-change hole cured for concrete targets** (L-R4): a param-type-changing redefinition now traps-or-recompiles instead of leaving compiled callers silently unsound. (T1 kinds — see Deferred.)
- **NULL-slot fn-as-value defect fixed at both seams** (wrapper GOT-indirect + curry unknown-builtin arm; shared emission cores per Principle 7) — all 7 `vec_query_value_use` guards green; the S100 carry closed.
- **Enablers that ride permanently**: 0470+0472 — `call_graph_edges` widened to every statically-resolved user-fn reference incl. impl/default/HKT method bodies (schema v11, one bump); R18 deterministic final persist + generation-counted staleness (also rooted out the latent S91 `search_burndown` flake); manifest global-key convergence class fix; `CRANELISP_NO_OWNERSHIP` toggle + manifest key + certified polarity lane (increment I's differential oracle); `default-members` — the canonical suite now runs every crate's lib tier.
- **Normative + explanatory surface**: `repl/spec.md` §18 (trap wording, cascade report, provenance, frozen-world, recovery, persistence floor, stage-M scope note); `design/int/session-transaction.md` as-built; `exec-flow-redefine` sequence diagram (+2 drifted diagrams reconciled); **Principle 22 authored** (ratification below); `user/guide/live-development.md`; `repl/demos/10-redefinition.demo` (+01–09 replay green); `examples/33-redefinition.cl` + `14-vecs.cl` value-use section (33/33 green); stdlib composed-shape self-tests (fold-body simplification correctly reverted — found the third 0488 signature).
- **Defect discipline output**: 19 failing-not-ignored guards + 9 green controls for the entire 6a/6b defect set (3 stdlib-free root-cause reductions among them — D1's mechanism fully isolated); grouped ledger entry; `tests/plan/coverage-audit-s101.md` (4 axes) + `s101-coverage-postmortem.md`.
- **Methodology (user rulings, now binding)**: METHOD §2.2 implementation-strategy unit scenarios at **submodule × scenario-class** granularity (anti-pattern named: monolithic crate-root tests.rs); FIXME 0494 carries the architecture half to /arch.

**Final canonical suite: 3480 run — 3458 passed / 22 failed / 1 skipped @ ~60s** (×2 identical, zero flakes; was 1812/4/1 @ S100 close — growth = default-members lib tiers + sprint tests + guards). All 22 REDs itemized intentional: 3× 0474 + 19 6b-batch.

### Deferred (with rationale)

| Item | To | Rationale |
|---|---|---|
| **T1 type-change hole for non-concrete targets** (silent split-world) | **S102, ranked #1** | Design-ruled stage-M downgrade (mid-turn reload would resurrect stale backing source); /port + audit establish it as the **at-scale default path**, not an edge. Interim cure candidate: make the downgrade print. |
| 0474 COW copy-branch leak (3 guards) | S102 /backend | User-approved carry; rides increment I's rework of the same `fn_as_value` seam with 0483 + 0476. |
| 0476 phantom-NULL-slot representation cure | Increment I first change-sets | /arch-ruled IN; rides the ModeSummary types surface — one schema cascade (Principle 8). |
| 6a/6b defect set: 0483/0484/0486/0488/0489/0491/0493 + trap-format + 0492 + D1–D3 | S102 (owners per ledger) | All guarded failing-not-ignored; guards are the record + trigger. Risk register orders them. |
| FIXME-only findings: 0485 (diag quality), 0487 (/mod scope), 0490 (phantom member), 0492 arbitration half | S102 | Usability class; 0492 needs /repl spec-vs-impl arbitration first. |
| Enabler FIXMEs 0494–0502 | S102 Phase 1/3 | 0494 /arch convention; 0495–0498 + 0500–0502 per-bounded-context unit-tier drains (every crate with a named thin spot now has a tracked carrier; primitives had none); 0499 /qa e2e lane refactor (7 lanes + 4 drafting rules, ordered). |
| 0482 `#[non_exhaustive]` | Next backend change-set | /arch-timed; attribute free today. |
| 0463 poll example; 0459 spark gate; 0408 perf; 0050/0052/0365/0416 | Unchanged pins | Unmet triggers / increment II / `--release` polish (0463 blockers re-verified this sprint). |
| Increments I (read path) + II (write path) | S102+ | Per ROADMAP; golden-CLIF capture MUST be increment I's first change-set. |

### Findings

1. **Six-pattern miss taxonomy** (audit, 17 defects classified): the ~12-defect 6a yield despite QA-first + 20 guards + 5 D/D/R reviews decomposes into P1 weak-assertions / P2 script-blindness / P3 unvisited-state-combos / P6 diagnostic-exemption (12/17 — curable by the 7 proposed e2e lanes + 4 drafting rules), P4 strategy-matrix single-cell (3/17 — cured by §2.2 + the 0495–0498 drains), and P5 design-blessed behavior (2/17 — **not a testing failure; the validation that Phase 6a must stay mandatory even in machinery sprints**). Evidence scale: 1,817 presence-style assertion sites vs 13 exact-output.
2. **The unannotated path is the at-scale default** (/port → audit #1): unannotated fns generalize → every redefinition takes the T1 downgrade → silent split-world. 39 concrete-annotation fixture sites vs ~2 polymorphic in the suite. S102's headline consideration.
3. **`lifecycle.rs`: 1,918 LOC, zero unit tests** — the largest untested surface in the repo, sitting exactly under the 6a persistence-defect cluster. FIXME 0496 is its drain.
4. **FIXME max+1 numbering is not collision-safe under parallel filing** (6-of-8 collided in 6a/6b; plus deleted-number reuse ambiguity — 0494). **User ruling at close: keep the current mechanism — /sprint resolves collisions at the gate** (earliest filer keeps the number; /sprint renumbers later filers + updates frontmatter). No METHOD §3.3 amendment; gate-resolution is the standing procedure.
5. **Parallel proxies on a mutating tree see different snapshots** (the /docs "cold-cache regression" was /stdlib's already-reverted transient state): defect reports from parallel 6b agents must be validated against the landed tree before hotfix dispatch. Cheap rule; saved a pointless hotfix this time only by luck of ordering.
6. **The user rulings of this sprint** — strategy-derived unit scenarios at submodule granularity (METHOD §2.2 + 0494) — are the structural answer to P4 and to finding 3; S102 D/D/R briefs inherit them.

### Close actions (on your approval)

1. ~~Root `CLAUDE.md` §Testing~~ **DONE — user delegated the edit to /sprint at close review**: intentional block rewritten S100→S101 (22 itemized, ledger pointer), total 3480, runtime ~60s + three-minute timeout expectation, `--no-fail-fast` canonicalized, stale "11 sketch_port + 2 v4_platform pre-existing failures" bullet removed (those tests left with the S87 sketch deletion; the two canonical runs show exactly the 22 intentional REDs and nothing else).
2. **Ratify Principle 22** ("published pointers have retention owners") — authored mid-sprint under your mandate; RetentionPool + 0479 + the drop-trio tripwires are its instance register. Full text presented to user at close review.
3. `tests/CLAUDE.md` §Plan documents: register `coverage-audit-s101.md` — folded into **FIXME 0499** item 3 (/qa's first S102 touch).
4. **Arch-principles adequacy check** (standing close prompt): P7/P8/P20/P21/P22 were all load-bearing in this sprint's rulings; the new P22 + the 0494 convention question are the only open principle-level items. Adequate in my judgment — your confirmation invited.
5. Archive: `git mv sprints/SPRINT.md sprints/archive/sprint-101.md`; ROADMAP row (S101 closed, S102 = increment I + defect wave, scope inputs = risk register + 0494–0498 + T1); commit.
