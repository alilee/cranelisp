# Sprint 113: Reliability Risk-First + S112 Defect-Family Drain

**Status**: PHASE 7 CLOSE (outcome authored; awaiting user close approval)

**Goal**: Stand up the memory-safety reliability design risk-first (`/qa` global-risk assessment gates depth), extinguish the S112 pinned defect families (multi-sig mono/carrier, binders, persistence), and discharge the twice-declined frontend audit — chunks A–F from Phase-1 scoping, stacked in manageable waves.

**Audit**: cranelisp-frontend (twice-declined rotation — MUST dispatch this sprint or obtain explicit user sign-off for a third decline; read-only, may dispatch early and run parallel)

## Scope

User-approved cut (2026-07-19): **chunks A–F, all in, waved**. The SROA / `--release` frontier (chunk G) explicitly queues behind this sprint.

### D2 ruling (user, 2026-07-19) — settled at Phase 1

**Importing a trait METHOD without its TRAIT suffices for dispatch.** The method import identifies the canonical module; impl coherence is global; whatever is needed for dispatch follows from the method's fully-qualified identity (keyed lookup, P24-consistent — no scan). `/spec` scribes this with the edge cells enumerated (see /spec plan). Consequences in-sprint: the D2 fix direction is settled (typecheck must ACCEPT the nullary method-only-import cell and codegen must link it — the leak closes on the accept side, not the reject side); `Default` prelude promotion unblocks.

### A. Memory-safety-soundness track (carried, deferral count 1 — anchor)

**Risk-first, user-directed (2026-07-19):** `/qa` FIRST assesses memory-safety **in the context of the global solution risk** — is this the biggest risk to correctness, ranked against the whole landscape? Only if the assessment confirms it do we go deeper. The reliability design is careful and deliberate, drawing on the user's mechanism palette:

- static analysis (compile-time invariant narrowing per P25 — every elision checkable against its conservative fallback)
- increased assertion density at the RC/alloc seams
- **diagnostic modes**: no-reuse-after-free (quarantine), scrub-freed-memory (poison), paired counters (allocs ≡ frees), differential oracle vs default mode
- generative harness + standing audit category (`tests/plan/memory-safety-coverage.md`)

**Gate**: `/qa`'s Phase-3 risk assessment + proposed depth returns to the user before the build wave (W5) is sized.

**Ownership (per Phase-2 review, revision 1)**: `design/arch/safety-invariants.md` is **/arch-owned alone** (R1–R13 register + §2 ladder + §3 frame; /design files FIXME `target: /arch` for register-row changes). `/design` per crate owns the mechanism elaborations at their own homes (typecheck §15 rule table, backend censuses, cache trust seam, int live-table asserts). `/qa` owns `tests/plan/memory-safety-coverage.md` + tier-4/5 lane mechanics. The mechanism palette maps 1:1 onto the existing §2 ladder (static analysis→tiers 1–2, assertions→tier 3, diagnostic modes→tier 5, oracle→tier 4) — W5 selects tiers per register row, no parallel taxonomy (P7).

**No types-crate or allocator-hook work pre-gate (revision 2)**: the diagnostic modes are intrinsics-internal, env-gated allocator behavior — no extern-signature change; any genuine interface need is a post-gate Phase-3 /arch approval. W5 owns the sprint's only permitted schema bump (21→22, if the §3b origin split lands — revision 4). FIXME cluster 0637/0638/0641 dispositions inside this track.

### B. Multi-sig mono/carrier defect family (all pinned failing)

R1 ×2 (cross-arity poly-clause self-call — primitive + prelude-`+`; fix direction §11.3.4, P26-shaped), R2 (carrier loss in mono bodies — producer-side), D3 (poly callee in cross-arity-reached clause never monomorphised — **typecheck-only per Phase-2 review revision 3**: producer-side `carrier-loss` family, backend delta limited to diagnostic hardening; /design(typecheck) produces call-chain evidence before the fix), TB-24 (poly-applied impl wrong-reject), D1 (multi-sig display drops constraints — Phase 3 confirms it stays inside W2 typecheck, no int-side echo patch, revision 9), plus the D2 fix per the ruling above (no new resolution machinery — root method resolution at the chain-followed home, populate the existing `MethodResolutions` carrier for the nullary cell; the unary "no impl" reject INVERTS). Pin-4 entanglement (trait-`+` twin hits carrier-loss) informs sequencing — /qa owes the PLAN rows ×4 + precise attributions from S112 Phase 6b. W2 expects **no schema bump** (behavior fixes over existing persisted carriers; any shape change escalates to /arch). **0553 re-pointed** (revision 8): opens a new three-surface public entry point — lands in the W5/S114 ownership-machinery window unless /design(typecheck) shows W2 opens the seam anyway; requires explicit /arch surface approval either way.

### C. Frontend wave: audit + binder family

The escalated `/audit` rotation on cranelisp-frontend. Binder ×3 pins (qualified defn/deftrait heads silently accept) + the veto-visible binder generalization to `def`/`def-`/`deftype`/`deftype-`/`defmacro`/`defmacro-`/`const`/`const-` (stood un-vetoed at S112 close). 0589 (qualified-lowercase annotation mints) is the same family — drain it here. 0613 status verified (S111 record says the quasiquote fold landed; owner closes or names the residual).

### D. REPL persistence + shadowing (src/)

RT-4 ×2 (impls lost from regenerated user.cl — **data-loss class**), shadowing ×2 (hang + overload-gate). All pinned. 0646/0647 (primer collision, empty-impl display) ride as small src/-side items.

### E. User-facing debt sweep (Phase 6)

0648 (traits/impl user guide), 0649 (errors catalog S112 diagnostics), 0631 (return-poly remedy catalogue), archive-demo de-leak (/repl), `Default` prelude promotion (/stdlib, now unblocked), 0577 (/repl primer coverage), 0463 (/examples network-poll — candidate).

### F. Phase-1/3 riders

0628 tracker retirement judgment (/design, early); P26 carrier→pass→window classification sweep; /design process change — **diff the design's case list against the spec's before Phase-3 exit** (S112 finding, adopt this sprint); 0632/0633 battery inputs + 0604 (/qa, Phase 3); 0605 stdlib compile smoke gate (/testing, W1); 0635 + 0590 (/design dispositions).

### Out of scope

- SROA / `--release` LLVM tier (chunk G) — next arc, S114+.
- 0050 / 0052 — Phase-H release-polish gated; deferral stands.

## FIXME debt

| FIXME | Target skill | Status | Disposition this sprint |
|---|---|---|---|
| 0628 | /design | **RETIRED P3** | Deleted — matrix closed by committed guards |
| 0637 | /design | open | Track A, W5 (gated) |
| 0638 | /qa | **RETIRED W1** | Repro pin landed ×3 modes; file deleted; attribution stands (/dev src marshal/invoke seam) |
| 0641 | /design | open | Track A, W5 (gated); fix gated on §3 frame per /qa depth rec |
| 0553 | /typecheck | **deferred P3** | W2 does NOT open the seam (design evidence) → S114 W5-if-deep window |
| 0589 | /dev | deferred (S110) | W3 (C) — designed: annotation-path one-liner (`parse_annotation_name`) |
| 0613 | /dev | open | VERIFIED fully landed — /dev deletes in W3; stale test-comment → /testing |
| 0590 | /design | **re-targeted P3** | Not frontend — /design(typecheck), S114, kept open |
| 0635 | /design | **RETIRED P3** | Drained by /design(backend): premise corrected, dead-copy deletion ruled (/dev W2), slice archived; file deleted |
| 0604 | /qa | open | Kept — W4 R7 rider is the diagnosis plan of record |
| 0632 | /qa | **RETIRED P3** | Deleted — all asks live in s111-principle24-register.md |
| 0633 | /qa | **RETIRED P3** | Deleted — DG-R1a/b/c REDs are the trigger; residuals routed (MS-P4, W5 R4 census) |
| 0650 | /design | **RULED P3** | Arch option (a): int re-anchoring seam, synthetic-location predicate, W4 in-sprint; re-targeted /design(src-int) |
| 0651 | /spec | **NEW P3** | §5 binder enumeration gap: deftrait method-signature names — P5-window rider |
| 0652 | /arch | **NEW P3** | `backend-keyed-consumer.md` §3 inventory row for the TCO fast-path — P5-window rider |
| 0605 | /testing | **RETIRED W1** | Gate confirmed already landed S110 (`stdlib_conformance.rs`); file deleted |
| 0646 | /dev | open | W4 rider (D) |
| 0647 | /dev | open | W4 rider (D) |
| 0648 | /docs | open | Phase 6 (E) |
| 0649 | /docs | open | Phase 6 (E) |
| 0631 | /docs | open | Phase 6 (E) |
| 0577 | /repl | open | Phase 6 (E) |
| 0463 | /examples | open | Phase 6 candidate (E) |
| 0050 | /int | deferred | Stands — Phase-H release polish |
| 0052 | /repl | deferred | Stands — Phase-H feature |

## Architecture review (Phase 2)

**Verdict: APPROVE-WITH-REVISIONS** (/arch, 2026-07-19). The scope is technically coherent, debt-first-weighted, and P8-clean — the risk-first reshape of track A is itself the P8-correct move (no reliability infrastructure built ahead of the depth ruling). Nine revisions below; none blocks Phase 3 from opening, but 1–3 must be reconciled into the draft before dispatch.

### Q1 — Track A ownership + interfaces

**Ownership correction (revision 1).** `design/arch/safety-invariants.md` is **`/arch`-owned alone** — it is in the canonical set (register R1–R13 + the §2 ladder + §3 frame); `/design` never edits it (files FIXME `target: /arch` for register-row updates). The split is: `/arch` = the register and the binding frame; `/design`(per crate) = the per-crate mechanism elaborations at their own homes (`design/typecheck/ownership-inference.md` §15 rule table per §6 task 1; backend elision/identity census, task 2; cache trust seam, task 3; int live-table asserts, task 4); `/qa` = `tests/plan/memory-safety-coverage.md` + the tier-4/5 lane mechanics (R8/R9). Reword the track-A gate line accordingly.

**The mechanism palette is not new taxonomy (P7).** The user's palette maps 1:1 onto the existing `safety-invariants.md` §2 ladder: static analysis per P25 → tiers 1–2/§3; assertion density at RC/alloc seams → tier 3; the diagnostic modes (no-reuse quarantine, scrub-freed poison, paired alloc/free counters) → tier 5 dynamic lanes (R8's family); differential oracle → tier 4 (R9). W5's design task is *selecting the strongest applicable tier per register row*, not inventing a parallel mechanism vocabulary.

**No types-crate work now (revision 2).** The diagnostic modes are **intrinsics-internal, env-gated allocator behavior** — `alloc.rs` already owns allocation metadata (it detects double-frees today), so quarantine/scrub/counters need no signature change at the backend→intrinsics extern seam and no `cranelisp-types` surface. The differential oracle is the existing analysis-off toggle + /qa lanes. I am making **zero `cranelisp-types` edits at this review**; if W5 design discovers a genuine cross-crate need (e.g. backend-emitted check sequences, a mode-selection contract visible to emission), that is a post-W0-gate Phase-3 `/arch` approval — building a hook surface ahead of the depth ruling would be the P8 violation this track was reshaped to avoid.

**Register re-audit (P25 Phase-2 obligation).** Rows unchanged since S111. R1 rides the W5 gate (§3 mechanism). R5/R6 = 0637, correctly gated in track A (forward obligation, no live hole — do NOT let it force a consumer into W5 that the gate doesn't justify). R7 (0604) is `unasserted` and intermittent — see revision 7. R13 stays parked, named.

### Q2 — D3 seam: typecheck-only, producer-side (revision 3)

D3 is a **producer failure, P26-shaped, same family as R2** (/testing class-mapped it `carrier-loss`): the poly callee reached only through a cross-arity clause's mono recheck is never enqueued/instantiated, so no mono instance or carrier entry exists and backend's keyed read has nothing to fetch. Under P24/BC §3 invariant 10 the backend is a pure keyed consumer — "backend leg" for the *fix* is architecturally excluded. Scope W2 **typecheck-only**; the sole admissible backend delta is **diagnostic hardening**: if the miss surfaces as a raw Cranelift/link `undefined function` rather than the P24 located hard-fail at the keyed seam, that is a small assert/diagnostic fix, not a design leg. Per the standing dual-path discipline, `/design`(typecheck) produces call-chain evidence (where the mono request is dropped) before the fix — the expected shape is §11.3.4's settled-state direction (derive the cross-arity callee's instantiation at/after the drain's settlement point), landing with R1/R2 in one coherent producer change-set.

### Q3 — Binder generalization: frontend-only, one seam (with one macro-routing note)

The head-parse seam is frontend `ast_builder.rs` for all natively-parsed forms: `get_defn_name` (defn/defn-, also impl-body method defns via line ~1202), `parse_deftype` head, `parse_deftrait` head, and `defmacro.rs::parse_defmacro`. **`def`/`def-`/`const`/`const-` are stdlib macros (spec §9.10; `stdlib/defs.cl`)** — there is no native `def` special form (user correction, Phase 2); they expand to the native binder special forms `defn`/`defn-`/`defmacro`/`defmacro-`, and their heads therefore flow through the SAME frontend seam post-expansion; no second seam exists. Fix shape (P7): ONE shared `reject_qualified_binder_head` primitive beside the existing `reject_reserved_binder_name`, applied at the enumerated head sites — never per-form copies. Two riders: (a) /qa's matrix still carries per-surface-form cells (def/const/defmacro × pos/neg) since the macro route is a distinct path to the seam; (b) verify the diagnostic span lands on the user's written form for the macro-expanded cases (the reject fires on synthesized `defn` — span provenance must survive expansion). No public-API impact (error path + `pub(crate)` helper only).

### Q4 — D2 accept-side: wiring at existing seams, no new machinery

Closing the leak on the accept side needs **no new resolution machinery**. The method import's FQ identity chain-follows (bounded keyed-lookup chain, P24) to the trait's home module, where the trait decl and the `TraitImpl` shells live (D45 as amended — shell at trait home carrying `impl_module`; method `Def`s at the writer's module). Dispatch resolution over that keyed candidate set and transport via the existing `MethodResolutions`/`ResolvedCall::TraitMethod` carrier (types-crate, `mono_expr.rs`) is the landed S110 machinery; the fix is making the trait-method resolution path root at the *method's chain-followed home* instead of requiring trait-in-scope, then populating the carrier for the nullary return-type-dispatch cell so codegen's keyed read links. Watch-cells: the impl-*declaration* trait-in-scope requirement stays (ruling edge (d)); the unary "no impl" reject **inverts** under the ruling (see revision 5). Expected types diff: none; if a carrier shape change emerges it is FIXME `target: /arch` + Phase-3 approval.

### Q5 — Wave seam coherence: five flags

1. **(revision 4) Schema-bump window.** `MethodResolutions`/`ModeSummary` are persisted carriers; pin the expectation now: **W2 = no bump** (R1/R2/D3/D2 are producer-behavior fixes over existing shapes; any W2 shape change escalates to /arch before landing) and **W5 owns the sprint's bump** (21→22) if the §3b conditional/unconditional origin split lands. Two uncoordinated bumps in one sprint is the anti-pattern the 0644 precedent guards.
2. **(revision 5) W1 fence-inversion sweep.** Before authoring D2-as-ruled fences, /testing sweeps for existing tests that pin the OLD behavior — specifically any cell asserting the unary method-only-import "no impl" reject, which the ruling makes a wrong-reject. Those flip to must-accept in W1, not when W2 trips over them.
3. **(revision 6) W3 corpus sweep precedes the rejects.** Turning 8 forms' silent-accepts into rejects can break fixtures/examples/demos that accidentally use qualified heads. S112 conformance found stdlib clean; W1 (/testing) extends that sweep to tests/, examples/, repl demos, exemplar before W3 lands, so W3 ships reject + fixture fixes atomically.
4. **W2→W4 coupling (RT-4).** The user.cl regenerator's data-loss class is almost certainly an **enumeration-completeness miss against the D45-as-amended storage model**: impl shells live at the TRAIT's home module (not the user module) with `impl_module` back-pointers, method `Def`s at the writer's module. W4's `/design`(src) must root the regeneration design in that settled model (`resolve-home-enumeration.md` §3 discipline: every source of the kind contributes rows or a legal skip) — and W4 runs after W2 precisely so any W2 touch on trait-impl records is settled first. No W2 carrier change is *expected* to reach RT-4, but the serial order makes the assumption checkable rather than raced.
5. **W0→W1 partial gate.** W1's "memory-safety probes" slice is gated on W0's assessment output; the rest of W1 (D2 fences, binder cells, 0605) is not. Dispatch W1 with the probe slice detachable so W0's user gate never blocks the defect-family fences.

### Riders and dispositions

- **(revision 7) R7/0604 seam asserts ride W4, not W5.** The §6-task-4 int-side work (always-cheap `debug_assert!` + `MODULE_TRACE` emit at every live-table insertion seam, converting the ~320-run ghost into a named seam) is src/-surface, tiny, and is exactly the user's "assertion density" palette item — it should NOT wait on the W0 depth gate. Add as a W4 rider; /qa's Phase-3 0604 work then consumes the named seam.
- **(revision 8) 0553 is not a default W2 rider.** It opens a NEW cross-crate public entry point (typecheck instantiate-at-types + backend codegen half + src consumer — three surfaces), which exceeds a rider and requires explicit /arch surface approval. Its FIXME names the ownership/ModeSummary machinery sprint as the natural co-landing — that is W5-if-deep or S114, not the defect wave. Keep as W2 *candidate* only if `/design`(typecheck) shows W2's producer work opens the seam anyway; default disposition: re-point to the W5/S114 window.
- **(revision 9) D1 surface note.** The multi-sig constraint-display fix is typecheck-side carrier/display (`/sig` rendering of inferred constraints) — confirm in Phase 3 it stays inside W2's typecheck deployment and does not leak an int-side display patch (the eval.rs display defect precedent: read recorded settled state, never re-derive at the echo).
- **Frontend audit**: read-only, parallel, no architectural constraint. **0628 retirement judgment** (/design, F): no objection — the matrix + leg-c record is sufficient evidence basis; recommend retire.
- **Public-API summary**: zero anticipated `public-api.txt` diffs in W1–W4; W5 carries the only candidate types-crate change (origin split) behind the W0 gate; binder work is error-path-only; W4 is binary-surface (no baseline). Any deviation is FIXME `target: /arch` before landing, per §Public-API discipline.

## Skill plans (Phase 3)

### /spec — DELIVERED

D2 ruling scribed at `spec/07-traits.md §7.11.2` (new) with all edge cells (a)–(e); supporting touches §7.4.1, §7.3, `05-definitions.md §5.11.1`, `08-modules.md §8.6.4`, §7.11 anchors. Binder generalization scribed at `05-definitions.md §5` intro (veto hedge removed; native-vs-stdlib-macro split; no-native-`def` correction; span-at-written-form) + per-form bullets §5.2/§5.5/§5.6/§5.7. All new rows `[S113]`. **Framed non-blocking note for user**: "impl coherence is global" scribed as *closure-bounded keyed coherence* (§5.11.1-consistent — reaching the method pulls the trait's canonical home into the import closure); confirm only if a stronger universe-wide reading was intended.

### /qa — DELIVERED

- **W0 verdict: CONFIRMED — memory-safety is the top correctness risk** (`tests/plan/s113-risk-assessment.md`; 7 families ranked; the compound: silent-corruption severity × structurally-zero detection of unknown members × 4+ confirmed-reachable live mechanisms × only-detector-is-adversarial-review). Recommended W5 depth (user rules at gate): build tier 4 FIRST (differential-oracle lane — acceptance: committed 0641 B-1 RED goes RED under the lane day one), tier 5 (quarantine + scrub-poison + paired-counter, intrinsics-internal env-gated), tier 3 (RC/alloc seam asserts; R7 int asserts ride W4 ungated), tiers 1–2 (§3 frame + 0641 fix, schema 21→22 if §3b lands, paired /dev(backend) B-2/I-2 consume fix). DEFER generative harness → S114.
- Test plan `tests/plan/s113-test-plan.md`: ~30 ungated W1 rows (D2 F-D2-1..9 incl. unary fence INVERSION + over-inversion fence F-D2-8 guarding edge (d); binder matrix BD-M1..M5 native+macro-route+span cell+con_var F3; qualified-head corpus sweep gating W3 — seed grep says corpus likely clean; 0605 gate; 0638 repro pin) + detachable 6-row MS-P slice gated on W0.
- Owed S112 debt cleared: MC-D1/D2/D3/R1v rows + MC-E1 Pin-4 sequencing (carrier family lands before R1-variant flips judged) + MC-X1 carrier×reaching-context sweep (trigger FIRED: R2+D3 = two producer misses).
- FIXMEs: 0632 + 0633 DELETED (fully actioned); 0638 updated (deletes on W1 repro landing); 0604 kept — W4 R7 rider is the diagnosis plan.

### /design × cranelisp-typecheck — DELIVERED

- W2 mono/carrier family designed as ONE producer change-set (`design/typecheck/monomorphisation.md §11.8`): R1/R2/D3 share one root — the mono harvest doesn't cover multi-sig dispatch/bodies and the pass-4 recheck runs PRE-drain (`finalize.rs:999/:1015/:1391` filter drops multi-sig bodies — D3 attribution CONFIRMED typecheck-only carrier-loss). Fix = settled-overload-derived harvest, 3 legs (D3 clause-body scan; R2 inner multi-sig-dispatch resolve; R1 widen inline gate), run POST-`finalize_multi_sig_variant_types` (option (i), single-sig path untouched).
- D2 (`traits.md §7.0.1`): P24 "resolve once then throw the home away" — `method_to_trait_with_state` discards `trait_origin`'s module; fix threads the home, roots impl lookup at `has_impl_in_home`; carrier populates on accept. TB-24 (§3.2): resolver-mirror — converge conventional impl-target path onto the shared ConVars resolver (P7). No types diff; **W2 NO schema bump confirmed** (failed-compile history → no stale-cache resurrection).
- **D1 re-attribution (contradicts arch rev 9, evidence)**: render seam is int-side (`format_type.rs:42`); typecheck records constraints correctly; no-bump fix = int-side read-follow of the recorded template scheme → **D1 moves W2→W4** (arch confirmation requested).
- 0553 DEFERRED (W2 doesn't open the seam) → S114 W5-if-deep. 0628 RETIRED/deleted (matrix closed by committed guards). Spec-diff EMPTY (inversion fences named: §5.1.2 same-arity reject, §3.11 bare ambiguity, §8.6.4 conflict — all untouched seams). P26 full sweep scoped to own S114 slot (seed table landed `typecheck.md §9.7`).
- **Shadowing addendum (`monomorphisation.md §11.8.7`)**: overload-gate pin DESIGNED — local-scope-first guard at `infer.rs:604` reusing `record_reference_target`'s recursion-self-ref discriminator; CO-LANDS with the R1 gate widening (same seam, composes — R1's recheck self-call is not locally bound, so unaffected); no schema bump. Spec-diff clean: pins are the §4.6/§5.1.2 lexical-shadow class, categorically distinct from §8.6.4 conflicts (a `let` is not a definition). **Single-sig hang pin — evidence CONTRADICTS typecheck routing**: the call-head carrier recorder correctly records ABSENCE (frame-guarded, 0619-item-2 intent working as specified); fault is the callee-lowering consumer (`MonoExpr::from_expr` — /arch-owned types crate — or backend keyed read) which must resolve a carrier-absent callee LOCAL-before-name → re-routed to /arch (ruling in flight). Non-colliding probe twin offered to unblock the hang-blocked /clif.

### /design × cranelisp-frontend — DELIVERED

- W3 designed: `design/frontend/binder-head-reject.md` — ONE `reject_qualified_binder_head` beside `reject_reserved_binder_name` at 5 head sites (`get_defn_name` covering defn/defn-/impl-method; `build_type_head`; `build_trait_head` — deftrait-caller policy, impl slot-1 stays a splittable reference; `parse_defmacro`; **+ deftrait method-signature names — a spec-enumeration gap, FIXME 0651 → /spec**) + con_var (BD-M4). Native-form rejects fully located; error-path only, zero public-api diff; W3 ships rejects + W1-corpus fixture fixes atomically.
- **Span-provenance finding (FIXME 0650 → /arch)**: macro-route heads (`def`/`const`) reject correctly but with SYNTHETIC spans (`marshal.rs:62` + `expander.rs:158` rewrite_spans_unique — deliberate span-uniqueness invariant; preserving spans through marshal REJECTED). Proposed paired int-side re-anchoring seam (0613 quote-shield mirror, 0485 origin-span discipline). Frontend reject inert-safe, lands W3 ahead of the pairing.
- 0589: sibling annotation-path seam (`parse_annotation_name:1750` — lowercase+`/` routes to Named not TypeVar), one-line W3 fix, decoupled from 0590. 0613 VERIFIED fully landed — /dev deletes in W3; one stale test-comment residual → /testing. 0590 NOT frontend — re-targeted /design(typecheck), S114, kept open.

### /design × src/ — DELIVERED

- RT-4 root-caused: **`generate_impls` at `src/save.rs:723` is a literal stub returning `""`** — the entire impl persisted-content kind unconditionally absent from regen (illegal "someone else owns it" skip per `resolve-home-enumeration.md §3` rule 2; the "no impl sexp anywhere" premise dissolved — verbatim source already captured at the defining turn). Fix designed `session-persistence.md §12`: enumerate impls-written-in-M (st shells `impl_module==M` ∪ introspection backstop; impls-written-in-N legally excluded), render via the shared reader (P7), + fail-loud section-completeness `debug_assert!` sweeping `all_symbols()` (later-added kind trips loudly). No types/schema change. Spec-diff vs repl/spec §15.4 EMPTY (5 kinds ↔ 5 sections; `__expr` legal exclusion).
- D1 read-follow designed (`multi-sig-introspection.md §2.4` corrected — its "constraints already in param_types" claim WAS the defect): keyed fetch of the recorded variant `Def` scheme (`register.rs:345`) via `mangled_name`, render via `format_scheme_display`; arch pins confirmed (miss = `debug_assert!` + bare-render release fallback only).
- 0650 re-anchoring designed (new `macro-diagnostic-reanchoring.md`): seam = `worker::build_program_compat` (`worker.rs:73`) where `origin_span` (0485) is already threaded; synthetic-location predicate via "outside origin form's real source extent" (catches both synthetic flavours, no 1M hard-code); append `in expansion of (…)` provenance, never re-phrase. W3-then-W4 sequencing; BD-M2/M3 the durable trigger.
- R7/0604 rider designed (`index-worker-isolation.md §8`): ONE shared `assert_prelude_closure` (P7), `debug_assert!` + release `MODULE_TRACE`, at named seams (`imports.rs::install_imports/install_exports`, `cluster.rs::insert_cluster`, `process_form/`+`worker.rs` register paths, *beside* `insert_detecting_ambiguity` — poison-consumer untouched). Observability deliverable, ungated by W0.
- 0646 (rename primer example `Show`→`Describe` — §8.6.4 conflict with prelude glob; /repl re-syncs agent-fluency.md) + 0647 (omit empty `; impl:` drawer, matching deftype `; match:` precedent) confirmed as /dev W4 riders.
- **Scope corrections**: shadowing ×2 are TYPECHECK seams (call-head scope resolution + `infer.rs:606` overload gate consulted before local scope; spec basis §5.1.2+§4.6 lexical shadow, NOT §8.6.4) — re-routed W4→W2 (typecheck design addendum in flight). 0635 NOT src/ surface — re-targeted /design(backend), kept open.

### /design × cranelisp-backend — DELIVERED

- Shadowing-hang fix designed (`design/backend/backend.md §2.7.1`): TCO fast-path 1 DELETEs the bare name-match, keys on callee `Var.resolved_target == current fn storage FQ (module+symbol)` (`current_fn_name` + `ctx.current_module` both present — false positive impossible); carrier-absent → `compile_var_apply` local path; carrier-absent + not-in-`variables` → LOUD located hard-fail. No schema bump / types diff / api delta — arch interface set UNAMENDED. Unit rows: §11.8.7 probe twin + colliding form + genuine-self-call TCO regression + fast-path-2 mono regression.
- **Fast-path-2 verdict: already module-safe** — both compared operands are the 0519 home-qualified mangle `{home}/{bare}${sig}` (one canonical composer), so cross-module same-sig dispatch differs by prefix; /dev adds a defensive comment only. Spec-diff §4.6/§5.1.2 EMPTY (4-row case table, none unhandled/over-matched).
- 0635 DRAINED + deleted: I3 — `audit-drain-s111.md §1.2` false "production-live" premise corrected; ruling DELETE the dead backend `generate_startup_object` family (int's `src/exe.rs:50` is the live, already-drifted copy — P7/P8, /dev actions in the W2 window, no api delta expected); I4 — `implementation-slice-s66.md` archived with banner + README row.
- Filed FIXME 0652 → /arch (name the TCO fast-path in `backend-keyed-consumer.md` §3 inventory — arch-owned doc; incl. the note that the §707 grep gate doesn't cover carrier-read sites, + the fast-path-2 verdict line).

## Waves (Phase 4 — FIRMED 2026-07-19; all designs delivered, D/D/R waves run as dev+review against the Phase-3 designs)

| Wave | Content | Chunk |
|---|---|---|
| W0 | ✅ DONE + **GATE RULED (user, 2026-07-19): /qa recommendation APPROVED** — W5 builds tiers 4+5+3+1–2 (oracle lane first → diagnostic modes → seam asserts → §3 frame + 0641 fix + paired backend B-2/I-2); generative harness deferred S114. MS-P probe slice UNGATED → joins W1. | A |
| W1 | ✅ DONE — ~40 rows landed: D2 family (incl. unary INVERSION flip + F-D2-8 over-inversion fence GREEN), 8-form binder matrix (native/macro-route/stdlib-route/con_var), 0638 repro pin ×3 modes, MS-P oracle lane (**B-1 RED-day-one acceptance MET**; MS-P1 combinator in `helpers/e2e.rs`), F-R1 entry-main teardown guard. Suite **4822 run / 46 RED (21 new intended + 24 attributed carries + 1 isolation-clean load-sensitive) / zero unattributed**. Corpus sweep CLEAN → W3 unblocked, no fixture fixes. Inversion sweep: 1 pin flipped, table recorded. 0605 confirmed-landed → FIXME deleted. **NEW FINDING: lane caught a previously-unknown `--link`-only UAF (COW-set→project, mode-divergent) — §3.7 fix does not cover it** → /qa. MS-P3 retro-wrap = mechanical follow-on; MS-P6 rides W5. | A,B,C,F |
| W2a | typecheck dev+review: mono/carrier family (R1×2, R2, D3, TB-24, D2 fix) per `monomorphisation.md §11.8` option (i) + overload-gate shadow fix (§11.8.7, co-lands with R1 gate widening). No schema bump. **/review pins (arch P3)**: ONE parameterized harvest fn at two settlement points (never a forked sibling — mirror-class defect); family filter complementary AND total; MC-E1 sequencing binding. | B,D |
| W2b | backend dev+review (serialized after W2a): TCO fast-path carrier-keyed fix per `backend.md §2.7.1` (shadowing-hang pin flips) + 0635-I3 dead `generate_startup_object` deletion + fast-path-2 defensive comment. | D,F |
| W3 | frontend dev+review: binder family per `binder-head-reject.md` (shared reject at 5 sites + con_var) + 0589 annotation-path fix + 0613 FIXME deletion; ships rejects + fixture fixes atomically (consumes W1 corpus sweep); /audit dispatches parallel (read-only) at W2a start | C |
| W4 | src/ dev+review: RT-4 persistence (`generate_impls` stub — `session-persistence.md §12`), 0646/0647 (shadowing re-routed to W2); **+ D1 int-side read-follow** (arch-confirmed W2→W4; keyed fetch of the recorded template `Scheme` via `mangled_name`; miss = `debug_assert!` + bare-render fallback — never silent strip, never re-derive); **+ 0650 re-anchoring seam** (arch-ruled in-sprint: synthetic-location predicate — any `build_form(s)` diagnostic over expansion output with a synthetic span re-anchors to the origin form + appends "in expansion of …" context; NO error-class sniffing — /review REJECT); **rider: R7/0604 int-side seam asserts (`debug_assert!` + trace at live-table insertion seams — revision 7; /qa's 0604 work consumes the named seam)** | D,F |
| W5 | memory-safety reliability build — sized by the W0 gate; owns the sprint's only schema bump (21→22 if §3b origin split lands) | A |
| P6 | user-proxy sweep: docs debt, Default promotion, de-leak, examples | E |

Source-touching waves serial (W1→W2a→W2b→W3→W4→W5); read-only work (audit, W0 assessment) parallelises. P5-window riders: 0651 (/spec enumeration line), 0652 (/arch inventory row), 0650 FIXME deletion by /design when the W4 seam lands. Wave-gate scans per METHOD before each advance.

## Dispatch log

| Wave | Agent | Surface | Model | Effort | Non-default reason |
|---|---|---|---|---|---|
| P2 | /arch | sprint scope draft | shim default | default | — |
| P3 | /spec | spec/ (D2 + binder scribing) | shim default | default | — |
| P3 | /qa | tests/plan/ (W0 risk assessment + S113 plan) | shim default | default | — |
| P3 | /design | cranelisp-typecheck | shim default | default | — |
| P3 | /design | cranelisp-frontend | shim default | default | — |
| P3 | /arch (resume) | D1 re-attribution + 0650 ruling + exit-gate check | shim default | default | — |
| P3 | /design | src/ | shim default | default | — |
| P3 | /design (resume) | cranelisp-typecheck — shadowing addendum | shim default | default | — |
| P3 | /arch (resume) | shadowing-hang consumer-seam ruling | shim default | default | — |
| P3 | /design | cranelisp-backend | shim default | default | — |
| W1 | /testing | tests/ sprint-wide | shim default | default | — |
| W1 | /qa (resume) | 0638 retirement + W1 findings | shim default | default | — |
| W2a | /dev | cranelisp-typecheck | shim default | default | — |
| W2a | /review | cranelisp-typecheck | shim default | default | — |
| W2a | /dev+/review+/design+/qa+/testing (resumes) | fix cycle + close tail | shim default | default | — |
| W2b | /dev | cranelisp-backend | shim default | default | — |
| W2 close | /dev×2, /review×3, /testing×2, /qa (resumes) | typecheck touch + A/B + 0654/0656 + fences + rows | shim default | default | — |
| W3 | /dev | cranelisp-frontend | shim default | default | — |
| W3 | /audit | cranelisp-frontend (rotation) | shim default | default | — |
| W3 | /spec (resume) | 0651 drain | shim default | default | — |
| W3 close | /testing + /review + /dev (resumes) | 0658/MC-X3/fences + review + 0661/0662 | shim default | default | — |
| W4 | /dev | src/ | shim default | default | — |
| W4 | /design (resume) | 0659 + 0660 disposition | shim default | default | — |

## Notes

- 2026-07-19 **PHASE 6B COMPLETE** (all five proxies executed): /repl (06-modules method-import beat green + PS-D1-safe; 12/12 demos replay green; ring0/v4a/v4b de-rotted, 18 remain classed; spec §1.3+§15.2.1/2; **NEW defect 0672** — nullary no-impl leaks `undefined function`, 3rd check-gate-leak instance → F-D2-10; 0657+0577 closed; 0671/0673/0674/0675 filed+routed); /stdlib (DECLINE path landed; default.test = live D2 guard 4/4); /examples (37-method-import landed, exit 4, --run≡--link); /docs (traits guide + errors catalog; **0648/0649/0631/0673 closed**; "in expansion of" absent on the def/const route = the KNOWN S114-scoped finalize-path residual, documented as-built); /port (is-solved collapsed; make-grid+peers deferred on MC-X4 — 6a viability was a probe artifact; **MC-X4b type-ambiguity face** discovered + attributed; +1 parity micro-datum → MS-P8 residual). /qa sweeps done. **Phase-7 mechanical audits: frontmatter 28/28 CLEAN; dispatch log — ZERO escalations, ZERO fallbacks, all shim defaults (3rd consecutive sprint).** Final /testing close-out batch in flight.
- 2026-07-19 USER DIRECTIVE 2 (0653 prong-3 refinement): the local/FQ dichotomy must be enforced IN THE TYPE. Audit: FQSymbol/FQTypeName clean (no sentinel-module); the guilty encoding = `resolved_target: Option<FQSymbol>` (None conflates local-by-design with unresolved-by-bug). Required: closed sum `Ref::Local|Ref::Global` constructed only by typecheck — "unresolved" gets NO constructor; sweep = migration aid only. Flagged same-audit: mono_expr:664 sentinel + string-embedded mangle identities. 0653 updated; /arch ratifies P7; types-shape change → /arch authors S114.
- 2026-07-19 Phase 6a /port DONE (last proxy — 6a COMPLETE): exemplar GREEN (user.cl 0, tests 40/40 ≡ NO_LENIENT, binder-conform); D3 verified fixed; make-grid+is-solved multi-sig collapses PROVEN viable → 6b headline; peers BLOCKED by a **NEW consumer-side residual** (poly Vec callee consuming a multi-sig fn's bare `(Vec Int)` return → `undefined function`, mode-uniform, two-fn control green, §5.1.2 divergence — repro handed to /testing+/qa, carrier-loss-consumer face). **Diagnostic-modes smoke: QUARANTINE+SCRUB CLEAN (no UAF in the solver!) but ALLOC_PARITY: 25,461 surviving allocs — conj/assoc leak 1 Vec/iter = the never-freed face of 0408** → /qa Track-A risk input (bounded non-corrupting leak class).
- 2026-07-19 **USER DIRECTIVE (0655 post-mortem)**: "the backend is supposed to have all names resolved to FQ canonical — otherwise unrepresentable." Confirmed gap: the invariant is convention+loud-miss today, NOT structural — no exhaustive resolved-ness gate at typecheck exit (the check-gate-leak class ×2 this sprint = "typecheck completed with a carrier-less non-local"). Recorded as **0653 prong 3** (phase-boundary completeness gate: every Var/Apply local-or-carried at typecheck exit, else located typecheck error) — /arch ratifies at Phase 7; the gate = S114 work item + the 0653 sweep's acceptance check.
- 2026-07-19 **PHASE 5 COMPLETE.** 0655 ruling (a) LANDED: `normalize_self_qualified` at the ONE Var entry (recursion-local/shadow-gates/carrier/backend all see the bare shape; batch codegen-leak flipped; carrier verdict = DROPPED not stale; §4.6 shadow-wins pins strong-RED→GREEN failing-first) + candidate-order twin COLLAPSED (0590 5th member retired). Suite **4942/17/1 — 17 attributed carries, zero unattributed all sprint**. Residuals → P6-window /testing batch: MC-X3a–d accept-assert cells, multi-sig self-qualified gate cell (out-of-scope untested, candidate follow-up), 0669 family e2e pins. **PHASE 6A OPENED** — five proxies serial (/repl first); frontend audit already landed (rotation discharged in W3).
- 2026-07-19 Rulings execution: /spec sweep DONE (binder-positions table §5, 15 [S113] rows, ctor+field+platform+let+params+match+aliases tightened, NOTHING framed-open). /qa dual-path audit DONE: **verdict ONE resolver** (invariant held) — qualified leg committed-tables-only blind spot (never got AN-5 view arm) + bare-name self-identity ×2; genuine duplicates = candidate-order twin (0590 5th member); fix = single-seam spelling normalization; register rows 7–9. /arch view-arm LANDED (types crate: shared `resolve_current_via_view` body — twins can't drift; §8.6.4 prelude-retry guard caught a would-be wrong-accept; 3 pins; no schema/api delta); int seam-assert approved as rider. Frontend ctor+field rejects LANDED + defmacro-params + module-alias; **value-level rejects (params/let/match) DEFERRED-REVERTED — surfaced pre-existing int defect: macro-expansion pass QUALIFIES A LOCAL BINDER on importable-name collision (`name`→`primitives/name`) → FIXME 0670 → /arch** (spec tightened, enforcement waits on root cause). Suite 4936/18/1. Final typecheck normalization dispatch in flight (flips MC-X3; inverts the 2 old-reject pins per ruling (a)).
- 2026-07-19 0668 sub-fix LANDED: `compile_vec_lit` element store now uses the call-seam consuming discrimination (heap Var ⇒ inc, temp ⇒ transfer; structural, both-toggle-correct). Cells A+E flip (both toggles), failing-first units; fences hold; suite 18 REDs zero unattributed. Cell G = let-bind-alias seam, out of scope, carries with 0668/S114 (fixme's flip estimate over-counted; dispatch scope was right).
- 2026-07-19 **PHASE-5 CLOSE RULINGS (user)**: (1) vec-lit sub-fix IN-SPRINT (approved, in flight); (2) BD-A family CARRIES with attribution; (3) Phase 6 runs FULL. **0655 RULED (a) — LEGAL** (qualified own-module self-ref = another spelling of the local binding, TB-25); **+ USER DIRECTIVE: the three-mode divergence itself is the deeper defect — there is supposed to be ONE code path through the typechecker; audit whether alternative paths/duplicate implementations opened up; find the truth.** Call-chain audit dispatched (/qa) BEFORE the fix (per the S98/S102 dual-path discipline); fix lands at the shared seam after evidence. **0660(b) RULED — ctor names are binders** ("you can't define a name in another module, only reference"); user asks WHERE the spec needs tightening → /spec sweep of ALL name-introducing positions dispatched; frontend reject queued behind the vec-lit fix (serial).
- 2026-07-19 **W5b REVIEWS: both APPROVE.** Typecheck: §16 faithful (unrepresentability verified constructor-by-constructor; B-1/B-2/I-2 live); **I-1 residual ADJUDICATED backend-owned ownership-independent** (only the let-ALIAS capture slot corrupts; typecheck facts verifiably published; backend flip-table "I-1" was a B-1 mislabel); 2 new toggle-independent cells probed (nested-match COW; let-indirected COW-into-container); 2 suggestions. Backend: Half-1 polarity PROVEN (one Borrowed producer, analysis-ON-gated; count/copy/release balanced CLIF-verified); Half-2 stash clobber-safe (nested probes green); §15 row-6 correct, rows 1–4 deferral sound (+row-5 assert tautology suggestion). **Adjudication: the indirection family = ONE pre-COW contract gap — syntactic temp-vs-owned at non-call consume sites** (vec-lit raw store; match temp-dec vs alias-forward; even COW-free twins garbage both toggles) → **FIXME 0668 (/design, S114 design-iteration; discriminator structural/analysis-independent — one rule both toggles) + 0669 (/qa: family pins + B-2 toggle-off face found UNCOVERED)**. One small in-sprint sub-fix available: vec-lit element store via call-seam discrimination (~15 lines, flips let-indirected cells, leak-side-safe). Suite 4926/18/1 all attributed. B-2×2 flipped by the final typecheck escape-recording touch (one-site, failing-first). **PHASE-5 CONCLUSION → user.**
- 2026-07-19 **W5b-ext LANDED** (backend): both ruled halves in one change-set — toggle-off all-Owned (Borrowed unreachable, live-Var counted ⇒ copy-by-construction) + analysis-on escape-gated inc (`node_escapes` threaded to the COW seam, clobber-safe stash; recur≠escape). **I-2 ×2 + I-1 ×2 FLIPPED** (toggle on/off × 3 modes); ZERO regression (l_c3 ×2 green analysis-on, golden byte-identical, no fence trips — /qa re-scope unneeded); fresh-temp + loop counterexamples now permanent cells. Suite **23→20**. **B-2 ×2 = typecheck match-var-pattern ESCAPE-RECORDING bug** (present-but-wrong `Some(false)` defeats the P25 absent-default — no backend gate can flip it) → final typecheck touch in flight (STOP⇒ruled S114 fallback). **MS-P7 --link face = REPL-vs-link seam** → S114 carry (REPL/run correct both toggles). R14 annotated partial pending. FIXME 0664 carries the landing record.
- 2026-07-19 **0664 RULED (/arch) — impasse DISSOLVED**: root = the syntactic `cow_source_ownership` classifier leaks `Borrowed` past the toggle (violates the ruled §6.2 all-Owned conservative definition — a defect against the toggle's definition, not a re-scope decision). Fix = ONE W5b-extension change-set: (1) toggle-off polarity restore (Borrowed unreachable analysis-off; correctness via truthful rc) + (2) analysis-on escape-gated producer inc (literal=Fresh/transfer kills the leak at classification; recur≠escape preserves l_c3; absent-fact⇒inc, P25). Consumer-seam alias-inc REJECTED (P7 mirror; escape is a recorded fact — producer reads it, P26). New register row **R14 COW count-truth**. /qa cascade: toggle-off reuse fences re-scope to analysis-on + scoped golden re-baseline (§6.2 precedent). Timing: IN-SPRINT; named fallback = S114 carry with R14 `unasserted` if polarity proves multi-seam. /dev(backend) extension in flight.
- 2026-07-19 **W5b fix wave — split outcome.** Typecheck half LANDED: Origin lattice `Fresh`/`Unconditional`/`Conditional` (hard-claim-from-conditional now UNREPRESENTABLE, internal-only, no bump), 10-row table with 0641 corrections (rows 3/5/7) + two forced escape-machinery reconciliations; **B-1 ×2 + lane differential FLIPPED under modes both toggles**. Backend half: **§13.7 FALSIFIED** (twice-burned discipline; attribution confirmed real but the unconditional inc over-retains fresh-temp [leak] + loop-churn [alloc-scaling]; correct discriminator = ESCAPE = analysis-dependent → conflicts with toggle-off acceptance; NO producer rule satisfies both) → **FIXME 0664(new) → /design+/qa with counterexamples + re-framing options** (consumer-seam alias-inc vs re-scoped toggle-off gate); B-2/I-2/MS-P7 stay RED attributed. §15 asserts LANDED (row-6 DEC_CHECK extension — immediately caught a real Var-source COW stale-dec; row-5 elision self-check; rows 1–4 deferred, SSA fact-carriers absent). Teardown residual = 0633 drop-glue-underkey seam, distinct, left attributed. Suite 4915/23/1 zero unattributed. /arch ruling on the 0664 re-frame in flight.
- 2026-07-19 W5b QA-first landed: BD-A1/A2 6 REDs (`:Type` ascription ×4 + trailing-drop ×2, /dev frontend); **BD-A3/A4 confirmed already-closed by W3** (located diagnostics, no pins); MS-P7 re-shaped config-independent (R-2) + separate GREEN detection fence; 0638 M1-OFF twin landed; PS-RT4 imported+prelude born-green (0664 guards); R-3 teardown-residual CONFIRMED scale-invariant → F-R1 family note. Suite 4928/25/1 zero unattributed. /testing's "modes not at HEAD" claim CORRECTED (working-tree vs committed confusion — modes live in tree); M1-ON twin + MS-P6 fences + parity discriminators landing now.
- 2026-07-19 **W5a LANDED** (intrinsics detector build): M1/M2/M3 + A1–A4 asserts, env-gated, byte-identical-off PROVEN (run (a): 4896/18/1, zero unattributed, A1 surfaced nothing). Attribution re-run (b) vs map: 0641 ×8 deterministic via M2 poison ✓; DG fires 7 axes (>3 mapped, positive); **0638 caught by M3 as LEAK (quarantine neutralizes the double-free — face flip)**; **MS-P7 flips GREEN under modes** (guard intent needs /qa); **ownership_reuse +6 small-leak parity aborts** (program-attributable — clean programs balance; real-vs-residual attribution owed). A2–A4 release variants = RC-field/size invariants only (LIVE_ALLOCS stays debug — coherent reading, noted to /design). 0656 drained. /qa reconciliation before W5b (arch's sharpen-before-fix ordering).
- 2026-07-19 W5 design stage COMPLETE (3 concurrent doc-only elaborations): **intrinsics** `diagnostic-modes.md` — M1 quarantine/M2 scrub-poison/M3 paired-counter (env-gated, byte-identical-off, R8-traced; keystone: quarantine makes existing stale-dec asserts DETERMINISTIC; acceptance maps each of the 15 REDs to its firing mode; multi-arity wrong-accepts characterized mode-invisible); **typecheck** `ownership-inference.md §16` — §3a Origin lattice + ⊤, §3c 10-row classified transfer table (0641 corrections = rows 3/5/7 in-frame), **§3b = internal reshape only, NO types touch, NO schema bump** (persisted model already carries MayAliasOf; S114 fallback only if a persisted-split consumer emerges); 0641 DELETED (pins are the trigger); **backend** `ownership-codegen.md §13.7+§15` — **B-2/I-2 root = ONE producer seam** (COW mutate/grow under Borrowed returns borrow-as-owned, no inc — hence toggle-off failure; fix = inc on the same-pointer branch, NOT byte-identical-off by design) + 6 tier-3 assert seams (category A debug/B env-gated-emission split). Joint acceptance: all 8 pins + MS-P7 flip under lane+modes only when BOTH halves land. W5a /dev(intrinsics) dispatching.
- 2026-07-19 **W5 OPENED.** /arch frame ruled: **W5a** tier-5 modes (quarantine/scrub/paired-counter, intrinsics-internal env-gated) + tier-3 RC/alloc seam asserts (detector multipliers; re-run the 15-RED acceptance set under modes BEFORE fixing); **W5b** tiers-1–2 fix wave (§3a origin-lattice + §3c rule table + 0641 corrections-in-frame + backend B-2/I-2 consume fix) — acceptance: W5-family REDs flip UNDER lane+modes; **W5c** §3b split + schema 21→22 (hardening AFTER fixes; named S114 fallback; types-touching ⇒ FIXME→/arch). /design set: intrinsics, typecheck, backend (censuses explicitly OUT). 0637 PARKED to first borrowed-sibling consumer (co-landing rule, R5 row). safety-invariants.md at W5 frame (R7 asserted, R9 gated, MS-P7 under R1). Riders done: 0660(a)+platform guards landed (18 REDs hold); 0650 retired; /qa pre-W5 batch done (PS-RT4 provenance axis; BD-A1..A4 attributed /dev(frontend) one-seam family; 0655/0665 deleted — MC-X3 fix ruling-blocked in user queue). USER CHECKPOINT presented (0655 legality; 0660(b) veto-visible; §5.10 scribe-notice).
- 2026-07-19 **W4 CLOSED — re-verify APPROVE**: 0664 Blocker fix verified end-to-end (imported-trait impl survives regen + cache-wiped restart; dedup exactly-once; structural dotted-record discriminator); 0666 guard-drop confirmed both feeders; 0667 literal. Suite **4900/18/1** identical attributed set, zero api delta, schema 21. In flight: 0650 deletion (/design src), settled frontend riders (0660(a) ctor gate + platform guard), then MC-X3 typecheck fix + /qa batch (0665 born-green cell, 0655 deletion, audit finding-1 `:Type` family attribution) → **W5**.
- 2026-07-19 W4 0663 drained (/testing): 0647 drawer + 0650 span cells GREEN. Suite **4898/18/1** — 15 W5-family + MC-X3 + 2 pre-existing. **W4 /review: FINDINGS-FILED, 1 BLOCKER (0664)** — RT-4 close PARTIAL: imported-trait impls still drop (shell lives at TRAIT HOME's table; §12.3 row-2 introspection backstop never implemented; unit test green-washed by staging the shell in the wrong table; verified e2e w/ cache-wiped restart). +0665 (/qa: PS-RT4 trait-provenance axis — how the Blocker escaped the matrix), +0666 (Important: D1 same-shard recursive read deadlock window under agent harvest), +0667 (suggestions). D1/0650/R7 judged sound (0650 structural + mode-uniform; R7 would catch the 0604 phantom). S114 candidate noted: typecheck-error spans over expansion output (outside 0650's ruled seam). /dev(src/) fix cycle in flight.
- 2026-07-19 **W3 CLOSED** (0661 third TypeVar decision point fixed — invariant named not counted; 0662 assert narrowed to the exact §8.5 dual; review APPROVE-WITH-FIXES all drained). **W4 /dev LANDED (src/)**: RT-4 `generate_impls` close (§12 enumeration + compile-time completeness guard) — both persistence pins GREEN, data-loss class dead; D1 keyed template-Scheme read-follow GREEN; 0650 re-anchoring seam landed (synthetic-location predicate + "in expansion of" context — diagnostic verified correct); R7 `assert_prelude_closure` at 3 insertion seams (no false-fires); 0646/0647 riders landed, FIXMEs deleted. Suite 4898/20/1. **FIXME 0663 → /testing** (2 cell alignments: 0647 drawer flip + span heuristic turn-relative fix) — draining. /design(frontend): **0659 drained** (predicate doc realigned + bare-`/` cell); **0660 dispositioned**: (a) ctor uppercase gate + platform `/`+`.` guard → settled /dev rider; **(b) ctor-name qualified reject → USER-QUEUE veto-visible**; (c) field names excluded justified + /spec §5.10 wording gap tracks in 0660.
- 2026-07-19 W3 close-out: audit gaps FIXED in-wave (deftype list-arm uppercase; mod/mod- §5.8 simple-symbol at `parse_mod_decl` — rejects `/` AND `.`; third §8.5 splitter RETIRED with debug_assert; bonus latent 0589-sibling closed at `build_type_expr` — both… now all decision points enforce TypeVar-never-carries-`/`). /testing W3 batch: 0658 drained (7 collisions → GREEN), MC-X3 pinned (repl-redefine RED; batch+fresh GREEN; 0655 record obligation met), 7 enforcement fences GREEN, span proxy TIGHTENED (caught offset 1000037; properly RED → W4/0650). **Whole binder family GREEN. Suite 4884/22/1, all attributed** (15 W5-family, 4 W4, MC-X3, 2 pre-existing). W3 /review: **APPROVE-WITH-FIXES** — 0660 (Important → /design: deftype CTOR NAMES are binders missed three-sided + lowercase-ctor uppercase-gate mirror + field/platform cells; user-queue question), 0661 (Important → /dev: third TypeVar decision point `build_impl_target` — draining in-wave), 0662 (Suggestion: debug_assert wording). 0659+0660 → one /design(frontend) resume in the W4 window.
- 2026-07-19 W3 /dev LANDED: `reject_qualified_binder_head` single-sourced at 5 sites + BD-M4 con_var; 0589 fixed (qualified-lowercase → Named); 0589+0613 FIXMEs deleted. **Load-bearing correction: design §2's `contains('/')` predicate would reject the bare `/` operator (Num method!) — implemented as both-halves-non-empty split (§8.5 dual); FIXME 0659 → /design wording fix.** 7 expected flips blocked by W1 assertion collisions with the name-quoting diagnostic → **FIXME 0658 → /testing** (one-line-per-test). Span cell green-but-LENIENT (proxy misses ≥1M synthetic offsets — /qa tightening note; true span = W4 0650 unchanged). Suite 4843/27/1 (effective 20 post-0658), zero unattributed, zero api delta. Backend §2.7.1 currency DONE; 0651 drained (/spec); 0652 note updated.
- 2026-07-19 **AUDIT LANDED** `audits/frontend-s113.md` (rotation discharged; disposal = S114 Phase 1): 7 recommendations + 1 meta (disposition-trail restoration — 3 findings carried across 3 audits with no trail). **Immediate routing**: finding-2 gaps close IN-W3 (deftype list-arm head never uppercase-confirmed `ast_builder.rs:606`; `mod`-head §5.8 simple-symbol MUST enforced NOWHERE — design §8 excluded it on a false premise; no BD mod row); finding-1 live defect family → /qa (`:Type` ascription wrong-rejects ×4 positions + trailing-form silent-drop siblings at `build_impl_method`/`build_method_sig`); finding-3 third §8.5 splitter copy should retire with 0589.
- 2026-07-19 **W2 WINDOW CLOSED.** 0654 drained (ONE `is_self_call` predicate — fp1+fp2 MERGED, gate-3 + spark converged; latent stack-slot-UAF divergence dead; gate-3 agreement unit test); 0656 backend half drained (residual: 3 intrinsics-crate citations → W5 rider). /testing 11 fences GREEN (non-tail sibling, let-rebinds-base, Fix-A ×3, Fix-B ×3+control, deep-TCO). Suite **4863 run / 34 attributed / 1 skip, zero unattributed** — every W2-family pin flipped (R1×2, R2, D3, TB-24, D2×4+inversion, shadow-overload, hang, MC-X2×3, TB24b). /qa close batch in flight (rows, 0632 battery 6-instance enumeration + Fix-A conditional row, 0655 attribution + framed user question, PS-C1 /repl FIXME). Then design currency ×2 → wave gate → W3.
- 2026-07-19 W2 A+B fixes LANDED + re-verified (**typecheck window CLOSED — APPROVE**): Fix A bare-name mangle (review's stored-mangle suggestion REGRESSED rp4 — pre-Phase-A `$Var` hazard; bare-name via the ONE `mangle_sig` is Phase-A-consistent; 0632 gains a fenced conditionally-sound row — tripwire vs future rename-imports; retirement path = carry storage base name as resolved data); Fix B ONE shared `callee_has_keyed_carrier` guard at 6 scan sites (the /arch scan discipline realized). Probes all green; units 764/764. **W2b backend review: APPROVE-WITH-FIXES** — fp1/deletion/tests clean, suite 4817/34/1; mirror hunt: **FIXME 0654** (/dev — gate-3 `body_has_self_call` + spark classifier still name-keyed; latent UAF divergence, unreachable today, P25), **FIXME 0655** (/qa — qualified own-module self-ref mode-divergence TRIPLE incl. well-typed→hard codegen error; needs /spec-routed legality ruling → USER), **0656** (/dev stale citations). Chain: /dev(backend) 0654+0656 → design currency ×2 → /testing batch → /qa batch → gate → W3.
- 2026-07-19 W2-final /review: **APPROVE** (carrier-presence = faithful consumption of the ONE discriminator — frames dead at the classifier; all cells verified by repro; hang pin FLIPPED). **Two Important ADJACENT pre-existing finds routed to in-wave fix**: (A) MC-X2 qualified face — drain composes identity from WRITTEN name `register.rs:851` → `(mlib/h 1)` doubled-key loud miss (fix: consume the stored `mangled_name` the variant tuple already carries); (B) **name-scan collector family SILENTLY WRONG-VALUES §4.6 shadows of poly/constrained callees** (4 sites — inner scanners + pass-4 collectors; instances 3–5 of the class). 0653 STRENGTHENED (six instances, two seam-families, scan-discipline prong — /arch named work item recommended). /dev resume for A+B; /testing cells batched after.
- 2026-07-19 W2 final typecheck touch LANDED: Fix 1 hang — `record_self_recursion_dispatch` consumes the ONE frame-guarded verdict via carrier-presence (shadowed → no record → bare Apply → local; genuine → records; ruling-5 composition RE-VERIFIED under the let-rebinds-base falsification, pinned). Fix 2 MC-X2 — lazy rehydration of imported overload bases + carrier keyed by HOME module (new transient `overload_homes`; cures the scoped-drain current-module face too; **MC-E1 doubled-prefix ruled BENIGN/distinct** — it's the home-qualified storage key rendering, backend keyed-read hits it). Fix 3 TB24b — impl-target `type_constraints` trait refs now resolve (unknown → TraitNotFound). 6 unit tests failing-first; 5 e2e flips (hang + MC-X2×3 + TB24b); suite 4812/35/1 = 34 attributed + 1 agent-lane provenance-race artifact (isolation-clean, documented lane — bare-run environment, → /qa stewardship). No schema bump. W2 close-out chain: review(typecheck touch) → review(backend W2b) → design currency ×2 → /testing non-tail cell → /qa rows.
- 2026-07-19 **/arch fp2 ruling: REDIRECT — PRODUCER-SIDE.** The wrong `SigDispatch` RECORD is the defect (two feeds disagreeing about one identity = distinction lost at producer; P24 forbids downstream re-derivation). fp2 locals-guard REJECTED: (1) backend name-resolution overriding a published carrier = two-resolvers / keyed-read-else-override hybrid; (2) patches ONE cell of a TWO-cell defect — the NON-TAIL sibling consumes the wrong SigDispatch into a keyed call to the wrong callee, untouched by the guard; (3) P26 record-provisionally-repair-afterwards shape. Directed fix: mono-recheck self-call classifier applies the SAME `is_recursion_self_ref` frame-guarded discriminator (ONE shared fn, thin callers — P7); deeper-frame local ⇒ no SigDispatch/resolved_call/carrier ⇒ backend falls to `compile_var_apply`. fp2 stays as ratified. §11.8.7's "base not locally bound during mono recheck" prediction FALSIFIED (let-rebinds-base case) — /design correction owed. Required cells: typecheck unit (no-record under shadow; genuine self-call still records) + e2e NON-TAIL sibling wrong-VALUE cell (/testing). 0652 gains fp2 as carrier-adjacent read site (truthfulness-conditional); /qa 0632 battery gains "identity from written-name comparison" row (2nd instance: fp1, recheck classifier). W2b fix moves to the typecheck change-set — merged into the queued typecheck touch (with MC-X2 + TB24b silent-accept).
- 2026-07-19 W2b /dev LANDED (partial): fast-path-1 carrier-keyed fix (name-match DELETED; colliding-form unit test failing-first verified; CLIF discriminator = two-operand call_indirect, drop-glue-proof); 0635-I3 dead family deleted (`PlatformLayoutCheck` KEPT — int consumes it; public-api diff EMPTY); schema 21; suite 4802/39/1 zero new regressions. **⚠️ HANG PIN DOES NOT FLIP — mechanism is FAST-PATH 2**: `s1 : (Fn [a] a)` is POLYMORPHIC → mono `user/s1$Int` → shadowed `(s1 x)` resolves to `SigDispatch{user/s1$Int}` with carrier correctly None; fp2's mangled-name match has NO locals check → TCO loop. Contradicts arch "fp2 safe" verdict (instrumented pipeline evidence); confirms the LOCAL-before-name principle at the fp2 consumer. /dev correctly HELD the one-line locals-guard (dispatch forbade fp2 behavioral change) → /arch ratification in flight. `backend.md §2.7.1` fp2 verdict + case-row 2 empirically wrong → /design(backend) currency owed after ratification.
- 2026-07-19 W2a tail COMPLETE: /design §11.8.3/§11.8.7/§7.0.1/§3.2 currency recorded (R2+R1 legs superseded by ONE scoped-drain; inline-gate widening proved unnecessary). /qa rows: MC-X2 (finding-8 = module-locality cell of the carrier-loss family; fix must key by HOME module not `state.current_module` — 0621 storage_fq lesson; MC-E1 doubled-prefix plausibly same face), MC-G1/TB24b/A1, PS-C1 discriminator, calibration items 3+4, 0653-seeded recurring-class record, risks S113-7. /testing landed all: **MC-G1 ×4 GREEN, MC-A1 foreign-sig axis GREEN, MC-X2 pinned ×3, TB24b reject twin caught a BONUS live silent-accept (`(Box :NoSuchTrait a)` — pinned, /dev typecheck)**; **PS-C1 → USABILITY** (prints 2; redefinition wins; /qa owes the /repl FIXME). Suite 4844/39/1 (35 carried + 4 new attributed), zero unattributed. W2a-tail typecheck touch (MC-X2 + TB24b-reject) queued post-W2b.
- 2026-07-19 **W2a CLOSED — /review APPROVE** (all three resolutions verified by independent repro incl. nested-recheck + shadow×drain composition probes; fences green; schema 21; sugg-7 benign agreed). Recurring-class routing: **FIXME 0653 filed → /arch** (P24 corollary "resolution products travel typed" + helper-classification sweep; scribe at Phase 7, sweep S114 — pairs with parked P26 sweep + 0590). Incidental stale-`.cranelisp-cache`+`user.cl` REPL observation → /qa disposition (with finding-8, calibration data, flake stewardship). W2a tail: /design §11.8.3 currency → /qa rows → /testing cells → W2b.
- 2026-07-19 W2a fixes RESOLVED (/dev resume): Imp-1 re-architected — scoped invocation of the REAL drain inside `recheck_body_for_mono` (isolate/drain/restore; full bifurcation + ret-unify; P7 one-drain — supersedes the bespoke R2 scan); Imp-2 one-line home-root; Imp-3 fixed in-crate (`resolve_type_in_module`, ADT-carried-fqtn / intrinsic-at-home P24 case-split — a mid-round prelude-trait×user-ADT regression caught+fixed in-wave); Sugg 4–7 done (fn-value corner pinned BENIGN with mint+carrier assert). 7 new failing-first unit tests. Suite 4798/35/1 — 35 byte-identical attributed baseline, zero unattributed. **Imp-2/Imp-3 = 2nd/3rd "resolve once then throw the home away" instances this sprint** — recurring-class watch for /qa+/arch. Subprocess-lane load-sensitivity flake (`…no_codegen_leak`) flagged → /qa §2.1 stewardship. **PROCESS INCIDENT: /dev ran forbidden `git checkout <file>` cleaning a probe — discarded session checker.rs edits, self-caught via compile errors, fully re-applied + verified. No loss. Recorded per forbidden-git protocol.** Design-doc currency owed: §11.8.3 must record the landed scoped-drain shape (P26) before Phase-5 exit → /design resume queued.
- 2026-07-19 W2a /review: **APPROVE-WITH-FIXES.** Arch pins ALL verified (one harvest, total partition + live assert, schema 21, no new hybrids); 3 deviations judged sound (TB-24 args-only SAFER — preserves Case-3 rejects). **3 Important repro-verified defects** (none regressions — dead/adjacent cells): (1) R2 incompletely shadows the drain — `$Var` template mangle into frozen mono view (`undefined function: h$Var+Var`) + un-unified ret + orphaned post-drain pendings; (2) P24 mirror `monomorphise.rs:743` `verify_constraints` bare-re-resolves while holding FQ home (2nd "throw the home away" this sprint — contained, all other callers checked clean); (3) F-D2 matrix hole: import-shape × sig-mentions-foreign-type (wrong-reject `unknown type Int`). 4 Suggestions (stale comments ×2, home-hop unit test + trait-name assert, fn_value_rewrites multi-sig corner). Finding-8 (pre-existing: imported multi-sig base `undefined function` even direct; R2 carrier hard-codes current_module) → /qa attribution + plan row. **Calibration datum (S108 memory): findings 1+3 are QA-first misses — R2's unit test was shaped around its own gap; the F-D2 matrix lacked the import-shape axis** → /qa. Routing: /dev fix 1+2 (+suggestion tests) failing-first; /testing e2e cells for 1–3; wave holds until resolved.
- 2026-07-19 W2a /dev COMPLETE: all targets flipped (R1×2, R2, D3, TB-24, D2×4, overload-gate shadow) — ONE parameterized `collect_defns_for_mono` harvest (complementary+total, totality debug_assert), two settlement points; D2 home-threading at dispatch + `find_trait_method_decl`; TB-24 args convergence; no schema bump. **MC-E1: prelude-`+` R1 variant flipped with carrier+R1 together — NO separate R4 defect (do not pin).** Suite 4792/35/1, zero unattributed. 3 design deviations flagged for /review (D2 nullary home-hop; R2 pending-span gate — the leg-R2×ruling-5 composition contract; TB-24 args-only). One self-inflicted regression found+fixed in-wave (scope-blind R2 scan). /review(typecheck) dispatching.
- 2026-07-19 W1 COMPLETE (counts + findings in wave row). Wave-gate scan for W2a: no open FIXME targets the typecheck surface (0613/0646/0647 are /dev-targeted but in-sprint-scheduled W3/W4 — not deferrals; 0589 deferred-status W3; 0638 → /qa deletion in flight). Gate PASSES. /qa resumed for 0638 retirement + the two W1 findings (COW-set link UAF row; load-sensitivity stewardship note).
- 2026-07-19 **PHASE 5 OPENED (user).** Gate rulings: (1) W5 depth = /qa recommendation approved as-is (tiers 4+5+3+1–2, harness→S114); MS-P slice ungated into W1. (2) /spec closure-bounded-coherence reading CONFIRMED (no §5.11.1 change). W1 /testing dispatched.
- 2026-07-19 **PHASE 3 COMPLETE + PHASE 4 FIRMED.** All six deliverables in (/spec, /qa, /design × typecheck/frontend/src/backend); /arch exit-gate: interface set complete, zero types diffs, zero schema bumps (W5 owns the only permitted bump), zero api deltas. /design(backend) drained 0635 (deleted) + designed the shadowing-hang fix; FIXME 0652 filed. Waves firmed: W1 → W2a(typecheck) → W2b(backend) → W3(frontend) → W4(src/) → W5(gated) → P6. **BLOCKED ON USER: W5 depth ruling (the risk-first gate) + the /spec closure-bounded-coherence confirmation.** Phase 5 W1 dispatch ready.
- 2026-07-19 /arch shadowing-hang ruling: fault is `backend/compiler/apply.rs:176-184` TCO fast-path 1 — self-call decided by BARE NAME EQUALITY vs `current_fn_name` (pre-carrier, bypasses the KC-N6 "variables check precedes keyed read" rustdoc; survived the S110 migration because it isn't a `.iter()`). Fix: DELETE the name-match (not demote) — key on callee `Var.resolved_target == current fn's storage FQ (module+symbol)`; carrier-absent ⇒ never self-call ⇒ falls to `compile_var_apply` → `variables` → local; carrier-absent + not-in-variables = hard located error. `MonoExpr::from_expr` exonerated (faithful transport; a types-crate resolver would be a THIRD resolver — P24). Interface set UNAMENDED (zero types diffs / schema bumps / api deltas). Placement: W2-window backend change-set, serialized either side of typecheck per one-source-agent; design folded into the /design(backend) dispatch. Riders: probe-twin + colliding-form backend unit tests; /qa P24 battery gains "identity comparisons against written names" as a scan-equivalent category; `backend-keyed-consumer.md` §4 names the TCO path as a covered keyed-read site; fast-path 2 module-identity comparison verified.
- 2026-07-19 /arch rulings returned: (1) D1 W2→W4 CONFIRMED — int read-follow architecturally preferred (option B = P7 second-home + forbidden bump); binding pin: fetch miss = `debug_assert!` + bare-render fallback. (2) 0650 RULED option (a) IN-SPRINT — int seam keyed on synthetic-location predicate (never error-class sniffing), append expansion context; closes the whole synthetic-span family; W3 frontend inert-safe first, BD-M2/M3 RED the durable trigger. (3) Interface set COMPLETE for Phase-3 exit; two W2 /review pins (P7 one-harvest-fn; enumeration-completeness total filter). Reconciliation done; /design×src/ dispatching.
- 2026-07-19 Phase 3 progress: /spec, /qa, /design×typecheck, /design×frontend DELIVERED (plans transcribed above). **W0 verdict: memory-safety CONFIRMED top correctness risk** — depth recommendation awaits user gate. D1 re-attribution W2→W4 + FIXME 0650 ruling sent to /arch (in flight); /design×src/ dispatch follows the rulings. FIXME deltas: 0628/0632/0633 deleted, 0553 deferred(S114), 0590 re-targeted, 0650/0651 filed, 0638/0604/0589 updated.
- 2026-07-19 Phase 3 opened (user approval): serial dispatch /spec → /qa → /design×{typecheck, frontend, src/}. User correction folded in: no native `def` special form — `def`/`def-`/`const`/`const-` (stdlib/defs.cl) expand to `defn`/`defn-`/`defmacro`/`defmacro-`; arch Q3 text amended.
- 2026-07-19 Phase 2 COMPLETE: /arch verdict APPROVE-WITH-REVISIONS (§Architecture review); all nine revisions reconciled into Scope/Waves/FIXME table by /sprint. Key deltas: safety-invariants.md /arch-owned alone; no types-crate/allocator-hook work pre-W0-gate; D3 typecheck-only; schema-bump window pinned (W2 none, W5 owns 21→22); W1 gains fence-inversion + qualified-head corpus sweeps; R7/0604 seam asserts → W4 rider; 0553 re-pointed to W5/S114 window; D1 confirmed-in-W2 is a Phase-3 check item. 0628 retirement: /arch no objection — /design judgment stands scheduled. Register re-audit clean. Ready for Phase 3 dispatch.
- 2026-07-19 Phase 2 opened: user approved the Phase-1 draft; /arch dispatched against it.
- 2026-07-19 Phase 1: user approved scope A–F stacked in waves; G (SROA/`--release`) queues behind. **D2 RULED** (method-import-sufficient; see §Scope). Track A reshaped risk-first: /qa assesses global solution risk before depth; mechanism palette recorded (static analysis, assertions, diagnostic modes — no-reuse-after-free / scrub-freed / paired counters). Frontend audit committed (escalation discharged). No S112 audit assessment to dispose (declined at S112 — recorded in archive §Audit line).

## Outcome (Phase 7)

**Status: CLOSED — user approved 2026-07-20.** Arbitrations at close: **Default prelude promotion DECLINED** (user concurs with /stdlib's recommendation; stays module-qualified; contingency parked); close + commit + archive approved. Audit assessment (`audits/frontend-s113.md`) disposal → S114 Phase 1 per METHOD §2.6. Suite trajectory: 4,790 (S112 close) → **4,986 run / 4,955 passed / 31 attributed carries / 1 skipped** — zero unattributed regressions across ~25 full-suite runs. Uncommitted working tree spans all crates + spec + design + tests + user-facing surfaces (single close commit series on approval).

### Delivered

- **Track B — the S112 defect-family drain, COMPLETE.** Mono/carrier family (R1×2, R2, D3, TB-24, D2×4 + inversion) via ONE settled-overload harvest (option (i), complementary+total partition, totality assert); both shadowing pins (overload-gate local-scope-first; the backend TCO bare-name relic DELETED for the shared `is_self_call` carrier predicate — fp1+fp2 merged, gate-3 + spark converged); MC-X2 module-locality (lazy rehydration + home-keyed carriers + qualified face); RT-4 data-loss closed INCLUDING the imported/prelude-trait cell (a /review Blocker — `generate_impls` was a stub; §12 enumeration + introspection backstop + compile-time completeness guard); D1 constraint display (int read-follow); 0650 span re-anchoring (synthetic-location predicate); the FULL binder-reject family (5 head sites + ctor + field + con_var + mod + platform + defmacro-params + module-alias; §5 binder-positions table scribed; value-level seams deferred on the 0670 int defect).
- **Track A — memory-safety, the ruled depth DELIVERED.** Tier 4 oracle lane (caught a new UAF day one = MS-P7); tier 5 modes M1/M2/M3 (byte-identical-off proven; attribution re-run sharpened every family; the exemplar got a clean no-UAF bill under quarantine+scrub); tier 3 seam asserts (int R7 + backend §15 + intrinsics A1–A4; row-6 DEC_CHECK caught a real stale-dec on landing); tiers 1–2: the §16 monotone provenance frame with hard-claim-from-conditional UNREPRESENTABLE (internal-only — no schema bump needed, W5c dissolved), the 0641 corrections in-frame; **0641 B-1/B-2/I-2 + the B-1 lane differential DEAD under both toggles × 3 modes** (B-2 needed the match-arm escape-recording fix; the falsified §13.7 produced the /arch R14 count-truth ruling — toggle-off all-Owned restore + escape-gated inc); the vec-lit consume sub-fix (cells A/E).
- **User rulings implemented same-sprint**: D2 (§7.11.2 + edge cells; method-only import live everywhere incl. stdlib's own regression guard); 0655(a) qualified self-ref LEGAL (ONE-resolver audit verdict; view-arm symmetry in types; spelling normalization at the one Var entry; candidate-order twin collapsed); 0660(b) ctor/field names as binders (scribed + enforced). Two structural user directives recorded for /arch ratification: **0653 prong 3** (phase-boundary completeness gate) + its refinement (**typed enforcement**: `Ref::Local|Ref::Global` closed sum — "unresolved" gets no constructor; `Option<FQSymbol>` is the guilty encoding).
- **Phase 6 (full, all 5 proxies)**: 06-modules method-import demo beat (12/12 demos replay green); ring0/v4a/v4b archive demos de-rotted; `user/guide/traits.md` + `user/errors/` catalog authored (0648/0649/0631/0673 closed); example 37-method-import (exit 4, --run≡--link); stdlib default.test = live D2 guard (promotion DECLINE recommended, user arbitrates); exemplar is-solved multi-sig collapse landed (make-grid/peers correctly held on MC-X4). Frontend audit rotation DISCHARGED (`audits/frontend-s113.md`, 7 recommendations + 1 meta → S114 Phase-1 disposal); its findings closed in-wave (deftype list-arm, mod-head MUST) or attributed (BD-A family).
- **Structural/class wins**: "written-name identity" cured at 6 sites + fenced battery; "resolve once, throw the home away" cured at 4 sites; the ONE-resolver verdict; R14 register row; the scan discipline (`callee_has_keyed_carrier`); 5 principle-grade records feeding 0652/0653/0590 S114 sweeps.
- **FIXMEs drained this sprint**: 0553(def)/0589/0605/0613/0628/0632/0633/0635/0638/0646/0647/0648/0649/0650/0651/0654/0655/0656(backend half)/0657/0577/0658/0659/0661/0662/0663/0664/0665/0666/0667/0672/0673/0631 — **31 resolved/deleted**.

### Deferred / carried (all attributed; ledger = the 31 suite REDs, each failing-not-ignored)

- **/dev(typecheck) ×10**: F-D2-10 ×4 (nullary no-impl check-gate-leak — 3rd gate-leak instance, prong-3 evidence), MC-X4 + MC-X4b (P26-temporal consumer harvest; two faces fence partial fixes), MC-X5 (raw-name overload gates), MS-P7 (--link divergence — red-flag class), 0641 I-1 ×2 (capture let-alias, backend-adjudicated but typecheck-pinned family), PS-SH1 multi-sig value-ref (NEW matrix-completion residual).
- **/dev(backend) ×5 + /dev(backend/intrinsics) ×2**: 0668/0669 binding-indirection consume family ×3 (ONE pre-COW contract gap, S114 design iteration — sub-fix landed), F-R1 teardown fixed-residual ×2, MS-P8 conj/assoc leak ×2 (the 0408 never-freed face, first-contact real-program datum).
- **/dev(src) ×6**: 0638 macro-alias double-free ×5 (never wave-scheduled — honest carry, now mode-face-pinned both ways), PS-D1 ×1 (0671 display face). Plus riders 0674 (startup notice), 0675 (cheatsheet facts).
- **/dev(frontend) ×7**: BD-A ascription family ×6 (user-approved carry; one-seam fix shape recorded), deftype-ctor trailing ×1 (pre-existing).
- **Cross-cutting S114 design items**: 0653 (P24 corollary 3 prongs — /arch ratification at close + the typed-Ref carrier change + sweep), 0668 (/design backend consume contract), 0670 (/arch: int macro-pass qualifies local binders — blocks the value-level binder seams), 0652 (keyed-consumer inventory), 0590 (resolver mirrors, S114), 0637 (parked to first consumer, co-landing rule), P26 full typecheck sweep + P24 register frontend leg; archive de-rot (18 demos, classed); "in expansion of" on the def/const finalize path; 0553 (S114 W5-if-deep); 0463/0050/0052 (deferrals stand).
- **Open user arbitrations at close**: Default prelude promotion (stdlib recommends DECLINE); audit assessment disposal (S114 Phase 1 per METHOD).

### Findings

- **The risk-first reshape worked as designed**: detectors-before-fixes sharpened attribution (the mode re-run re-classed 3 families before W5b spent anything); the falsification discipline (twice-burned) caught an unsound design fix BEFORE it landed, and the resulting /arch ruling (R14) was better than the original design. The lane found 2 unknown members mechanically (MS-P7 day one; MS-P8 first real-program contact).
- **The check-gate-leak class (×3 this sprint) + the `Option<FQSymbol>` conflation are one story** — the user's prong-3 directive (typed local/global dichotomy) is the structural close; every instance this sprint would have been a located typecheck error under it.
- **Review probing + matrix completion out-found the planned surface again**: both wave Blockers, the indirection family, MC-X4b, and PS-SH1 came from adversarial probes and twin-discipline cells, not the planned rows — consistent with S112's finding; the S108 coverage-matrix directive gains "probe-found > planned" as a standing calibration.
- **Process incidents (2)**: /dev ran a forbidden `git checkout <file>` (self-caught, fully recovered, recorded); a 529 API cut mid-batch (resumed clean from transcript). Zero worktree corruption; the serial-agent discipline held across ~45 dispatches.
- **Zero model-tier escalations for the 3rd consecutive sprint**; frontmatter 28/28 CLEAN; dispatch-by-shim held throughout (zero fallbacks). The §II.4 triggers were never needed — default-tier review probes + direct user rulings absorbed all hard judgment.
- **Audit calibration**: the frontend audit's findings were HIGH-yield (2 closed in-wave, 1 live defect family attributed, 1 process meta) — the rotation-debt escalation was worth discharging.
