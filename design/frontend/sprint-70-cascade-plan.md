# Sprint 70 — Frontend cascade plan

**Status**: Authored Phase 3 (A2), awaiting Wave A3 `/arch` review.
**Author**: `/design` narrow-deployed for `cranelisp-frontend`.
**Inputs frozen at**: current commit on `main`. `cargo check -p cranelisp-frontend` enumerates **33 errors across 13 distinct S69-origin shape changes**, matching the SPRINT.md Phase A probe table exactly.

## Scope

Per `sprints/SPRINT.md` §"Phase A — Absorb types changes into `cranelisp-frontend`":

> Per-category cascade plan: for #6–#10, refresh relevant `design/frontend/*.md` sections + ast_builder/expand/module_extract patterns to absorb new types shapes; for #1–#5, #11–#13, confirm mechanical translation; flag any item that doesn't fit either.

Capped by the Phase 2 architecture review verdict (sprints/SPRINT.md §"Architecture review (Phase 2)"):

1. **#6 boundary cap.** Macro-storage *shape rotation only* — dispatcher lookup reads through `Def { kind: DefKind::Macro }` instead of `ModuleEntry::Macro`. `expand`'s invocation-vs-Gap policy is FIXME 0175's call, not S70's. No Gap-emission changes, no `is_macro_head` narrowing, no `expand` invocation deepening.
2. **No incidental public-API changes.** Every public-API diff line at A5 must trace to a probe row.
3. **Frontend-narrow.** No `cranelisp-types` edits; no other-consumer-crate edits; no facade edits.
4. **No source touches in this document.** `/dev` executes at A4.

This plan describes the work to be done at A4 — destination shapes, action class per row, design-doc refreshes (done now where they clarify A4, deferred otherwise), and a sequencing recommendation. It does NOT execute source changes.

---

## Per-row dispositions

Probe sites are grouped by class. Sites are listed file:line per `cargo check -p cranelisp-frontend` (current commit). Production-vs-test split named where relevant — both are owned by `/dev (cranelisp-frontend)` since they live inside the crate.

### Row #1 — `Symbol.0` private (Principle 18 newtype opacity, Group H)

- **Probe sites**: `ast_builder.rs:516`, `ast_builder.rs:523` (2 production sites).
- **Disposition**: **mechanical**.
- **Destination shape**: `Symbol` is a string newtype generated by `string_newtype!` (`crates/cranelisp-types/src/newtype.rs:8`). Public accessors available: `Deref<Target=str>` (so `&*sym` or `&sym[..]` yields `&str`), `AsRef<str>`, `Borrow<str>`, `PartialEq<str>`, `Display`. No `.0` access; no `.as_str()` method.
- **Action**: at `ast_builder.rs:516`, replace `fname == &f.name.0` with `fname == f.name.as_ref()` (the LHS is `&String`; switch the comparison side or coerce). At line 523, replace `f.name.0.clone()` with `f.name.as_ref().to_string()` (the local store is `String`-typed). Either both sites adapt to `&str` comparisons (preferred — change `field_to_var: Vec<(String, Symbol)>` to `Vec<(Symbol, Symbol)>` and use newtype throughout) or keep the current `String` storage and bridge via `as_ref().to_string()`. /dev's call which is cleaner; either is mechanical.
- **Design refresh**: none. The opacity rule is grounded in Principle 18 + S69 Group H + the newtype Hard Rule in `design/arch/CLAUDE.md` §"String Newtypes". No frontend design doc names `.0` access — there is nothing to retract.
- **S69 origin overlap**: this is Group H newtype opacity; S69 settled the rule, frontend now reads through the public accessors. No facade contradiction.

### Row #2 — `TypeName.0` private (Principle 18 newtype opacity, Group H)

- **Probe sites**: among the `cargo check` output, this row is folded into Row #12's "explicit construction required" cluster — `TypeName` is the receiver type of `TypeRef::new(None, name)` (see #12). Direct `.0` access on `TypeName` was not visible in current `cargo check` output (the 3 sites the SPRINT.md probe attributes to this row appear to be among the `TypeRef: From<&str>` E0277 errors at `ast_builder.rs:1368`, `:1481`, `:1520` — those are #12, not direct `.0`). **Re-classification noted**: the SPRINT.md probe table likely conflated TypeName-related construction with the underlying `TypeRef: From<&str>` errors at the same sites.
- **Disposition**: **mechanical, folded under #12**. No standalone TypeName.0 fix is needed — all three sites are reached through the explicit `TypeRef::new` construction in #12's action.
- **Action**: see #12. After #12 lands, recheck for residual `TypeName.0` access; if any survive, replace with `.as_ref()` / `Deref<Target=str>` per the same mechanical rule as #1.
- **Design refresh**: none.
- **Note for /arch at A3**: if A3 finds the SPRINT.md attribution is binding and a distinct row was intended, please clarify — current source shows 0 direct `TypeName.0` sites. Folding-under-#12 is the most coherent reading.

### Row #3 — `ModDecl.is_private → visibility` (D51 visibility cascade)

- **Probe sites**: `module_extract.rs:106` (1 production site in `parse_mod_decl` returning `ModDecl { ... is_private, ... }`). Five test sites at `:487`, `:498`, `:511`, `:716`, `:718` read `ms.mod_decls[0].is_private` directly. The boolean-construction site at `:411–412` (`let is_private = head == "mod-"; parse_mod_decl(elems, *span, is_private)`) is the entry point and changes signature accordingly.
- **Disposition**: **mechanical**.
- **Destination shape**: `ModDecl.visibility: Visibility` (per `crates/cranelisp-types/src/module.rs:1076`). `Visibility` is `Public | Private` (per `crates/cranelisp-types/src/ast.rs:309`). Per `ModDecl` rustdoc: "`visibility == Visibility::Private` distinguishes `(mod-)`".
- **Action**:
  1. Change `parse_mod_decl`'s signature: `is_private: bool` → `visibility: Visibility`. Internal: `ModDecl { ... is_private, ... }` → `ModDecl { ... visibility, ... }`.
  2. At call site `:411–412`: replace `let is_private = head == "mod-"; parse_mod_decl(elems, *span, is_private)` with `let visibility = if head == "mod-" { Visibility::Private } else { Visibility::Public }; parse_mod_decl(elems, *span, visibility)`.
  3. Test sites read `.is_private` → `.visibility == Visibility::Private` (or `matches!(..., Visibility::Private)`). All five are simple substitutions.
  4. Import `cranelisp_types::Visibility` if not already in scope at `module_extract.rs`.
- **Design refresh**: `design/frontend/modules.md` does not name the field; no refresh needed.
- **S69 origin overlap**: D51 visibility cascade (S69 Submission 12). The audit-s69 doc does not flag this — `ModDecl` is internal to types; the cascade is mechanical at consumer side.

### Row #4 — `ModuleEntry::Import { visibility }` field add (D51 visibility cascade)

- **Probe sites**: `expand.rs:270` (1 production pattern-match site). Test fixture sites at `expand.rs:447–453` and `module_extract.rs` (none — module_extract does not pattern-match `ModuleEntry::Import`).
- **Disposition**: **mechanical**.
- **Destination shape**: `ModuleEntry::Import { source: FQSymbol, visibility: Visibility }` (per `crates/cranelisp-types/src/module.rs:645–648`). Per D51 + `Import` rustdoc: visibility discriminates `(import)`-effect (`Private`) from `(export)`-effect-on-imported (`Public`, was the prior `Reexport` variant).
- **Action**: at `expand.rs:270`, the existing `ModuleEntry::Import { source } | ModuleEntry::Reexport { source }` arm collapses (per #5) to `ModuleEntry::Import { source, .. }` (or `{ source, visibility: _ }` if /dev prefers explicit-ignore). The expand-side lookup does **not** filter by visibility — chain-follow walks `Import` edges regardless of visibility per D51 + Principle 17 (see `ModuleEntry::Import` rustdoc lines 641–644: "Chain-follow per Decision 45 walks `Import` edges regardless of visibility — the variant collapse simplifies the pattern-match"). This is the cap on #4 + #5 work in frontend: `expand` just collapses arms; no visibility-aware filtering is added.
- Test fixture at `:447` constructs `ModuleEntry::Import { source: FQSymbol { ... } }` — add `visibility: Visibility::Public` (consistent with the "imported macro is visible to user" intent of the test). Same in expand.rs:304 macro-fixture builder (after #6).
- **Design refresh**: none. Frontend design docs do not name `Import` visibility semantics — that's a types-side / typecheck-side concern.
- **S69 origin overlap**: D51 Submission 12 + Submission 22 (variant retirement). Naturally subsumes #5.

### Row #5 — `ModuleEntry::Reexport` variant deletion (collapse to `Import`) (D51 visibility cascade)

- **Probe sites**: `expand.rs:270` — same site as #4's collapse; the `| ModuleEntry::Reexport { source }` arm is the explicit reference. (1 site.)
- **Disposition**: **mechanical**.
- **Destination shape**: `ModuleEntry::Reexport` no longer exists. The export-effect-on-imported case is `ModuleEntry::Import { ..., visibility: Public }` per #4's destination.
- **Action**: at `expand.rs:270`, drop the `| ModuleEntry::Reexport { source }` alternation; the surviving `Import { source, .. }` arm covers both edges per the D51 collapse. Update inline comment at `:271–273` ("One-hop chain follow per Principle 17. Avoids infinite loops by NOT recursing further: an Import-of-an-Import is treated as a non-macro for resolution purposes") — it remains correct but should drop the "or Reexport" phrasing if any survives.
- **Design refresh**: none. `design/frontend/wave-3a-build-form.md` §6 mentions `ModuleEntry::Macro` but not `Reexport`. No removed-variant text needs a goodbye paragraph.
- **S69 origin overlap**: D51 cascade. Subsumed by #4's action.

### Row #6 — `ModuleEntry::Macro` deletion → `Def { kind: DefKind::Macro { … } }` (Submission 13 macro-unification)

- **Probe sites**: `expand.rs:269` (`Some(ModuleEntry::Macro { .. }) => true` in `macro_entry_present`), `expand.rs:276` (`matches!(home.get(...), Some(ModuleEntry::Macro { .. }))` — the chain-follow leg). 2 production sites. Test sites at `expand.rs:304` (fixture construction `ModuleEntry::Macro { name, clauses, docstring, visibility, sexp, source, callees }`) are inside the crate and within /dev's remit.
- **S69 origin**: Submission 13 macro-unification cascade — `ModuleEntry::Macro` retired (Submission 22 deleted the variant from `crates/cranelisp-types/src/module.rs`); destination is `ModuleEntry::Def { kind: DefKind::Macro { clauses_meta, sexp, source }, … }` per S69 Submission 13 design + frontend facade §80–82 (existing target text: "when an FQ macro reference resolves to `Def { kind: DefKind::Macro { clauses_meta, … }, … }`, it walks `clauses_meta` to pattern-match the call sexp against each clause's shape, then GOT-dispatches to the matched clause's mangled-variant Def").
- **Disposition**: **design refresh, capped at lookup-shape rotation per Phase 2 arch ruling.**
- **Cap reaffirmed**: this row is the **dispatcher lookup shape** only. Specifically:
  - `macro_entry_present` returns `bool` on whether the entry-at-name is a macro. The post-cascade reading is "is the entry `ModuleEntry::Def { kind: DefKind::Macro { … }, .. }`". The two `matches!` patterns rotate; the function's signature, return type, and the surrounding `lookup_macro_fq` walk do not change.
  - `expand_recursive` continues to emit `Err(ExpansionError::Gap(ResolutionGap::MacroInMem(fq)))` for every macro head encountered (per FIXME 0175 deferral; see `expand.rs:178, :193`). **Nothing in the gap-emission path changes.**
  - The `is_macro_head` logic — bare-symbol detection at `:175–179`, FQ-symbol detection in `lookup_macro_fq` — is structurally identical. The pattern that says "this is a macro reference" still says it; the storage shape behind that signal rotates.
- **Source pattern change**:
  - `Some(ModuleEntry::Macro { .. }) => true` → `Some(ModuleEntry::Def { kind, .. }) if matches!(**kind, DefKind::Macro { .. }) => true` (note `kind: Box<DefKind>` — see `crates/cranelisp-types/src/module.rs:489`; double-deref required). Or use an `if let` pattern. /dev's call on the precise Rust shape.
  - `matches!(home.get(source.symbol.as_ref()), Some(ModuleEntry::Macro { .. }))` rotates the same way: `matches!(home.get(...), Some(ModuleEntry::Def { kind, .. }) if matches!(**kind, DefKind::Macro { .. }))`. (`matches!` with `if`-guard is supported in current Rust; alternatively unfold to an explicit match.)
  - Test fixture at `:304` rebuilds: instead of `ModuleEntry::Macro { name, clauses, docstring, visibility, sexp, source, callees }`, construct `ModuleEntry::Def { scheme, visibility, docstring, param_names, kind: Box::new(DefKind::Macro { clauses_meta, sexp, source }), callees, got_slot, trait_origin, ast, code }` with the macro-relevant fields filled and irrelevant ones at sensible defaults. The test asserts only that `lookup_macro_fq` returns `Some(_)` for the registered name → the fixture can be minimal.

- **CRITICAL FINDING — escalate to /arch at A3**: `DefKind::Macro` is referenced in `cranelisp-types` rustdoc (`module.rs:343, :513, :704, :705, :710`; `parsed.rs:51, :52, :78, :82`) and was the S69 Submission 13 destination shape — **but the `DefKind` enum at `crates/cranelisp-types/src/module.rs:864–925` does NOT include a `Macro` variant**. It defines `Primitive`, `PlatformEffect`, `UserFn`, `Overloaded`, `Constructor` only. The S69 Submission 22 source catch-up deleted `ModuleEntry::Macro` but did **not** add `DefKind::Macro`.

  This is a **cross-crate blocker** for #6's source touches: /dev cannot rotate to `DefKind::Macro` at A4 because the variant does not exist in `cranelisp-types`. The probe-discovered errors at `expand.rs:269, :276` confirm — the compiler reports "no variant named `Macro` found for enum `ModuleEntry<_>`" not "missing field on `DefKind::Macro`".

  **Disposition options for /arch arbitration at A3**:
  - **(a)** S70 scope expands to add `DefKind::Macro { clauses_meta: Vec<MacroClauseInfo>, sexp: Option<Sexp>, source: Option<String> }` to `cranelisp-types` as a hot-fix paired with the frontend cascade. This violates frontend-narrow scope (S70 § "Out-of-scope" lists wave-3 consumer cascade for the 5 other crates as deferred to S71+ — but `cranelisp-types` itself is a Phase A dependency, not a consumer). The S69 close is the natural manifestation site; if S69 omitted it, S70 absorbing is /arch's call.
  - **(b)** S70 files FIXME `target: /arch` for the missing `DefKind::Macro` variant, and dispositions row #6 as **blocked-on-types-side**. The 2 expand.rs sites remain failing-to-compile until /types-narrow lands the variant. This violates Phase A exit gate row 1 (frontend nextest green) unless the gating is renegotiated.
  - **(c)** Re-confirm that S69 Submission 13's design actually retired the storage-shape rotation in favour of an alternative (e.g., macro metadata lives somewhere other than `DefKind`). Cross-check `facades/types.md` §"DefKind" + the rest of `module.rs` for the actual destination.

  My (`/design`-frontend) reading of the configuration points at (a) — every rustdoc reference, every S69 archive entry, the frontend facade §80–82, and the wave-3a-build-form §6 reference `Def { kind: DefKind::Macro { … } }` with full field list. The variant's substance is fully specified in writing; only the enum declaration is missing. But **adding a variant to `cranelisp-types` is `/arch`'s prerogative**, not `/design`'s. Filing FIXME `target: /arch` is the protocol; A3 should pull this question forward in its scope.

  **Recommendation for A3**: confirm the destination shape (a) or (b) or (c) before A4 fires. If (a), the FIXME closes on `/arch`'s in-sprint cascade (S70 absorbs the types-side variant add, since the missing variant blocks the frontend cascade the sprint scopes). If (b), the SPRINT.md scope amends to defer row #6 to S71's types-side wave.

- **Design refresh required**: yes — see "Design-doc refreshes" section below. `design/frontend/frontend.md` §3.3 + §5.1 + §5.4 + Decision 21 row reference `ModuleEntry::Macro`; `design/frontend/wave-3a-build-form.md` §6.1.1 + Phase 2 §"Frontend reads" reference it; `design/frontend/macro-plan.md` is heavily stale (Sprint-50-era; Decision 8 retraction predates this row; row #6 doesn't add new staleness, only intensifies existing).

- **What this plan explicitly does NOT do (0175 boundary)**:
  - Does NOT change `Err(ExpansionError::Gap(ResolutionGap::MacroInMem(fq)))` to anything else.
  - Does NOT change which sexp positions are recognised as macro heads.
  - Does NOT add invocation code paths or marshal hooks.
  - Does NOT widen frontend dependencies (no new `cranelisp-runtime` / `cranelisp-primitives` / `cranelisp-marshal` imports).
  - Does NOT migrate any logic out of `src/expander.rs`. That migration is FIXME 0098 Phase 2, gated on FIXME 0175.

### Row #7 — `DefnVariant.param_annotations` → fused `params: Vec<(Symbol, Option<TypeExpr>)>` (Submission 23)

- **Probe sites**: `ast_builder.rs:864–866` (impl method-defn construction inside `parse_impl`'s method-builder), `ast_builder.rs:865–866` mirror (same fields — folded), plus implied site at `:858` where `build_annotated_params` returns the parallel-vec pair. 3 sites per probe; the actual struct-construction call sites are at `:864–866` and one other (likely `parse_defn`'s loop — `/dev` enumerates at A4). The probe attributes "3" — accept.
- **S69 origin**: Submission 23 fused `params + param_annotations` into `params: Vec<(Symbol, Option<TypeExpr>)>` per Principle 18 (enforce invariants structurally — the lockstep invariant `params.len() == param_annotations.len()` was unenforced; fusing eliminates by construction). See `crates/cranelisp-types/src/ast.rs:378–390`:

  > `params: Vec<(Symbol, Option<TypeExpr>)>` (replaces the prior parallel-vec `params: Vec<Symbol>` + `param_annotations: Vec<Option<TypeExpr>>` shape per S69 Submission 23 — the lockstep invariant was unenforced).
- **Disposition**: **design refresh** — frontend's `build_annotated_params` helper currently returns `(Vec<Symbol>, Vec<Option<TypeExpr>>)`; the cleanest cascade rewrites the helper to return the fused shape. This is an internal-helper API change, not a public-surface change.
- **Source pattern change**:
  1. Change `build_annotated_params`'s return type at `ast_builder.rs:1422–1424` from `Result<(Vec<Symbol>, Vec<Option<TypeExpr>>), CranelispError>` to `Result<Vec<(Symbol, Option<TypeExpr>)>, CranelispError>`.
  2. Internally: at `:1426–1427` replace `let mut names: Vec<Symbol> = Vec::new(); let mut annotations = Vec::new();` with `let mut params: Vec<(Symbol, Option<TypeExpr>)> = Vec::new();`. At `:1441–1442, :1446–1447`, replace the dual-push pattern with `params.push((name.into(), Some(te)))` / `params.push((name.into(), None))`. Adjust the duplicate-name check at `:1454–1462` to iterate `&params` and read `.0`.
  3. All four call sites — `:260`, `:309`, `:688`, `:858`, `:1211` — change from `let (params, param_annotations) = build_annotated_params(&children[...])?` to `let params = build_annotated_params(&children[...])?`.
  4. Struct-construction sites — `DefnVariant { params, param_annotations, body, span }` at `:864–866` and `:1214–1215` (the latter for `Expr::Lambda` per #8, sharing the cascade) — drop the `param_annotations` field reference and pass the fused `params` directly.
  5. Trait-method site at `:688–725` (`parse_deftrait_method`) — see #9 for the per-trait rework (the helper feeds it but the destination shape is different).

- **Design refresh**: minor — `design/frontend/frontend.md` §3.3 / §4 mentions `build_annotated_params` in a sentence ("`defmacro.rs` knows `build_annotated_params()` expects `:` + type-expr + name as separate bracket items; this shape lock is implicit"). The bracket-shape lock invariant is unaffected by the return-shape change; no doc edit needed there. The wave-3a-build-form.md §3 references `params: Vec<Symbol>` indirectly via the `ParsedEntry::Def` shape (`design/frontend/wave-3a-build-form.md:36`); not load-bearing for the fused tuple discussion. **No design-doc edit needed for #7** beyond the §3.1 file-LOC line in `frontend.md` which is approximate anyway.

### Row #8 — `Expr::Lambda.param_annotations` → fused tuple shape (Submission 24)

- **Probe sites**: `ast_builder.rs:1214–1215` (1 site — mirror of #7 in `build_fn`).
- **S69 origin**: Submission 24, mirror of #7's fix for `Expr::Lambda`. See `crates/cranelisp-types/src/ast.rs:145–155`:

  > Lambda expression — `(fn [param ...] body)` (spec §4.5). … The fused tuple `params: Vec<(Symbol, Option<TypeExpr>)>` (replaces the prior parallel-vec `params: Vec<Symbol>` + … the lockstep invariant was unenforced). Mirrors `DefnVariant`'s shape per Principle 18.
- **Disposition**: **design refresh, folded with #7** — same helper-API rotation drives the fix at this site.
- **Source pattern change**: at `:1211`, take the fused `params` from `build_annotated_params`. At `:1213–1219`, `Expr::Lambda { params, param_annotations, body, span, inferred_type }` → `Expr::Lambda { params, body: Box::new(body), span, inferred_type: None }` (drop `param_annotations`).
- **Design refresh**: none beyond #7. The cascade is the same helper-return rotation; one extra call site.

### Row #9 — `TraitMethodSig.default_param_names` field deletion (S69 trait-cascade follow-on)

- **Probe sites**: `ast_builder.rs:708, :725` (2 production sites — both inside `parse_deftrait_method` / `build_trait_method`, one for the default-body branch and one for the no-default branch).
- **S69 origin**: trait-cascade follow-on to Principle 18 — the `default_param_names` field carried a hidden lockstep invariant (`default_param_names.is_empty() == default_body.is_none()`) that no type rule enforced. See `crates/cranelisp-types/src/ast.rs:444–490`:

  > Per Principle 18 (enforce invariants structurally), name + annotation belong together on each param rather than across parallel vectors. … fusing names into `params` and dropping the separate `default_param_names` field eliminates that invariant by construction. Names belong with the params, not with the default body.

  Plus a secondary change at `:489`: `default_body: Option<Expr>` (was `Option<Sexp>`) per S69 Submission 26 — "Building the AST at trait-decl time catches structural errors in special forms (`let`, `if`, `match`, etc.) immediately, rather than per-impl". This surfaces as the second error at `ast_builder.rs:709` ("expected `Option<Expr>`, found `Option<Sexp>`").

- **Disposition**: **design refresh** — frontend's trait-method parser changes shape on two axes:
  1. **Param shape**: from `params: Vec<TypeExpr>` (current source — `ast_builder.rs:715` returns `bracket_items.iter().map(build_type_expr).collect()`) to `params: Vec<(Symbol, TypeExpr)>`. Per types-side rustdoc lines 442–450: "Per spec §5.3.1, bare parameter names default to the implementing type; the parser synthesises `TypeExpr::SelfType` for bare params at parse time so `params: Vec<(Symbol, TypeExpr)>` is unconditional".
  2. **Default-body shape**: from `default_body: Option<Sexp>` to `default_body: Option<Expr>`. Parser must call `build_expr(&children[ret_pos + 1])?` instead of `children[ret_pos + 1].clone()`.

- **Source pattern change** (per current `ast_builder.rs:660–728`, `parse_deftrait_method`):
  - The "with default body" branch (current `:680–710`): the existing code already calls `build_annotated_params` at `:688` to get `(param_names, annotations)`. The new shape needs to **carry the param names AND annotations through to the `TraitMethodSig.params` field as a single `Vec<(Symbol, TypeExpr)>`**. Where the current code synthesised `TypeExpr::SelfType` for bare params at `:686–696` (the existing trait-method bare-param-defaulting logic), it now folds into the fused tuple: `params: Vec<(Symbol, TypeExpr)> = param_names.into_iter().zip(annotations.into_iter().map(|a| a.unwrap_or(TypeExpr::SelfType))).collect()`. Then `build_expr` lowers `default_body` at `:699` instead of `clone()`.
  - The "no default body" branch (current `:711–728`): currently the bracket items are interpreted purely as type expressions (no names — params are by-position). Under the new shape, each "param" still needs a name slot. Two readings:
    - (i) Synthesize fresh placeholder names (e.g., `Symbol::from(format!("__p{}", i))`) — keeps the shape regular at the cost of opaque names that show up in diagnostics.
    - (ii) Require the user to write `: Type name` even for required (no-default) methods, matching spec §5.3 EBNF `param = ':' type_expr symbol | symbol`. If the spec already requires this, (ii) is correct and the no-default branch should call `build_annotated_params` too.
    - The types-side rustdoc (`ast.rs:432–443`) cites spec §5.3 EBNF as `required_method = '(' name docstring? '[' param+ ']' type_expr ')'` with `param = ':' type_expr symbol | symbol` — so **required methods do carry param names per spec**. Reading (ii) is the spec-grounded answer. The current no-default branch's "params are bare type expressions" reading is a parser shortcut that the new shape retires; /dev should switch to `build_annotated_params` for both branches.

  - At `:708, :725`: drop the `default_param_names: ...` field on both branches. The names live in `params` now.

- **Design refresh required**: minor — `design/frontend/ast-builder.md` is already marked stale on ring-gating (per `frontend.md` §9 staleness register: "Stale on ring-gating. Claims 'Ring 0, non-Ring-0 rejected' while the file handles traits, impls, strings, vec literals, trace forms"). The new shape adds another data point to the deferred refresh, but does not introduce a new contradiction. **Defer the full `ast-builder.md` refresh to a follow-up sprint per `frontend.md` §9** — call this out in the cascade plan but do NOT inline-edit. /dev at A4 implements against the spec §5.3 EBNF directly + the types-side rustdoc.

- **S69 audit-s69 overlap**: none. The S69 frontend audit (`facades/cranelisp-frontend-audit-s69.md`) does not flag this row — the audit was triple-narrowed at `lib.rs` × facade × `public-api.txt`, and `TraitMethodSig` shape lives inside `ast_builder.rs` (a deeper-layer file).

### Row #10 — `TraitImpl.{type_args, target_type}` field rename / restructure (S69 trait-cascade follow-on)

- **Probe sites**: `ast_builder.rs:762–765` (4 fields named in one struct literal — counted as 2 by the probe per `target_type`+`type_args` removal; `type_constraints` separately mis-types via #10b below). The structurally-bound site at `:753` (`let (target_type, type_args, type_constraints) = build_impl_target(&children[2])?;`) is the helper return that feeds the construction.
- **S69 origin**: trait-cascade follow-on (Submission 27 — TypeExpr.Named/Applied carries TypeRef; consequent restructure of `TraitImpl` to take a unified `target: TypeExpr`). See `crates/cranelisp-types/src/ast.rs:503–548`:

  > Per spec §5.4 EBNF the `impl_form` treats `target_type` as one grammatical unit … The unified `target: TypeExpr` field captures that grammatical unit directly — the simple `target_type = qualified_symbol` case lowers to `TypeExpr::Named(TypeRef)` and the polymorphic `target_type = '(' qualified_symbol type_arg+ ')'` case lowers to `TypeExpr::Applied(TypeRef, Vec<TypeExpr>)`. The prior 6-field shape (`target_type: TypeName + type_args: Vec<Symbol>`) was an implementation detail with no Decision-level grounding and is replaced …
  >
  > `type_constraints: Vec<(Symbol, TraitRef)>` carries polymorphic-impl constraints …

  Also affects #10b: `type_constraints` is now `Vec<(Symbol, TraitRef)>` (was `Vec<(Symbol, TraitName)>`); this surfaces as a separate `cargo check` error at the same site.

- **Disposition**: **design refresh** — `build_impl_target` helper needs to fold the prior `(TypeName, Vec<Symbol>, Vec<(Symbol, TraitName)>)` return into the new `(TypeExpr, Vec<(Symbol, TraitRef)>)` shape, then the construction at `:761–768` simplifies dramatically.
- **Source pattern change**:
  1. `build_impl_target` rotates its return type from `(TypeName, Vec<Symbol>, Vec<(Symbol, TraitName)>)` to `(TypeExpr, Vec<(Symbol, TraitRef)>)`.
  2. Inside `build_impl_target`: the simple-target case builds `TypeExpr::Named(TypeRef::new(None, TypeName::from(name)))` (or with the module path if the symbol is qualified — `build_impl_target` already parses `module/name` shapes elsewhere; thread through). The polymorphic-target case builds `TypeExpr::Applied(TypeRef::new(None, head), args)` where args are `Vec<TypeExpr>` synthesised from the type-args bracket items (currently `Vec<Symbol>` — change to TypeExpr).
  3. Constraint carriers rotate from `(Symbol, TraitName)` to `(Symbol, TraitRef::new(None, name))` — the constraint can be qualified per types-side rustdoc lines 534–537.
  4. At construction site (current `:760–768`): rotate to `TraitImpl { trait_name: TraitRef::new(None, TraitName::from(trait_name)), target, type_constraints, methods, span }` — `target_type` and `type_args` fields gone; `target: TypeExpr`.
  5. `trait_name: trait_name.into()` (current `:762`) — fails because `TraitRef: From<&str>` no longer exists (see #13). Replace with explicit `TraitRef::new(None, TraitName::from(trait_name))`. This is the row #13 fix at this site.

- **Design refresh required**: `design/frontend/ast-builder.md` is stale anyway (see #9); the impl-target lowering is an existing-text-stale item, not a new contradiction. Defer the full refresh as per #9. /dev at A4 implements against spec §5.4 EBNF + the types-side `TraitImpl` rustdoc.

### Row #11 — `FieldDef.span` now required (S69 span-discipline)

- **Probe sites**: `ast_builder.rs:527` (1 production site — single `FieldDef { ... }` literal in `parse_deftype`-area code). The probe attributes "3" sites; the additional sites are likely a `field.span()` carry-through in `build_field_def` (one more construction) and one in a `match`-arm somewhere. /dev enumerates at A4 — accept "3 sites" as the probe count.
- **S69 origin**: span-discipline cascade — per Decision 39 + S69 Group D's bounded-context settlement (each field carries its own span so "field has wrong type" diagnostics can point at the field's source location). See `crates/cranelisp-types/src/ast.rs:410–419`:

  > each field carries its own `span` so "field has wrong type" diagnostics can point at the field's source location (not the enclosing constructor).
- **Disposition**: **mechanical** (per SPRINT.md probe-table "mechanical (thread Span through)").
- **Action**: at every `FieldDef { name, type_expr }` construction site, add `span: <field-source-span>`. The span is the source span of the field-name symbol (or the `:Type name` annotation block as a whole — /dev's call, but use the symbol-name span for consistency with `Defn.span` discipline). At `:527`, the surrounding `parse_deftype`-area logic walks an `f: &FieldDef` from a `.iter()` chain — `f.name.span()` is available if `Symbol`-typed name carries span (it does — `Sexp::Symbol(String, Span)` is the source-side; once parsed, the construction site has the original `Sexp::Symbol`'s span in scope as part of the surrounding loop).
- **Design refresh**: none. Frontend design docs do not name `FieldDef.span`; the span-discipline rule is generic (every node carries a span).
- **S69 origin overlap**: Decision 39 substance manifested in `bounded-contexts.md` §7 + `repl/spec.md` §15.4; this row is purely consumer-side carrying.

### Row #12 — `TypeRef: From<&str>` removed (newtype opacity follow-on)

- **Probe sites**: `ast_builder.rs:1368, :1481, :1520` (3 production sites — all `TypeExpr::Named(name.into())` or `TypeExpr::Applied(head.as_str().into(), …)` patterns).
- **S69 origin**: newtype-opacity follow-on. `TypeRef` is constructed via `TypeRef::new(module: Option<ModuleFullPath>, name: TypeName)` per `crates/cranelisp-types/src/newtype.rs:192–193`. The `From<&str>` impl was retired (per audit Group H closure narrative — the trait impls that hid module-vs-name structure inside a string get explicitly retired so consumers cannot accidentally construct without naming the module/name structure).
- **Disposition**: **mechanical**.
- **Action**: at each site, replace `name.into()` with `TypeRef::new(None, TypeName::from(name))` (or carry the module if available — at `:1368, :1481, :1520`, the source code is parsing a bare `TypeExpr::Named` from a single symbol-name; module is `None`). Specifically:
  - `:1368`: `TypeExpr::Named(name.into())` → `TypeExpr::Named(TypeRef::new(None, TypeName::from(name)))`.
  - `:1481`: `TypeExpr::Named(name.as_str().into())` → `TypeExpr::Named(TypeRef::new(None, TypeName::from(name.as_str())))`.
  - `:1520`: `TypeExpr::Applied(head.as_str().into(), args)` → `TypeExpr::Applied(TypeRef::new(None, TypeName::from(head.as_str())), args)`.
- **Design refresh**: none. The construction rule is grounded in Principle 18 + the newtype Hard Rule; `design/arch/CLAUDE.md` §"String Newtypes" carries the principle. Frontend design docs do not name `TypeRef::From<&str>`; nothing to retract.
- **Note**: this row may **incidentally surface qualified-name handling** at the parser. If the parser currently treats `"module/Type"` as a single symbol via `.into()`, the explicit `TypeRef::new` calls must decide: parse out `module/` or leave as `None`. **Out-of-scope for S70** per the no-incidental-changes constraint — /dev preserves the current behaviour exactly. The qualified-name lift to `Some(module)` is a separate concern tracked by FIXME 0151 (FQTypeName).

### Row #13 — `TraitRef: From<&str>` removed (newtype opacity follow-on)

- **Probe sites**: `ast_builder.rs:762` (1 production site — `trait_name: trait_name.into()` in `TraitImpl` construction inside `parse_impl`).
- **S69 origin**: newtype-opacity follow-on, parallel to #12. `TraitRef::new(module: Option<ModuleFullPath>, name: TraitName)` per `crates/cranelisp-types/src/newtype.rs:154–155`. Same audit rationale as #12.
- **Disposition**: **mechanical**, folded with #10's `TraitImpl` construction fix.
- **Action**: at `:762`, replace `trait_name: trait_name.into()` with `trait_name: TraitRef::new(None, TraitName::from(trait_name))`. Subsumed by #10's full restructure of the construction site.
- **Design refresh**: none. Same as #12.

---

## Design-doc refreshes

Three frontend design docs reference the soon-rotated shapes. Refresh strategy per doc:

### `design/frontend/frontend.md` — DONE inline (this commit)

Affects: §3.3 (implicit pipeline contracts), §5.1 (macro-expander internal arrangement), §5.4 ("Why MacroResolver is not the public boundary"), §8 Decision 21 row, §3.1 file partition table.

Decision: refresh **inline now** for the `ModuleEntry::Macro` → `Def { kind: DefKind::Macro }` rotation. Reasons:
1. The facade (`facades/frontend.md` §80–82) already cites the new shape ("when an FQ macro reference resolves to `Def { kind: DefKind::Macro { clauses_meta, … }, … }`"). Frontend's own design doc is now the stale half.
2. /dev at A4 reads this doc as design context; the refresh-now timing keeps the doc consistent with the source it will produce.
3. The change is bounded: rotate ~6 sentences from `ModuleEntry::Macro` to the destination shape; cite the row #6 cap.

Status: **PENDING-A4** (deferred). The design refresh requires the `DefKind::Macro` variant to actually exist (see #6 CRITICAL FINDING). Without /arch arbitration on disposition (a)/(b)/(c) at A3, refreshing `frontend.md` to cite the destination shape is **premature** — if A3 routes via (b) (FIXME-to-arch + defer row #6), this doc should NOT yet rotate. **Deferred to A4 conditional on A3's #6 ruling**. If A3 confirms (a) — types-side variant add lands in S70 — /dev refreshes `frontend.md` §3.3/§5.1/§5.4/§8 in the same A4 commit. If A3 routes via (b), the refresh waits for the S71 wave that lands `DefKind::Macro` on the types side.

### `design/frontend/wave-3a-build-form.md` — PENDING-A4 (conditional on #6 ruling)

Affects: §6.1.1 ("`ModuleEntry::Macro` entries are reachable through `SymbolTables[module].get(&name)`"); §6 list of types-side items names `ModuleEntry::Macro` indirectly.

Decision: same conditional as `frontend.md`. If A3 rules (a) — refresh inline at A4 to cite the new shape. If (b) — defer to post-types-side wave.

### `design/frontend/macro-plan.md` — already stale; defer per `frontend.md` §9

Affects: heavy. The doc is Sprint-50-era and predates Decision 8 retraction + Submission 13 macro-unification. `MacroEnv` references at `:256, :277, :284, :295, :298, :306, :322, :324, :355, :380, :381, :398, :423, :533, :558, :593` all describe a retired architecture.

Decision: **NO INLINE EDIT**. The frontend.md §9 staleness register correctly catalogs this doc as "partially stale (Decision 8 retraction)"; row #6's `ModuleEntry::Macro` → `DefKind::Macro` rotation adds another stale dimension but does not change the doc's overall staleness verdict. Refresh is a follow-up sprint's work (the §9 refresh-order list ranks `macro-plan.md` as priority #4 behind `plan-frontend.md`, `macro-resolver-trait.md`, `ast-builder.md`).

### `design/frontend/macro-resolver-trait.md` — already stale; defer

`frontend.md` §9 marks this as "Stale (Sprint-50-era)" — Decision 8 retraction supersedes the trait. Row #6 does not interact. **No edit**.

### `design/frontend/ast-builder.md` — already stale; defer

`frontend.md` §9 marks this stale on ring-gating. Rows #7/#8/#9/#10/#11 affect builder internals, but the doc is comprehensively stale; partial refresh is misleading. **No edit**. /dev at A4 implements against spec + types-side rustdoc directly.

### `design/frontend/frontend.md` — staleness register §9 update

A minor update to §9: rotate the `ast-builder.md` and `macro-plan.md` rows to add "S69 fused-tuple cascade" and "S69 macro-unification cascade" as new stale-dimensions. Decision: **PENDING-A4** — /dev at A4 makes a small §9 edit confirming the new stale-dimensions to track for future refresh sprints. Single-paragraph touch; coherent inline with the production source changes.

---

## A3 review checklist

`/arch` at A3 should verify:

- [ ] **All 13 probe rows dispositioned.** No `TBD`. (Confirm.)
- [ ] **#6 cap intact.** No proposed work touches `expand`'s invocation path, Gap-emission, `is_macro_head` logic, or marshal/runtime deps. (Re-read row #6's "What this plan explicitly does NOT do" section.)
- [ ] **#6 CRITICAL FINDING resolution.** Arbitrate disposition (a) / (b) / (c) on the missing `DefKind::Macro` variant in `cranelisp-types`. This is the single arch-arbitration point this plan surfaces. **A3 cannot pass without resolving this** — /dev cannot execute row #6 at A4 against a variant that does not exist.
- [ ] **No incidental public-API surface changes.** The plan proposes zero `pub`-visibility narrowing, zero helper extraction with re-export, zero "tidying". The `build_annotated_params` return-type change (#7) is `pub(crate)` — internal only.
- [ ] **Frontend-narrow.** No proposed change to `cranelisp-types`, `cranelisp-typecheck`, any other consumer crate, or to `facades/frontend.md`. (Row #6's CRITICAL FINDING flags a cross-crate need but does not propose to satisfy it inside this plan; the FIXME is the next step.)
- [ ] **Design-doc refresh strategy is sound.** Inline refresh of `frontend.md` deferred to A4 + conditional on #6 ruling; staleness register update at §9 is a minor follow-up; deeper-stale docs (`macro-plan.md`, `macro-resolver-trait.md`, `ast-builder.md`) deferred per existing staleness register.
- [ ] **S69 audit-s69 overlap reviewed.** Frontend audit-s69's H1/H2/S1/S2/U0 findings are not in S70's Phase A scope — they live in `lib.rs` × facade × public-api triple (S70 Phase B audit-walk territory) and the SPRINT.md scope explicitly sequences Phase A first. C1/C2/C3 are /qa-S70 brief, not in either phase. No new overlap surfaced.
- [ ] **Row #2 re-attribution accepted.** Confirm folding `TypeName.0` under #12 is coherent given current source (no direct `.0` sites surfaced by `cargo check`).
- [ ] **Trait-method spec reading (#9) confirmed.** Confirm reading (ii) — required methods carry param names per spec §5.3 EBNF — is the spec-grounded answer for the no-default branch. (If `/spec` arbitration is needed, FIXME `target: /spec`; surface at A3.)
- [ ] **Sequencing recommendation acceptable** (see §"Sequencing for /dev at A4" below).

---

## Open questions for /arch

1. **#6 — Missing `DefKind::Macro` variant in `cranelisp-types`.** S70 absorb in-sprint (a), defer-via-FIXME (b), or re-confirm an alternative destination shape (c)? *Blocks A4 execution of row #6 unless resolved.*
2. **#9 reading on no-default trait-method param names.** Reading (ii) — required methods carry param names per spec §5.3 EBNF — is `/spec`-grounded; if A3 disagrees, route via FIXME `target: /spec`.
3. **Row #2 re-attribution.** Accept folding `TypeName.0` under #12 (no direct `.0` sites in current `cargo check`)? If A3 reads the SPRINT.md probe as binding a distinct row, please clarify.

No other open questions. Rows #1, #3, #4, #5, #7, #8, #10, #11, #12, #13 are settled per the configuration; row #6 has a single named blocker; rows #2 and #9 have small clarifications.

---

## Sequencing for /dev at A4

Recommended ordering, in execution order:

1. **Row #6 first** — once /arch's A3 ruling is known. The lookup-shape rotation in `expand.rs:269, :276` (and the fixture at `:304`) compiles in isolation if `DefKind::Macro` exists (disposition (a)) or is FIXME-deferred (b). Doing #6 first surfaces any secondary errors that the rotation reveals — if any test fixtures break in unexpected ways, isolate before mixing with the other rows.
2. **Rows #1, #3, #11, #12, #13 — pure mechanical** — in any order. These are the lowest-risk fixes. Doing them together compresses the cargo-check feedback loop. Recommended bundle as a single commit ("mechanical newtype + span + visibility cascade").
3. **Rows #7 + #8 + helper API rotation** — `build_annotated_params` return-shape change cascades through 5 call sites + 2 struct-construction sites. Single commit; compile-clean per call site as you go.
4. **Row #9 + #10 — trait/impl shape rework** — the largest semantic change. Spec-grounded; carry the new `params: Vec<(Symbol, TypeExpr)>` shape from `TraitMethodSig` through to the surrounding `parse_deftrait` glue. Then row #10's `TraitImpl` restructure folds in (sharing the type-name → TypeExpr lift logic from #12).
5. **Row #4 + #5 — `ModuleEntry::Import` cascade** — likely subsumed by the #6 fixture rebuild. Confirm the `expand.rs:270` arm-collapse is clean; add `visibility:` field to the few fixture builders.
6. **Test fixture pass** — after production source is green, run `cargo nextest run -p cranelisp-frontend` and chase any test-only construction sites the cascade missed.
7. **Public-API baseline regen** — last step before A4 hand-off. `cargo public-api -p cranelisp-frontend > crates/cranelisp-frontend/public-api.txt`. Confirm every diff line traces to a row in this plan (Phase A exit gate row 5).
8. **Design-doc inline refresh** — if #6 routed via (a), refresh `frontend.md` §3.3/§5.1/§5.4/§8 + §9 staleness register update in the same commit as the row #6 source touches. If (b), skip — defer to S71.

Rationale: surface secondary errors early (step 1), bundle mechanical for compile-loop speed (step 2), tackle semantic cascades together where they share helpers (steps 3–4), pick up the small remaining sites last (step 5), then close discipline gates (steps 6–8).

---

## Cross-references

- `sprints/SPRINT.md` §"Phase A — Absorb types changes into cranelisp-frontend" — probe table + exit gate
- `sprints/SPRINT.md` §"Architecture review (Phase 2)" — #6 boundary cap + public-API row-traceability gate
- `design/arch/facades/frontend.md` §80–82 — target shape for `Def { kind: DefKind::Macro }` macro dispatch
- `design/arch/facades/cranelisp-frontend-audit-s69.md` — frontend audit-s69 findings (Phase B-territory; not in S70 Phase A scope)
- `design/arch/fixmes/0175-arch-frontend-expand-invocation-gap.md` — `expand` invocation path; bounded by row #6 cap
- `design/frontend/frontend.md` §9 — subordinate-docs staleness register
- `crates/cranelisp-types/src/ast.rs:99–155` — `Expr::Lambda` fused-tuple shape (#8)
- `crates/cranelisp-types/src/ast.rs:378–419` — `Defn` / `DefnVariant` / `FieldDef` shapes (#7, #11)
- `crates/cranelisp-types/src/ast.rs:430–490` — `TraitMethodSig` shape (#9)
- `crates/cranelisp-types/src/ast.rs:503–548` — `TraitImpl` shape (#10)
- `crates/cranelisp-types/src/module.rs:475–828` — `ModuleEntry` + `DefKind` (#4, #5, #6 — note `DefKind::Macro` referenced in rustdoc but not present in enum declaration — CRITICAL FINDING for #6)
- `crates/cranelisp-types/src/module.rs:1074–1079` — `ModDecl.visibility` (#3)
- `crates/cranelisp-types/src/newtype.rs:8–70` — string-newtype API surface (#1, #2)
- `crates/cranelisp-types/src/newtype.rs:149–195` — `TraitRef::new` / `TypeRef::new` (#12, #13)
