# Sprint 112: The 0628/I-C Compiler Wave — corrected §5.1.2 multi-sig inference + the settled trait/impl form

**Status**: PHASE 7 CLOSE — outcome authored 2026-07-19; awaiting user close approval (archive + ROADMAP update follow approval)

**Goal**: Implement the two S111-settled normative resolutions — I-C (multi-sig `defn` inference-equivalent to separate mutually-recursive functions, §5.1.2) and the 0628 trait/impl redesign (kind-by-usage traits, `self` as the implementing type, echo-the-head impl form, §7.3.5 kind-matching) — as one coordinated typecheck + backend + test-migration wave, unwinding every superseded asset.

**Audit**: — (user declined the rotation at Phase-1 scope selection 2026-07-18; `cranelisp-frontend` rotation debt persists — longest-unassessed since s87, declined S111 Phase 6 and again here; re-present at S113 Phase 1.)

## Scope

All normative questions are ALREADY RULED — the spec landed in S111 (`c9f05b64`: §5.1.2 back-flow correction; §7.1.1 param-or-return dispatch; §7.2/§7.3.4–6 trait kind-by-usage + impl form + kind-matching table; DRAFT amendments ratified `b37d77e6`). This sprint is pure implementation against settled spec; no user arbitration is expected mid-sprint (any new normative question found is a stop-and-ask, per standing rule).

**Surfaces (post-Phase-2): `cranelisp-frontend` (b0 `parse_impl` + types carrier), `cranelisp-typecheck` (legs a+b), `cranelisp-backend` (leg c, repro-gated), `cranelisp-types` (/arch pinned diff), tests/ + examples/ + repl/ (corpus migration A3).** Schema bump 20→21 at b2. See §Architecture review for the binding adjustments A1–A4 and the b0→b1→b2 ordering.

Three legs (per FIXMEs 0642, 0628, 0639 and the S111 close record):

**(a) §5.1.2 multi-sig back-flow — `cranelisp-typecheck` (FIXME 0642).**
Clause inference flows exactly as separate mutually-recursive top-level functions: remove the artificial independence/no-back-flow block and the "cannot be left polymorphic" restriction; keep §5.1.1 same-arity-unifiable ambiguity + dispatch-coherence checks. Anchor: un-annotated `rp4` compiles clean as `(Fn [Int Int] Int)`.

**(b) Trait/impl form — `cranelisp-typecheck` + registry (FIXMEs 0628 + 0639).**
The 0639 arbitration question is RESOLVED by the settled spec — `/design` actions against §7.1.1/§7.2/§7.3.4–6 and deletes both FIXMEs. Re-land the impl-target gate per the settled kind model; fix the `registry.rs::register_trait_decl` usage-derived HKT guard (the companion `:a 7` display defect's root); migrate ~7 e2e + ~24 typecheck unit fixtures off the `(X a)`-head mismodel to the `self` form (mechanics prototyped + green-verified in S111 CS-4, then reverted; constraint-carrying fixtures REQUIRE `SelfType`, not just empty `type_params`).

**(c) Return-type-dispatch codegen fix — `cranelisp-backend` (0628 tail).**
The `undefined function` codegen leak on the ill-formed HKT-on-primitive path closes check-side via (b); the admitted return-type dispatch (§7.1.1 param-or-return) must work end-to-end — the S111 close record names a backend return-type-dispatch fix as leg (c) of the wave.

**Test unwind + new coverage — `/testing` to `/qa`'s plan.**
- UNWIND superseded rejection assets: `multi_arity_clause_param_51_2` + CS-4.1 param-subtraction + carried CS-4.2 assets → the `rp4`-shaped program becomes an ACCEPTING test; poly+concrete non-overlapping multi-sig positive coverage; same-arity-unifiable still errors (§5.1.1). FIXME 0642's superseded-repro list is the checklist.
- 0628 rejection matrix (the variant×polarity lens): con_var {applied, bare-return, bare-arg} × target {primitive, wrong-arity ADT, well-kinded ADT}, routed per the settled §7.3.5 kind-matching table; the check-gate-leak guard flips (clean §7.2-class diagnostic, no backend leak).
- Expected suite delta: several of the 16 attributed REDs flip green (the multi-arity/I-C-coupled family: B-2/CS-4.2, `rp4`); no new REDs without owner+trigger attribution.

**Out of scope (explicit deferrals):**
- **Memory-safety-soundness infrastructure** (the S111 NEW TRACK: `/qa` oracle lane + generative harness, `/arch` safety-invariants cascade; FIXMEs 0641/0638/0633/0637/0632 ride it) — S111 close directed it to S112; the user re-scoped at S112 Phase-1 selection (2026-07-18) to keep this sprint focused. **Deferral count 1 → S113.** 0641 stays held behind the mechanism (no instance-patching).
- **`cranelisp-frontend` audit rotation** — declined (see Audit line). Debt recorded, twice-declined; escalate the presentation at S113 Phase 1.
- **SROA / `--release` frontier** — standing next track, untouched.
- Attribution carries not in this wave: 0604 (index-feed race, `/qa`), R2 entry-`main` IO-teardown leak, I-3 renamed-import (§8.3.5 — needs `/arch` scoping, own increment), 0553.
- Spec-open §7.3.6 sub-q1 (non-`self` type parameter) — parked; a quick user confirm may ride this sprint opportunistically but is not a gate.

## FIXME debt

| FIXME | Target skill | Status | Notes |
|---|---|---|---|
| 0642 | /design | **resolved** | Leg (a) landed `b5fa3f14`; /design verified all 3 actions + deleted (2026-07-18, staged). |
| 0628 | /design | open | Leg (b)+(c) — trait form + codegen leak. In scope; deletes on landing. |
| 0639 | /design | open | Leg (b) — arbitration RESOLVED by settled spec; `/design` actions + deletes. In scope. |
| 0621 | /sprint | **resolved** | Reaped at S112 Phase 1 — `storage_fq()` flip + unit pin verified landed in S111 CS-5. |
| others (18) | various | open | Out of scope this sprint; memory-safety family (0641/0638/0633/0637/0632) rides the S113 track. |

## Architecture review (Phase 2)

**Verdict: APPROVED-WITH-ADJUSTMENTS** (/arch, 2026-07-18). Three-leg scope coherent, faithful to the settled spec, no interim-architecture risk (P8). Four required adjustments (folded into §Scope):

- **A1 — `cranelisp-frontend` is a missing fourth surface.** The settled echo-the-head impl form is a hard parse error today (`parse_impl`, `ast_builder.rs:954` expects a bare symbol at slot 1). Needs a narrow frontend change-set (b0) + the pinned types carrier. Slot 2 rides existing `target: TypeExpr` (as `Applied`), kind-interpreted at the ONE §7.3.5 Case-3 seam — no second classifier.
- **A2 — rejection moves to DECLARATION time.** Settled §7.1/§7.2.1 rejects a never-applied `(X a)` head at `deftrait` (malformed), not at impl. The CS-4 gate prototype is NOT a verbatim re-land; `hkt.md` §5.1/§5.4 must be reconciled by /design(typecheck). Downstream consumers read `TraitDeclInfo.type_params` (non-empty ⟺ HKT), never scan usage — Principle-24-shaped collapse of the two divergent kind derivations (`registry.rs:117–126`, `impl_check.rs:39–92`).
- **A3 — user-facing corpus migration joins the wave**: `examples/26-functor.cl:47` + `30-parallel-map-reduce.cl:123` (old HKT impl form → echo-the-head, in b2); `repl/spec.md:432` + `repl/demos/05-traits.demo:31` (never-applied `(X a)` → bare-head+`self`, in b1). Otherwise the examples gate goes red at b2.
- **A4 — `CACHE_SCHEMA_VERSION` 20→21 REQUIRED, pinned to b2** (meaning change to persisted `TraitDeclInfo.type_params`; stale schema-20 cache could silently resurrect a now-rejected trait via cache-hit typecheck bypass — the CS-2/P25 cache-trust class). Leg (a) alone needs NO bump. One bump covers the sprint window (S110 precedent).

**Pinned types diff** (/arch lands at b0 + `public-api.txt` regen): additive `TraitImpl.head_con_var: Option<Symbol>` with `#[serde(default)]` — carries the written slot-1 head shape for echo validation. No other types change.

**Ordering edges (Phase-4 input):** (a) ⊥ (b) semantically — serialize only for the one-agent-edits rule, either order. (b) internal: **b0** (frontend+types, additive, green) → **b1** (conventional-half corpus migration to self-form, green→green — old compiler fully supports self-form, CS-4-verified) → **b2** (atomic: declaration reject + echo validation + §7.3.5 matching + registry guard fix + HKT-form corpus swap + schema bump + matrix flips green). **(c) is REPRO-GATED**: 0628's own "Not this" section shows the admitted form working at the REPL while S111 asserts a leak — contradiction ⇒ mode/context-dependent; pinned cross-mode repro FIRST (standing dual-path rule); attribution (backend vs typecheck-producer) follows evidence; if producer-side it lands inside the schema-21 window after b2; if no repro fires, (c) collapses to cross-mode e2e coverage of the admitted form.

**New completeness hole (a) opens — constrained-poly × multi-sig cell**: a constrained clause (`([:a x] (+ x x))`) is spec-admissible under the equivalence rule but rejected-by-construction today (`collect_defns` filters multi-sig out of `detect_constrained_fns`; `ConstrainedFn` single-variant invariant, `module.rs:2302`). **DISPOSITION (user, 2026-07-18): IMPLEMENT IN-SPRINT** — the `ConstrainedFn` single-variant invariant + `collect_defns` filter rework joins leg (a); `/design`(typecheck) designs the cell; `ConstrainedFn` rustdoc invariant note updates via FIXME `target: /arch` when the filter changes.

**Determinism obligation on (a):** admitted polymorphic clause's `OverloadVariant.mangled_name` must be deterministic (normalized var spelling, never session-dependent `t{id}`); /qa checks fresh-build `.meta.json` byte-identity.

**Notes for /qa** (feeds the mandated quality-risk assessment): blast-radius argument for (a) — previously-accepted multi-sig programs keep identical schemes/mangles/dispatch (back-flow only adds pins where none existed; polymorphic clauses were rejected outright before), residual verification is an empirical before/after acceptance run over the green multi-sig corpus. Matrix additions: declaration-reject + echo-mismatch rows (diagnostic names the new form); stale-cache wholesale refusal e2e; diagnostic mode-uniformity REPL/`--run`/`--link` on every new rejection; constrained-cell row; `.meta.json` byte-identity; return-type dispatch end-to-end ×3 modes ×{annotation, argument-context}. Unwind traceability: §5.1.2/§7.x `[Tested …]` bands re-pointed, never dropped; re-verify `tests/fixtures/preludes` clean of `(X a)` at b1. The ~7 green e2e were the authority that made the wrong form look right — the matrix is the structural cure.

## Skill plans (Phase 3)

{Pending — expected invocations: /design (typecheck), /design (backend, narrow — leg (c)), /qa, /arch (Phase 2 + any interface touch). No /spec invocation expected — semantics settled S111.}

**/qa mandate (user, S112 Phase-1 approval): beyond the test plan (unwind list + rejection matrix), /qa MUST assess (1) the QUALITY RISK of this wave — the §5.1.2 inference change and the trait-form gate re-land both touch high-blast-radius typecheck seams, and the S111 finding says the suite is structurally blind to some defect classes — and (2) the TEST-COVERAGE IMPACT of the unwind: retiring rejection assets must not orphan spec rows (§5.1.2/§7.2/§7.3.5 traceability re-pointed, not dropped), and the fixture migration must not silently narrow what the ~24 unit fixtures exercised.**

## Waves (Phase 4)

Ordering constraints honored: (1) source-editing agents STRICTLY SERIAL (worktree isolation broken — S81); (2) Stage-1 QA-first precedes implementation; (3) b0→b1→b2 staging per /arch (no intermediate red gate); (4) leg (a) lands before b2 so the 20→21 bump covers it regardless of the 0644 ruling ("before-or-with the bump" satisfied either way); (5) leg (c) repro-gated, last.

### W1 — Stage 1: QA-first failing e2e (leg-a families + everything authorable pre-b0)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /testing | tests/ (sprint-wide) | Author failing e2e per `tests/plan/s112-0628-ic-wave.md`: MS family (rp4 accepting, poly+concrete, constrained cell CP, MS-6 definition-site ambiguity, §3.11 negatives), UW conversions incl. UW-7 (0432 Face-B trio), oracle fences MS-1b/CP-1b, AG-1 stale-cache. §3.5/matrix/RT rows deferred to their gated waves per plan. | **done** (2026-07-18: 20 intended REDs, 0 regressions; suite 4702/4670/32/1; live carry baseline = 12 not 8; 5 plan under-specs flagged → /qa reconciliation batched with Phase-5 coverage pass — MS-2/UW-5/UW-6/UW-9/UW-12 already-green must-holds; MS-7/UW-10 re-grounded to disposition-equality) |

### W2 — Leg (a): §5.1.2 back-flow + constrained cell (typecheck)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /dev | cranelisp-typecheck | `monomorphisation.md` §11: delete `ClauseIndependence` + param-subtraction, one post-drain scan, mangle post-drain; constrained-cell rework (`collect_single_sig_defns` filter, per-clause one-variant `ConstrainedFn`, dispatch routing); **`CACHE_SCHEMA_VERSION` 20→21 in THIS change-set** (0644 ruling: leg (a) merges first ⇒ carries the bump; W5 does NOT re-bump); MS-8 message re-grounding + `program/tests.rs:27` pin update same change-set; MS-6 definition-site check (in-sprint, not gate); unit tests at the inference seam (METHOD §2.2). W1 leg-a rows flip green. **GATED on FIXME 0645** (§11.4 no-bump paragraph amendment by /design). | **done** (2026-07-18: 19/20 leg-a REDs GREEN, 0 regressions; suite 4704/4691/13/1 — 12 attributed carries + MS-8 which stays RED as a /testing fixture defect, fixture is admissible-poly under settled §5.1.2. Bump 20→21 landed; no public-API change; 6 unit tests at inference seam, typecheck lib 721/721. Two flagged deviations for /review+/design: (i) `collect_single_sig_defns` is_multi_sig filter LEFT IN — cell is drain-driven, literal removal risks variant-0-only body scan; (ii) NEW `is_self_call` tagging — self-call unify vs external monomorphise; §11.3's concreteness-only split was insufficient, monomorphic-recursion distinction is load-bearing → §11.3 amendment owed. Uncommitted, awaiting review.) |
| /review | cranelisp-typecheck | Change-set review vs §11 design + the wrong-accept-inversion risk (twin-assertion discipline honored; preserved-facet column). Adjudicate the two /dev deviations (filter left in; is_self_call addition). | **done — BLOCK** (2026-07-18. B1 Blocker: self-call `SigDispatch` derived mid-drain → ≥2-hop delegation chain (`f3`) leaks dangling `$Var` dispatch to codegen, violates §11.3(B)'s own MUST; lives exactly in the un-honored u3 unit deferral (I3). I1: self-recursive genuinely-poly clause wrong-rejects w/ internal-name leak (post-drain re-entry pushes unresolved pending entry). I2: `search_cf2_neg` fixture uses the 0432 `sum-to` shape leg (a) now compiles — unwind-checklist miss, + timing race (explains 13-vs-14 RED discrepancy). M1 pre-drain §5.1.1 timing undocumented; M2 overload gate shadowing-blind (+ pre-existing single-sig shadowing HANG found); M3 double-mangled template instance names. Deviations: (i) ACCEPTED w/ §11.4 amendment (`Defn::body()` single-variant assert verified — literal removal would panic); (ii) SOUND, §11.3 amendment owed. MS-8 fixture-defect classification CONFIRMED. No wrong-accept inversion; cache-trust/AG-1/determinism verified; ONE `mangle_sig`. Recurring-class note → /arch: B1+I1 = "resolution recorded against a name a later pass invalidates" (P22/P24 published-pointer shape), escalate on 3rd instance. Probes in scratchpad/probe/.) |

### W2.1 — Remediation (BLOCK disposition: fix-in-wave; strictly serial)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /design | cranelisp-typecheck | §11.3 amendment (two-pass drain, self-call tag semantics + its 3 caveats, post-drain SigDispatch derivation as the B1 fix shape) + §11.4 step-3 rewrite (filter retained, drain-driven cell) + M1 pre-drain §5.1.1 timing recorded (flag /spec ONLY if judged normative) + M3 mangled-name-grammar note. Design docs only. | **done** (2026-07-18: §11.3.1 two-pass drain + tag caveats; §11.3.2 B1 fix = DEFERRAL — pass 1 records NO SigDispatch, ONE post-drain `mangle_sig` in `finalize_multi_sig_variant_types` feeds all 6 carriers (Concrete key, OverloadVariant, resolved_overloads, multi_sig_mangled_names, method_resolutions.resolved_calls, record_dispatch_target) — order-independence by construction (P24), rejected carrier-repair option as the P22/P24 patched-not-eliminated shape; §11.3.3 MS-6 landed + **M1 FLAGGED NORMATIVE, not ruled** — probe `t` finalises disjoint `[String]`/`[Int]` via INTERNAL pin, pre-drain check wrong-rejects a spec-well-formed program; §11.4 step 3 rewritten (filter retained, drain-driven); §11.5 M3 wart + 0519 pointer. No spec contradiction.) |
| /testing | tests/ | Failing e2e: B1 (`f3` 3-clause delegation chain, cross-mode), I1 (recursive-poly-clause twin divergence vs standalone `g1`). Fixture repairs: I2 (`search_cf2_neg` → genuinely-unindexable fixture, no timing race), MS-8 re-point (concrete-sig internally-unpinned clause). | **done** (2026-07-18: T1 `f3_delegation_chain_backflow_accepted_and_runs` RED + standalone-twin GREEN fence; T2 `recursive_poly_clause_accepted_matches_standalone_twin` RED + `g1` in-test fence; T3 both search_cf2 tests GREEN race-free — unresolved-symbol fixture makes the asserted-absent write never occur, + `good.meta.json` settle anchor; the racy search test WAS the 13-vs-14 source AND one of the counted "carries"; T4 MS-8 flipped GREEN on `qq` concrete-sig fixture. Suite 4707/4694/13/1 = 11 durable carries + T1 + T2, zero unattributed. Carry baseline now 11, not 12.) |
| /dev | cranelisp-typecheck | B1 fix (post-drain self-call dispatch derivation or Phase-A carrier re-point) + I3 u3 unit pin SAME change-set; I1 fix (or attributed carry w/ failing-not-ignored repro + rationale if it balloons). B1/I1 e2e + MS-8 flip green. | **done** (2026-07-18: B1 = §11.3.2 DEFERRAL as pinned — pass 1 records NO SigDispatch, defers (span, base, variant-idx); Phase A drives carriers 5–6 from the LITERAL same `concrete_mangled` binding as the entry key (flagged strengthening: variant-index payload, tighter single-sourcing than the doc's params-vector phrasing). I1 = FIXED, design candidate 1 — `mono_recheck_self` context set only at the pass-2 template-monomorphise site; inner self-call with args ≡ instance params unifies inline to the instance mangle; PLUS load-bearing extra: callee base Var node retyped to the instance's concrete Fn (else `from_expr` NotConcrete::Var hard-error) — flagged for /review adjudication as outside strict B1 scope but within §11.3.1(b) authorization. I3 pin + I1 seam unit test landed same change-set. Both e2e GREEN all modes; MS-2 two-instantiation cell verified green. Suite 4709/4698/11/1 — exactly the 11 durable carries, zero unattributed. Typecheck lib 723/723, zero warnings, no public-API drift. Uncommitted.) |
| /review | cranelisp-typecheck | Re-review (same reviewer context): B1/I1/I3 closure verified; commit-safety verdict. | **done — APPROVE-WITH-FINDINGS, committed `b5fa3f14`** (2026-07-18: B1 six-carrier single-sourcing verified at register.rs:295–363 — order-independence by construction; variant-index payload ACCEPTED as strengthening (index cannot drift from finalised subst, params-vector could); I3 u3 pin verified genuine (fails on B1 revert); I1 mechanism SOUND — nesting-safe, no cross-base misclassification, MS-2 intact, callee-Var retype semantically exact + correctly recheck-scoped; design amendment for the full I1 mechanism OWED. Residuals, neither blocking: **R1 (Important)** cross-arity sibling self-call from genuinely-poly template clause still wrong-rejects w/ internal-name leak (probe `g2`; also rejected pre-leg-(a) — no regression); **R2** pre-existing carrier-loss — multi-sig-base call inside ANY monomorphised body loses resolved_target (fails identically on standalone twin — NOT leg-(a)); N1 minor `unwrap_or(0)` → loud invariant. Suite 4709/4698/11/1 confirmed independently; anchor probes re-verified. Recurring-class watch now has content: B1+I1+R1 = "resolution recorded against state a later pass owns".) |

### W3 — b0: additive form support (types + frontend)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /arch | cranelisp-types | Land pinned `TraitImpl.head_con_var` diff + `public-api.txt` regen (may already be landed with the 0644 change-set — verify). **PLUS recurring-class ruling**: B1/I1/R1 = 3 instances of "resolution recorded against state a later pass owns" — meets the escalate-on-3rd threshold; assess whether a principle/design rule is warranted. | **done** (2026-07-18: verified NOT previously landed (0644 was rustdoc-only); field at ast.rs:~670 `#[serde(default)]`, serde-default ≡ bare-head fresh-parse so schema-bump-exempt; 28 mechanical `None` sites (1 production `parse_impl` — b0 /dev replaces; 27 typecheck fixtures); public-api.txt +1 line; interfaces.md §TraitImpl reconciled (was showing pre-S69 shape). Suite 4709/4698/11/1 byte-identical baseline, zero warnings. RULING: PRINCIPLE-SHAPED → **DRAFT Principle 26 "Record from settled state"** authored (`design/arch/principles/26-…md` + index + 4 command imports) — temporal-window intent, DEFER/derive-on-demand admissible, record-then-repair inadmissible, acid test = P24 applied to time; R1 fix MUST take settled-state shape; /review REJECT shapes named; carrier→pass→window sweep = post-ratification audit. **USER RATIFIES (or declines) AT PHASE-7 CLOSE — gate item.** Uncommitted, rides b0 commit.) |
| /dev | cranelisp-frontend | `parse_impl` both slot-1 shapes per `design/frontend/trait-impl-head-parse.md`; shared head-shape helper with `build_trait_head` (P7); malformed-slot-1 diagnostic table; lowercase con_var enforcement row (/qa ruling — rides b0, ONE seam); parse-shape unit matrix. Additive-green: old corpus byte-identical. | **done** (2026-07-18: shared `parse_trait_head_shape` helper — ONE grammar for deftrait+impl, name policy caller-side, lowercase enforcement at the one seam covering both forms; `head_con_var` recorded verbatim. 7 unit tests incl. grammar-parity pin + table-driven six-row malformed matrix. Suite 4716/4705/11/1 — +7 all green, failure set byte-identical, zero flips. Frontend lib 358/358, zero warnings, no frontend public-API drift. Two flagged deviations: (i) design-sanctioned neutral "trait head" wording; (ii) design-doc gap — §4 row `((Functor f))` is structurally len-1, not 2-element-non-symbol-head; /dev reconciled to table INTENT (head-symbol check before arity dispatch), one-line doc note owed → /design(frontend). Serializer round-trip = b2 e2e per design §6/§7 (nothing frontend-side at b0). Uncommitted, rides b0 review.) |
| /review | cranelisp-frontend | b0 review: additive-green verified, one-classifier line held (no kind logic parser-side). | **done — APPROVE-WITH-FINDINGS** (2026-07-18: additive-green independently verified 4716/4705/11/1 byte-identical carries, zero flips; corpus-clean greps confirm no parenthesized impl head / uppercase con_var anywhere; honesty note — lowercase rider is a deliberate sanctioned acceptance narrowing of `(deftrait (Functor F) …)`, corpus-clean. Serde-exemption VERIFIED stronger than claimed (ast::TraitImpl not persisted at all; ModuleEntry::TraitImpl separate). One-classifier + P7 held (old build_trait_head logic deleted not mirrored; parity pin real). Both deviations ACCEPTED. Findings: **F1 Important** → /dev(frontend): malformed-matrix test asserts substrings only, design §7 requires span-points-at-head assertion; F2 Minor → /design(frontend): doc still says lowercase NOT enforced at b0 + the len-1 note; F3 Minor → /qa matrix: slash-bearing con_var `(Functor prim/x)` passes the after-slash case check, spec says bare lowercase identifier; F4 Minor → /dev(frontend): stale CLAUDE.md RED-guard entry (impl_qualified pair now green); F5 Minor → /qa: parity pin exposes qualified-deftrait-head parse acceptance as a conscious-cell candidate. P26 DRAFT sanity passes. Safe to commit.) |

### W4 — b1: conventional-half corpus migration (green→green on OLD compiler)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /testing | tests/ | Migrate the conventional-half e2e + `tests/fixtures/preludes` re-verify clean of `(X a)`; TB/§3.5 rows now authorable; per-fixture old→new mapping table in change-set. **PLUS R1 failing e2e** (cross-arity sibling self-call from genuinely-poly template clause — probe `g2` + standalone-twin fence; joins as attributed carry, /sprint disposition) **and R2 repro** (multi-sig-base call inside monomorphised body loses resolved_target — pre-existing, fails on standalone twin too; pin as failing-not-ignored w/ `// defect:` pending /qa attribution). | **done** (2026-07-18: 8 sites migrated GREEN→GREEN, mapping table in report + inline `// b1-migration (S112):` comments; ZERO premise-voids (all assertion subjects head-form-independent); HKT-half correctly untouched (swaps b2); preludes re-verified clean (AG-6). TB §3.5 six-row parse-neg + §7.2 uppercase-both-forms authored GREEN. R1 `cross_arity_sibling_self_call…` RED w/ twin fence. R2 in NEW `tests/multi_sig_base_mono_carrier_loss.rs` RED + single-sig GREEN control; REDUCTION SHARPENED: called clause must be CONCRETE (poly clause masks behind ambiguity gate); likely producer-side (typecheck never writes carrier in mono harvest). Suite 4721/4708/13/1 = 11 carries + R1 + R2, zero unattributed. Flags → /qa batch: R2 REPL-vs---run divergence in which gate fires first (cross-mode uniformity cell?); `carrier-loss` needs `// defect:` vocabulary ruling; repl_persist.rs stale "RED on HEAD (0538)" framing.) |
| /repl | repl/ | Migrate `repl/spec.md:432` + `demos/05-traits.demo:31` off never-applied `(X a)` → bare-head+`self`. `runs/` historical, untouched. | **done** (2026-07-18: both A3 sites + sweep-found `demos/archive/ring2a.demo:40` (replay-guard, would go red at b2); genuine HKT heads (agent-fluency.md, ring4i.demo) correctly untouched; prelude.cl already bare. Migrated Sizeable portion PTY-verified green. FLAG → /qa batch: pre-existing NON-DETERMINISTIC 05-traits Zeroable failure under PTY replay (fails identically on HEAD pre-edit; no minimal repro; inconsistent between runs — suspected PTY playback artifact, needs /testing non-PTY repro BEFORE attribution). No broken traceability pointers.) |

### W5 — b2: the atomic kind-model change-set (typecheck + corpus swap)

> Sequencing adjustment (2026-07-18, /sprint): the HKT e2e swap moves from /dev to a dedicated /testing row (tests/ ownership); serial order /dev(tc) → /testing → /examples → /dev(int) → /review, tree red-in-places between agents but committed only when coherent (no committed red ≙ no intermediate red gate).

| Skill | Crate | Task | Status |
|---|---|---|---|
| /dev | cranelisp-typecheck | Atomic: declaration-time never-applied reject + `register_trait_decl` guard fix (HKT → `register_hkt_trait`) + Case-3 seam (shape + SPELLING echo vs `type_params[0]`) + §7.3.5 kind-matching + ~24 unit-fixture migration (constraint trio REQUIRES `SelfType`; old-model positives → declaration-reject negatives, never delete). NOT tests/ e2e (moved to /testing row below). **NO re-bump — 20→21 landed with leg (a) (`b5fa3f14`).** Rider: N1 minor — `register.rs:827` `unwrap_or(0)` → loud invariant (`expect`/`debug_assert`). | **done** (2026-07-18: declaration reject w/ §7.2.1 diagnostic naming con_var + self-form fix; kind derived ONCE at decl registration — all 3 former usage-scans collapsed (registry guard, impl_check 39–92 deleted, is_hkt reads type_params); Case-3 seam = shape + spelling echo (names both spellings) + slot-2 per-kind incl. effective-target rewrite to Named(Constructor); 24 fixtures converted (trio → SelfType; zero deletions; no old positive asserted the malformed shape as valid, so reject-negatives are new units); 11 new unit tests at every seam; N1 done. Typecheck lib 734/734, zero warnings, no API drift. Full suite: 13 carries + 5 EXPECTED (4 awaiting-/testing-swap incl. 2 message-drift, 1 awaiting-/examples) + 1 flaky `agent_flag_errors_on_non_agent_build` (env race, passes isolated, not in stashed baseline → /qa batch). ZERO unexpected. int-display pair unexpectedly GREEN (reconciliation may be no-op — verify at that step). DEVIATION flagged: slot-2 pairing-HEAD (`Functor` in `(Functor Option)`) not independently validated vs resolved trait — design's exact 3-rejection list followed, spec §7.3.5 "bad pairing" not operationalized → /review adjudicates. Uncommitted.) |
| /testing | tests/ | HKT e2e swap to echo-the-head form (b2 half of the corpus); 0628 rejection matrix (con_var {applied, bare-return, bare-arg} × target {primitive, wrong-arity ADT, well-kinded ADT} per §7.3.5) + RT rows; check-gate-leak guard flips. | **done** (2026-07-18: 4 swaps GREEN zero premise-voids (arity-neg's `prelude-scope-miss` defect found FIXED, re-grounded past-tense); 13 matrix tests GREEN incl. check-gate-leak flip (×3 modes, asserts reject AND leak-absence), full-0628-repro row (`:a 7` never appears), echo-spelling-names-both, 2 mode-uniformity rows; RT-1/RT-2 GREEN. Suite 4747/4733/14/1 = 13 carries + awaiting-/examples, zero unattributed. FLAGS: (1) applied-type-in-pairing is PARSE-layer reject — §7.3.5 kind-mismatch wording unit-reachable only (reachability fact, TX-6-style split documented); (2) impls NOT source-regenerated to user.cl (conventional AND HKT, pre-existing, cache-backed persist is the real carrier) → /qa may want an impl-source-regen row, owner int/save.rs; (3) int-display drift CONFIRMED CONCRETE: `impl user/Functor for user/Functor` (pairing head rendered instead of resolved impl_type) → the int reconciliation step is REAL, not no-op.) |
| /examples | examples/ | Migrate `26-functor.cl:47` + `30-parallel-map-reduce.cl:123` to echo-the-head (same wave as b2 — examples gate must not go red). | **done** (2026-07-18: both A3 sites migrated, sweep found nothing else (only genuine `(Functor f)` HKT heads; all other traits bare-head kind-*); runs verified exit 91/56 per documented expectations; gate test PASS; teaching prose added in 26-functor (echo-form explanation, file's voice), 30 defers to 26.) |
| /dev | src/ (int) | `format_trait_display`/`impls_for_type_in_view` reconciliation to the settled kind model (the two `repl_introspection` e2e). | **done** (2026-07-18: root cause = `src/eval.rs` definition-echo re-derived the impl label from SURFACE syntax (`target.head_ref()` = pairing head) — a P26-shaped miss; fix = `impl_echo_type_name` mirrors the Case-3 effective-target extraction (`head_con_var.is_some() ∧ Applied` → `args[0]`); `impls_for_type_in_view`/`format_trait_display` verified CORRECT (registry keyed off the rewritten target — confusion never reached it). 3 unit tests at the display seam. Live-verified `impl user/Functor for user/Option` + `; impl: Option`. Suite 4750/4737/13/1 = EXACTLY the 13 attributed carries, zero unattributed. COVERAGE GAP reported: HKT echo-form registration echo + `; impl:` section unguarded at e2e (the conventional-only e2e is why the defect stayed green) → /testing micro-row dispatched in-wave.) |
| /review | cranelisp-typecheck + src/ | b2 review vs hkt.md §5.1/§5.4 + duplication lens (one classifier, one seam). | **done — BLOCK** (2026-07-18. **B1 Blocker: pairing-head WRONG-ACCEPT confirmed live** — `(impl (Functor f) (NotFunctor Option) …)` (nonexistent OR different real trait) silently accepts + dispatches; `impl_check.rs:98` binds `Applied(_pairing_head, …)` and discards; spec §7.3.5 Case-3 "bad pairing" MUST + §7.3 grammar `hkt_target = '(' trait_name con_target ')'`; ATTRIBUTION: design fidelity gap (hkt.md §5.4 lists 3 rejections, spec names 4; /dev followed + flagged). **I1: over-applied conventional target `(impl Disp (Option Int Int))` wrong-accepts** (pre-existing acceptance, seam now exists, matrix row missing). **I2: src/syntax/cheatsheet.txt:293 primer teaches the now-rejected form** (+ half-stale kind-inference line; sibling `repl/demos/agent-fluency.md:64`). M1 one-sided mirror comment (impl_check side lacks reciprocal); M2 Case-1 diagnostic suggests the exact rejected form for multi-param ctors; M3 stale `test_build_impl_hkt` framing (b0, opportunistic). ALL ELSE CLEAN: P24 collapse genuine (grep-verified), twin discipline held, fixture migration lost nothing, P26 sweep clean in code (I2 is the doc sibling), mode-uniform, suite exactly the 13 carries. Fix-in-wave per W2 precedent; commit blocked until B1+I2 land + re-verify.) |

### W5.1 — Remediation (BLOCK disposition: fix-in-wave; strictly serial)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /design | cranelisp-typecheck | hkt.md §5.4 Case-2 amendment: 4th rejection (pairing head must name the slot-1-resolved trait) + diagnostic wording + the qualified-spelling sub-question (`(impl (fmt/Functor f) (Functor Option))` — check spec §7.3/§8.5 first; if spec settles, design it; if genuinely open, FLAG to user, do not rule). Also M2 wording fix (arity-aware fix suggestion). | **done** (2026-07-18: Case-2 → FOUR rejections — pairing head resolved as `trait_name` and compared by FQ-equality vs slot-1's resolved trait, validated FIRST; diagnostic names written vs expected pairing. Case-1 → TWO rejections — under-applied `>` generalises to `!=` (I1), poly-applied positive explicitly preserved. M2 template arity-aware. Qualified-spelling: SPEC-SETTLED → RESOLVED identity (3 citations: same `trait_name` nonterminal both positions; "resolve by name" Case-3 step 1; §8.5.1 — "verbatim" grounds in the con_var BINDER, not the trait reference). ONE divergent cell flagged → user: qualified/bare SAME-trait pairing head; not a wave blocker (different/nonexistent heads reject under both readings); /testing leaves that cell unpinned pending ruling. Matrix note: axes `conv-over-applied` + `pairing-head-mismatch` added.) |
| /testing | tests/ | Failing e2e: B1 pairing-head rows (nonexistent head; different-real-trait head; matching positive twin) + I1 over-applied-conventional row (+ poly-applied positive twin). Matrix axis addition per /qa plan conventions. | **done** (2026-07-18: 3 intended REDs — `hkt_impl_pairing_head_nonexistent…neg` (cross-mode ×3), `hkt_impl_pairing_head_different_real_trait…neg` (+ not-registered introspection assert), `conventional_impl_over_applied…neg`; exactly-arity fence GREEN; UNPINNED cell commented at matrix site. Suite 4755/4739/16/1 = 13 carries + 3 intended. **SURPRISE FINDING → /qa batch**: `(impl Disp (Option a))` poly-applied target is a PRE-EXISTING WRONG-REJECT (`unknown type a` before the arity gate; also the canonical §7.3.3 `(Option :Display a)` form) — spec §7.3.5:352 `✓` row has NO [Tested] annotation, admissible-untested-broken; deliberately NOT pinned in W5.1 to avoid mis-attributed unfixable RED; needs /qa row + attribution, then its own pinned repro this sprint.) |
| /dev | cranelisp-typecheck | B1 fix (head comparison + located diagnostic at the Case-3 seam) + I1 fix (over-applied gate, care for poly-applied positive) + M1 reciprocal mirror comment + M2 template fix; unit twins {matching ✓, other-trait ✗, nonexistent ✗, over-applied ✗}. | **done** (2026-07-18: pairing head resolved via same scope_resolve path, FQ-equality vs HOISTED slot-1 `fq_trait_name` (old :238 minting site deleted — ONE site, P24), validated first; `>`→`!=` split into two distinct diagnostics; M2 `arity_var_suggestion` template; M1 reciprocal comment. 3 REDs→GREEN incl. cross-mode, fence green. Suite 4761/4748/13/1 = exactly the carries. Typecheck lib 740/740. 7 unit twins. FLAG for /review: pre-existing slot-1 dual-resolve (resolve_trait_decl + resolve_trait, different data needs) left uncollapsed — scope discipline.) |
| /dev | src/ | I2: cheatsheet.txt HKT FORM block → echo form + fix the kind-inference line to declared-head+applied-usage. | **done** (2026-07-18: hkt FORM/EXAMPLE/NOT/SPEC + impl-topic + traits-topic all re-grounded (traits-topic had a THIRD stale site: bare parenthesized-head deftrait made never-applied look valid); every taught form probe-verified compiling, every NOT form probe-verified rejecting, on the b2 binary; sweep found no other stale sites; cheatsheet e2e assert only markers — no breakage; lib 664/664.) |
| /repl | repl/ | I2 sibling: agent-fluency.md:64 stale impl line (transcript re-ground). | **done** (2026-07-18: FORM line re-grounded byte-identical to live cheatsheet (PTY-captured, ANSI-stripped compare) + NOT line added demonstrating the old form's rejection; sweep found no other staled content; "byte-identical to /syntax hkt" claim true again.) |
| /review | (same reviewer) | Re-verdict: B1/I1/I2 closure + commit safety. | **done — APPROVE** (2026-07-18: B1 probes re-run — both reject, located, both-pairings-named, FIRST-ordered at the seam, ONE fq mint grep-confirmed; I1 probe rejects + exactly-arity accepts in same session (`!=` didn't over-reach); poly-applied deferral sound + recorded 3 places; I2 primer/transcript verified teaching only accepted forms; dual-resolve ACCEPTABLE (same walk, immutable state, cannot diverge) → R-2 opportunistic collapse suggestion; unpinned cell grep-verified unpinned. Suite 4761/4748/13/1 byte-identical carries. NEW R-1 (minor, batch w/ unpinned-cell ruling): pairing-head mint resolves BARE name only — `module` qualifier not fed in; nonexistent-module corner caught upstream; when user rules the qualified cell, /dev must honor the written qualifier. Commit-safe.) |

### W6 — Leg (c): return-type-dispatch repro → attributed fix

| Skill | Crate | Task | Status |
|---|---|---|---|
| /testing | tests/ | Pinned CROSS-MODE repro (REPL/`--run`/`--link` × annotation/argument-context) of the 0628 leak claim — the contradiction (FIXME "Not this" shows it working) resolves by evidence. | **done** (2026-07-18: **NO CELL FIRES** — full matrix green end-to-end (annotation + argument context × 3 modes × fresh/cached × Int/Float impls; unresolved bare call = clean §3.11, no leak); the S111 leak claim does NOT reproduce on HEAD; the already-green `nullary_return_poly…` test's stale "RED today" framing was the giveaway. 4 GREEN coverage tests landed (AG-4 lattice). CONSOLIDATED PINS also landed: TB-24 poly-applied RED (both forms; control accepts); shadowing hang RED (8s bounded timeout) + GREEN value twin + multi-sig-gate sibling RED; RT-4 ×2 RED (impl silently dropped from regenerated user.cl — data-loss face confirmed). Zeroable: 25/25 non-PTY byte-identical green → attribution = PTY replay harness (/repl), green guard landed. 0615 lane isolation implemented + FIXME deleted (isolation-by-construction; full dual-build acceptance not executed — budget). Maintenance: R2 owner line, 2 framing strips. Suite 4772/4754/18/1 = 13 prior carries + 5 new intended, zero unattributed.) |
| /dev or /design | per attribution | If producer-side (typecheck `resolved_targets` for return-type-dispatch sites, §11.6): /dev(typecheck) inside schema-21 window. If backend-side: /design(backend) narrow then /dev(backend). If no repro fires: (c) collapses to cross-mode e2e coverage (already in RT rows). | **n/a — no-repro branch taken** (no fix dispatch; per /qa ruling 1, R2 therefore CARRIES to S113 with /dev(typecheck) attribution + P26 fix shape recorded) |
| /review | per attribution | Change-set review. | **n/a** (condition — a fix change-set — never materialized; W6 delivered tests/plan/harness artifacts only, no compiler source touched) |

**Wave-gate note:** FIXMEs 0642/0628 remain `status: open` targeting /design as WAVE TRACKERS — their /design portions are actioned (Phase 3); the open status records the /testing+/dev remainder. Gate satisfied by this rationale; they retire at leg-(a) landing (0642) and leg-(c)+matrix landing (0628). 0644 RESOLVED+deleted (2026-07-18): /qa position ADOPTED — whichever of {leg-(a), b2} merges first carries the ONE 20→21 bump; `TraitImpl.head_con_var` b0 timing confirmed; ConstrainedFn+ParametricFn rustdoc landed (uncommitted, rides the sprint change-sets). **NEW: 0645 (target /design, typecheck) GATES W2** — §11.4 no-bump paragraph reversed, one-paragraph amendment before /dev implements. /arch also recommends AG-1 gains the leg-(a) stale-cache cell (cached `$Var`-Concrete module refused wholesale).

## Dispatch log

| Wave | Agent | Surface | Model | Effort | Non-default reason |
|---|---|---|---|---|---|
| P2 | /arch | design/arch + scope review | shim default | default | — |
| P3 | /design | cranelisp-typecheck | shim default | default | — |
| P3 | /design | cranelisp-frontend | shim default | default | — |
| P3 | /design | cranelisp-typecheck (echo-spelling amendment) | shim default | default | — |
| P3 | /qa | sprint-wide (plan + user-mandated risk/coverage assessment) | shim default | default | — |
| P3 | /arch | FIXME 0644 ruling + ConstrainedFn rustdoc + exit gate | shim default | default | — |
| W1 | /testing | tests/ (Stage-1 QA-first, leg-a + UW + fences + AG) | shim default | default | — |
| W2-gate | /design | cranelisp-typecheck (0645 §11.4 amendment) | shim default | default | — |
| W2 | /dev | cranelisp-typecheck (leg a + constrained cell + bump) | shim default | default | — |
| W2 | /review | cranelisp-typecheck (leg-a change-set) | shim default | default | — |
| W2.1 | /design | cranelisp-typecheck (§11.3/§11.4 amendments + M1/M3) | shim default | default | — |
| W2.1 | /testing | tests/ (B1/I1 failing e2e + I2/MS-8 fixture repairs) | shim default | default | — |
| W2.1 | /dev | cranelisp-typecheck (B1 fix + I3 u3 pin + I1) | shim default | default | — |
| W2.1 | /review | cranelisp-typecheck (re-review, same reviewer context) | shim default | default | — |
| W2.1 | /design | cranelisp-typecheck (§11.3.4 I1 as-built + R1 limit; 0642 retirement) | shim default | default | — |
| W3 | /arch | cranelisp-types (head_con_var diff + recurring-class ruling) | shim default | default | — |
| W3 | /dev | cranelisp-frontend (b0 parse_impl both shapes) | shim default | default | — |
| W3 | /review | cranelisp-frontend (b0 change-set) | shim default | default | — |
| W3.1 | /design | cranelisp-frontend (F2 + len-1 doc notes) | shim default | default | — |
| W3.1 | /dev | cranelisp-frontend (F1 span assertions + F4 stale guard entry) | shim default | default | — |
| W4 | /testing | tests/ (b1 conventional-half migration + R1/R2 repros) | shim default | default | — |
| W4 | /repl | repl/ (spec.md:432 + demos/05-traits.demo:31 migration) | shim default | default | — |
| W5 | /dev | cranelisp-typecheck (b2 kind model + N1) | shim default | default | — |
| W5 | /testing | tests/ (HKT e2e swap + 0628 matrix + RT rows) | shim default | default | — |
| W5 | /examples | examples/ (26-functor + 30-parallel-map-reduce migration) | shim default | default | — |
| W5 | /dev | src/ (int format_trait_display reconciliation) | shim default | default | — |
| W5 | /testing | tests/ (HKT introspection e2e row — display-surface guard) | shim default | default | — |
| W5 | /review | cranelisp-typecheck + src/ + tests/ (whole b2 change-set) | shim default | default | — |
| W5.1 | /design | cranelisp-typecheck (§5.4 4th-rejection amendment) | shim default | default | — |
| W5.1 | /testing | tests/ (B1/I1 failing rows) | shim default | default | — |
| W5.1 | /dev | cranelisp-typecheck (B1+I1 fixes + M1/M2) | shim default | default | — |
| W5.1 | /dev | src/ (I2 cheatsheet primer) | shim default | default | — |
| W5.1 | /repl | repl/ (agent-fluency.md stale impl line) | shim default | default | — |
| W5.1 | /review | b2 re-review (same reviewer context) | shim default | default | — |
| P5 | /qa | sprint-wide batch (attribution + coverage reconciliation) | shim default | default | — |
| W6 | /testing | tests/ (leg-c repro + consolidated pins + 0615) | shim default | default | — |
| P5-rulings | /spec | spec/ (M1 + TB-25 + TB-27/defn scribe) | shim default | default | — |
| P5-rulings | /arch | design/arch (P26 ratification) | shim default | default | — |
| P5-rulings | /design | cranelisp-typecheck (M1/TB-25 flags → ruled) | shim default | default | — |
| P5-rulings | /dev | cranelisp-typecheck (R-1 qualifier resolution) | shim default | default | — |
| P5-rulings | /testing | tests/ (TB-25 rows + defn qualified-head probe) | shim default | default | — |
| P6a | /repl | repl/ (assessment + 6b plan) | shim default | default | — |
| P6a | /examples | examples/ (assessment + 6b plan) | shim default | default | — |
| P6a | /stdlib | stdlib/ (assessment + 6b plan) | shim default | default | — |
| P6a | /docs | user/ (assessment + 6b plan) | shim default | default | — |
| P6a | /port | exemplar/ (assessment + 6b plan) | shim default | default | — |
| P6b | /testing | tests/ (D1/D2/D3 + R1-variant repro batch) | shim default | default | — |
| P6b | /repl | repl/ (6b: demo + harness fix + ring4i) | shim default | default | — |
| P6b | /examples | examples/ (6b: 36 back-flow rework) | shim default | default | — |
| P6b | /stdlib | stdlib/ (6b: Default self-test + naming note) | shim default | default | — |
| P6b | /port | exemplar/ (6b: Display Cell affordance + doc tidy) | shim default | default | — |

## Notes

- 2026-07-18 Phase 1: FIXME 0621 verified landed (checker.rs:1472 `storage_fq()`; `callees_records_renamed_import_by_storage_key` pin) and reaped by /sprint (target skill).
- 2026-07-18 Phase 1: user scope selection — 0628/I-C wave ONLY; memory-safety track deferred to S113 (count 1); frontend audit declined again.
- 2026-07-18 Phase 5: W1 DONE (see wave table). 0645 amended+deleted (untracked file, plain rm). W2 gate CLEAR; /dev(typecheck) dispatched — leg (a) + constrained cell + 20→21 bump.
- 2026-07-18 Phase 5: W2 /dev DONE (see wave table). QUEUED post-review: (1) /testing re-points MS-8 fixture (admissible-poly under settled rule — needs concrete-sig internally-unpinned clause); (2) /design(typecheck) §11.3 amendment recording the self-call-unify vs external-monomorphise distinction. /review(typecheck) dispatched on the uncommitted change-set.
- 2026-07-19 Phase 6b /stdlib DONE: `default/test.cl` shipped GREEN (4/4, trait+method import = D2 green fence, harness-free, `--no-cache` clean-CWD verified); S87 deferral retired at source; promotion DEFERRED to S113 (concurs — bare `default` is a heavy §8.6.4 glob reservation + D2 interaction risk), recorded in plan-stdlib.md; §1.5 naming-reservation table added (14-row globbed BOUND set, conflict-not-shadow rule, 0646 cited, decay-flagged). /port 6b dispatched (last).
- 2026-07-19 Phase 6b /examples DONE: 36 rework landed — `between` 2-arg clause un-annotated (live back-flow demo), section-3 teaching beat expanded (equivalence + measure dispatch contrast), exit 8 verified fresh-cache (`--no-cache`) and cached. Zero churn. /stdlib 6b dispatched (Default self-test + naming-reservation note; promotion guided to DEFER pending the D2 user ruling).
- 2026-07-19 Phase 6b /repl DONE: harness fixed — TWO root causes (bare `"> "` substring matched echoed input; + continuation-prompt blindness found during verification, 08-sudoku 120s-timeout → 1s green); 05-traits byte-stable ×12 w/ S112 beats (back-flow `grow` re-verified fresh-dir; D3 caution CONFIRMED live — call-site-literal variant reproduces; avoided); ring4i migrated + de-rotted GREEN (also surfaced: trait-method impl + imported-fn `/source` unavailable — REPL gap noted); run-dir uniquified; 38-demo sweep ALL rc=0 — **latent archive de-leak rot (25 archive demos, ring0=18 worst) = /repl S113 candidate**; `; impl:` spec pin correctly HELD pending 0647; observation: 6a §4.1.1 prose writes `:core.num/Num` vs live bare `:Num` — FQ-consistency check for a later pass. /examples 6b dispatched.
- 2026-07-18 Phase 6b /testing batch DONE: 4 pins RED + 5 fences GREEN; suite 4790/4765/25/1 (21 prior + 4 intended), zero unattributed. Surprises: D3 fires PRIMITIVE-free-standing (prelude NOT load-bearing; axes = poly callee × cross-arity-self-call reach); D3 value 6 (brief slip); **Pin-4 trait-`+` standalone twin ITSELF hits carrier-loss (`user/user/fb$Int+Int` doubled prefix — R2 family evidence); prelude-`+` R1 variant likely needs R1 AND carrier-loss to flip** — documented in-pin, /qa row candidate. Class mappings: D1 → `display-envelope-mirror`, D3 → `carrier-loss`. OWED /qa S113 Phase-1: PLAN rows ×4, precise attributions, D2 normative question, Pin-4 entanglement row. /repl 6b dispatched.
- 2026-07-18 Phase 6a /port DONE (6a COMPLETE — all 5 proxies): exemplar GREEN (user.cl exit 0, tests.cl 40/40, NO_LENIENT parity holds), conformance CLEAN (zero traits/multi-sig in exemplar). **NEW DEFECT D3**: multi-sig cross-arity self-call + POLY CALLEE in the sibling clause → codegen `undefined function` (idpoly/conj repros, stdlib-free twin passes; typecheck passes; concrete clause — contradicts "concrete clauses safe"; distinct from R1 wrong-reject + R2 single-sig-twin-also-fails; the callee-mono sibling of the family) — blocks the exemplar's Vec-helper multi-sig showcase → collapse DEFERRED. 6b: optional `Display Cell` debug affordance (approved as scoped — not the hot format path). 6a exit gate MET: 5 plans, FIXMEs 0646–0649 filed. Phase 6 execution: /testing batch (D1+D2+D3+R1-prelude-variant) dispatched; then 6b /repl, /examples, /stdlib, /port; commit; Phase 7.
- 2026-07-18 Phase 6a /docs DONE: ONE stale surface — `user/guide/functions.md` taught independence model AS FACT w/ inverse worked example — corrected in-phase (back-flow + M1 written-signatures sections, `rp4` verified exit 7; + CLAUDE.md/getting-started back-refs + 0631 context re-ground). Traits/errors teaching = PRE-EXISTING gaps → FIXMEs 0648 (traits guide missing) + 0649 (errors catalogue, S112 diagnostics) target /docs, future sprint. 6b = nothing further required. Zero defects. /port 6a dispatched (last).
- 2026-07-18 Phase 6a /stdlib DONE: conformance CLEAN (no old forms, no qualified binders, no HKT — all conventional; derive.cl generates correct forms; 38-module tree loads). S87 `Default` deferral STALE — return-type dispatch works trait-in-scope → 6b ships `default/test.cl` + weighs prelude promotion + prelude naming-reservation doc (0646-class prevention). **NEW DEFECT D2 (check-gate-leak class, NEW cell)**: nullary return-type-dispatch method imported WITHOUT its trait → typecheck ACCEPTS, codegen `undefined function` leak; unary analogue caught cleanly at typecheck ("no impl"); free-standing repro + fences provided → /testing batch (pin ruling-agnostic: assert no-leak, valid under either ruling). **NEW OPEN NORMATIVE QUESTION (user): does importing a trait METHOD without its TRAIT suffice for dispatch (impl coherence global), or is trait-in-scope required?** Either ruling fixes the leak; pin doesn't block on it. /docs 6a dispatched.
- 2026-07-18 Phase 6a /examples DONE: healthy; ONE accuracy defect — 36-multi-arity taught the pre-S112 independence model AS FACT (+ plan-examples.md mirror) — corrected in-phase, verified exit 8; 3 of 4 new capabilities already taught (self-form 17/15/29, echo-head 26/30, return-type dispatch 29); GAP = §5.1.2 back-flow itself → 6b: rework 36 (drop `between` 2-arg annotations = live back-flow demo, zero exit churn; keep `measure` annotated as the honest dispatch contrast); optional poly+concrete beat SKIPPED (exit churn not worth it). Zero FIXMEs, zero compiler defects; R1 shape avoided. /stdlib 6a dispatched.
- 2026-07-18 Phase 6a /repl DONE: assessment strong (multi-sig + kind model + diagnostics all deliver; primers accurate; display fix verified live). **NEW DEFECT D1** (display, §1.4/§4.1.1 MUST): multi-sig variant display DROPS inferred trait constraints (`/sig h` shows `(Fn [a a] a)` not `(Fn [:Num a :Num a] a)`; bound enforced — display-only) → /testing repro batch. **R1 EVIDENCE STRENGTHENED**: fires on idiomatic prelude `+` (trait-method path), not just the `add-i64` fixture — /qa note: R1 repro needs a prelude-`+` variant → same batch. /repl's own 6b: sprint demo folded into 05-traits (avoiding the R1 shape), PTY harness fix (root-caused: prompt-detection race drops leading chars + run-dir second-resolution collision), ring4i.demo archive guard RED (W4 conflated deftrait head w/ impl form) — migrate. FIXMEs 0646 (primer `Show` collides w/ prelude glob, masks failure) + 0647 (empty `; impl:` drawer) → /dev, S113 scope input. spec.md §4.1.1 constraint clarification landed in-phase. /examples 6a dispatched.
- 2026-07-18 Rulings rider COMPLETE: P26 ratified (banner+index, sweep task parked for S113 Phase-1); /design flags → ruled; /dev R-1 landed — qualifier flows through the ONE resolution path via TypeRef Display render; scenario (c) different-module same-named trait was a LIVE WRONG-ACCEPT under the wrong trait pre-fix (3 units); /testing: TB-25 3 e2e rows GREEN (nosuchmod reachability split documented — module resolver fires first at e2e, FQ-compare wording unit-reachable, TX-6-style), + binder probes: **defn qualified head SILENTLY ACCEPTS (binds `user/fmt/foo`!), deftrait both shapes silent-bind or incidental unlocated error** → 3 RED binder pins `class=silent-accept owner=/dev` (frontend head-parse seam), [S113]. RED baseline = 21 (18 + 3 binder pins), zero unattributed. Suite 4781/4760/21/1. Committing rider; Phase 6a next.
- 2026-07-18 Rulings rider: /spec SCRIBED all three (M1 carve-out w/ probe example + remedy; TB-25 reference-identity w/ user's words + binder/reference contrast; TB-27+defn binder principle at §5 intro — deftype/defmacro/const/def extension marked VETO-VISIBLE; mod already had §5.8; impl correctly excluded). New coverage cell flagged: defn qualified-head needs its own /qa row. No contradictions. /arch(P26) next.
- 2026-07-18 PHASE 5 CLOSED (user) + FIVE RULINGS (user, verbatim-adjacent): (1) close accepted, RT-4 stays a carry; (2) R1 CARRY to S113; (3) **M1 = WRITTEN signatures** — "the signatures are ambiguous so let's put the carve-out in the spec": §5.1.1 "can unify" judged on written (pre-inference) clause signatures; the internal-pin program (`t` probe) stays rejected BY DESIGN; as-landed pre-drain check is correct; /spec scribes the carve-out; (4) **TB-25 = RESOLVED identity** — "valid references to the same thing are the same thing, even if they use a different reference syntax. this could be import, fq, or fq through two different import paths" — pairing head is a reference; any spelling resolving to slot-1's trait accepts; R-1 seam fix (honor written qualifier) now in-sprint; (5) **TB-27 = REJECT + "this should also be true for defn"** — declaration heads are BINDERS, bare symbols only; qualified deftrait AND defn heads reject; /spec scribes (def-form family generalization framed veto-visibly). (6) **P26 RATIFIED** (user agrees) — /arch flips DRAFT→ratified now, not at close. Rider chain: /spec → /arch(P26) → /design(tc micro) → /dev(tc R-1) → /testing(TB-25 rows) → commit → Phase 6a fan-out.
- 2026-07-18 Phase 5: W6 DONE — leg (c) NO-REPRO (matrix fully green; S111 leak claim falsified on HEAD; 0628's "Not this" vindicated); Zeroable = PTY-harness artifact (25/25 non-PTY green) → /repl Phase-6 item; 5 new attributed pins (TB-24, shadowing ×2, RT-4 ×2); 0615 RESOLVED+deleted; RED baseline at Phase-5 end = 18, all attributed. FIXME 0628 retirement condition: leg (c) closed as no-defect + matrix landed → /design may retire at Phase-6/7 (target skill action). Phase-5 conclusion → user.
- 2026-07-18 Phase 5: /qa BATCH DONE (rulings 1–7 + rows 8–12 + reconciliation 13–15 in `tests/plan/s112-0628-ic-wave.md` §11/§12 + bands updated, verifiers green 705/0). Key rulings: R2 = PRODUCER-side /dev(typecheck), P26-constrained fix shape, rides leg-(c) window if that lands producer-side; `carrier-loss` admitted to vocabulary; poly-applied wrong-reject = /dev(typecheck) impl-target resolution seam, pin THIS SPRINT (TB-24); shadowing hang = provisional /dev(typecheck) `wrong-scope-lookup`, repro-gated; agent-flag test = NOT flaky — the 0615 provenance race, deterministic, recommend pull-forward; PTY Zeroable = non-PTY repro FIRST, leg-(c)-adjacent; TB-27 qualified-deftrait-head = spec-derived REJECT (veto-visible at close, adjacent to TB-25); RT-4 impl-source-regen = DEFECT row (spec §15.4 lists impl as persisted; schema-bump+restore = silent data loss), owner /dev(src). Standing-category synthesis: ALL S112 blockers sat in RELATIONAL cells (topology/slot/scope/carrier×context), form-variant axes caught nothing late; the standalone-twin discipline was the instrument that worked → twin row per axis; "axes are relations, not just forms" joins the checklist. USER-ARBITRATION LIST NOW: M1, TB-25 (pairing-head qualification), TB-27 (veto-visible). W6 GATE: 0615 (target /testing, open) PULLED INTO W6 (fresh trigger: the race fired in-suite; /qa recommends); 0605 stays blanket-deferred (no new evidence). /testing W6 dispatched.
- 2026-07-18 Phase 5: W5+W5.1 CLOSED — re-review APPROVE, b2 COMMITTED `40f19693`. New residuals routed: R-1 (pairing-head mint ignores module qualifier — rides the user's unpinned-cell ruling), R-2 (dual-resolve collapse, opportunistic), M3 (stale b0 test framing, cleanup note). /qa batch dispatched (15 items). W6 (leg c) after.
- 2026-07-18 Phase 5: W5.1 /design DONE. **SECOND OPEN NORMATIVE CELL (minor) → user**: pairing-head qualification — under the spec-settled resolved-identity reading, `(impl (fmt/Functor f) (Functor Option) …)` (qualified slot-1, bare pairing head, SAME resolved trait) ACCEPTS; under a verbatim-spelling reading it would reject. Design + /dev implement resolved-identity core now (both readings agree on different/nonexistent heads); the qualified/bare-same-trait positive twin stays UNPINNED until user rules. /testing dispatched.
- 2026-07-18 Phase 5: W5 /review BLOCK — B1 pairing-head wrong-accept (spec-MUST; design listed 3 of the spec's 4 rejections) + I1 over-applied-conventional wrong-accept + I2 stale primer. /sprint disposition: FIX-IN-WAVE (W5.1), b2 stays uncommitted. Serial: /design §5.4 amendment (qualified-spelling sub-q: spec-check first, flag user if open) → /testing failing rows → /dev(tc) → /dev(src) → /repl → re-review → commit. M3 → Phase-5 cleanup notes. NOTE the S112 pattern: BOTH big waves' Blockers were wrong-accepts/carrier-losses at cells the matrix/design didn't enumerate — /qa's "coverage by definition variants" standing category has fresh content (pairing-head axis, over-applied axis).
- 2026-07-18 Phase 5: b1 COMMITTED `191124a3` (W4 both halves + W3.1 riders). W5 /dev(typecheck) DONE — b2 kind model landed, zero unexpected reds (see wave table; pairing-head deviation → /review; flaky agent-flag test → /qa batch). /testing dispatched (HKT swap + matrix).
- 2026-07-18 Phase 5: W4 /testing DONE (see wave table; RED baseline now 13 = 11 carries + R1 + R2). /qa batch grew: R2 attribution (+sharpened reduction: concrete called clause required; REPL/--run gate-order divergence noted), `carrier-loss` vocabulary, repl_persist stale framing. /repl dispatched (b1 corpus half #2).
- 2026-07-18 Phase 5: W3.1 DONE — /design(frontend) doc reconciliation (F2 + len-1 + F3 known-open sentence) and /dev(frontend) F1 span assertions (all PASS — no location defect; `assert_err_span_at` helper, whole-form-span regression now fails) + F4 stale guard entry removed. Frontend lib 358/358. W3 CLOSED; edits ride the W4 commit. /testing dispatched (W4/b1).
- 2026-07-18 Phase 5: W3 /review APPROVE-WITH-FINDINGS → b0 COMMITTED `c08e68fc` (types carrier + frontend + DRAFT P26 + §11.3.4 + 0642 retirement). Finding routing: F1 (Important, span assertions) + F4 (stale CLAUDE.md guard entry) → /dev(frontend) micro-rider NOW (Phase-5 exit gate requires Important resolved); F2 + len-1 note → /design(frontend) micro NOW; F3 (slash-bearing con_var) + F5 (qualified-deftrait-head cell) → /qa batch. Then W4.
- 2026-07-18 Phase 5: W3 /dev(frontend) DONE (see wave table). /review(frontend) dispatched — b0 gate: additive-green verified, one-classifier line held. Post-review routing: §4 len-1 doc note → /design(frontend) micro-fix (batch with any review findings).
- 2026-07-18 Phase 5: W3 /arch DONE (see wave table). **PHASE-7 GATE ITEM ADDED: user ratifies/declines DRAFT Principle 26 "Record from settled state" at close** (P21/P23/P24/P25 precedent). /dev(frontend) dispatched — b0 parse_impl.
- 2026-07-18 Phase 5: W2 CLOSED. /design(typecheck) recorded §11.3.4 (I1 as-built mechanism + R1 known-limit incl. fix direction: widen inline match to the base's post-drain-settled overload clauses) and RETIRED FIXME 0642 (3/3 actions verified, `git rm` staged). W3 GATE CLEAR: no open FIXME targets an in-wave skill on in-scope work — 0613 (/dev, quasiquote) is pre-S112 and outside the user-approved Phase-1 scope (blanket deferral rationale; same for 0605/0615 → /testing at W4). /arch dispatched (W3: types carrier + the 3rd-instance recurring-class ruling).
- 2026-07-18 Phase 5: W2.1 /review APPROVE-WITH-FINDINGS → leg (a) COMMITTED `b5fa3f14` (W1+W2+W2.1 + Phase-2/3 artifacts + the uncommitted S111-close sprint-110 archive fix; IDE caches excluded). Follow-up routing (/sprint): R1 → failing e2e rides W4 /testing (attributed carry; fix if capacity after W6, else carries w/ attribution); R2 → repro rides W4 /testing + /qa attribution row (pre-existing, NOT leg-(a)); N1 → rider on W5 /dev(typecheck); I1-mechanism design amendment + 0642 retirement → /design dispatched (serial, before W3). /qa BATCH (updated list): W1 5 under-specs; delegation-chain + recursive-poly + R1 coverage rows; I2 unwind-completeness note; M2 shadowing-blind gate; pre-existing single-sig shadowing HANG; R2 attribution; M1 pending user ruling. /arch recurring-class watch: 3 instances now (B1/I1/R1) of "resolution recorded against state a later pass owns" — meets the escalate-on-3rd threshold; fold into /qa batch or next /arch touch.
- 2026-07-18 Phase 5: W2.1 /dev DONE — B1 + I1 both FIXED (see wave table), suite at exactly the 11 durable carries. Re-review dispatched to the SAME reviewer (context intact) for B1/I1/I3 closure + adjudication of the two flagged /dev items (variant-index payload; I1 mechanism scope) + commit-safety verdict.
- 2026-07-18 Phase 5: W2.1 /testing DONE (see wave table). Live RED baseline corrected again: 11 durable carries (the retired "12th" was the racy search test T3 fixed). /dev(typecheck) dispatched — B1 deferral fix per §11.3.2 + I3 u3 unit pin same change-set + I1 (fix preferred; attributed carry w/ rationale if it balloons).
- 2026-07-18 Phase 5: W2.1 /design DONE (see wave table). **OPEN NORMATIVE QUESTION (M1) → user via /sprint report**: is §5.1.1 "can unify" judged on settled (post-inference) or written (pre-inference) clause signatures when the pinning site is INTERNAL to the defn? Pre-drain check as-landed wrong-rejects the review's probe `t`, which finalises disjoint under §5.1.2. Orthogonal to W2.1 — leg (a) ships the check pre-drain; placement not treated as settled until user rules (/spec scribes after). /testing dispatched next (serial).
- 2026-07-18 Phase 5: W2 /review BLOCK (B1 + I1/I2/I3 + M1–M3; see wave table). /sprint disposition: FIX-IN-WAVE (W2.1) — B1 is bounded, the change-set stays uncommitted through remediation. Serial chain: /design amendments → /testing failing e2e + fixture repairs → /dev fix (B1+I3 same change-set; I1 fix-or-attributed-carry) → /review re-verdict. FOR /qa BATCH (with the W1 5-item reconciliation): coverage rows for delegation-chain + recursive-poly cells; I2 unwind-completeness note (S112-7 class); M2 shadowing-blind gate + the pre-existing single-sig shadowing HANG (`(defn s1 [x] (let [s1 (fn [y] y)] (s1 x)))` runtime loop — separate defect, needs attribution row). FOR /arch (watch, no action yet): B1+I1 recurring-class candidate "resolution recorded against a name a later pass invalidates" — escalate on 3rd instance per standing rule.
- 2026-07-18 Phase 3: /qa COMPLETE — plan `tests/plan/s112-0628-ic-wave.md` (families MS/UW/TB/AG/CP/RT, unit-deferrals u1–u9) + risks.md S112-1..7 + PLAN.md index + `[S112]` bands on §5.1.2/§7.3.5. QUALITY-RISK verdict: wrong-accept inversion is the dominant hazard (twin-assertion cure; 2 cheap `[oracle]` fences MS-1b/CP-1b close the highest-signal gap w/o pulling S113 forward; no new ownership mechanism but widened reachable set — residual rides to S113). COVERAGE verdict: unwind narrows nothing IF preserved-facet column honored; **0642 checklist INCOMPLETE — 0432 Face-B trio (`sum-to`) caught + rowed (UW-7)**. Rulings: MS-6 §5.1.1 definition-site check owed (in-sprint, not gate); lowercase con_var row rides b0; tests.rs:27 same change-set; 0644 no-bump rationale FALSIFIED (B-2 wrong-accepts persisted bogus schema-20 entries) → recommend leg (a) rides 20→21 window. /arch dispatched to rule 0644 before wave freeze.
- 2026-07-18 Phase 3: echo-spelling amendment LANDED (hkt.md §5.4 step 3 = shape + spelling bits at the one seam; /qa matrix axis widened to 3-valued echo). /qa dispatched (last Phase-3 invocation).
- 2026-07-18 Phase 3: /design(frontend) COMPLETE — b0 designed (`design/frontend/trait-impl-head-parse.md`): both slot-1 shapes, shared head-shape helper with `build_trait_head` (P7), NO kind classification parser-side, slot-2/`build_impl_target` unchanged, serializers form-agnostic by construction (round-trip e2e owed at b1/b2). Flags: (i) echo-SPELLING validation missing from hkt.md §5.4 Case-3 → amendment dispatched (spec §7.3 settles it, no user question); (ii) con_var lowercase enforcement = pre-existing shared gap → /qa matrix candidate, out of b0; (iii) int `format_trait_display` output drift under b2 kind model → int-surface reconciliation joins b2 (two e2e already in §5.4 migration list).
- 2026-07-18 Phase 3: /design(typecheck) COMPLETE — leg (a) = finalize-pipeline reordering (`monomorphisation.md` §11: delete `ClauseIndependence` scan, mangle post-drain); constrained cell rides standalone `ConstrainedFn` path, NO types reshape; mangle already deterministic (`Var` constant spelling); hkt.md §5.1/§5.4 rewritten to declaration-time reject. FIXME 0639 actioned+deleted; 0642/0628 stay open as wave trackers; **FIXME 0644 filed → /arch** (ConstrainedFn rustdoc + no-bump-for-leg-(a) confirmation; if /arch disagrees, leg (a) folds into the b2 20→21 window). /qa flags: §5.1.1 unifiable-overlap definition-site check owed?; `program/tests.rs:27` message-text assertion churn.

## Outcome (Phase 7)

**Status: authored 2026-07-19; awaiting user close approval.** Nine commits: `b5fa3f14` (leg a), `c08e68fc` (b0), `191124a3` (b1), `40f19693` (b2), `710bc454` (W6+leg-c), `b191e162` (rulings rider), `384d824b` (Phase 6) + the two mid-sprint SPRINT.md rides. Suite 4702→4790 tests; REDs 32 (start, incl. 20 intended)→25, every one attributed.

### Delivered

- **Leg (a) — §5.1.2 multi-sig back-flow, complete.** Clause inference ≡ separate mutually-recursive functions; `ClauseIndependence` scan deleted; two-pass drain (self-call unify / external monomorphise) with post-drain single-sourced dispatch across all six carriers (the B1 deferral fix); constrained-poly×multi-sig cell (user-directed in-sprint); MS-6 definition-site check; diagnostics re-grounded to §3.11. `rp4` anchor holds. `CACHE_SCHEMA_VERSION` 20→21 (0644: leg (a) carried the one bump).
- **Leg (b) — settled trait kind model, complete.** Kind derived ONCE at declaration (3 usage-scans collapsed — P24); declaration-time never-applied reject with fix-naming diagnostic; echo-the-head impl form with shape + spelling + pairing-head-resolved-identity + slot-2 kind/arity validation at the one Case-3 seam; b0→b1→b2 staging held (no committed red); whole corpus migrated (tests, examples, repl, primers, fixtures — zero premise loss); `:a 7` display defect + 0628 codegen leak dead; `TraitImpl.head_con_var` carrier (additive, schema-exempt).
- **Leg (c) — closed by evidence: NO defect.** Return-type dispatch green in every cell (2 contexts × 3 modes × fresh/cached × 2 impls); the S111 leak claim does not reproduce; landed as green cross-mode coverage. The 0628 "Not this" observation vindicated.
- **Five user rulings scribed + implemented** (M1 written-signatures carve-out; TB-25 resolved-identity incl. the R-1 qualifier fix — which killed a live wrong-accept-under-wrong-trait; TB-27+defn binder principle; R1 carry; Phase-5 close). **Principle 26 "Record from settled state" authored and RATIFIED** (user, 2026-07-18); classification sweep parked as S113 Phase-1 candidate.
- **Phase 6**: all five proxies assessed + executed. PTY harness fixed (2 root causes; 08-sudoku 120s→1s; 38-demo sweep all complete); S112 demo beats live; example 36 now teaches back-flow (was teaching the dead model as fact — likewise `user/guide/functions.md`, corrected); stdlib `Default` self-test shipped (stale S87 deferral retired); exemplar `Display Cell` affordance; prelude naming-reservation table.
- **FIXMEs resolved+deleted this sprint**: 0621, 0639, 0642, 0644, 0645, 0615 — six retired (0628 remains open as the wave tracker; see Deferred).

### Deferred / carried (all attributed, each with a failing-not-ignored pin where it's a defect)

- **25 suite REDs**: 11 pre-sprint durable carries (memory-safety family ×8+1, ownership_reuse, deftype silent-accept) + 14 S112-pinned: R1 ×2 variants (cross-arity poly-clause self-call — primitive + prelude-`+`; fix direction §11.3.4, P26-shaped), R2 (carrier loss for multi-sig dispatch in mono bodies — /qa-ruled producer-side, P26 fix shape), TB-24 (poly-applied impl targets wrong-reject — spec-admissible, never tested), shadowing ×2 (hang + overload-gate), RT-4 ×2 (impls lost from regenerated user.cl — data-loss class), binder ×3 (qualified defn/deftrait heads silently accept), D1 (multi-sig display drops constraints), D2 (nullary method-only-import codegen leak), D3 (poly callee in cross-arity-reached clause never monomorphised).
- **Open FIXMEs**: 0628 (retires when /design judges the matrix+leg-c record sufficient — recommend S113 Phase 1), 0646–0649 (Phase-6 gap captures), plus the pre-S112 out-of-scope set.
- **Memory-safety-soundness track** → S113 (deferral count 1, user-approved). **Frontend audit rotation**: twice-declined; escalate presentation at S113 Phase 1. **Default prelude promotion** → S113 (awaits D2 ruling). **Archive demo de-leak rot** → /repl S113 candidate.
- **OPEN USER QUESTION (D2)**: does importing a trait METHOD without its TRAIT suffice for dispatch (impl coherence is global), or is trait-in-scope required? Pin is ruling-agnostic; Default prelude promotion and the D2 fix both wait on it.
- **VETO-VISIBLE (stand unless user objects)**: binder-principle generalization to `def`/`def-`/`deftype`/`deftype-`/`defmacro`/`defmacro-`/`const`/`const-` (scribed from the stated principle); TB-27 qualified-deftrait reject (/qa spec-derived ruling).

### Findings

- **The sprint's defect signature was RELATIONAL cells** (call topology, pairing slot, scope shadowing, carrier×context): both wave Blockers (W2 delegation-chain SigDispatch, W5 pairing-head) plus I1/R1/R2/D3 sat in cells no form-variant matrix enumerated. The instrument that caught them was the **standalone-twin/equivalence discipline** and adversarial review probing, not the matrices. /qa's standing category gains "axes are relations, not just forms" + twin-row-per-axis; this is the S112 calibration datum for the S108 coverage-matrix directive.
- **P26 earned ratification empirically**: five instances in one sprint (B1, I1, R1, R2, eval.rs display), two fixed by the deferral mechanism it names, before it was even ratified.
- **Review discipline > model escalation** (S110 finding re-confirmed): zero tier escalations; both BLOCK verdicts + every residual came from default-tier /review with probes. Frontmatter-vs-§II.3 audit: 28/28 CLEAN. Dispatch-by-shim held throughout (zero fallbacks).
- **QA-first held its bar**: W1's 20 intended REDs flipped exactly as designed; wrong-accept inversion (the dominant modeled hazard) never materialized — the twin discipline was the cure that held.
- **User-proxy phase earned its keep**: 3 of the sprint's 4 genuinely new defects (D1/D2/D3 — the 4th being the binder silent-accepts) were found by outside-in assessment AFTER the compiler waves closed green, and two teaching surfaces were still teaching the dead independence model as fact.
- **Process wrinkle (recorded)**: the leg-(a) design under-specified the self-call/external split (§11.3 concreteness-only) and the §5.4 rejection list (3 of the spec's 4) — both caught downstream at cost. The pattern "design enumerates fewer cases than the spec names" is a /design checklist candidate: diff the design's case list against the spec's before Phase-3 exit.

### Audit line

Declined by user at Phase-1 scope selection (recorded); `cranelisp-frontend` rotation debt now longest-standing — re-present at S113 Phase 1 with the twice-declined escalation.
