;; num.cl — Numeric trait group
;;
;; Submodules:
;;   num.num   — Num trait + impls
;;   num.int   — Int operations
;;   num.float — Float operations

(import [prelude []])

(mod num)
(mod int)
(mod float)
