## Issues:

- startup time
- cache write time (concurrent with next prompt)
- testing (assert)
- logging - display function calls params and returns by module
- macro syntax
- circular imports (eg. super)
- .a file in lib cache
- dict
- cow vec (log n)


## Features:

- (mod foo (defn x [] 3))
- platform friction
- dead code
