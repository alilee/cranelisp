# design/arch/

Architecture deliverables for the Cranelisp reimplementation. Owned by `/arch`.

Per `sprints/METHOD.md` §1.4 (three-way content split), this `CLAUDE.md` carries domain-local content only — local conventions, the in-progress Decisions-drain backlog, and pointers to canonical documents. Methodology rules live in `sprints/METHOD.md`; architectural principles live in `principles.md`; skill workflows live in `.claude/commands/arch.md`. **Architectural commitments manifest at their natural home in the permanent set** (facades / BC / principles / sequences); no separate Decision log is authored — see `.claude/commands/arch.md` §"The manifestation-site question".

## Canonical documents (the target documentation set)

| File | Purpose |
|---|---|
| `overview.md` | Bridge document — the newcomer entry point. How the language is realized through the surfaces, tested by `/qa`'s integration suite, embodied in the crates. |
| `principles.md` | Architectural principles — index. Each Principle is one file at `principles/NN-{slug}.md`. Auto-imported by `.claude/commands/arch.md`. |
| `principles/` | Architectural Principles register; one file per Principle; index in `principles.md`. |
| `bounded-contexts.md` | Per-surface bounded-context full statements (Frontend, Typecheck, Backend, Runtime, Platform, Binary/int, plus types crate). |
| `facades/{crate}.md` | Per-surface facade specs — as-designed public surface. One file per surface. **ALL NINE RETIRED — no live facade-spec documents remain (int retired S81 W-Retire, FIXME 0298, completing the arc):** `facades/types.md` (S69 Sub 42); `facades/frontend.md` (S70 Phase B group B3-C); `facades/platform.md` (S71 Wave 4); `facades/typecheck.md` (S72 Wave 5); `facades/intrinsics.md` (S74 Wave 3 → BC §4b + source rustdoc); `facades/primitives.md` (S74 Wave 3 → BC §4a + source rustdoc); `facades/backend.md` (S75 Wave 5b → BC §3 + source rustdoc); `facades/backend-cache.md` (S75 Wave 5b → **cache submodule source rustdoc only**, an implementation detail of backend — NOT promoted to a BC entry; no §3a); `facades/int.md` (S81 W-Retire → BC §6 + `design/int/` design docs + `src/` source rustdoc + `src/main.rs` `//!` CLI narrative; a binary has no `public-api.txt` boundary — its conformance gate is the e2e suite). For the crate-shaped library retirees, source rustdoc (crate-root `//!` + per-item `///`) is the canonical surface and cross-surface narrative lives in `bounded-contexts.md` (§7 for types, §1 for frontend, §5 for platform, §2 for typecheck, §4b for intrinsics, §4a for primitives, §3 for backend, §6 for int); for `backend-cache` — backend's persistence half — the canonical home is the cache submodule rustdoc (`crates/cranelisp-backend/src/cache/mod.rs` `//!` + per-submodule `//!` + per-item `///`), with **no** bounded-contexts entry, because the cache is an implementation detail of the backend bounded context, not a context of its own. For `int` — a binary, not a library — the cross-surface narrative + the cadence/handoff/window model live in BC §6, the interior contracts live in `design/int/` + `src/` source rustdoc, and the outside-in CLI contract is the `src/main.rs` `//!` narrative (REPL → `repl/spec.md`; language → `spec/`). |
| `interfaces.md` | Narrative companion to `crates/cranelisp-types/`. |
| `tracing.md` | Subsystem design for the `(trace ...)` execution-trace feature — **TARGET STATE (user-decided 2026-06-04)**: tracing encapsulated in the trace keyword-node + `cranelisp-intrinsics` (the 12 bodies + table + nested-trace runtime guard) + backend (codegen + display-descriptor baking + discovery-in-codegen, swap-all-symbol-tables), with **no int runtime involvement**. `trace_format` is a pure intrinsic over codegen-baked self-contained `DisplayDescriptor`s (survives `.o` caching). Works in ALL modes incl. `--link`. D40's trace-half (REPL/`--run`-only + relocate-to-int) RETRACTED; io_trace half stands. §7 is now a compact History appendix (the proposal was enacted). Cited by BC §3 (backend emits/bakes/discovers) + §4b invariant 12 (intrinsics hosts) + §6 (int deletes). |
| `display-protocol.md` | Subsystem design for type-directed value render — the List/Seq surface-form pretty-printer (FIXME 0050). **DESIGN (S106 Phase 3), mechanism-only, pre-implementation; both §10 forks settled by the user 2026-07-10.** Extends the landed `DisplayDescriptor` ABI (`trace_format.rs`; `tracing.md` §3.4) with ONE `DescriptorKind::Collection` + a declarative render marker on the type def (single-sourced on `TypeDefInfo`, consumed by both the REPL-echo formatter `src/display.rs` and the trace descriptor baker). **Both forks settled 2026-07-10: no language surface — the marker is compiler-internal (a bootstrap seed recognizes the built-in stdlib `List`/`Seq` and stamps the marker; general user custom-render routes to a future Display-style trait, not a structural annotation — a user-ratified narrowing of Principle 19 for the built-ins only); non-forcing Seq render (`+more`, byte-identical to trace).** §1.5 promotion is the follow-on sprint (exit gate §9) — now an ordinary implementation sprint, no user gate. No `cranelisp-types` edit in S106. |
| `repl-styling-seam.md` | Subsystem design for the ONE token-styling formatter + ONE styling spec across all token-styled REPL output (S108 Inc3 **E4**; subsumes FIXME 0561's mechanism half + the two-highlighter duplication). **DESIGN (S108 Phase 2), pre-implementation; the E4 north star is user-ratified 2026-07-12; `/repl` scribes the normative role spec from its §3 and the USER reviews it before Wave-D implementation (hard gate).** Actor model (§2): nine producers (values, introspection, `/doc`, code printer, verbatim-source styler, search rows, errors/warnings, REPL frame, agent gutter) → one missing named function — "here is a line as (role, text) spans; render it". Mechanism (§4): int-side role-span carrier (`Role`/`StyledDoc`) + ONE `render` (the only `styled()` call site) + `role_style` as the single code manifestation of the spec; producers emit roles at construction (never reparse their own output); code roles assigned by ONE walk over the `Sexp` tree with two emitters (computed layout for `pp`; spans-over-original-bytes for verbatim source — deletes `style_tokens`; as landed the `pretty_print_str` round-trip is RETAINED as the verbatim-vs-relayout SELECTOR over source, per the amended §5 row — the eliminated anti-pattern is re-parsing formatter OUTPUT, not re-laying-out source); shared `:Type <subject> ; metadata` envelope constructor closes the Inc1-D2-class value-vs-introspection drift. NO `cranelisp-types`/public-API/cache change in the base design; ONE precisely-scoped contingency if the user ratifies dimming the `module/` prefix inside `:module/Type` — an additive `render_type_spans` variant in `cranelisp-types` (`render_type` reimplemented as its concatenation, Principle 7), with the explicit anti-goal that no int-side lexical pass over rendered type text is ever the mechanism. Colour-off output stays byte-identical (goldens + agent `strip_ansi` membrane). `/list`/`/imports`/`/exports` name bodies (the layout concern) explicitly OUT of scope. |
| `resolve-home-enumeration.md` | The class ruling for the recurring **`enumeration-miss` / wrong-scope-lookup** family (S108 Inc3 **E3 + FIXME 0558**; register: Inc1 D1 type-display, Inc2 seeded-index, E3 loaded-module index, 0558 trait display). **DESIGN (S108 Phase 2), pre-implementation.** Rule (§3), binding on these paths: (1) resolve the home ONCE at the canonical gate (`lookup_with_prelude_fallback` → `resolve_entry_for_display`); formatters take `(entry, home)` and root every section chain-lookup AT the home (depth-0 local — the prelude outer-scope question cannot arise); Decision-45 Pattern-B *view questions* stay scope-rooted by semantics. (2) An enumeration covers ALL sources of its kind through ONE reader per kind — the `/search` importable set is seeded ∪ loaded/registered ∪ file-only; a mounted module's rows come from the live table via `public_entries_from_table`; no source is marked complete without contributing rows. E3 mechanism: branch (a)'s `mark_skipped` is replaced by a live-table feed — arm-time sweep of already-terminal registered modules + a publication-edge hook at the terminal typecheck transition (covers in-flight-at-arm, late `/import`, and watcher-reload refresh with replace-rows semantics; accounting invariants pinned; per FIXME 0562/0563 the in-flight FAILURE exit is covered by the symmetric `on_module_failed` zero-row skip — every enumerated source reaches `indexed` by rows or a legal skip — and the cache-hit terminal-entry edge (`register_module_cached{,_no_object}` bypasses the publication hook) is covered-by-construction under the lib-dirs-immutable-after-arm precondition). 0558 mechanism: `format_trait_display` takes the resolved home from `format_def_entry` (which already holds it), internal scope-rooted re-resolution deleted; trait-side `; impl:` at the home is complete by construction (Decision 0045 writes impls to the trait's module). **E8 extension (S108 Inc3, repro-confirmed):** the Pattern-B prelude-VIEW sibling gap graduated from repro question to fifth register instance — the type-side `; impl:` view's candidate-trait enumeration walks only the asking module's inner table, so prelude-globbed traits never enter the candidate set (§5a); fix = int-side candidate-set UNION (inner-scope run ∪ prelude-hop run of the SAME `get_impls_for_type_chain` reader, I-1 public-head post-filter, ONE session wrapper feeding both `format_type_display` and `format_builtin_type_display`) — "scope-rooted by semantics" governs per-candidate rooting, never the candidate SET (§3 rule 2 applied to view walks). NO `cranelisp-types`/public-API change (E8 included — existing readers, second run + post-filter); NO cache impact (`ImportableIndices` is int-private, unserialized, rebuildable). E9 (the CHECK-path sighting, `impl`-form `unknown trait` on a prelude-globbed trait) reproduced and was fixed in `cranelisp-typecheck` (S108, `lookup_trait_decl_or_prelude`); its variant folds into the Wave-G convergence (`prelude-import-convergence.md` §3.3). Reframed S108 Wave G off "outer scope" onto the fallback-mechanism model (§8.8.1 — no outer scope as a language concept). |
| `prelude-import-convergence.md` | **RULING — LANDED (ruled S108 Wave G; both §7 `/dev` change-sets landed S108 Inc3; `/review` structural grep CLEAR, R1–R8 GREEN)** — prelude ≡ explicit import at the resolution layer. ONE lookup with the prelude fallback **intrinsic** (`cranelisp_types::ResolutionScope` — fallback decided once at scope construction from the Principle-19 role bit; NO public fallback-less entry point; the six `_or_prelude` variants + the two fallback-less lookalikes + three hand-rolled copies collapse per the §3.3 map) and ONE §8.6.4 definition seam (types-owned `reject_def_over_binding(scope, name, span)` — `defn`/`deftype` arms existing; `deftrait` name + method names at the `check_form_register` `TraitDecl` arm; `defmacro` gated in int's `register_macro_in_module`; import-direction predicate widened to `TraitDecl`). Corrects the spec-inverted typecheck-CLAUDE.md "reference hops / binding doesn't" rule (§4.3 — a binding consults the prelude to REJECT, §8.6.4). Acceptance: `tests/plan/PLAN.md` §"Prelude ≡ explicit import" matrix — 8 committed REDs R1–R8 flip; structural criterion = zero `_or_prelude` names, bit consulted only at scope constructors + enumerated non-resolution readers. Blast radius scouted SMALL (stdlib already suppresses the prelude in every definition-bearing module; examples/exemplar/test-corpus clean) — S108-completable, two serial `/dev` change-sets (§7). Approved `cranelisp-types` diff pinned in §6 + `interfaces.md`; NO cache/schema impact. Worked exemplar for FIXME 0564's divergent/entry-point duplication facets. **Post-landing amendments (Inc3 close, §3.5):** the display gate `lookup_with_prelude_fallback{,_opt}` stays hand-rolled — the planned "alignment step" is RETIRED as the settled end-state (a raw-head + resolving-module display operation; re-expression over the scope would break the S20/S21 byte-identity pins — do NOT "complete" it); the display-vs-resolution **I-1 divergence is RULED a defect** (a PRIVATE prelude binding must not classify "in scope" for display/`/search`/harvest — §8.8.1 provides prelude's *public* names only; `/dev` fix named in §3.5.2: the head `is_public()` filter at the repl.rs display gate + the eval.rs bare-symbol hop); resolve's terminal-vs-head filter residual (was FIXME 0567) FIXED S109 Phase 3 — the retry now gates on the prelude HEAD binding's `is_public()`, unit-pinned in `cranelisp-types/src/resolve/tests.rs`; §3.4 writer census corrected (writers = `ensure_prelude_bit` + `install_module_session_env`; `inject_prelude_if_needed` only loads) and its non-resolution readers gain `find_trait_method_decl` + the eval.rs display hop. |
| `dotted-ctor-canonical-keys.md` | **WORKING (S109 Phase 3; REVISED P5 — the W1.1a COORDINATE re-ruling)** — the binding cross-crate contract for the dotted-`Type.Ctor` keying change after the W1.1a revert (73 int-side regressions, classes empirically pinned by `/arch`). User-ruled **(b) COORDINATE**: canonical-real keying stays and becomes UNIFORM across every ctor writer (user `deftype` adt.rs + int `bootstrap.rs::register_synth_adt` seeds incl. `IO.Bind` + typecheck fixture seeds), with every reader canonical-aware: backend `lookup_constructor` collapses onto the ONE `resolve_driven` driver (its one-hop copy caused BOTH the `unknown constructor` prelude cascade AND the silent wrong-value nullary-as-closure "match failed" class), display `ctor_field_types` probes canonical, member-glob installs bare aliases, and the types-owned chain-follow takes same-module `Import` hops through the caller's staging VIEW (fixes the latent same-cluster field-accessor `--run` defect at the true seam). Landing = ONE `/dev` deployment, two commits: reader-widening (behaviour-invariant vs baseline) then writer-flip + `CACHE_SCHEMA_VERSION` 16→17. §7 confirms scrutinee-directed bare-pattern resolution (ruling #2) is TRACTABLE (scrutinee inferred before arms, structural). **§10 (W1 review Blocker, DC-11): the `pattern_ctors` sidecar must REACH codegen** — cure (a′): the sidecar carries the ctor `Def`'s STORAGE key (whichever key `instantiate_ctor`'s probe hit), transported on a new `MonoMatchArm.resolved_ctor: Option<FQSymbol>` populated via a required `MonoExpr::from_expr` `pattern_ctors` parameter (unforgettable, P18); backend `compile_constructor_pattern` does a direct keyed read and HARD-ERRORS on a missing resolution (no context-free re-resolution, no DashMap-order global fallback — the run-to-run wrong-tag nondeterminism class); `CACHE_SCHEMA_VERSION` 17→18. §10.4 folds the I-1 sparkability exclusion drift onto the landed `bare_member_name` (the `member_key` projection inverse); §10.6 depth guard landed. Archive trigger: the W1 coordinate wave lands; contract folds into `type_ctor_names` rustdoc + BC §7 + consumer rustdoc. |
| `backend-keyed-consumer.md` | **LANDED (S110 Phase 5; centrepiece-close verified 2026-07-16 — FIXMEs 0583/0584/0585 closed; delivered W0 `41fab350`+`144828d1` → W1 `86038e27` → W2 `369c226c` → W3 `4c899dd9`+`be06f6cb`, resolver family DELETED −993 LOC; §3 grep gate verified: zero resolver CODE in `crates/cranelisp-backend/src/`, `resolution.rs` keeps only the two naming primitives)** — the binding cross-crate design for the 0583 centrepiece: backend = pure keyed-lookup consumer (Principle 24 "Resolve once"; BC §3 invariant 10 / §2 producer note). One carrier (`MethodResolutions.resolved_targets` span-keyed sidecar + `MonoExpr::{Var,Apply}.resolved_target`, populated via the REQUIRED `from_expr` param — the S109 §10 template; semantics = "whichever storage key HIT"), ONE backend keyed fetch (`entry_at`) + kind arms replacing the ten `resolution.rs` resolvers, per-site inventory S1–S24 (§3), 4-wave plan (W0 producer + `CACHE_SCHEMA_VERSION` 18→19 PINNED for Phase-5 `/dev` → W1 call seam → W2 value seam + 0585 guard → W3 delete `resolve_driven` + grep gate). Rev-2 binding: NO soft fallback ever — keyed-read-else-resolver hybrids are a per-wave `/review` REJECT. W3 residual RULED (§5): typecheck becomes the SOLE mono-view producer (lenient view relocates beside `from_expr`; synthetic bodies' ctor identities populated directly at synthesis — `Span::SYNTHETIC` is structurally outside span-keyed transport); `lenient_mono_from_expr` + the `lib.rs:909` arm delete in W3. **§1.1.1 (W0.1 cross-module ruling, 2026-07-15): trait-leg storage = the impl-WRITER's module (D45 method-co-location clause amended; only the shell lives at the trait's home) — W0.1b pinned diff (`TraitImpl.impl_module` + `ResolvedCall::TraitMethod.impl_module`, same schema-19 window) + two further producer fixes (AutoCurry callee-span transport; fn-value-rewrite sidecar update); completeness sweep table per kind; W1 GATED on W0.1b (landed `144828d1`).** **§1.1.2 (W1.1 ruling, FIXME 0620, 2026-07-16): the alias-class close — for member-canonical-keyed symbols (sum ctors, accessors) AND renamed imports/re-exports the carrier recorded the WRITTEN-spelling alias, because `Resolved.fq` composes the written name and only the walk knows the terminal key (a `ModuleEntry` doesn't carry its own key); uniform fix = `Resolved.storage_key`/`storage_fq()` surfaced BY the walk (types half landed with the ruling; `fq` untouched — display byte-identical) + the `/dev` recorder flip (`record_reference_target` inserts `storage_fq()`); §1.1's carrier value-source rule (walk-resolved / mint-resolved / transport — never a written spelling) is the structural close; recorder-grounded 19-row re-sweep supersedes the §1.1.1 table (its ctor row had mis-attributed the recorder as `instantiate_ctor`); after the recorder flip W1 has NO remaining producer gaps; callees alias residual = FIXME 0621.** **§1.1.3 (W3.1 ruling, FIXME 0622, 2026-07-16): the MAP-PROVENANCE close — the fourth and final producer axis.** A span-keyed sidecar has exactly three axes (key values → 0616; carrier values → §1.1/§1.1.2; map instance → §1.1.3); 0622 was a provenance mismatch: the mono view built with the ENCLOSING run's `pattern_ctors` while the body was annotated by the per-instance recheck — misses cross-module AND cross-check-run (same-module REPL-incremental), which kills the union candidate (the defining run's map no longer exists at mint time). Ruling = the **check-run pairing rule** (a view is built from the SAME `MethodResolutions` instance its body-check populated) + the exhaustive 3-carrier × 9-path matrix, every cell dispositioned; the recheck already re-records every ctor pattern into the per-instance map under the home switch, so the `/dev` fix is read-the-right-map (`finalize_mono_codegen_view` + the sibling `register_test_fn_mono_roots` cell + the `sweep_post_pass_outputs` 3-field hygiene sweep; typecheck-only, no types diff, no schema bump). **Producer ruled COMPLETE across carriers × paths — W3 re-deploys from `stash@{0}` with no further producer prerequisites.** Cross-file span-aliasing recorded as a latent evidence-gated `Some(wrong)` hazard (not a W3 blocker). R-2 ADT-entry builder (`AdtCtorSpec`+`build_adt_entries`) LANDED S110 Phase 3 additively (§6); typecheck caller wired Phase 5 — the `src/bootstrap.rs::register_synth_adt` half remains sprint-tracked (S110 src-chain tail). **Archive trigger MET for the contract (verified 2026-07-16)** — folded into `mono_expr.rs`/`check.rs` rustdoc + BC §2 producer obligation/§3 invariant 10 + `interfaces.md` §"`resolved_targets`" (0618) + Principle 24 (ratification = S110 Phase-7); the physical move to `archive/` is PARKED on the bootstrap R-2 tail so the active sprint's citations stay live — the next `/arch` archive triage executes it. Residuals at their own homes: FIXME 0621 (`callees` alias, S111 schema-bump window); stale present-tense resolver mentions in backend comments (cosmetic `/dev` sweep). |
| `test-discovery.md` | Subsystem design for the test-discovery/error-capture feature (`discover-tests` + `catch-runtime-error` primitives + `/run-tests`/`/run-all-tests` commands) — **SETTLED (user 2026-06-06, fourth convergence); cascaded; implementation tracked by FIXMEs 0269–0273** (cascade landed: `cranelisp-types` `DefKind::PrimitiveExtern` variant + got_slot/serde, BC §3 invariant 8 / §4b invariant 13 / §6 int / §7 types, the `int_intrinsics()` deletion + extern (was `facades/int.md`; that facade retired S81 W-Retire → BC §6 + `design/int/` + source rustdoc), `interfaces.md` DefKind narrative; FIXME 0261 resolved → 0269). The user answered every open question of the third convergence; supersedes the PM names-only §TARGET, now §8d. **Ruling 1 (composability):** `discover-tests` returns **fn-value pairs** `(Vec (Pair String (Fn [] (Option String))))` (FQ name + late-bound GOT-slot-indirect callable wrapper), restoring §8c's return shape and **retiring the macro runner** — the stdlib runner is ordinary `map`/`filter` over the pairs, fresh on every call. `Pair` and `Result` are NOT seeded as-built — both **join the primitives bootstrap seeds** (modelled on `register_option_type`; Option already seeded). `discover-tests` stays `DefKind::PrimitiveExtern` (body via `Jit::define_symbol`, closes FIXME 0261, dev-session only; one kind-driven `Linkage::Import` arm). **Fourth-convergence settled rulings:** q-scope — no-arg `(discover-tests)` = the **current** module; q-overload — **ONE** extern taking `(Vec String)` + stdlib-macro sugar (no-arg + single-`String` normalise to it), NOT `DefKind::Overloaded`; q-eligibility — discovery returns wrappers for fns matching BOTH the `test-` prefix AND the exact signature `(Fn [] (Option String))`. **Ruling 2 (combinator):** named **`catch-runtime-error`** (q-rte-name settled — the language/ABI name; the intrinsics-internal Rust slot-reader `take_runtime_error()` `panic.rs:43` KEEPS its name as the combinator's internal mechanism — two-layer naming). Standalone `primitives` entry, protected-call combinator `forall a. (Fn [(Fn [] a)] (Result a String))` — invoke thunk, clear+capture slot, return `Ok`/`Err`. **PLAIN INTRINSIC, no backend codegen change** — calling a language closure from intrinsic code is established as-built (`io::call_continuation` `io.rs:351`, `ivar` `ivar.rs:137`, `run_test_by_name` `session_v4.rs:4778`); literal `(fn [] …)` thunks are ALWAYS closures (`compile_lambda`); forall scheme is a plain `bind`-style primitive (`bootstrap.rs:572`, empty constraints). Needs a new C-ABI `#[export_name="catch-runtime-error"]` + heap `Result` marshalling + an `intrinsics_table()` entry; works in ALL modes incl. `--link`. **q-rte-purity ANSWERED (lenient-eval):** lenient eval is LIVE (`compile_let`→`compile_let_lenient` `control_flow.rs:34/55/122`; sparks over IVars `ivar_spark`→`rayon::spawn` `ivar.rs:84`) — the doc's prior "lenient let does not move work off-thread" claim was WRONG (struck). **OWED-IMPLEMENTATION item (not an open question): the fork-join error-slot FERRY obligation.** As-built NEITHER boundary ferries the slot (IVar `ivar_force` `ivar.rs:137`; Par `dispatch_par_branches_with_trace` `io.rs:456–473` — both return bare i64, no worker-side `take_runtime_error()` check) → worker panic silently swallowed on the joining thread + slot pollution; **PRE-EXISTING defect** violating spec §12.4.3 observational-equivalence — **flag to file when actioned** (not filed from design pass). Obligation: every fork-join boundary MUST ferry (worker-side `take_runtime_error()`→`(result, Option<err>)`; join-side first-error re-raise via a new `set_runtime_error` companion + sentinel). Soundness: both forms are STRUCTURED fork-join (§12.4.3 + §10.12 — expression does not return until all branches join), so every spark joins inside the dynamic extent of any enclosing `catch-runtime-error` → with ferrying the combinator stays a plain own-thread slot-reader, ZERO special-casing (the earlier "Par-internal panic is the trampoline's boundary" position is superseded). RC mid-panic caveat carried forward. **`run-test` subsumed**. **NO friendly `--link` rejection** for `discover-tests` (interim). Zero frontend + zero typecheck special-casing; §2.9 + grammar keyword rows RETRACT `discover-tests`/`run-test` (`trace` stays); appendix-A BOTH rows re-type (+ `catch-runtime-error` row); §4.12.3 exclusion note; `TestResult` RETIRES; `Result`+`Pair` join primitives docs; **NEW cascade — spec §12.4.3 gains a sentence pinning error propagation across fork-join joins** (proposed wording in §6); FIXME 0266 stays trace-only. FOUR superseded layers retained as §8a–§8d. |
| `platform-interface.md` | Subsystem design for how a platform DLL exposes its interface to the host (the S-PLAT-1 resolution; FIXME 0282 deleted as superseded) — **TARGET STATE, USER-RATIFIED 2026-06-07; CASCADED (S76 /arch).** The three-exports model (GOT + manifest + schema+layout-hash), dual-artifact (cdylib/rlib) deployment, compiler-generated schema (`/platform-schema` command + backend generator sharing the trace descriptor-baker closure-walk), layout-hash dual gate (REPL warns / `--run`+`--link` refuse), GOT-indirect dispatch (retiring `jit_name`/`JITBuilder::symbol`), and FQ sigs (retiring `inject_primitives_import_for_platform` + `validate_schema` + `schema_literal` + the schema DSL) are set in place in **`bounded-contexts.md`** §6 (int — load path + `/platform-schema` command + injection/jit_name retirement; the prior `facades/int.md` recording moved here when that facade retired S81 W-Retire → BC §6 + `design/int/` + source rustdoc), §5 (platform — three exports, schema generator-not-here, no exported fn names), §3 (backend — generator + GOT-indirect arm + startup hash bake), §7 (SymbolTable.schema_literal retires). **Implementation status (updated S79 /arch Phase-2, 2026-06-12): LANDED.** FIXMEs **0286** (platform macro), **0287** (backend generator + dispatch + bake), **0288** (int load path + command + PlatformError variant), and **0293** (the `PlatformError::LayoutHashMismatch` variant + `schema_literal` removal) are all RESOLVED + deleted; ABI v3; `schema_literal` is gone from `cranelisp-types`. **Only 0289 (/qa — e2e) remains open**, gated on the `shapes` ADT-typed test-DLL fixture (`/platform`). 0233/0238 STALE → close; 0229 `validate_schema` half retired. FIXMEs 0232/0235 re-pointed; 0282 + 0234 deleted (superseded). **Naming reset (fourth convergence):** the exported function-pointer table is the **GOT** (not "slab"; symbol stays `__cranelisp_got_platform_<name>`), the declarative function-data block is the **manifest** (what a session builds a symbol table from), the generated type-layout artifact is the **schema**. **§1 states three exports** — GOT (linker-fixed-up fn pointers, manifest order), manifest, schema+layout-hash — and their consumers (**`--link` = GOT only**; **REPL/`--run` = all three**), and answers the four user confusions in plain prose: (1) **manifest array order IS GOT slot order** (one declared order, macro emits both; host assigns `got_slot = manifest index`); (2) GOT is **const-initialised in Rust AND emitted as relocations at the object level** (dynamic loader for dylib, static linker for `--link`; no runtime population; same mechanism as `define_module_got_data`); (3) host builds the SymbolTable **in its own memory from the manifest**, its GotTable **wraps the dlsym'd GOT in place — no copy** (BC §3 invariant 3); (4) naming. The DLL exports the GOT + manifest as C-ABI facts; the host builds the SymbolTable from manifest facts (name + **FQ** type_sig→scheme + got_slot=manifest-index + scheduling_class + metadata) plus host-owned invariants — **no injected imports**. **Platforms stop declaring ADTs** — types become ordinary importable `.cl` modules; the schema **declaration arm** + DLL-local `LazyLock<Schema>`-as-DSL + `HostCallbacks::validate_schema` + the `schema_literal` cache field + **`inject_primitives_import_for_platform`** all RETIRE. **§5.5 field-by-name design (the doc's center) — third convergence:** the schema becomes a **compiler-generated build artifact**, never hand-authored. A new **`/platform-schema <name>` REPL command** (introspection-family sibling) derives the referenced-type set from the loaded platform's symbol table, takes the **transitive closure** over field types, and emits the schema as text; the platform embeds it (`schema: include_str!("<name>.platform-schema")`) and the **retained parser** reads it for `read_field` name→index. **Shape:** `Map<FQTypeName, Vec<(CtorName, tag, Vec<(Symbol, FieldType)>)>>` (ordered NAMED+TYPED fields; `FieldType` recursive; typed fields make nesting work); **concrete instantiations keyed by the structured type expression** (machine-read — supersedes FIXME 0234 §1.3's mangle). **`layout_hash`** ONE per platform (`__cranelisp_layout_hash_<name>`, canonical hash of the generated artifact) — host **regenerates from live tables** and compares: **`--run`/`--link` REFUSE, REPL WARNS-AND-LOADS** (the regeneration bootstrap). **One generator, one checker, both in `cranelisp-backend`** (sharing the closure-walk + substitution with the trace `DisplayDescriptor` baker) — the second convergence's canonical-form/DAG problem **dissolves**. **Replaces** the second-convergence embed-the-`.cl` (`include_str!`) design (user: "brittle" — `include_str!` captures lexical file content, but layout truth lives in the resolved module graph; breaks on import/re-export). **0234 `/abi` SUBSUMED-NOT-IMPLEMENTED.** §2 near-empty (residue: tag stability across regenerations; artifact textual grammar). Cascade-when-actioned: new `PlatformError` variant for the hash refusal (Decision 0042); FIXME 0234 retirement. |
| `effect-concurrency.md` | Subsystem design for the **language-level** concurrency model — **ratified target architecture (S92), pre-implementation.** Thesis: **throughput is free; control is explicit** — two complementary peers, not primary-plus-footnote. Inferred half: a scheduler-**trampoline** extracts maximum SAFE concurrency from dataflow independence + platform-declared concurrency descriptors (token + cardinality + budget + blocking?), provably identical to sequential. Control half: an in-language, trampoline-interpreted combinator layer (`race`/`select` + structured cancellation; `timeout` derived) — the irreducible vocabulary for an uncooperative I/O boundary; **the combinator layer is separable-but-COMMITTED (an additive peer of the inferred half, not a deferred/optional tail) — per-request timeout, cancel-on-disconnect, graceful shutdown are what make a server survive the open internet.** **Substrate: async/await over a feature-gated host-owned async runtime (tokio-or-equiv), NOT hand-rolled fibers** — IO is reified as data so the trampoline is an `async fn` and all awaiting lives in Rust; two pools (rayon CPU sparks + reactor I/O), not unifiable. **Platform ABI: A2 host-owned-reactor C-ABI-async (ABI v4)** — platforms are C-ABI async leaves (own the *what*); the host reactor owns the *when* via a host-provided `HostCtx` vtable + C-ABI waker (the one new designed artifact); deployment/three-exports model preserved. Resource-token model + fork-join error-slot ferry (as-built) preserved/generalized; supervisor policy (500+log+drop) co-lands with launch-and-continue. **First-class OBSERVABILITY commitment (groundwork): a structured, strand-correlated event stream emitted from the trampoline, extending `trace`/`io_trace`/`IoObserver` + the S90 log↔trace `turn` correlation — load-bearing because the concurrency is invisible in source, supervisor drops vanish without a sink, and the perf standard is unmeasurable without it. The indispensable primitive is STRAND IDENTITY (correlation id, `turn`-precedent) threaded through every suspend/spawn/cancel; expensive to retrofit, so hooks+strand-id land WITH the async substrate; sinks stay minimal/dev-facing + feature-gated byte-identical-when-off.** Standard: per-workload acceptance benchmarks (web = reference), floor = never *dramatically* slower than sequential (scoped S94 — see the floor-scope ruling at the end of this entry + §3.1). Rejects the Roc/"Model B" platform-owned-loop degeneration (now DEFINITIVE — §12.1 rules the platform-effect boundary poll-in/wake-out only, no closure-callback-into-cranelisp capability; retires the former FIXME 0407). Distinct from `design/int/concurrency-architecture.md` (the *compiler-internal* scheduling axis; its dependency-service race + D0030 mutual-import deadlock resolved by the S93 signature/body pre-pass — FIXMEs 0425 item 1 + 0426 resolved-by-deletion S93; 0425 items 2–4 remain inventory there, untracked). **Slice 2 OPENED S93** (the reactor gate): the ABI-v4 cascade is **actioning** as numeric `ABI_VERSION` **6→7** (NOT the doc-label "v3→v4") in `platform-interface.md` **§6.8**, and the layout contracts are **landed-and-dormant** behind an off-by-default `concurrency` feature — `cranelisp_types::{ConcurrencyDescriptor, Poll, PollFn}`, `cranelisp_platform::{HostCtx, Waker, WakerVTable, PollFn, ConcurrentPlatformFn}`, `cranelisp_intrinsics::{StrandId, StrandEvent}`. Manifested S93: BC §3/§5/§6 (slice-2 notes), `sequences/concurrency-scheduler.mmd` (new) + `concurrency-dependency-service.mmd` (reconciled to the signature/body pre-pass), spec cascade filed as **FIXME 0447** (`target: /spec`, §10.12/§12). The candidate new principle ("confine mutable-state concurrency to the interpreter") was a Phase-7 action (arch R6); at S94 close it is DEFERRED to next-sprint /design (see entry end). Remaining: the reactor *implementation* (host runtime + `HostCtx` impl + trampoline emit-hooks) is the `/dev` slice-2 work, feature-gated/byte-identical-when-off. **S94 /port floor-scope ruling (§3 + new §3.1):** the "never slower than sequential" floor was over-stated as universal — VIOLATED up to ~10× for allocation-/RC-heavy parallel workloads (Sudoku copy-per-guess), because the spark-budget create-gate bounds spark *count* but not per-branch heap-allocation + atomic-RC (Decision 13) contention. Verdict BOTH: scope the claim now (unconditional for compute-bound sparks; contention-bounded otherwise) AND specify a **contention-aware gate** (backend cost-heuristic + create-gate, static allocation/RC-density axis first) as the in-track path back; Phase H (thread-local RC / escape→stack/region / reuse) is the structural cure and the sequencing edge is REINFORCED not pulled forward (Principle 8). Backend-side correction filed FIXME 0459 (`target: /backend`). **Candidate-principle disposition (S94 Phase-7 close):** the candidate "confine mutable-state concurrency to the interpreter" is **DEFERRED, not added** — its wording/boundary is coupled to the contention-aware-gate design (0459) that `/design` picks up next sprint (S95); add/refine at that sprint's close. A hand-off, not a final resolution (see effect-concurrency.md §15 candidate-principle bullet). |
| `ownership-inference.md` | Subsystem design for the **memory-model spine** — **DESIGN (S100 Phase 3), pre-implementation; the scope authority the per-crate proposals cite** (`design/typecheck/ownership-inference.md`, `design/backend/ownership-codegen.md`, `/qa`'s verification plan). Thesis: escape analysis, ownership inference, and RC optimisation are **one interprocedural lifetime/flow analysis** — computed **in typecheck, post-monomorphisation, over the resolved call graph, no user annotations** (Roc/MLKit/GHC precedent) — answering **five queries** (param borrowed? escapes frame? crosses thread? uniquely owned? how duplicated?), each driving a distinct backend mechanism on the **shared Cranelift lowering** (superseding D-Rel-4 for the memory-model subset; conservative all-Owned lowering stays reachable as the differential oracle — R7). Measured basis: the S99 settlement ((b) atomic-RC bouncing dominant; vec-COW leaf-refcount volume ~170M; (a)/(b) coupled). Mode lattice `Copy ⊑ Borrowed ⊑ Owned` + `Unique` as an Owned-refinement flag (uniqueness is call-site-dynamic — R4: rc==1 reuse tokens are a first-class increment-II outcome; mode stays out of the mono key in increment I); escape + confinement as distinct axes — confinement **op-wise** (Crossing = RC ops may run *concurrently* on >1 thread, so borrowed joined-spark reads leave the parent cell Confined/non-atomic, directly attacking the S99 (b) term; `Transferred` named, adoption routed to part 9) — with **suspension points as escape edges** (R6 — the §5.5.2 ParBind caveat dissolves by classification, never by widening the borrow). **Two-class contract (R1):** ABI-bearing per-param mode vectors on statically-resolved calls only (R2 — HOF/closure sites, constructors, externs, platform effects stay Decision-24 at the ABI; extern primitives additionally carry a REQUIRED increment-I hand-declared per-param fact table as analysis inputs — §3.1(a), seeding the fixpoint at the leaves; absence ⇒ the Decision-24 Owned default, strictly additive) vs advisory per-site facts (sound to ignore); **monotone soundness** — widening toward Owned/Escapes/Crossing is always correct, only slower. Summaries ride the callable's symbol-table entry → `.meta.json` (schema-gated; batch invalidation already conservatively covered by source-hash + recompiled-set cascade). **§5 is the load-bearing new subsystem (R3, user-directed 2026-07-02): symbol-level dependent recompilation in the dev session** — mode-as-ABI (and the latent type-change hole) means redefinition can no longer be a GOT-patch-only; reverse who-calls-whom index derived from the persisted `ModuleEntry::Def.callees` (Decision 21; same edges the ownership fixpoint walks), summary-diff fast path (ABI-preserving redefinitions stay a GOT patch), reverse-topo recompile with cluster fixpoints, **cascade errors surfaced as BROKEN-marked symbols with in-place trap-stub slots + provenance** (generalising S45 reset_module/embedded-original-error), and commit via **ABI-epoch slot versioning — an ABI-changing redefinition allocates a FRESH GOT slot and freezes the old slot on the retained old code** (Principle 20: mixed-ABI edges unrepresentable; heap closures/suspended continuations embed direct code ptrs recompilation cannot reach, so in-place rebinding is unrescuable even stop-the-world — no quiesce needed, per-slot writes independently safe; `Code::Jit` retention extends D31-Scenario-2); machinery MUST land before ABI-bearing modes reach the dev session (§5.7). `Copy` row's mechanism named (R5): value-representation flattening of Copy-eligible ADTs (S83 §12.1 relaxation) — routed to the backend proposal; until it lands the Copy point is scalars-only. G3 standing precondition: **no reference-identity observer** in the language. Two-increment staging (Principle 8): I = borrow inference (read path; subsumes capture-by-borrow + `borrowed_vars` via the escape/projection queries) → II = uniqueness → mutable-borrow → reuse + Copy-flattening; I subsumed-not-discarded by II and by `--release` (same facts, same `MonoExpr` input). Staged F1–F4 acceptance per increment (R8). **No `cranelisp-types` edit in S100** — fields designed (§3.3), landed by the first implementation sprint with the full baseline/schema cascade. Cascaded S100: `release-llvm-backend.md` §6/§7/§8.3/§13 amendments, `effect-concurrency.md` §3.1 forward-pointer, FIXME 0465 (`target: /design` — ring2-rc §3 Decision-24-as-conservative-point; actioned + deleted stage 2). **Phase-3 exit gate PASSED 2026-07-03 (§12, PASS-with-notes)** — the four-doc set (spine + typecheck parts 6–11 + backend parts 12–16 + qa parts 17–18) confirmed complete/coherent; one seam deliberately deferred-with-pinned-interface (the R3 session-transaction orchestration → `design/int/` fire at the machinery sprint, interface = backend §8.3). FIXMEs drained at the gate: **0467** folded into §3.3 (enriched `ModeSummary`: ABI-bearing `result: ResultMode` + advisory `param_flow`/`spark_ops`/`result_unique` + projection-provenance site fact + value-use mark + primitive declared-fact payload — all landing at the implementing sprint, still no S100 types edit); **0468** ruled into §6.3 (single-sourced Copy/value-layout predicate in `cranelisp-types` beside `HeapHeader`, R5-increment carrier change-set; design-level pin only); **0469** actioned in §3.1(b)/§10-14 (sibling template = `str-len`; `vec-len` has no pair to elide). §3.1/§9 updated with the `/qa` triage verdict: vec-query-family value-use NULL-slot SIGSEGV is a REAL defect (`tests/vec_query_value_use.rs`, owner `/backend`, fix precedes sibling + R2-wrapper reach into primitives). **S101 Phase-3 exit gate PASSED 2026-07-03: the deferred seam landed** — `design/int/session-transaction.md` (the R3 transaction interior, consuming backend §8.3's pinned three-call interface unchanged); spine §5.3/§5.5 reconciled to its rulings (reverse index derived on-demand at transaction time, incremental maintenance a named increment-I upgrade option; BROKEN code moves to the paired retention pool, never `None`d in place); **FIXME 0470 filed** (`target: /typecheck` — `callees` edge widening to every statically-resolved user-fn reference, `CACHE_SCHEMA_VERSION` bump same change-set; load-bearing for S101 L-R3/L-R4, no `cranelisp-types` edit). **S102 Phase-2 ruling (2026-07-03) recorded in §6.2**: golden-CLIF capture sequencing vs the co-scheduled defect wave — capture is the first Block-B change-set, not gated on defect fixes; green-only corpus; emission-affecting classifier; scoped re-baseline in the fixing change-set (extension ≠ re-baseline); corpus pin routed to `/qa` as FIXME 0503. **S102 Phase-3 CS-A LANDED (2026-07-03, §3.3 blockquote)**: the §3.3 carrier fields + typecheck §13.1 needs-list enrichments + FIXME-0476 `PrimitiveBody::{Extern,Inline}` reshape landed as ONE `cranelisp-types` change-set (`CACHE_SCHEMA_VERSION` 11→12; new `crates/cranelisp-types/src/ownership.rs`; toggle relocated as `ownership_analysis_off()` — item-12 ruling YES); cascade: types `public-api.txt` + `interfaces.md` §"Ownership-inference carriers" + BC §7 "S102 — Principle 20 applied one level down"; carrier-only (no producer/consumer until typecheck CS-1..4 / backend B1-be); 0476 annotated, deletes at B1-be. **§3.7 (S110 W2-review COW ruling, 2026-07-16; lands S111):** the vec-assoc UAF class is a three-layer summary-truth defect — `ResultMode` lacks the COW point (a legal state with no representation forced the false `Fresh` declaration on `vec-set`/`vec-push`), and the declared-leaf facts are UNREACHABLE from prelude-fallback modules (`ClusterEnv` resolves via the fallback-less `resolve_terminal_entry_and_home`, so §3.1(a) precision is silently dead in production and `transfer.rs:590` defaults the result to the anti-conservative `Fresh`). Ruled: add `ResultMode::MayAliasOf(k)` (+ schema 19→20), declare the COW pair truthfully, make the ownership envs prelude-fallback-aware; the `:590` `Fresh` default STAYS (co-sound with ⊤-`Owned` under the consuming convention — the premise now explicit in rustdoc); the W2 `return_cow_source` recognizer is RE-SCOPED to a leak-exactness optimization (safety subsumed by the summary), deleted only when a walk-emitted per-site fact generalizes it; candidate (b) — flipping the default — REJECTED (broad churn, zero soundness gain). Carried to S111 as ONE coordinated change-set with the S110 `/testing` sibling repros (let/match COW-return) as the failing-not-ignored trigger; `/qa` matrix gap = FIXME 0623. **§3.7.1 (S111 Phase 3):** the carrier-extension completeness matrix — four binding axes (reachability: the FIVE `fixpoint.rs` raw probes cured by ONE shared prelude-hop helper, never five hand-rolled hops; variant: the one exhaustive match + the two verified `== Fresh` grep escapes, no third; producer: `origin_to_result_mode` flip + the whole-table `ownership_facts.rs` sweep; behavioural: 0623) — binding on the `/design`(typecheck) + `/qa` Phase-3 plans. Pinned `MayAliasOf` diff + consumer census: `interfaces.md` §"Ownership-inference carriers"; the mode enums' no-`#[non_exhaustive]` exception: `ownership.rs` module rustdoc + types `CLAUDE.md`. |
| `fixmes/` | FIXMEs register; one file per FIXME (`design/arch/fixmes/NNNN-name.md`). |
| `sequences/` | Current target sequence diagrams (`.mmd` + rendered `.svg`). |

## Sorting buckets

| Directory | Purpose |
|---|---|
| `decisions/` | **Draining.** Existing Decision files migrate into the facade / BC / principle section where they manifest, then delete. No new Decisions authored — see `.claude/commands/arch.md` §"No separate Decision log". When the directory is empty it is removed. |
| `legacy/decisions/` | **Draining.** Same as above for outcome-fully-embodied Decisions kept for narrative continuity. Same disposition: migrate substance into the manifestation site (if not already there), delete the file. |
| `legacy/` | Documents not part of the approved configuration but kept for reference. `/arch` and the per-crate `/design` skills pull back content (or re-author from it) when needed; otherwise files here are triaged into top-level (if they prove still load-bearing) or down to `archive/` (if confirmed superseded). Not a permanent home. |
| `archive/` | Frozen historical content — superseded by canonical work, kept for context only. See "## Archive" below. |

## Archive (`archive/`)

Historical pipeline designs (v1, v2, v3) and superseded migration artefacts. Reference only — not the target architecture.

- `archive/v1/` — v1 architecture, interfaces, pipeline orchestration, sketch audit
- `archive/pipeline-v2.md` — v2 pipeline design (stages, unified multi-pass check)
- `archive/pipeline-v3.md`, `archive/pipeline-v3-roadmap.md` — v3 migration (complete)
- `archive/pipeline-convergence-review.md` — Sprint 26 dual-pipeline defect analysis (origin of principles 11–13)
- `archive/pipeline-convergence-playbook.md` — convergence execution plan
- `archive/session-restructure.md` — session restructure target data model (phases A–F complete)
- `archive/per-module-got-cleanup.md` — GOT unification design
- `archive/sprint-40a-design.md` — cancelled Sprint 40a design
- `archive/codegen-convergence.md` — Sprint 54 Wave 3a; superseded by Decisions 22, 23, 25 (S63 archive)
- `archive/ast-annotation-examples.md` — Sprint 55 Step 1b annotation spec (S63 archive)
- `archive/pipeline-v4.md`, `archive/pipeline-v4-roadmap.md`, `archive/concurrent-pipeline.md` — v4 scheduler-driven pipeline design + roadmap; superseded by per-crate `design/{crate}/{crate}.md` and Decisions 21–27, 31, 36–41 (S65 Phase 2 legacy triage)
- `archive/reconciliation-plan.md` — Sprint 63 close procedural reconciliation plan; substance + procedural waves executed (S65 Phase 2 legacy triage)
- `archive/roadmap.md` — pre-S63 ring-by-ring architectural roadmap; delivery tracked by `sprints/ROADMAP.md`; per-crate intent in `design/{crate}/{crate}.md` (S65 Phase 2 legacy triage)
- `archive/substance-scoping-brief.md` — input brief for the substance-scoping pass; pass executed and resolved into Decisions 40–43 (S65 Phase 2 legacy triage)
- `archive/macro-resolver.md` — Sprint 50 macro resolver design; superseded by FIXME 0098 (Decision 8 retracted; `MacroResolver` trait drops in favour of direct `&SymbolTables<C, L>` lookup) (S65 Phase 2 legacy triage)
- `archive/traitimpl-symbol-table.md` — Sprint 51 ImplRegistry-deletion design; landed in source (`ModuleEntry::TraitImpl` exists; `ImplRegistry` deleted per `crates/cranelisp-typecheck/src/{checker,traits}.rs` source comments) (S65 Phase 2 legacy triage)
- `archive/sequence-diagram/` — pre-S63 v4-target sequence diagrams; superseded by `sequences/` (S65 Phase 2 legacy triage)
- `archive/facades-runtime.md` — pre-D43 runtime facade; content migrated to the primitives + intrinsics surfaces (S65 W1 — Decision 43 crate split). Both successor facades subsequently retired (S74 Wave 3); their canonical homes are now `bounded-contexts.md` §4a (primitives) / §4b (intrinsics) + the crate-root source rustdoc.

## Decisions drain backlog

**This register is being drained.** Per `.claude/commands/arch.md` §"The manifestation-site question", each Decision's substance migrates into the facade / BC / principles section where a reader expects to find it. The Decision file deletes once migrated. New architectural commitments are NOT filed here; they go directly to their manifestation site.

**Per-Decision target identification format:** As each Decision is queued for drain, mark it `[→ {manifestation-site}]` inline. Then in a follow-up fire: migrate substance, sweep cross-references, delete the file, strike the line here. When the section hits zero lines it is removed.

Current backlog:

- [0010](decisions/0010-base-pointer-abi.md) — Base-pointer ABI (environmental — captures rejected interior-pointer alternative)
- [0011](decisions/0011-embedded-drop-glue-ptr-in-closures.md) — Embedded drop_glue_ptr in closures (environmental — captures rejected side-table alternative + cross-module closure constraint)
- [0027](decisions/0027-g8-lands-before-g9.md) — G8 lands before G9 (environmental — borrow-checker sequencing rationale)
- [0030](decisions/0030-form-by-form-scheduler-mutual-imports.md) — Form-by-form scheduler deadlocks on mutual imports (environmental — coordination constraint future readers will hit)
- [0035](decisions/0035-code-enum-integration-layer.md) `[got_slot placement amended S83 → BC §7 "Callability is structural" + interfaces.md + Principle 20; Code-enum substance → bounded-contexts.md §3 + crates/cranelisp-backend/src/code.rs rustdoc]` — `Code` enum location (operative; amended Sprint 64 per Decision 41 — Code now in `cranelisp-backend`; amended Sprint 66 — variants slim to lifecycle owner only `Code::Jit(Arc<Jit>)` / `Code::Linker(Arc<Linker>)`. The S66 unification (`b09ec76`) briefly relocated the per-entry `ptr` to a sibling `ModuleEntry::Def.fn_ptr` field; the same-day rollback `1dc57ae` removed that field as redundant with the per-module `GotTable`. **Post-rollback canonical statement: GOT is the single source of truth for callable addresses; `ptr` lives in `SymbolTable.got()` indexed by `ModuleEntry::Def.got_slot`. No per-entry pointer field.** **Sprint 83 amendment (Phase 2, 2026-06-14, FIXME 0356/0357, Principle 20): the `got_slot` index moves OFF the flat `ModuleEntry::Def` field and ONTO the callable `DefKind` variants** (`UserFn` concrete-callable / `Primitive` / `Constructor` / `PlatformEffect` — the four GOT-indirect-dispatched callable kinds; `PlatformEffect` ratified into this set S83 per FIXME 0358, correcting the Phase-2 gating-decision-2 omission); non-callable / non-GOT-dispatched kinds carry no slot field (incl. `PrimitiveExtern`, which dispatches by-name via `Linkage::Import` — FIXME 0360), making the constrained-template-with-callable-slot pairing structurally unconstructable. The single-source-of-truth property (one slot per callable, GOT-indexed) is unchanged — only the slot's *placement* moves to where its determinant (the kind discriminator) lives. The flat-field placement was correct against the rejected sibling-`fn_ptr` alternative; it is superseded against the correlated-invariant alternative. Timing-wall resolution: defer slot allocation past Pass-2 constraint detection. Canonical statement: BC §7 + interfaces.md + Principle 20; the S82 `callable_got_slot()` stopgap retires its flip-and-clear writer when the reshape lands. The Code-enum half of D0035 is unaffected and drains independently to BC §3 + backend `code.rs` rustdoc.)
- [0040](decisions/0040-runtime-trace-io-trace-relocate-to-int.md) `[trace half RETRACTED 2026-06-04 → tracing.md §§1–6 + BC §3/§4b-inv-12/§6; io_trace half STANDS → BC §4b + §6]` — **(trace ...) half retracted by the 2026-06-04 user ruling**: the 12 trace bodies relocate BACK to `cranelisp-intrinsics` (publish via `intrinsics_table()`), trace works in ALL modes incl. `--link`, discovery + descriptor baking move to backend codegen, nested trace is a runtime error. The `io_trace`/`IoObserver` half STANDS (ring buffer → int; ~50-line registration API → intrinsics). See the PARTIAL-RETRACTION BOX atop the D40 file + `tracing.md` §7. `IoObserver` callback registration API resides in `cranelisp-intrinsics` post-Decision-43 (originally specified to remain in runtime; the registration-site host moved with the D43 split — see `bounded-contexts.md` §4b + the `cranelisp-intrinsics` crate-root `//!` rustdoc §"Int-owned intrinsics" / `io_observer` module rustdoc; `facades/intrinsics.md` retired S74 Wave 3). BC §4b carries the contract. Amended 2026-05-16 (S67 W4) to Path B1 (full deletion; user-arbitrated) — supersedes the earlier B2-flavoured §"Shape" reading. Pre-implementation; tracked by FIXME 0103 + cascading FIXMEs 0197–0202.
- [0041](decisions/0041-compile-to-module-per-symbol-jit-direct-writes.md) `[→ bounded-contexts.md §3 invariant 3 + crates/cranelisp-backend/src/{lib,code}.rs rustdoc]` — `compile_to_module` per-symbol JIT cardinality; `Code` moves to `cranelisp-backend` (backend *names* it); backend writes the **GOT slot** directly (#2), the **caller composes `Code`** (#1 — both `Code::Jit` and `Code::Linker`; S75 W2 Finding-A correction — backend only borrows `&mut M`, never owns the `Arc<Jit>`, so cannot construct `Code::Jit`; symmetric with the existing Linker path); returns `CompilationArtifacts`; `produce_disasm(fq, code_size, symbol_tables)` is a separate on-demand fn with a **caller-supplied `code_size`** + capstone raw-bytes disasm (S75 W2 Finding-C correction). Amends 31, 35. **Corrected substance now lives in `bounded-contexts.md` §3 + the backend source rustdoc** (`facades/backend.md` retired S75 W5b → BC §3 + `lib.rs`/`code.rs` rustdoc; per "No separate Decision log"); D41 file annotated with an S75 correction box for drain-consistency.
- [0042](decisions/0042-platform-error-adopts-error-location.md) — `PlatformError` is a `cranelisp-types`-hosted enum with `ErrorLocation` carriers per variant; surfaces via `CranelispError::Platform` (pre-implementation)
- [0043](decisions/0043-runtime-split-into-primitives-intrinsics.md) — `cranelisp-runtime` splits into `cranelisp-primitives` + `cranelisp-intrinsics`; backend has no trait knowledge (retracts 14, reframes 15; pre-implementation; tracked by FIXME 0150)
- [0044](decisions/0044-cluster-atomic-typecheck-orchestrator-staging.md) — Cluster-atomic typecheck via orchestrator-owned staging; `View<'_, C, L>` newtype; reframes 0038's per-form check_form shape (pre-implementation; filed by FIXME 0166; **amended Sprint 66 Phase 3 by FIXME 0167** — Approach B + `SymbolTableAccess` introduction (originally named `ClusterContext`; renamed in the post-S72 facade-coherence pass — "Cluster" privileged one of two modes, "Context" was contentless); pass signatures take `&mut SymbolTableAccess` and return `Result<(), CheckError>`; staging mutation flows through the existing `current_symbol_table_mut` accessor so the 91 register-call sites in typecheck do not change individually; invariant 2 revised — passes are pure with respect to live state, may mutate orchestrator-handed staging via the same accessor used in committed-mode; **further amended Sprint 66 Phase 3 by FIXME 0168** — Sequencing splits Wave 3a into α — locality-correctness refactor — before β — triad re-fire — per Decision 0046; **further amended 2026-05-13 third amendment** — the two-pass facade split (`check_form_signatures` + `check_form_body`) collapses into a single `check_forms(parsed: Vec<ParsedEntry>, ctx: &mut SymbolTableAccess, symbol_tables: &SymbolTables) -> Result<(), CheckError>` free function; internal two-pass discipline + Pass-1-to-Pass-2 working state are internal to the call frame; `ModuleCheckAccumulator` retired from both typecheck and `int` public surfaces — cross-symbol bookkeeping migrates onto `ProcessedCluster` fields; state-threading hole closed by construction)
- [0045](decisions/0045-traitimpl-storage-in-trait-defining-module.md) — `ModuleEntry::TraitImpl` is written to the **trait's defining module**; importers discover impls by chain-following the trait reference (per-symbol `Import`/`Reexport`) back to its home module and probing for `impl$FQTypeName$FQTraitName`; pattern (b) selected over (a)/(c)/(d) on chain-follow simplicity (no closure walk; no cycle detection) + Principle 7 + Principle 17 grounds; user-arbitrated 2026-05-10 (Sprint 66 Wave 3a-α post-mortem); α's first pass `ab068e2` embodied pattern (a) and is rolled back + redone (filed by FIXME 0168). **AMENDED S110 W0.1 (`backend-keyed-consumer.md` §1.1.1):** the method-body co-location clause is corrected — only the `TraitImpl` SHELL lives at the trait's home (discovery record); the mangled method `Def`s live in the **impl-writer's module** (structurally forced by `compile_to_module`'s own-table entry+GOT invariant); the shell gains `impl_module` as the discovery→storage pointer (W0.1b pinned types diff)
- [0046](decisions/0046-wave3a-locality-refactor-precedes-triad.md) — Wave 3a splits into α — locality-correctness refactor across `crates/cranelisp-typecheck/src/{checker,infer,traits,builtins}.rs` per Principle 17 — before β — cluster-atomic triad implementation per Decision 44 (amends 0044; pre-implementation; ~3–5d α + ~3–4d β; filed by FIXME 0168)
- [0047](decisions/0047-fqtypename-binding-at-resolved-stage-boundaries.md) — FQTypeName is binding as the cross-crate boundary type for resolved-stage type identifiers; two exceptions named (reverse-lookup; receiver-pinned). Closes FIXME 0151 at S67 W5 acceptance. Filed Sprint 67 (Phase 3 W0 — second user-challenge scope amendment).
- [0048](decisions/0048-primitives-static-symboltable-and-got-in-crate.md) — `cranelisp-primitives` owns a statically-constructed `SymbolTable` + `Arc<GotTable>` referenced from CompilerSession at startup; from session-init onward primitives dispatch is functionally equivalent to any other module (pre-implementation, S68; **A2 reversed 2026-05-31 (S73 Phase 2, FIXME 0244)** — `Code::Primitive` marker dropped; primitives entries carry `code: None` (the `ModuleEntry::def(..).build()` default), primitive-ness read from `kind: DefKind::Primitive`; **dep-ban → bidirectional severance 2026-05-31 (S73 Phase 2 top-up)** — `cranelisp-primitives ⟂ cranelisp-backend`: with `code: None` everywhere primitives never names `Code`, builds `SymbolTable<(), ()>`, drops `cranelisp-backend` from its manifest; `int` concretizes to `<Code, ()>` via `into_concrete` at the session mount; primitives-side lands S73, backend-side cleanup deferred to a future backend sprint; motivates Principle 18).

Legacy Decisions (outcome fully embodied in architecture; preserved in `legacy/decisions/` for narrative continuity) — `0001`–`0006`, `0008`–`0009`, `0012`–`0013`, `0016`, `0018`–`0019`, `0021`–`0026`, `0029`, `0032`–`0034`, `0036`–`0038`. Retracted/superseded Decisions deleted (rely on git for history): `0007`, `0014` (retracted by 43; commit `754d525`), `0015` (reframed by 43; commit `754d525`), `0017`, `0020`, `0028`, `0031` (substance fully amended into 0041 at Sprint 64; Cranelift `Memory::drop` evidence + safety invariant + callback support forward commitment relocated to 0041 at S69 Phase 3 — per-symbol JIT cardinality is the operative model; D31's stale "per batch" title was a confusion source), `0039` (commitment fully cascaded into `repl/spec.md` §15.4 + `facades/types.md` §"Symbol table" + `cranelisp-types/src/module.rs` at S69 Phase 3 — per-entry `seq: u64` + `next_seq` + `StructuralDeclEntry` upgrade; D39's `defn_order: Vec<Symbol>` shape obsoleted).

## Cross-References

- `principles.md` — architectural principles (index; per-Principle bodies in `principles/NN-*.md`; auto-imported by `.claude/commands/arch.md`)
- `bounded-contexts.md` — per-surface full statements
- `facades/` — **all facade specs retired** (S69–S81); the directory holds only S69/S70 facade-audit records. The as-designed public surface now lives in per-item source rustdoc; cross-surface narrative lives in `bounded-contexts.md`.
- `overview.md` — newcomer entry point bridging spec ↔ tests ↔ design ↔ code
- `sprints/METHOD.md` — methodology
- `.claude/commands/arch.md` — `/arch` skill definition (the workflow layer)
- `sprints/reimplementation.md` — full reimplementation strategy (historical)
- `src/CLAUDE.md` — cross-cutting source conventions (error handling, code structure, naming)
- `archive/pipeline-convergence-review.md` — dual-pipeline defect analysis (origin of principles 11–13)

## Architectural Principles

Extracted to `principles.md` (S63). That file is the single canonical home and is auto-imported by `.claude/commands/arch.md`. Cite principles by name from `principles.md`; do not re-summarise the list here.

## String Newtypes

**Hard rule**: All identifier fields in boundary types MUST use the appropriate newtype, never bare `String`. This prevents accidental mixing of identifiers across semantic categories (e.g., passing a module path where a symbol name is expected).

| Newtype | Semantic meaning | Examples |
|---|---|---|
| `Symbol` | Local identifier — variable, function, operator, constructor name | `"foo"`, `"+"`, `"Some"`, `"_"` |
| `TypeName` | Type name (uppercase) — ADT, builtin, constructor | `"Int"`, `"Option"`, `"Color"` |
| `TraitName` | Trait name (uppercase) | `"Num"`, `"Display"`, `"Eq"` |
| `ModuleName` | Single module component (no dots) | `"core"`, `"option"`, `"math"` |
| `ModuleFullPath` | Dotted module path | `"core.option"`, `"user"` |
| `LinkerSymbol` | JIT linker name (mangled) | `"add$Int+Int"` |
| `FQSymbol` | Fully qualified: module + symbol | `{ module: "core.option", symbol: "Some" }` |

**When in doubt**: if a `String` field identifies something in the language (a name, a type, a module), it should be a newtype. The only bare `String` fields allowed are:
- Error messages
- Documentation strings
- Source text
- User-visible descriptions (e.g., `SpecialForm.description`)

All newtypes are generated via `string_newtype!()` which derives the standard trait set and implements `Deref<Target=str>`, `From<String>`, `From<&str>`, `AsRef<str>`, `Display`.

## Conventions

- All types in `cranelisp-types` derive `Serialize` + `Deserialize` for module caching

## Baseline-diff discipline (Sprint 67 close)

The S67 edge-settlement sprint established `cargo-public-api` baselines (`crates/cranelisp-{crate}/public-api.txt`) as the frozen contract at every crate edge. Future edge changes — anything that touches a crate's `public-api.txt` baseline — must, in the SAME change-set:

1. **Regenerate** the affected crate's `public-api.txt` via the canonical command `cargo public-api --omit blanket-impls,auto-derived-impls -p <crate> > crates/<crate>/public-api.txt` — mechanical and reproducible. The `--omit blanket-impls,auto-derived-impls` flags strip auto-generated noise (`::into`/`::borrow`/`::from`/`::clone`/`::Owned = T`/Debug etc.) that carries no semver signal; auto-trait impls (`impl Send/Sync/Freeze/Unpin/RefUnwindSafe for ...`) are deliberately KEPT because they are a real semver signal (e.g. a `Mutex` field flipping a type to `!Freeze`/`!Sync` is exactly the regression the baseline diff must catch).
2. **Update** the canonical surface record for each added/changed/removed item. All `facades/{crate}.md` specs are retired (S69–S81, `int` last per line 15); the canonical surface is now the source rustdoc: per-item `///` + crate-root `//!` name the boundary, and the cross-surface narrative + invariants live in `bounded-contexts.md` (the cache submodule's surface lives in `crates/cranelisp-backend/src/cache/` rustdoc — it has no facade and no BC entry, being an implementation detail of backend).
3. **Include the diff** in the commit, side-by-side with the source change that produced it. Reviewers (`/review`, the user) read the baseline diff alongside the facade diff to assess whether the change is a legitimate edge evolution or accidental surface leakage.

The facade compliance test scaffolded in S67 Wave 0 (`/qa`) asserts that every pub-api line in the baseline is named in the corresponding facade (or marked internal-but-exposed with rationale). Skipping the facade update breaks the test; skipping the baseline regeneration breaks the next baseline-diff check at PR time. The two-update discipline is the durable enforcement mechanism — no edge change is "done" until both files have caught up.

Skill responsibility split: `/dev` (per crate) regenerates the baseline as part of the implementing change-set; `/design` (per crate) updates the canonical surface record (source rustdoc + `bounded-contexts.md`) to match; `/review` confirms both are present in the same diff at PR time.
