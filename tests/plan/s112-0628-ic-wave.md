# S112 test plan — the 0628/I-C compiler wave (Phase 3, /qa, 2026-07-18)

The QA-first drafting spec for `/testing` (Phase 5 Stage 1: author the failing
e2e to THIS plan before per-crate D/D/R begins). Includes the two USER-MANDATED
assessments (§8 quality risk, §9 coverage impact) and the flag rulings (§7).

**Scope**: `sprints/SPRINT.md` §Scope (legs a/b/c + surfaces) + §Architecture
review (A1–A4, b0→b1→b2, the "Notes for /qa" block).
**Spec contracts**: `spec/05-definitions.md` §5.1.1/§5.1.2 (settled
inference-equivalence, S111 `c9f05b64`); `spec/07-traits.md` §7.1.1/§7.2/§7.2.1/
§7.2.3/§7.3–§7.3.6 (kind-by-usage, echo-the-head, kind-matching table);
`spec/03-types.md` §3.3/§3.11.
**Design contracts**: `design/typecheck/monomorphisation.md` §11 (leg a +
constrained cell §11.4 + determinism §11.5 + leg-c framing §11.6);
`design/typecheck/hkt.md` §5.1/§5.4 (declaration-time reject, Case-3 seam,
3-valued echo axis, migration lists, diagnostic-uniqueness note);
`design/frontend/trait-impl-head-parse.md` (b0 parse contract, §4 malformed
slot-1 table, §7 testability).
**FIXME anchors**: 0642 (unwind checklist), 0628 (rejection-matrix seed +
`check-gate-leak` class), 0644 (no-bump judgment — /qa position in §7.4).

Discipline reminders binding on this plan: REDs are failing-not-ignored; every
fix pairs a `/dev` unit test in the same change-set (METHOD §2.2); every
deferral to the unit tier ENUMERATES its cases (S108 Inc2); fixtures are
stdlib-free; language-semantics rows run all modes (load-bearing cells ×3
explicitly, the rest REPL with one mode-uniformity guard per rejection CLASS);
RC-trace rows run serially.

**Wave gating**: §1/§2/§5-constrained rows are leg (a) — independent of (b),
either order. §3 declaration/echo/kind rows land with **b2** (the settled form
is a parse error until b0, and the gate until b2); the §3.5 parse-diagnostic
rows land at **b0**; §6 round-trip rows at **b1** (conventional half) / **b2**
(HK half). Stage-1 authoring order: write ALL rows as failing tests up front
where the form already parses (leg a, §3.1 declaration rows use existing-parse
shapes); rows needing the b0 parse land immediately after b0 merges — still
before the b2 `/dev` change-set (QA-first per wave, not per sprint).

---

## 1. Leg (a) — §5.1.2 back-flow: positives, boundary, negatives

File: `tests/multi_arity_clause_param_51_2.rs` (REWRITTEN in place — see §2)
+ `tests/spec_05_definitions.rs`. All rows `// spec: spec/05-definitions.md
§5.1.2` unless noted.

| Row | Cell | Test (proposed name) | Modes | Status |
|---|---|---|---|---|
| MS-1 | **The rp4 anchor** — un-annotated delegating self-call compiles + runs; type is `(Fn [Int Int] Int)` | `rp4_unannotated_backflow_accepted_and_runs` — the 0642 program verbatim; assert accept, `(rp4 3 4)` runs, and `/sig rp4` (REPL) shows `(Fn [Int Int] Int)` — the TYPE assertion is load-bearing (back-flow pinned, not re-generalized) | ×3 (`run_through_all_modes` for the run facet; sig facet REPL) | [S112] — RED |
| MS-1b | `[oracle]` heap-integrity fence on the flagship new shape | `rp4_link_repeated_dispatch_does_not_corrupt_heap` — the MS-1 program driven K× (200–2000 iterations) under `--link`; assert exit 0 (the sustained-repetition pattern, `tests/CLAUDE.md`) | `--link` | [S112] — graduates into the S113 oracle lane |
| MS-2 | Poly+concrete non-overlapping multi-sig | `poly_clause_nonoverlapping_arity_accepted_both_dispatch` — `(defn f ([:a x] x) ([:Int x :Int y] (add-i64 x y)))`; `(f 5)`→5, `(f "s")`→"s", `(f 2 3)`→5 (the §5.1.2 admissible-poly example, both clauses exercised at two instantiations) | ×3 | [S112] — RED |
| MS-3 | Same-spelled written var does NOT link across clauses | keep/verify `multi_arity_same_written_var_independent_per_clause` (C-4 row, S110): `:a` in clause 1 pins Int by own body, `:a` in clause 2 pins String — separate functions ⇒ separate vars. GREEN control; the over-linking guard for the back-flow implementation | ×3 | must-hold GREEN |
| MS-4 | Sibling-call type MISMATCH is a plain type error | `backflow_pinned_param_call_with_wrong_type_rejected_neg` — rp4 accepted, then `(rp4 "x" "y")` is a clean String≠Int type error, **never** a wrong-accept and **never** `<invalid:`/heap-garbage output — the durable memory-safety negative the dissolved "multi-arity UAF saga" leaves behind | REPL + `--run` | [S112] — RED (pairs with MS-1) |
| MS-5 | §5.1.1 same-arity-unifiable WITH a call still errors | `same_arity_unifiable_clauses_call_site_ambiguous_neg` — `(defn f ([:Int x] x) ([:a x] x))` + `(f 1)` → dispatch-ambiguity error (≥2 variants match) | REPL + `--run` | [S112] — verify vs HEAD (expected GREEN-able today; keep as must-hold if so) |
| MS-6 | §5.1.1 same-arity-unifiable at the DEFINITION (no call) | `same_arity_unifiable_clauses_definition_site_error_neg` — same defn, NO call site; §5.1.2 MUST: "reported at the definition (both colliding clauses named)". RED expected — the current impl only catches strict-equal duplicates at the definition (flag F1 ruling, §7.1) | REPL + `--run` | [S112] — **RED**, owner /dev(typecheck), trigger = this row + §5.1.2 MUST |
| MS-7 | Genuinely-unpinned local → §3.11, with the EQ-twin | `unpinned_local_in_clause_matches_standalone_twin_neg` — twin fixture (highest-signal shape): `(defn f ([n] (let [u []] n)) ([a b] a))` AND the standalone `(defn f1 [n] (let [u []] n))`; SAME assertion both: §3.11-class ambiguous diagnostic. The equivalence rule is the invariant, robust to the monomorphic-let boundary | REPL + `--run` | [S112] — RED on the multi-sig half if HEAD still uses the pre-drain scan message; twin half expected GREEN |
| MS-8 | Diagnostic re-grounding | `ambiguous_clause_diagnostic_cites_standalone_equivalence` — the §3.11 multi-sig diagnostic names clause arity + param (kept) and NO LONGER claims "each arity clause is type-checked independently (§5.1.2)" (the false rationale, `finalize.rs:45–48`) | REPL | [S112] — RED on message facet |
| MS-9 | Duplicate-sig definition reject unchanged | existing `defn_multi_clause_duplicate_sig_neg` | — | must-hold GREEN |
| MS-10 | Dispatch floor unchanged | existing `defn_multi_clause_arity`, `defn_multi_clause_type_dispatch` | — | must-hold GREEN |

Unit-tier enumerated deferrals (`/dev` typecheck, same change-set as the fix;
per S108 Inc2 the cases are named here): (u1) post-drain per-clause
`allowed_vars` = free vars of the settled scheme — one test per boundary side
(poly-admissible vs genuinely-unpinned) at the `find_ambiguous_top_level_form`
seam; (u2) drain-then-mangle ordering: no `$Var` **Concrete** entry survives
finalize when back-flow pins the params (the §11.3(B) invariant); (u3)
`SigDispatch`/entry-name agreement derives from ONE `mangle_sig` call (P7);
(u4) the `AmbiguousForm` message pin update — see §7.3.

## 2. The unwind — superseded-asset checklist (0642 + the ones 0642 missed)

Every asset below asserts the DRIFTED §5.1.2 (independence / no-back-flow /
"cannot stay polymorphic"). Disposition column is binding on `/testing`;
**Preserved facet** is the coverage-impact guard — the thing the converted test
MUST still pin (§9). Conversions are Stage-1 work: rewrite FIRST (they go RED),
leg (a) flips them GREEN. Strip stale present-tense `// defect:` framing per
`tests/CLAUDE.md`; the B-1/B-2 history lives in git.

| # | Asset | Today | Disposition | Preserved facet |
|---|---|---|---|---|
| UW-1 | `multi_arity_clause_param_51_2.rs::rp4_delegating_let_body…rejected` (GREEN guard) | asserts REJECT of back-flow | REWRITE → MS-1 (accept + type) | the rp4 shape itself stays the anchor |
| UW-2 | `…::rp2_body_ascription…rejected` (GREEN guard) | asserts REJECT of `:a`-ascribed self-call | REWRITE → accepting: ascription over the pinned self-call return is a CHECK (§3.3.3), compiles, runs | body-ascription × self-call shape kept |
| UW-3 | `…::rp15_leaf_body…should_reject` (RED B-2) | asserts REJECT of leaf-var clause | REWRITE → accepting (`(Fn [Int Int] Int)` via sibling pin); `(rp15 "x" "y")` → clean type error | the cross-batch memory-safety observable: no heap-ptr-as-Int read — now guaranteed by the type error, assert its ABSENCE stays |
| UW-4 | `…::rp19_mirror…should_reject` (RED B-2) | asserts REJECT + `<invalid:1>` absence | REWRITE → accepting (String-pinned); `(rp19 1 2)` → clean type error AND still no `<invalid` | the deterministic `<invalid:` negative — keep the assertion verbatim |
| UW-5 | `…::lf1_leaf_literal…should_reject` (RED B-2) | asserts REJECT of unused poly param | REWRITE → accepting: genuinely-poly clause admissible (§5.1.2 example class); `--run` exit 42 becomes the POSITIVE | unused-param + literal-leaf shape kept |
| UW-6 | `…::lf2_leaf_body_returns…should_reject` (RED B-2) | asserts REJECT of `([:a p] p)` | REWRITE → accepting — this is the spec's own admissible example `([:a x] x)` | leaf-identity shape kept |
| UW-7 | `spec_05_definitions.rs` 0432 Face-B trio: `multi_clause_defn_self_call_repl_clean_error_not_panic` / `…_run_clean_error` / `…_repl_equals_run_neg` | assert `ambiguous type` on unannotated `sum-to` | REWRITE → accepting: `sum-to` now compiles, `(sum-to 5)` = 15, ×3 modes. **NOT in 0642's list — caught here** | the three NEGATIVE facets survive verbatim: no panic banner / no `build_mangled_name`/`non-concrete param` leak, session stays alive, REPL ≡ `--run` output |
| UW-8 | `spec_05_definitions.rs::defn_multi_arity_unpinned_clause_ambiguous_error_names_clause_neg` (SS-3) | asserts error on the OLD spec error-example (delegating clause) | RETARGET → the fixture now compiles; move the diagnostic-quality assertion onto MS-7/MS-8's genuinely-unpinned fixture | diagnostic names clause + param, no `__` binder leak (0568) |
| UW-9 | `spec_03_types.rs` OA-1a/OA-1b (RED wrong-reject) | assert accept, RED today | KEEP — they flip GREEN at leg (a); refresh the "Flips at CS-4" comments | — |
| UW-10 | `spec_03_types.rs` OA-3 `multi_clause_unconstrained_let_bound_param_rejected_per_clause` (GREEN) | asserts per-clause reject of let-bound param | RE-GROUND as MS-7's twin discipline: disposition must EQUAL the standalone twin's. If the standalone twin rejects (monomorphic-let), keep with the new message; if it accepts, convert. `/testing` verifies the twin on HEAD and records which | the multi-sig/standalone equivalence itself |
| UW-11 | `spec_05_definitions.rs` FV-12 `multi_arity_unpinned_free_var_variant_ambiguous_not_unknown_type_neg` | SOFT disposition pending I-C | HARDEN: I-C is settled — delegation pins ⇒ accept; keep the hard negatives (never `unknown type`, never silent sibling-`:Int` acquisition into a DIFFERENT var) | the two hard negatives |
| UW-12 | Face-A pair `defn_multi_clause_annotated_self_call{,_minimal_repro}` (RED, `undefined function`) | assert accept | KEEP — flip GREEN at leg (a)(B) mangle-ordering fix; they are the acceptance for §11.3(B) | — |

**Traceability re-pointing (owed by /qa, §9):** no `spec/` band cites a
retired-name asset today (verified: `multi_arity_clause_param_51_2` tests are
uncited; §5.1.2's heading cites the surviving `defn_multi_clause_arity`).
Bands to touch at Phase 6: §5.1.2 heading gains the MS-1/MS-2 cites (the
`[S112]` marker is placed now); §3.7 (03-types) + §7.11/§7.3.4/§5.4.4 bands
re-verified after the §3 migrations (names may survive; citations re-run
through `plan/spec_coverage_reconcile.py`). Nothing is dropped.

## 3. Leg (b) — the trait/impl rejection matrix

Files: `tests/spec_07_traits.rs` (+ frontend parse rows may sit beside them).
Spec cites per row: §7.2.1 (declaration), §7.3/§7.3.4 (form), §7.3.5 (kinds).
`// defect: class=check-gate-leak locus=crates/cranelisp-typecheck/src/traits/{registry,impl_check}.rs found=S110 owner=/dev` on TB-1/TB-2/TB-9/TB-10.
All rejection rows are b2-gated except §3.5 (b0).

### 3.1 Declaration-time (the A2 move — reject at `deftrait`)

| Row | Cell | Test | Modes | Status |
|---|---|---|---|---|
| TB-1 | never-applied `(X a)` head → declaration reject | `deftrait_parenthesized_head_never_applied_rejected_neg` — `(deftrait (Zeroable a) (zed [] :a))` rejected AT the deftrait; diagnostic names the fix (`self` + bare head, the §5.1 step-1 wording) | ×3 (mode-uniformity load-bearing) | [S112] — RED (silently accepted today) |
| TB-2 | the check-gate-leak flip | `hkt_shape_on_primitive_no_backend_undefined_function_leak_neg` — the full 0628 repro transcript (`deftrait`+`impl`+`(zed)`/`:Int (zed)`); assert clean §7.2-class diagnostics only, and assert ABSENCE of `undefined function` and `codegen error` anywhere | ×3 | [S112] — RED |
| TB-3 | `:a 7` display defect row | `bare_convar_container_never_displays_unresolved_var` — the `(Container a)`/`(unwrap 7)` transcript; decl now rejects; assert `:a 7` NEVER appears in output | REPL | [S112] — RED |
| TB-4 | applied head still registers | `hkt_deftrait_applied_convar_registers` (existing `hkt_deftrait_declaration_…succeeds` covers; verify + keep) | — | must-hold GREEN |
| TB-5 | bare head + `self` registers (conventional) | existing §7.1.1 coverage (`trait_method_no_impl_then_recovery`) | — | must-hold GREEN |
| TB-6 | occurrence rule stays DISTINCT | `no_occurrence_method_rejected_as_nothing_to_dispatch_neg` — a bare-head trait whose method mentions the implementing type nowhere → "no occurrence" diagnostic, NOT an HKT/kind message (§7.1.1 "the diagnostic MUST name the correct reason") | REPL | [S112] — verify vs HEAD |

### 3.2 Slot-1 echo × trait kind (the 3-valued axis × {conv, HK})

Fixtures: conventional `Display`-like (bare head + `self`), HK `Functor`
(applied `f`). b2-gated.

| Row | echo | kind | Expect | Test | Status |
|---|---|---|---|---|---|
| TB-7 | None (bare) | conv | ACCEPT (today's form) | existing corpus post-migration | must-hold |
| TB-8 | Some-matching `(Functor f)` | HK | ACCEPT | `hkt_impl_new_form_echoed_head_accepted_dispatches` — the §7.3.4 Option example, `fmap` runs | [S112] — RED (parse error until b0, gate until b2) |
| TB-9 | None (bare) | HK | REJECT, diagnostic NAMES THE NEW FORM | `old_form_hkt_impl_bare_head_rejected_names_new_form_neg` — `(impl Functor Option …)` → "trait `Functor` is higher-kinded; its impl head must echo the declared form `(Functor f)`" | [S112] — RED (accepted today — the migration-era diagnostic; highest user-facing value) |
| TB-10 | Some `(Display x)` | conv | REJECT (shape) | `conventional_impl_parenthesized_head_rejected_neg` | [S112] — RED |
| TB-11 | Some-mismatched `(Functor g)` | HK | REJECT (spelling), located diagnostic names BOTH spellings + the verbatim form (§5.4 step 3 "Spelling") | `hkt_impl_echo_wrong_convar_spelling_rejected_neg` | [S112] — RED |

### 3.3 Slot-2 × impl-target kind (§7.3.5 table, one row per worked example)

| Row | Case | Test | Status |
|---|---|---|---|
| TB-12 | Case 1: conv × primitive/applied/poly-applied targets accept | existing corpus (`user_trait_simple`, `polymorphic_impl_on_concrete_adt_instantiation`) | must-hold |
| TB-13 | Case 1 sole reject: conv × bare constructor | `conventional_impl_bare_constructor_target_rejected_neg` — `(impl Display Option …)` → "constructor, not a type; apply it" | [S112] — verify vs HEAD (likely RED on message) |
| TB-14 | Case 2 × primitive | `hkt_impl_pairing_primitive_rejected_neg` — `(impl (Functor f) (Functor Int) …)` → "not a type constructor" (migrate existing `hkt_impl_on_primitive_type_is_rejected_neg` to the new form — keep its name if practical, the §3.7 band cites it) | [S112] — REWRITE of the GREEN guard |
| TB-15 | Case 2 × fully-applied type | `hkt_impl_pairing_fully_applied_rejected_neg` — `(Functor (Option Int))` → "bare constructor, not an applied type" | [S112] — RED |
| TB-16 | Case 2 × wrong-arity ADT | migrate `impl_hkt_arity_neg_prelude_provided_target_wrong_arity_rejected` (2-param `Pair`-like) to the new form; message per §7.3.5 | [S112] — REWRITE (message drift named in §5.4) |
| TB-17 | Case 2 × well-kinded ADT | TB-8 doubles as this positive; add the second constructor (List-like) for the two-impl dispatch positive | [S112] |

### 3.4 Registry-guard / display fix (the `:a 7` root, §5.1 step 3)

| Row | Cell | Test | Status |
|---|---|---|---|
| TB-18 | genuine HK trait registered via the HKT path — method result displays CONCRETE type | `hkt_method_result_displays_concrete_type` — post-b2 `fmap` result echoes `:user/Option …`, never a bare var | [S112] |
| TB-19 | `TraitDeclInfo.type_params` non-empty ⟺ HKT — no usage re-scan | unit-tier, `/dev`(typecheck), enumerated: (u5) `register_trait_decl` routes on `type_params` alone; (u6) the two former usage-derived derivations (`registry.rs:117–126`, `impl_check.rs:39–92`) are gone — `/review` grep criterion (P24) | unit + structural |

### 3.5 Malformed slot-1 parse diagnostics (frontend §4 table — b0)

One e2e per row of the design table, all located `parse_err`s naming the fix;
plus the grammar-parity pin at unit tier (`/dev` frontend, design §7):
`(impl (Functor) …)`, `(impl (Functor f g) …)`, `(impl () …)`,
`(impl ((Functor f)) …)`, `(impl (functor f) …)`, `(impl (Functor 3) …)`.
Proposed: `impl_head_malformed_variants_located_parse_errors_neg` (one test,
six sub-asserts, or six tests — `/testing` picks). [S112] — RED at HEAD
(today they all die on `expect_symbol` with the generic message; the ROW
asserts the per-shape diagnostic). Unit deferral enumerated: the §7 list
(bare→`None`, `(Functor f)`→`Some`, qualified `(fmt/Functor f)` D-qual split,
parity pin). **F2 ruling adds the lowercase row — §7.2.**

## 4. Arch-mandated gates (A4 + determinism + acceptance)

| Row | Gate | Test / process | Status |
|---|---|---|---|
| AG-1 | Stale schema-20 cache → WHOLESALE refusal | `tests/cache.rs` (CW-S1/KC-W0-3 template): cold-build under 21, then a doctored/pre-bump schema-20 cache is refused wholesale — never partially deserialized; specifically a cached now-rejected `(X a)` trait CANNOT resurrect via cache-hit typecheck bypass (the CS-2/P25 class) | [S112] — author with the b2 change-set |
| AG-2 | Mode-uniformity of every new rejection CLASS | one `run_through_all_modes`-style compile-error-equivalence guard per class: declaration-reject (TB-1), echo-mismatch (TB-9), kind-reject (TB-14), §5.1.1 definition reject (MS-6), §3.11 clause diagnostic (MS-7) — REPL ≡ `--run` ≡ `--link` on the diagnostic core (the E3 extract-and-compare pattern) | [S112] |
| AG-3 | `.meta.json` byte-identity with a polymorphic clause present | two FRESH cold builds of a fixture project containing MS-2's poly clause + a constrained clause (CP-1); byte-compare `.meta.json` — pins §11.5 (normalized `$Var` spelling, no session-dependent `t{id}`/`TyCon{id}`) | [S112] — RED-able only post-landing; author at Stage 1, gate at Phase 6 |
| AG-4 | Return-type dispatch end-to-end ×3 modes ×{annotation, argument-context} | `tests/spec_07_traits.rs`: `(deftrait Zeroable (zed [] self))` + Int/Float impls; {`:Int (zed)` → 0, `(add-i64 (zed) 5)` → 5} × {REPL, `--run`, `--link`} = the leg-(c) acceptance lattice (6 cells; §11.6). Plus the unresolved-use control: bare `(zed)` → clean §3.11, mode-uniform | [S112] — expected PARTIALLY RED (the 0628-tail leak is mode/context-dependent; these cells ARE the pinned cross-mode repro the standing dual-path rule demands — attribution follows whichever cells fail) |
| AG-5 | Before/after acceptance run over the existing green multi-sig corpus | PROCESS gate, not a test: at the leg-(a) wave close `/sprint` runs the suite + examples + exemplar + stdlib-conformance before and after; previously-accepted multi-sig programs keep identical results (the /arch blast-radius argument made empirical). `/qa` verifies at Phase 6 | process |
| AG-6 | Prelude fixtures clean of `(X a)` at b1 | re-verify `tests/fixtures/preludes/*.cl` — verified clean at Phase 3 (4 deftraits, all bare heads); re-run the grep at b1 (gate) | process |

## 5. The constrained-poly × multi-sig cell (§11.4, USER-RULED in-scope)

Prelude: `TestStandard` (Num + `+`). Files: `tests/spec_05_definitions.rs` or
the rewritten §5.1.2 file.

| Row | Cell | Test | Modes | Status |
|---|---|---|---|---|
| CP-1 | Constrained clause admitted + dispatches + monomorphises | `constrained_clause_nonoverlapping_arity_dispatches_two_instantiations` — `(defn g ([:a x] (+ x x)) ([:Int x :Int y] (add-i64 x y)))`; `(g 3)`→6, `(g 1.5)`→3.0 (TWO mono instances of the clause template), `(g 2 3)`→5 | ×3 | [S112] — RED (rejected-by-construction today) |
| CP-1b | `[oracle]` RC balance on constrained-clause dispatch | `constrained_clause_dispatch_loop_rc_balanced` — CP-1's `(g x)` in a loop under `CRANELISP_RC_TRACE=1` (serial, `--test-threads=1`); assert alloc/free balance bounded | REPL | [S112] — graduates into the S113 oracle lane |
| CP-2 | Same-arity constrained × concrete overlap still errors | `constrained_clause_same_arity_concrete_overlap_ambiguous_neg` — `([:a x] (+ x x))` + `([:Int x] x)` → dispatch-ambiguity (can-unify, §5.1.2 overlap rule) | REPL + `--run` | [S112] — RED-or-verify |
| CP-3 | Unsatisfied constraint at the call site | `constrained_clause_unsatisfied_constraint_call_rejected_neg` — `(g "s")` where String lacks Num → clean constraint error, not a codegen leak | REPL | [S112] |
| CP-4 | Determinism rider | CP-1's fixture joins AG-3's byte-identity project | — | — |

Unit deferrals enumerated (`/dev` typecheck): (u7) `resolve_variant_types`/
`register_mangled_variants` bifurcation — a constrained clause keeps its
`Constrained` template, never a bogus `Concrete{got_slot}` (§11.4 step 2);
(u8) `collect_single_sig_defns` filter removal feeds constrained `__vN` to
`detect_constrained_fns` (step 3); (u9) drain dispatch to a `Constrained`
entry routes through monomorphisation and records the INSTANCE (step 4).

## 6. Round-trip (b1/b2) — the form-agnostic-serializer proof

| Row | Cell | Test | Status |
|---|---|---|---|
| RT-1 | New HK impl form through `/sexp` + `/source` | `hkt_impl_new_form_sexp_and_source_roundtrip` — enter the §7.3.4 form; `/sexp`/`/source` output re-parses to the same `TraitImpl` (assert re-entry of the emitted text succeeds + dispatches) | [S112] — b2 |
| RT-2 | `user.cl` session regeneration | `hkt_impl_new_form_persists_and_reloads` — define, `/quit`, restart on the regenerated `user.cl`; trait + impl live, method dispatches (the design-§6 verbatim-slice + `render_decl_sexp` fallback both exercised if practical) | [S112] — b2 |
| RT-3 | Conventional `self`-form round-trip at b1 | migrate `repl_persist.rs:1180` (old `(Sizeable a)` decl) to bare-head+`self`; the SAME regeneration assertion — the b1 conventional-half guard | [S112] — REWRITE at b1 |

## 7. Flag rulings (QA authority; none language-normative)

**7.1 — §5.1.1 same-arity-unifiable overlap (from `/design` typecheck).**
RULED: call-site coverage is NOT sufficient. §5.1.2 states a MUST on the
report site — "a dispatch-ambiguity compile-time error, **reported at the
definition** (both colliding clauses named)". The current strict-equal-only
definition check under-implements it. MS-6 is the failing-not-ignored record
(owner `/dev` typecheck). Recommendation to `/sprint`: land in-sprint — the
wave already rewrites the finalize ambiguity scan, and a post-drain pairwise
`types_compatible` sweep over same-arity variants is small; but MS-6 is not a
sprint gate if the wave lands without it (it stays RED with attribution).

**7.2 — con_var lowercase enforcement (from `/design` frontend §8.2).**
RULED: matrix row NOW, not a recorded gap. Grounds: spec-clear
(`con_var = lowercase_symbol`, §7.2 grammar), and b0 creates the ONE seam
(the shared head-shape helper) where a single fix covers `deftrait` AND
`impl` — deferring re-opens exactly the two-parser drift window the helper
exists to close. Row: `trait_head_uppercase_convar_rejected_both_forms_neg`
— `(deftrait (Functor F) …)` and `(impl (Functor F) …)` both rejected at
parse, located, naming the lowercase rule. [S112] — RED; owner
`/dev`(frontend); small, rides b0 if `/sprint` agrees, else carries with
attribution.

**7.3 — `program/tests.rs:27` message-assertion churn (from `/design`).**
Note for `/dev` (not a plan row): `ambiguous_form_message_names_clause_and_param`
pins the §5.1.2 citation + clause/param naming + no-`__`-leak. When the
message re-grounds (MS-8), update THIS pin in the SAME change-set: keep the
structural assertions (clause arity, param name, no `__`), replace the
"§5.1.2 independence" expectation with the settled §3.11/standalone-equivalence
wording. A separate churn commit or a stale-message-passing pin are both
wrong.

**7.4 — FIXME 0644 position (leg (a) no-schema-bump; /arch rules).**
POSITION: the design's no-bump rationale has a hole. §11.4 argues "a stale
schema-20 cache from the old compiler cannot contain the newly-reachable
state, since that compiler rejected the program" — FALSE for the B-2
wrong-accept family: `lf1`/`lf2`/`rp15`/`rp19`-shaped programs were ACCEPTED
by the old compiler in all modes (committed wrong-accept guards prove it) and
`register_mangled_variants` force-installed bogus `Concrete{got_slot}` entries
over `Var` params (`$Var` mangles) that persist in schema-20 `.meta.json`.
A schema-20 cache CAN therefore contain multi-sig overload state whose
meaning the new compiler reinterprets (a `$Var` **Concrete** entry where the
new model mandates a `Constrained`/poly template — §11.3(B) "no `$Var`
concrete entry survives"). Cache-hit typecheck bypass on an unchanged source
file would resurrect it — the same CS-2/P25 class A4 names for leg (b).
RECOMMENDATION: leg (a) lands inside the SAME 20→21 window as b2 (one bump
covers the sprint, the S110 precedent /arch already cites) — the cheap,
structurally-safe answer; the ordering freedom "(a) ⊥ (b), either order"
then carries one constraint: leg (a) merges before or with the b2 bump, or
takes the bump itself if it lands alone. If /arch instead keeps no-bump, it
must name the mechanism that invalidates the B-2-era bogus entries
(source-hash alone does not — the source is unchanged). AG-1's stale-cache
row then extends to a leg-(a) fixture (a cached `$Var`-entry module). FIXME
0644 stays /arch's to resolve; this position is recorded here and in the
report to /sprint.

## 8. Quality-risk assessment (USER-MANDATED)

Full register entry: `risks.md` §"S112 risk read". The verdict:

**Where this wave can break something the suite cannot see:**

1. **Wrong-accept inversion at the collapsed scan (highest).** Deleting
   `ClauseIndependence` leaves the post-drain ValueScan as the ONLY §3.11
   guard for multi-sig clauses. The failure mode is not wrong-reject (loud,
   caught by MS-1/MS-2/CP-1) but wrong-accept: a mis-computed per-clause
   `allowed_vars` classifies a genuinely-unpinned param as "admissibly
   polymorphic" — publishing a free-var scheme, the exact S110/S111
   `wrong-accept` class whose downstream face is a non-output-perturbing
   wrong-type read the suite is ~97% blind to. Cure in-plan: the boundary is
   pinned from BOTH sides at the closest shapes (MS-2/UW-5/UW-6 admissible
   vs MS-7 unpinned twin), and the memory-safety observables are retained
   through the unwind (UW-3/UW-4: type-error + `<invalid:` absence).
2. **Persisted-state reinterpretation without a bump (0644 hole)** — §7.4.
   Silent by construction: cache-hit bypasses typecheck, so no test that
   compiles fresh can see it. AG-1 + the §7.4 recommendation close it.
3. **Mangle/dispatch split-brain.** If entry-name and `SigDispatch` name ever
   derive from two mangle calls (pre- vs post-drain), the divergence surfaces
   as `undefined function` (loud) OR as dispatch to a stale `$Var` entry
   (silent wrong-code). AG-3 byte-identity + unit u2/u3 + AG-5 pin it.
4. **Trait-gate re-land regressing the green HKT corpus.** The S111 CS-4
   prototype was reverted precisely because the naive gate regressed 5 e2e;
   the re-land is NOT verbatim (A2 moved rejection to declaration time).
   The migration lists + TB must-holds + AG-5 are the fence; the named 5
   migrate deliberately (§9), not silently.
5. **Ownership-relevant behaviour: what changes, what rides uncovered.**
   This wave adds NO new ownership mechanism — admitted poly/constrained
   clauses monomorphise through the EXISTING `pass4_monomorphise`/
   `ConstrainedFn` machinery, and back-flow only pins types earlier.
   What changes is the REACHABLE PROGRAM SET: shapes that never reached
   codegen (poly clauses, constrained multi-sig clauses, back-flow-pinned
   recursion) now produce mono instances that flow through the ownership
   walk and RC insertion. Until the S113 oracle lane lands, a false-`Fresh`/
   RC-miscount on one of these NEW shapes with a non-output-perturbing face
   would be invisible — that risk rides uncovered, accepted by the S112
   scope ruling. **Targeted this-sprint mitigation (no S113 pull-forward),
   using only established observables:** (i) MS-1's `--link` cell repeated
   K× (the sustained-repetition pattern, exit-0 — heap-integrity fence on
   the flagship new shape); (ii) one serial RC-trace balance assertion over
   CP-1's constrained-clause dispatch loop (alloc/free imbalance bounded).
   Two rows, cheap, both reuse existing harness machinery — add as MS-1b /
   CP-1b. Everything deeper (generative harness, ASAN lane) stays S113.
6. **Mode divergence on the new diagnostics** — every new reject is a new
   opportunity for the REPL/`--run`/`--link` split; AG-2 makes each CLASS
   loud once rather than sampling cells.

## 9. Coverage-impact assessment (USER-MANDATED)

**The unwind (§2) narrows nothing:** every conversion row names its preserved
facet; the three facets at risk of silent loss are called out explicitly —
UW-7's no-panic/session-alive/mode-equality negatives, UW-4's deterministic
`<invalid:` read-absence, UW-8's diagnostic-quality assertions (clause+param
named, no `__` leak). §5.1.2/§7.x traceability: no band cites a retired name
(verified by grep); the two bands citing MIGRATING names (§3.7 in 03-types →
`hkt_impl_on_primitive_type_is_rejected_neg`; §7.11 →
`trait_deftrait_impl_in_child_module_imported_dispatch_from_parent`) survive
if `/testing` keeps the names through migration (recommended); either way
`spec_coverage_reconcile.py` runs at Phase 6 and I re-point directly.

**The fixture migration (~24 unit + ~7 e2e) — what each asset exercises and
the preservation rule.** The S111 lesson operationalized: the corpus WAS the
authority that made `(X a)`-as-parametric look right, so migration fidelity
is checked per-asset, not in bulk.

*E2e (grep-derived enumeration; `/testing` completes with
`grep -rn "(deftrait (\|(impl Functor" tests/` at authoring — my sweep found
the §5.4 five plus three more):*

| Asset | What it currently exercises (must survive migration) |
|---|---|
| `spec_05_definitions::deftrait_with_docstring_and_method_docstring_does_not_affect_dispatch` (:964, `(Sizeable a)`) | BOTH docstring positions (trait + method) not affecting dispatch — migrated bare-head+`self` form keeps both docstrings AND the dispatch call |
| `spec_07_traits::trait_deftrait_impl_in_child_module_imported_dispatch_from_parent` | cross-MODULE decl/impl/import/dispatch — must stay cross-module (§7.11 band cites it) |
| `spec_07_traits::impl_hkt_arity_neg_prelude_provided_target_wrong_arity_rejected` | wrong-arity target NEGATIVE via a PRELUDE-provided target (provenance axis!) — TB-16 keeps both the arity reject and the prelude provenance |
| `repl_introspection::bare_user_trait_lookup_impl_section_lists_type_not_others` | introspection `; impl:` completeness + its NEGATIVE (other types absent) |
| `repl_introspection::impl_form_display_result_is_exactly_impl_trait_for_type` | the exact display envelope (int `format_trait_display` — shifts at b2, the int-surface reconciliation the frontend design flags) |
| `repl_persist.rs:1180` (`(Sizeable a)` decl) | deftrait through `user.cl` regeneration — becomes RT-3 |
| `spec_07_traits.rs:476` (`(Classify a)`) | per-asset: `/testing` records what its assertion pins before rewriting |
| `spec_05_definitions.rs` §5.3.2 + `repl_introspection.rs:1786/1889/2623/3879/3904` (`(Sizeable a)`/`(Display a)` old-style decls) | introspection/display of trait decls — migrate to bare-head or applied-HK per what each asserts; any asset that POSITIVELY exercised the never-applied `(X a)` form converts to a TB-1-class NEGATIVE (the form is now the reject), never deletes |

*Unit (~24 `TraitDecl{type_params: vec!["a"]}` hand-built fixtures across
`traits/{dispatch,impl_check}/tests.rs`, `traits/test_helpers.rs`,
`checker/tests.rs`, `infer/tests.rs`, `form/tests.rs`, `program/tests.rs`).*
Rules binding on `/dev` + `/review` (unit tier is `/dev`'s; these are the
enumerated preservation criteria):

1. **Hand-built fixtures bypass the parse + registration gates** — a wrong
   migration (empty `type_params` without `SelfType` methods) silently
   changes what the fixture feeds the seam under test. The
   constraint-carrying trio (`register_num_trait_inline`,
   `register_num_for_int`, the inline `Double` decl) REQUIRES `SelfType`
   methods so the `Num self` constraint still drives constrained-fn
   detection (the resolved-0639 grade split; S111 CS-4 green-verified
   mechanics in git history are the reference).
2. **Per-fixture mapping recorded in the change-set** (old head shape → new
   shape → assertion subject unchanged); `/review` checks the table against
   the diff — a fixture whose assertion subject changed is a REJECT.
3. **Dispatch-only fixtures** → bare head + empty `type_params`; genuinely-HK
   fixtures keep `type_params` AND gain an applied use (they must satisfy
   the new declaration invariant the registration gate now enforces —
   otherwise the fixture models a state the language can no longer produce,
   the exact mismodel class this sprint retires).
4. Any unit fixture that positively pinned the OLD usage-scan guard
   (`registry.rs:117–126` routing) converts to pinning the NEW
   declaration-derived routing (TB-19 u5/u6), not deleted.

## 10. Expected suite delta (Phase-6 audit input)

Flips GREEN at leg (a): OA-1a/OA-1b, Face-A pair (UW-9/UW-12), the §2
conversions (RED at Stage 1 → GREEN at landing). Flips at b2: §3 TB rows,
AG-4 cells (or leg-(c) follow-up per attribution). New REDs carried only
with owner+trigger: MS-6 (if not landed, /dev typecheck), F2 row (if not
landed, /dev frontend). No other REDs without attribution; Phase-7 RED-vs-
known-defect integrity audit runs against this list.

## Next skills

- `/testing` — author Stage-1 failing e2e to §§1–6 (conversion-first for §2);
  complete the §9 e2e enumeration grep; strip stale defect framing.
- `/arch` — rule FIXME 0644 with the §7.4 position (bump-window
  recommendation).
- `/sprint` — scope calls on MS-6 + F2 in-sprint vs attributed-carry; AG-5
  scheduling at wave close.
- `/dev` (typecheck, frontend) — unit deferral lists u1–u9 + §9 unit
  migration rules ride the implementing change-sets.
