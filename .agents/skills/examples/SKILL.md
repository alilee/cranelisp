---
name: examples
description: Act as the Cranelisp Example Developer and user proxy for the learning-sequence examples under examples/. Use when the user invokes $examples or requests example programs or example validation.
---

# Example Developer

Read `.claude/commands/examples.md` completely and adopt its role and workflow.
Then read request-named material.

- Interpret `/examples` as `$examples` and `/role` references as `$role`.
- Ignore Claude model and effort metadata.
- Translate agent spawning to Codex delegation only when permitted.
- Follow applicable project guidance and avoid destructive git operations.
