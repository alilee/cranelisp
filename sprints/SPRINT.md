# Sprint 80: Phase G closeout — platform-ADT round-trip + `main : IO _` conformance sweep

**Status**: ~~PHASE 1–4~~ → ~~Wave 0–2 ✅~~ → ~~Wave 2D (D1/D1b/D2/D3/D4) ✅~~ → ~~Wave 2E Linker encapsulation ✅~~ → ~~Wave 3a e2e infra ✅~~ → **PHASE 5 GREEN (1224/0/9)** — close-prep remaining: consolidated `/review` (D1b/D2/D4) + `/port` + `/spec` annotations → PHASE 6/7 | PHASE 7 CLOSE | COMPLETE

**Goal**: Retire the 8 standing reds by finishing the two pillars S79 deferred — complete the platform-ADT round-trip (pillar A, now incl. the Linux `PLATFORM_EXT` discovery fix) and enforce `main : IO _` suite-wide with an output-coverage reshape (pillar B) — leaving the suite fully green on **both macOS and Linux**.

## Scope

> **2026-06-13 — Linux refresh (post-plan).** S80 was scoped against S79's macOS close (`1196 / 7`). Between the plan landing (`60901d7`) and execution, a **Linux aarch64 porting arc** landed off-plan (`622d3d8` sigsetjmp, `51a8dc3` ELF symbol/GOT relocs, `74da7f1` `--link` ELF standalone exes, `4109c3e` public-api baselines). Re-baselined on the Linux VM: **`1197 passed / 8 failed / 8 skipped`** (full suite, 39s). Findings: (1) the feared "~46 Linux failures" never materialised — the porting arc made the ~38 macOS-only `--link` tests **pass** on Linux (they're in the 1197), NOT skip them. The **8 skips are platform-independent** pre-existing `#[ignore]`s — 5 TCO (FIXME 0141), 2 backend `Code::Primitive` (FIXME 0221/0191), 1 perf-budget — identical count on macOS; **nothing is skipped due to platform**. (2) All **7 originally-targeted reds reproduce identically** on Linux — both pillars' source-level plans hold. (3) There is **one new (8th) red** — `examples::every_example_runs_with_documented_exit` (4 IO examples fail `platform 'stdio'/'test-capture' not found`), root-caused to `src/platform.rs:61` `const PLATFORM_EXT: &str = "dylib"` (hardcoded macOS extension; Linux `--run` can't dlopen platform `.so` at runtime). **Folded into Pillar A** (user 2026-06-13) — it is Pillar A's own file and a prerequisite for Pillar A's round-trip `--run`/cache-restore green-up on Linux. (4) The plan's "serialized green-ups / dyld cold-load tax" discipline was a **macOS dyld** concern; the Linux suite runs clean in 39s with no thrash, so that sequencing constraint **relaxes** (parallel green-ups now safe).

S79 closed at **1196 passed / 7 failed / 8 skipped**; the Linux-refreshed baseline is **1197 / 8 / 8** (the 7 carried-FIXME reds + the new `PLATFORM_EXT` red). S80 drives all 8 to green on macOS **and** Linux. We remain in **Phase G (Ring 4 — Effects/platforms)**; this is its closeout. The two pillars are largely disjoint (platform/backend/qa vs typecheck/qa/examples) → parallelizable across pillars; both converge on `/qa` for the green-up.

### Pillar A — Platform-ADT round-trip completion (clears 6 reds + the Linux `PLATFORM_EXT` red + adds drift coverage)

The platform-ADT-round-trip *machinery* landed S79 (R1 `--link` wiring, 0318 platform-fn-IO, the `shapes` fixture, the product-ctor-as-`Def` correction making `read_field("w")` reachable). The 6 `tests/spec_platforms_adt.rs` tests ride RED on one remaining layer: the test program defines `Rectangle` in its **entry module**, but the platform sig is FQ `shapes/Rectangle`, so module `shapes` has no `Rectangle` to resolve.

- **0323** — make `Rectangle` a loadable `shapes` `.cl` module the program imports (`(import [shapes [Rectangle]])`), per `platform-interface.md` §2 (q-assoc-discovery); ensure the `shapes` ADT module is loaded/resolvable during `register_platform_in_tc` sig checking; regenerate `platforms/shapes/src/shapes.platform-schema` (real `w`/`h` field names post-0319) + rebuild the dylib → round-trip + dual hash-gate + cache-restore go green. **Discovery-shaped**: this path has never run e2e and has surfaced one layer per exercise (FQ-split → resolve_named → arity → display → module-loading); budget a triage step.
- **0289 item 4** — perturbed-ABI DLL e2e (`AbiVersionMismatch { expected, found }`). **Real + wired** (`check_abi_version` at load, `src/platform.rs`); the `shapes-badabi` DLL (declared ABI=2 vs host=3) triggers it. Goes green after the Pillar-A `:Int`→`:primitives/Int` fixture fix (the test currently dies at the same fixture typecheck as the 6 ADT reds). (Items 1-3 — clean round-trip + build-load-embed walk + dual hash-gate — overlap 0323 and complete with it.)
- **0289 item 5 — DEFERRED (user 2026-06-13).** `PlatformError::DispatchError { fn_name }` is **defined but never constructed by any live code path** (only a `#[cfg(test)]` Display unit test at `cranelisp-platform/src/lib.rs:1755`); the IO trampoline calls platform fn-pointers directly with no fault guard. Producing it is a genuine new runtime feature (fault-guarded FFI dispatch — precedent `invoke_jit_protected`/sigsetjmp), out of S80's red-clearing scope. **Disposition:** `/qa` marks `platform_dispatch_error_carries_fn_name` `#[ignore = "<explains: DispatchError has no live construction site; deferred pending a host-side dispatch-fault funnel — NOT a defect, unbuilt scope>"]` (narrow, user-sanctioned exception to failing-not-ignored — it is unbuilt scope, not a defect; comment is the record, **no new FIXME**). `/platform` **removes** the `shapes-dispatch-fail` fixture DLL (+ its workspace member) — its self-describing-stderr was the fake-green trick, and its only consumer is now ignored.
- **Examples platform-discovery via search path (8th red — root-cause CORRECTED Wave-0, approach SET by user 2026-06-13)** — the 8th red (`examples::every_example_runs_with_documented_exit`, 4 IO examples `platform 'stdio'/'test-capture' not found`) is **NOT** a `PLATFORM_EXT` bug: `src/platform.rs:59-65` is **already `cfg`-conditional** (`dylib`/`so`/`dll`). The cause: `examples/platforms/` (git-ignored, machine-local) holds only macOS `.dylib` symlinks that dangle on Linux, and the examples test runs without `CRANELISP_PLATFORM_PATH`, so Tier-1 discovery (`{root}/platforms/{name}.so`) finds nothing. **User decision: drop the symlink convention entirely; put `target/debug` on the platform search path** — `resolve_platform_path`'s `check_dir` already tries cargo's `libcranelisp_{name}.{ext}` naming, so discovery finds `libcranelisp_stdio.so` directly in `target/debug` with zero symlinks, automatically correct on every platform. Done as a **dedicated examples wave (Wave 1E)**, independent of Wave 1. **No `/dev` change** (the `PLATFORM_EXT` no-op is dropped); `/qa` sets the search path in the `run_example` harness + reworks the Wave-0 guard `platform_dll_resolves_on_current_platform` (currently copies `examples/platforms/`) onto the search path + updates the legacy `observability_io.rs` symlink expectation; `/examples` replaces the symlink doc-comments with the search-path mechanism (committed `examples/Cranelisp.toml` listing `../target/debug`, or a justfile recipe — `/examples`'s design call) so the documented `cargo run -- --run examples/…` user workflow works too. **Build-ordering caveat handed to the wave:** the platform `.so`s must be in `target/debug` before the examples test runs (latent in the old symlink model too) — `/qa` ensures the platform cdylibs are built.

### Pillar B — `main : IO _` enforcement + suite-wide conformance sweep (clears 1 red)

The spec MUSTs (`02-grammar.md:25`, `10-io.md:244`, `12-runtime.md:173`) that a batch `main` returns `IO _`; the compiler leniently accepts bare-`Int`, and existing tests positively certify the violation. `batch_main_pure_int_return_is_rejected` rides RED as the forcing guard.

- **0317** — (1) `/spec` confirms the MUST stands as enforceable + upgrades the three stale `[R4 S10]` annotations; (2) **`/dev` (int — `src/exe.rs`)** rejects a batch `main` whose return type is not `IO _`, with a clear `(Fn [] (IO _))` error — a one-arm deletion in `classify_main_return_type` (the `Type::Int` accept arm); **NOT typecheck** (Phase-2 arch correction — typecheck has no clean batch-vs-REPL signal; the REPL-exemption lives at the int execution boundary); (3) `/qa` suite-wide sweep — ~125 batch bare-`Int` mains across ~11 test files → `IO` (`(pure 0)` smoke / `(print …)` observable), fix the 3 test-design defects that certify the violation, rework the examples exit-code-checksum convention; (4) **output-coverage reshape** — `run_through_all_modes_output` stdout harness + convert the mode-equivalence corpus so the majority of programs produce + assert observable output verified byte-equivalent across REPL/`--run`/`--link` (today 3 of 911); (5) `/examples` (~22 files) + `/port` (exemplar inline repros) rewrap their mains; (6) the RED guard flips green.

### Out of scope (deferred, with rationale)

- **0316** — import-ambiguity model decision + `resolve_with_fallback` unification (collapses the S78 5×-duplicated prelude-fallback retry). Design-led, blocks nothing, no red rides on it. Deferred to a focused S81 design increment alongside any other module-resolution debt. *(User chose "both pillars" over the import-model rider.)*
- **Multi-platform `--link`** — the `cranelisp_platform_manifest` symbol collision (single-platform fully wired S79). Future item; not on any S80 red.

## FIXME debt

| FIXME | Target skill | Status | Notes |
|---|---|---|---|
| 0323 | /qa | open | Pillar A — platform-ADT round-trip: `shapes` module loading + schema regen (6 reds). Discovery-shaped. |
| 0289 | /qa | open | Pillar A — **item 4** (perturbed-ABI e2e) lands via the `:Int` fixture fix + `shapes-badabi` DLL. **item 5** (dispatch-error) DEFERRED (user) → `#[ignore]`+comment, no FIXME, `shapes-dispatch-fail` DLL removed (defined-but-never-produced `DispatchError`). Items 1-3 complete with 0323. |
| 0317 | /spec | open | Pillar B — `main : IO _` enforcement + suite-wide sweep + output-coverage reshape (1 red). Largest workstream. |
| 0318 | /spec | open | **Compiler-side enforcement already landed S79** (`require_io_return`, `src/platform.rs:560`, passing tests — Phase-2 arch). Residual = `/spec` text (`08-modules.md:783` conditional→unconditional) + the `shapes` `area`→`(IO Int)` fixture (Pillar A delivers). |
| 0316 | /spec | deferred → S81 | Import-ambiguity model + `resolve_with_fallback`. Out of scope (see above). |
| Examples platform search path | /examples + /qa | carried in-plan (no FIXME) | Pillar A 8th-red — **dedicated Wave 1E** (user 2026-06-13): drop the git-ignored symlink convention, put `target/debug` on the search path (`check_dir` already tries `libcranelisp_{name}.{ext}`). `/qa` harness + Wave-0 guard rework; `/examples` doc + committed config. **No `/dev` change** (`PLATFORM_EXT` already `cfg`-conditional). |

*(Wave-gate scan of `design/arch/fixmes/` for `target: /skill-in-wave` + `status: open` runs before each Phase-5 wave advance. The §7.2 pre-resolve and `PLATFORM_EXT` fix are carried in-plan, not as FIXME files.)*

## Architecture review (Phase 2)

**Verdict: SIGN-OFF** — scope is technically coherent, no cross-crate interface is needed, both pillars are terminal-shaped and cleanly separable. One scope correction (the 0317 enforcement seam is `src/exe.rs`/int, NOT typecheck) and two "already-landed" reconciliations below. `/arch` made **no** `cranelisp-types` change — no baseline regen.

### Decisive public-API questions (answered against source, not FIXME prose)

**Pillar A (0323) — NO `cranelisp-types` / public-API change. Pure `src/` (int) load-path wiring on top of S79 work.** Validated:
- FQ type resolution for `shapes/Rectangle` already works through existing APIs — `parse_and_check_platform_type_sig` (`src/platform.rs:507`) → `fqize_type_expr` (re-partitions the parser's under-qualified `{module:None, name:"shapes/Rectangle"}` into `{module:Some("shapes"), name:"Rectangle"}`) → `cranelisp_typecheck::check_type_expr` → `cranelisp_types::resolve`. The S79 `resolve_named` product-ctor-as-type fix is landed and confirmed at `crates/cranelisp-typecheck/src/resolve.rs:80–114` + test `test_resolve_product_ctor_as_type` (resolve.rs:450). Once `Rectangle` is *reachable in module `shapes`*, it resolves.
- The remaining blocker is purely **module-load ordering**: `shapes.cl` must be in the symbol tables before the platform sig naming `shapes/Rectangle` is checked. `platform-interface.md §7.2` (line 1012) already specifies this as the design target ("resolve + compile associated `.cl` type module(s) … BEFORE sigs"). As-built reaches the same end-state via the orchestrator's `ResolutionGap` auto-load retry (`drive_module_dep`, the FIXME-0268 FQ-autoload path) rather than an inline pre-resolve step — when the sig check hits an unloaded `shapes`, the gap drives `register_dep`+`register_module`+retry-from-top. So the fixture just needs `shapes.cl` resolvable on the ordinary module search path (`resolve_module_file`, `src/pipeline.rs:27` — project tree + `CRANELISP_LIB`, NOT `CRANELISP_PLATFORM_PATH`) and the program/sig to name `shapes/Rectangle`. **No new type, no new public fn.** All resolution machinery exists.
- 0289 items 4-5 (perturbed-ABI + dispatch-error e2e) consume already-landed `PlatformError::{AbiVersionMismatch, DispatchError}` variants (unit-proven in `src/platform.rs`); the e2e just needs the new ADT-typed `shapes` test-DLL fixture. No interface work.

**Pillar B (0317) — NO `cranelisp-types` / public-API change. The enforcement seam is SETTLED, but it is `src/exe.rs` (int), NOT typecheck — FIXME 0317 prose is imprecise.** Validated against source:
- The `main : IO _` gate already exists: `validate_main` (`src/exe.rs:341`) → `classify_main_return_type` (`src/exe.rs:359`). It is called in **both** batch modes — `--link` via `link_by_name` (`session_v4.rs:3911`) and `--run` via `lookup_main_return_type`→`trampoline` (`session_v4.rs:3695`). REPL never calls it (correctly exempt).
- **Enforcement is a one-arm deletion**: `classify_main_return_type` currently has `Type::Int => Ok(MainReturnKind::Int)` (exe.rs:362) — *this* is the lenient acceptance 0317 targets. Removing/tightening that arm (keep the `IO` arm, reject bare `Int` with the `(Fn [] (IO _))` diagnostic) IS the enforcement. `MainReturnKind` keeps both variants only if a transition wants `Int` to warn; for a hard reject the `Int` variant could even retire, but that is an int-internal enum (`src/exe.rs`), not a `cranelisp-types` boundary type — **no baseline impact either way**.
- **Scope correction for `/sprint`/Phase 4**: 0317 item (2) says "`/dev` (typecheck) rejects a batch `main`". That is the wrong owner. Typecheck has no clean batch-vs-REPL signal (it checks both identically; the REPL-exemption lives at the int execution boundary). The enforcement belongs to **`/dev` (int — `src/exe.rs`)**, paired with `/qa`'s sweep. Recommend re-labelling the workstream owner. (No interface decision needed — this is purely "which crate hosts the gate", and source already answers it: int.)

### Already-landed reconciliations (FIXME/source drift caught — flag to `/sprint`)

- **0318 item #2 (the enforcement check) is ALREADY DONE.** `require_io_return` (`src/platform.rs:560`, with the full FIXME-0318 rationale in its rustdoc) is already called in `register_platform_in_tc` (`platform.rs:378`) after `parse_and_check_platform_type_sig`, with passing unit tests (`platform.rs:694/711/729` — rejects `(Fn [Int] Int)`, accepts `stdio` IO sig). So 0318 reduces to: (1) `/spec` text edit at `08-modules.md:783` (conditional→unconditional) + annotation, and (3) the `shapes` fixture `area`→`(IO Int)` (which 0323/0289 deliver anyway). The compiler-side work is closed. 0318 is correctly folded into Pillar B `/spec` + Pillar A fixture — confirm the `/dev` cascade item is marked done-on-arrival.

### Interim-architecture risk (Principle 8)

Both pillars are **terminal-shaped** — no stepping-stones requiring later rework.
- Pillar A rides the already-settled `platform-interface.md` target (user-ratified 2026-06-07, cascaded S76); the load path is the §7.2 sequence; nothing here is provisional.
- Pillar B's enforcement is a permanent tightening of an existing gate, not scaffolding.
- **The output-coverage reshape (0317 item 4) is a `/qa` test-harness detail, NOT an architectural commitment.** `run_through_all_modes_output` is a test-side harness (lives in `tests/`, owned by `/qa`) that asserts byte-equivalent stdout across REPL/`--run`/`--link`. It does NOT change any crate's public surface, any `cranelisp-types` type, or any sequence diagram. The *invariant* it encodes ("observable output is identical across run modes") is real and already lives in `tests/plan/PLAN.md` + Principle 11; 0317 item 4 proposes also stating it normatively in `spec/10-io.md` (a `/spec` decision, precedent `spec/04-expressions.md:850`). `/arch` recommendation: that spec sentence is worth adding (it makes Principle 11's mode-equivalence axiom user-visible and traceable), but it is `/spec`-owned and gates nothing architecturally. The harness shape is `/qa`'s to own outright — no `/arch` artefact moves.

### Pillar separability (parallel D/D/R, serialized green-ups — as user-approved)

Cleanly separable. Pillar A touches `platforms/shapes/` (`/platform`) + `tests/spec_platforms_adt.rs` + `tests/platform_errors.rs` (`/qa`) + possibly `src/platform.rs`/`src/worker.rs` load wiring (`/dev` int, only if the auto-load needs a nudge). Pillar B touches `src/exe.rs` (`/dev` int) + `spec/{02-grammar,08-modules,10-io,12-runtime}.md` (`/spec`) + ~11 `tests/` files + `tests/plan/` (`/qa`) + `examples/` (`/examples`) + `exemplar/` (`/port`).

**Shared collision seams to manage (name them in Phase 4 wave org):**
1. **`/qa` / `tests/`** — both pillars converge on `/qa` for test authorship and the green-up. This is the intended single integration point; serialize the green-ups (already the approved sequencing).
2. **`src/` (int) — `/dev` narrow** — Pillar A *may* touch `src/platform.rs`/`src/worker.rs` (load ordering), Pillar B touches `src/exe.rs`. **Disjoint files**, same crate/skill (`/dev` int). If both fire `/dev` int in the same wave they share the working tree — sequence the two int fires or batch them into one int D/D/R, but they don't logically collide (different functions, different files). NOT a typecheck collision — Pillar B does **not** touch typecheck (correcting the 0317 mislabel removes the only apparent typecheck overlap).
3. **`tests/plan/`** — Pillar B reshapes `PLAN.md §"Mode canonicalisation"`; no Pillar A overlap.

No file is written by both pillars except via the shared `/qa` integration point, which the serialized-green-up discipline already covers.

### Crate-touch map (input for Phase 3/4)

**Pillar A:**
| File / surface | Skill | Note |
|---|---|---|
| `platforms/shapes/src/{lib.rs,shapes.cl,shapes.platform-schema}` | `/platform` | `area`→`(IO Int)` (0318 #3); `shapes.cl` as loadable module; schema regen (real `w`/`h`) + dylib rebuild |
| `tests/spec_platforms_adt.rs` | `/qa` | 6 round-trip/hash-gate/cache tests + the `(import [shapes [Rectangle]])` fixture |
| `tests/platform_errors.rs` (or sibling) | `/qa` | 0289 items 4-5 e2e (perturbed-ABI, dispatch-error) |
| `src/platform.rs`, `src/worker.rs` | `/dev` (int) | §7.2 pre-resolve of the platform's associated `.cl` type-module(s) (Phase-3 finding: `ModuleError`-not-`ResolutionGap`, real change not no-op) |
| `tests/examples.rs::run_example` (harness) + `examples/Cranelisp.toml` or justfile | `/qa` (harness) + `/examples` (doc/config) | **Wave 1E** — put `target/debug` on the platform search path (drop the symlink convention); discovery finds `libcranelisp_{name}.{ext}` directly. Clears the 8th red + the Wave-0 guard. `/qa` ensures platform cdylibs are built. (`PLATFORM_EXT` already `cfg`-conditional — NO `/dev` change.) |
| `tests/platform_errors.rs::platform_dll_resolves_on_current_platform` + `tests/legacy/observability_io.rs` | `/qa` | rework the Wave-0 guard (currently copies `examples/platforms/`) + the legacy `stdio_dll_available` symlink expectation onto the search path |
| _no_ `cranelisp-types` / _no_ typecheck source change | — | confirmed |

**Pillar B:**
| File / surface | Skill | Note |
|---|---|---|
| `src/exe.rs` (`classify_main_return_type`) | `/dev` (int) | remove the `Type::Int` accept arm; emit `(Fn [] (IO _))` reject — **the enforcement seam** |
| `spec/02-grammar.md §2.1`, `spec/10-io.md §10.6/§10.6.1`, `spec/12-runtime.md §12.6`, `spec/08-modules.md §8.11` | `/spec` | 0317 MUST-confirm + annotation upgrade; 0318 conditional→unconditional + rationale; optional `spec/10-io.md` mode-output-equivalence sentence |
| ~11 `tests/*.rs` (~125 mains) + `tests/plan/PLAN.md` | `/qa` | sweep bare-`Int` mains → `IO`; fix 3 violation-certifying test-design defects; `run_through_all_modes_output` harness; flip the RED guard |
| ~22 `examples/*.cl` | `/examples` | rewrap mains; rework exit-code-checksum convention |
| `exemplar/` inline repros | `/port` | rewrap mains |
| _no_ `cranelisp-types` / _no_ typecheck source change | — | confirmed |

### Scope adjustments for `/sprint`

1. **Re-own the 0317 enforcement workstream from `/dev` (typecheck) to `/dev` (int — `src/exe.rs`).** Source-confirmed; not a typecheck change.
2. **Mark 0318's compiler-side enforcement (item #2) as already-landed** (`require_io_return`, S79). 0318 residual = `/spec` text + the `shapes` `area` IO fixture (delivered by Pillar A).
3. No scope is added or cut otherwise; the 7-red target stands.

## Skill plans (Phase 3)

> **Two decision-grade findings surfaced (see Notes 2026-06-13 Phase-3):** (1) the Pillar-B sweep is **~200 mains across 21 files**, not the ~125/11 estimated; (2) Pillar A needs a **real `/dev` int change** (§7.2 associated-`.cl`-module pre-resolve) — the Phase-2 "gap-retry auto-loads it, may be no-op" assumption is source-corrected (unresolved platform sig type-ref → `ModuleError`, never a `ResolutionGap`).

### /spec — Pillar B semantics (DESIGN-COMPLETE; spec edits landed this phase)

- **Ruling (0317 fork)**: `main : (Fn [] (IO _))` MUST **stands as enforceable** — spec NOT relaxed. Rationale: a non-IO main that drives the program's effects is a category error against the §10.1.2 purity invariant (same principle 0318 enforces at the FFI boundary); exit code is already the *inner* Int of `IO Int` (§10.6.1/§12.6) — a bare-Int main would need special-casing. REPL exempt (§10.6.2) so ergonomic cost is nil. No spec-wording change needed for the ruling — the defect was enforcement + traceability, not wording.
- **Spec edits made (test-independent, applied)**: `08-modules.md §8.9.3` conditional→**unconditional** "Every platform function MUST return `IO _`" + soundness rationale (canonical 0318 site); `10-io.md §10.10.2` + `12-runtime.md §12.8` consistency tightening + cross-ref §8.9.3; **NEW `10-io.md §10.6.3` Mode-Output Equivalence** (byte-equivalent observable output across `--run`/`--link`/REPL — gives /qa's output-reshape a normative anchor; precedent §4.12.9). `03-types.md:71` left as-is (general fn rule, correct).
- **Annotation upgrades (close-time, NOT applied — earned by the passing test)**: `02-grammar.md:23` (§Batch Mode), `10-io.md:242` (§10.6) + `:252` (§10.6.1), `12-runtime.md:171` (§12.6) → `[Tested+Neg tests/spec_10_io.rs::batch_main_pure_int_return_is_rejected]`; `08-modules.md §8.9.3` → platform-IO enforcement test (0318 item 5).
- **Acceptance**: all three platform-IO sites read unconditional (✅ done, grep-verified); §10.6.3 present (✅); the 4 annotation flips land at close after the RED guard is green.

### /qa — test plan, both pillars (PLAN-COMPLETE; mass authoring is Phase-5 Stage-1)

- **Pillar A — 6 RED inventory** (`tests/spec_platforms_adt.rs`, all same blocker, confirmed matches 0323): `platform_adt_roundtrip_run/_link/_cache_restore` (exit 12) + `_hash_gate_run_refuses/_repl_warns_and_loads/_link_refuses`. **Reconciliation**: `area` already returns `IO Int` (0318 DLL-side landed) → `(defn main [] (area (Rectangle 3 4)))` is already spec-conformant; **Pillar A mains are EXCLUDED from the Pillar B sweep**.
- **Pillar A fixture plan**: program drops `shapes.cl` into the per-test tmpdir via `.file("shapes.cl", "(deftype Rectangle [:Int w :Int h])")` (self-contained, no `platforms/` coupling) + `(import [shapes [Rectangle]])`. Hash-gate tests (3,4,5) additionally **blocked on /platform schema regen** (real baked hash vs the `0000…` sentinel) — sequence regen first. Discovery-budget one /dev int triage (now confirmed real — see /platform).
- **0289 i4-5**: new tests in `tests/platform_errors.rs` — `platform_abi_version_mismatch_e2e` (`AbiVersionMismatch{expected,found}`, both values in stderr) + `platform_dispatch_error_carries_fn_name` (`DispatchError{fn_name}`). **Hard-dependent on /platform test-DLLs** — the natural cut if /platform can't land both (ride RED un-ignored, ledgered).
- **Pillar B sweep — ENUMERATED**: **221 `defn main` / ~220 batch sites across 21–23 files; ~200 bare-Int mains need reshaping** (S79's ~125 was a 60% undercount). Heaviest: `cache.rs` (51 sites→`(Pure 0)`), `spec_08_modules.rs` (35), `regression.rs` (18), `link.rs` (15), `spec_12_runtime.rs` (12). Transform is **mechanical**: `(defn main [] EXPR:Int)` → `(defn main [] (Pure EXPR))`, **exit codes preserved, assertions unchanged**. Default `(Pure …)` (seeded, no prelude coupling) over lowercase `(pure …)`.
- **3 test-design defects** (certify the violation, must fix): `spec_10_io.rs::run_mode_main_returns_int_exit_code` (`main []7`→reshape+recomment), `spec_12_runtime.rs` exit-witnesses (incl. `main []true` — needs a `IO Bool` exit-semantics confirm from /dev int, or convert to neg test), `link.rs::link_error_when_main_returns_wrong_type` (`Int||IO` disjunction → require `IO`, drop `Int`).
- **Mains that STAY non-IO** (rejection subjects): the RED guard (`main []0`), `link.rs` String main (`"hello"`), and the `Bool`-main witness (→ new neg test `batch_main_bool_return_is_rejected`).
- **Output-coverage reshape (FLOOR)**: new `run_through_all_modes_output` stdout harness (byte-equal program stdout across the 6 mode×cache permutations, REPL echo stripped); convert **~8 feature-class representatives + ~2-3 existing IO tests = ~10-12 all-modes-output tests** (up from 3 single-mode). `PLAN.md §Mode canonicalisation` reshaped: output-equivalence primary, exit-code-equivalence the pure-smoke minority. Annotate the harness to `spec/10-io.md §10.6.3`. **Full-corpus conversion = S81.**
- **RED guard flip**: `batch_main_pure_int_return_is_rejected` flips when /dev int deletes the `Type::Int` accept arm (`src/exe.rs:362`); /dev updates the message to name `(Fn [] (IO _))` + drop "or Int".
- **Sizing verdict**: **NOT comfortably one sprint at floor; recommends cut/sequence** (see Notes + the user sizing fork). Recommended order: land the int one-arm deletion FIRST (the suite then names every unconverted main), Pillar A core in parallel, sweep in 2-3 file-cluster waves (one serialized green-up each), output-floor last. If cut: drop 0289 i4-5 + trim output-reshape to the ~8-class floor; **do NOT cut the sweep** (all-or-nothing once enforcement lands).

### /platform — Pillar A `shapes` fixture (PLAN-COMPLETE; fixture authoring is Phase-5)

- **S79 fixture already correct safe-now**: `area : (Fn [shapes/Rectangle] (primitives/IO primitives/Int))` returning `CLIO<CLInt>` (✅ 0318 holds, no edit); `(deftype Rectangle [:Int w :Int h])`; ABI v3.
- **The one surfacing layer (CORRECTS Phase-2 arch)**: `register_platform_in_tc`→`parse_and_check_platform_type_sig` maps an unresolved FQ sig type-ref to `CranelispError::ModuleError`, **NOT** a `ResolutionGap` — so the FIXME-0268 FQ-autoload retry never fires for platform sig types. `shapes.cl`-on-search-path is **necessary but not sufficient**; needs a **§7.2 pre-resolve** in int (resolve+register the platform's associated `.cl` type-module(s) BEFORE the sig-check loop, via `drive_module_dep`). **/platform is filing a FIXME → /dev int** for this. Disjoint from Pillar B's `src/exe.rs` (collision-seam #2: same crate/skill, different files).
- **/qa contract (handed off)**: module `shapes` = `shapes.cl` at project_root; `Rectangle` single-ctor product tag 0; fields `w`(0),`h`(1) `:Int`; entry program de-defines `Rectangle`, adds `(import [shapes [Rectangle]])`; witness `(area (Rectangle 3 4))`⇒IO 12.
- **Schema regen (Phase-5 only)**: `/platform-schema shapes` → real `layout-hash` + `(schema (shapes/Rectangle (Rectangle 0 ((w primitives/Int)(h primitives/Int)))))` → rebuild dylib. Placeholder `w`/`h` names already correct (clean round-trip works pre-regen); only the **drift hash-gate** needs the real baked hash. Generator output is authoritative.
- **0289 i4-5 DLLs**: hand-rolled `platforms/shapes-badabi/` (stale `abi_version` literal — `declare_platform!` has no override arm; distinct dylib name dodges the manifest-symbol collision) for item 4; a dispatch-failure sibling DLL for item 5 (injection point pending a /dev int read). Both /platform-owned; tests/-side e2e is /qa's.

## Waves (Phase 4)

**Cut-line: EVERYTHING, no cuts** (user 2026-06-13) — int enforcement + full ~200-main sweep + 6 Pillar-A reds + the Linux `PLATFORM_EXT` red + 0289 i4-5 + output-floor; fully green on **macOS and Linux** with full platform drift coverage. Two parallel tracks (A platform / B conformance). **Green-up sequencing relaxed (Linux refresh 2026-06-13):** the original "serialized green-ups, patient on the dyld cold-load tax" was a macOS dyld concern; the Linux full suite runs clean in ~39s with no thrash, so parallel green-ups are now safe — serialize only if a real contention shows up. The **two** int changes (`exe.rs` enforcement + `platform.rs` §7.2 pre-resolve) **batch into one `/dev` int D/D/R** (same skill/crate, disjoint concerns in `src/exe.rs` + `src/platform.rs` — resolves collision-seam #2). *(The `PLATFORM_EXT` `cfg`-fix once planned here is a NO-OP — already landed in the porting arc; the 8th red is `/examples` `.so`-symlink provisioning, Wave 2 — corrected Wave-0.)*

### Wave 0 — QA-first (Phase 5 Stage 1, one `/qa` fire, sprint-wide failing tests)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /qa | tests/ | Author the failing tests for the whole sprint: Pillar-A 6 round-trip reshaped to the new fixture (`.file("shapes.cl",…)` + `(import [shapes [Rectangle]])`); 0289 i4-5 e2e (failing, pending DLLs); `run_through_all_modes_output` harness + ~10-12 output-floor tests; new `batch_main_bool_return_is_rejected` neg test. (RED guard `batch_main_pure_int_return_is_rejected` already exists.) **NOT** the ~200-main sweep — that is coupled to enforcement (Wave 2). | **✅ done** — 1209/12/8; 4 new reds authored (6 ADT reshaped RED on the §7.2 gap, 2× 0289 RED pending DLLs, bool-main neg RED pending enforcement, platform-DLL-resolve narrow guard RED); 12 output-floor GREEN. **Discovery: 8th-red root cause corrected** — `PLATFORM_EXT` already `cfg`-conditional; real cause is missing `.so` symlinks in `examples/platforms/` (re-owned `/dev int → /examples`). |

### Wave 1 — Enabling changes (parallel; gates both tracks)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /dev int → /review int | src/ | **Batched int D/D/R, both pillars**: (B) `classify_main_return_type`/`validate_main` (`src/exe.rs`) — deleted the `Type::Int` accept arm; **also added the missing `validate_main` call to `--run`/`trampoline`** (it never enforced before — only `--link` did); removed dead `MainReturnKind`. ✅ both `batch_main_pure_int_return_is_rejected` + `batch_main_bool_return_is_rejected` green; `IO Bool` exits 0. (A) §7.2 pre-resolve — `handle_platform` (`src/worker.rs`) now drives referenced `.cl` type-modules via `drive_module_dep` (returns `BlockAction`) before `register_platform_in_tc`; `src/platform.rs` gained `referenced_sig_modules`/`load_platform_checked`. **Verified working** (error advanced `unknown type Rectangle`→`unknown type Int`; manual FQ-`:primitives/Int` run = exit 12). + unit tests, 0 warnings. **`/review` int: APPROVED** (no Blockers/Importants; §7.2 termination verified; +120 reds confirmed enforcement collateral; 3 Minor notes — 2 stale doc-comments→/dev, 1 pre-existing `eprintln!`). | **✅ done (APPROVED)** |
| /platform | platforms/ | ✅ `platforms/shapes-badabi/` (`libcranelisp_shapes_badabi.so`, stale ABI=2) + `platforms/shapes-dispatch-fail/` (`libcranelisp_shapes_dispatch_fail.so`, dispatch-fault). Workspace members updated. `cargo check -p`/clippy clean. **Flag:** no live `DispatchError{fn_name}` construction site — fixture self-describes via stderr (`dispatch`+`area`); /qa+/dev reconcile whether the e2e asserts the structured carrier. | **✅ done** |

*Gate: int compiles + enforcement live; `/review` int Blocker/Important resolved. After this lands, the suite NAMES every unconverted bare-Int main (per /qa) — making Wave 2's sweep mechanical.*

### Wave 1E — Dedicated examples wave (depends on Wave 0 + Wave 1 enforcement; runs after Wave 1)

**CONSOLIDATED (user 2026-06-13):** the full `examples/` green-up in one wave — both the platform-discovery fix AND the `main → IO` rewrap. Rationale: with Wave-1 enforcement live, the umbrella `every_example_runs_with_documented_exit` (runs all ~27 examples) needs BOTH the discovery fix (4 IO examples find platforms) AND the example bare-Int mains rewrapped to `IO` (the rest now hit the same `main : IO _` gate as the suite). So the example `main → IO` rewrap moves here from Wave 2.

**Mechanism (settled against source):** `CRANELISP_PLATFORM_PATH` (Tier-3) is the ONLY clean lever — it searches `target/debug` directly and resolves cargo's `libcranelisp_{name}.{ext}`. `Cranelisp.toml` only feeds `lib-dirs` → Tier-2 (`{lib_dir}/platforms/`), which can't reach the bare `target/debug` artifacts — so no committed-config option; the user workflow gets a justfile recipe.

| Skill | Crate | Task | Status |
|---|---|---|---|
| /examples | examples/ | ✅ (1) **Discovery:** root `justfile` `run-example FILE` (builds cdylibs + `CRANELISP_PLATFORM_PATH=target/debug`); symlink doc-comments in `21`-`24` rewritten to it. (2) **Rewrap:** 24 pure mains `(Pure EXPR)` (exit codes preserved); `21`-`24` left already-IO; `Pure` added to examples-local prelude (free-standing). All 27 verified match documented exits. **Found (pre-existing):** `16-modules/main.cl` `submodule 'main.math' not found` — predates rewrap, outside umbrella. | **✅ done** |
| /qa | tests/ | ✅ `run_example` sets `CRANELISP_PLATFORM_PATH=<repo>/target/debug` + null stdin + `Once`-guarded `ensure_platform_cdylibs_built()` (verified clean-tree reproducible); ex-24 tightened `[20,133,141]`→`[20]`; guard reworked onto search path; legacy `observability_io.rs` (quarantined) provenance-updated. **GREEN:** `every_example_runs_with_documented_exit` (all 27) + `platform_dll_resolves_on_current_platform`. | **✅ done** |

*Gate: `every_example_runs_with_documented_exit` (all ~27) + `platform_dll_resolves_on_current_platform` green; symlink convention removed from docs + tests; reproducible from a clean tree (cdylibs built). `/examples` is now DONE for the sprint (removed from Wave 2).*

### Wave 2 — Sweep + fixture wiring + drift e2e (parallel tracks; green-ups parallel-safe on Linux)

| Skill | Crate | Task | Status |
|---|---|---|---|
| /qa (Pillar B) | tests/ | ✅ ~200-main sweep DONE across all 21 files (`(Pure EXPR)`, exit codes preserved); 3 test-design defects reshaped (`run_mode_main_returns_int_exit_code`, `spec_12_runtime` exit-witnesses, `link::link_error_when_main_returns_wrong_type`→require IO); rejection subjects intact. **Suite 1217/8/8** — the only 8 reds are the Pillar-A set (`:Int` fixture), zero `main:IO` reds remain. | **✅ done** |
| /qa (Pillar A) | tests/ | ✅ `:Int`→`:primitives/Int` in `spec_platforms_adt.rs` + `platform_errors.rs` (error moved off `unknown type Int` everywhere — fix confirmed). **GREEN:** item 4 (`platform_abi_version_mismatch_e2e`, test-assertion aligned to the real ABI-mismatch Display) + 2 hash-gate (`_repl_warns_and_loads`, `_link_refuses`). **IGNORED:** item 5. **Correction:** the 3 round-trip/cache reds + `hash_gate_run_refuses` did NOT green on the fixture fix — ALL 4 block on the SAME `/platform` schema regen (DLL bakes `0000…` sentinel; host regen = `239228b4b2e2ecb1`). | **✅ done** |
| /platform | platforms/ | ✅ (1) Schema regen — baked real `239228b4b2e2ecb1` via the `/platform-schema shapes` generator (verified FNV-1a digest of the 3-entry body incl. the `area` IO return-type closure), rebuilt dylib. (2) `shapes-dispatch-fail` crate + workspace member removed. **Only `roundtrip_run` greened — the other 3 reds revealed 3 deeper cross-skill defects (D1/D2/D3 below).** | **✅ done (schema correct; 3 defects surfaced)** |
| /port | exemplar/ | Rewrap exemplar inline-repro mains → `IO` (`(Pure …)`). | pending |
| /qa (output-floor) | tests/, tests/plan/ | ✅ DONE in Wave 0 — 12 all-modes-output tests green; `PLAN.md §Mode canonicalisation` reshaped. | ✅ done (Wave 0) |

### Wave 2D — Pillar A drift-correctness (NEW; user 2026-06-13 — the 3 defects Pillar A exposed)

Sequential (per user pref + shared tree). D1 leads with an `/arch` ruling (data-model change), then implementation.

| Skill | Crate | Task | Status |
|---|---|---|---|
| /arch | crates/cranelisp-types + design | ✅ **D1 ruling** (`design/arch/d1-introspection-repl-only.md`): D41 was symmetry not serde → reversal re-homes ONLY `sexp` on `DefKind::Macro`. Added serialized `DefKind::Macro.macro_sexp: Sexp` (solves cache-restore, no rehydration) + regenerated public-api baseline. New int-internal `RunMode{Repl,Run,Link}` on `SharedState` (from `main.rs Action`); hash-gate reads `run_mode.is_repl()`. Precise `/dev` scope handed off. | **✅ done** |
| /dev int → /review int | src/ (+ cranelisp-types per /arch) | ✅ **D1 impl** — all 8 ruling sites landed; `macro_sexp` set-on-register + read-on-recompile (cache-restore fix). `hash_gate_run_refuses` **GREEN**; macro/cache/REPL-introspection all green. **Collateral:** FIXME 0324 → /qa (field-count guard 15→16). **`/review` int: APPROVED** (no Blockers; confirmed compile-path introspection dep fully removed, cache-restore correct, RunMode refuses under --run+--link). 3 Minor: M1 codegen-product writes still populate introspection in batch (principle-residual, harmless — optional gate); M2 gate-block triplicated (extract helper); M3 commit-hygiene (whole-sprint bundle un-isolatable). | **✅ done (APPROVED)** |
| /dev backend → /review backend | crates/cranelisp-intrinsics (+platform/backend comments) | ✅ **D2 DONE** — root cause refined: the symbol IS a Rust `&str` fat-pointer static (ptr,len); the `--link` reader did `CStr::from_ptr` on the fat-pointer bytes. Fix = `cranelisp-intrinsics/src/layout.rs:69` reads `*(linked as *const &str)` — the SAME `(ptr,len)` view `--run` uses (`src/platform.rs:300`), so both modes read identically. Reader-side only; no producer NUL-buffer; cross-platform (Rust `&str` layout, not object-format). `roundtrip_link` **GREEN**; `--run`/stdio/drift-refuse all green. **/review backend pending.** ⚠️ **Build-skew caveat (load-bearing): verify only after `cargo build --workspace` (one invocation) — piecemeal `cargo build -p` skews platform rlib vs exe-bundle snapshots → spurious `--link` failures.** | **✅ done (/review pending)** |
| /qa | tests/ | **D3:** `roundtrip_cache_restore` asserts on the phantom `CRANELISP_MODULE_TRACE` env var (read by no source). Round-trip works (exit 12). Re-assert on real observable cache-hit behaviour (or drop the trace-dependent assertion). → green. **+ FIXME 0324:** bump `shared_state_field_count_at_target_14` guard 15→16 (D1 collateral). | **in progress** |
| /arch → /dev int → /review int | src/ | ✅ **D1b DONE** (`/dev` int): `SharedState.introspection`→`Option<DashMap>` (None in batch — store absent, not just unpopulated); 2 un-gated sinks + readers→`.as_ref()`; field count stays 16; no public-API change. **Verified green:** REPL introspection 191/191, macro+cache 130/130, `spec_platforms_adt` (D1+D2 intact). `/review` int owed. (Full run also surfaced **D4** — a *pre-existing* Linux `--link` bug, NOT D1b — see Notes.) ⟶ ✅ **/arch D1b ruling** (`d1-introspection-repl-only.md` §B): **history finding — REPL-only was intended from day one** (origin commit `0bc433f` doc said "Not populated during batch"; `git log -S 'introspection: Option'` empty; always-on was S78 drift, already fixed by D1) → reversal SAFE, ruled FOR. `SharedState.introspection`→`Option<DashMap>` (None in batch, same `RunMode` carrier); readers→`.as_ref()`; `code_size` byproduct retain-or-drop; **CLIF-in-batch generation → follow-up FIXME `/backend`** (below int boundary, carries baseline regen — out of D1b). No public-API change. **Now /dev int.** | **D1b — introspection store = `Option<DashMap>` (user 2026-06-13, completes the D1 increment).** D1 moved the macro-sexp (compile-data) to the symbol table; this is the other half — the introspection STORE must not *exist* outside REPL. `SharedState.introspection` is an always-allocated `DashMap` (`session_v4.rs:885`/`:1201`) → make it `Option<DashMap>`, `None` in batch. Gate the two un-gated codegen-product sinks (`worker.rs:3644`/`:4312` — pass `None` in batch so no record/CLIF/disasm is generated; `code_size`-type byproducts return-to-worker retain-or-drop). **MANDATORY FIRST STEP (user):** `/arch` does a broad review of decisions + design docs INCLUDING legacy/archive to determine whether REPL-only was the intended-but-undocumented shape vs a deliberate reversal with a rationale to preserve (cf. Decision 41). Then extend `design/arch/d1-introspection-repl-only.md`. Int-internal `Introspection` → no public-API impact. **Sequenced AFTER D2** (shares `src/` working tree). | pending |

### Wave 2E — Linking-subsystem encapsulation (NEW; user 2026-06-13 — fixes D4 by construction)

Sequential. `/arch` designs the abstraction first, then `/dev` refactors `src/exe.rs` onto it.

| Skill | Crate | Task | Status |
|---|---|---|---|
| /arch | src/ (design) + design/backend | ✅ **DONE** (`executable-generation.md §12`): `LinkRequest` (intent-only) + `Linker` trait (`link`+`describe` from one `build_args` path → diagnostic can't drift); `AppleLdLinker`/`GnuCcLinker`(+MSVC sketch); GNU `.o`-extraction+ordering housed in `GnuCcLinker`; new `src/link/` module; `for_host()->Box<dyn Linker>`; no public-API. Acceptance: `grep force_load src/` only in `AppleLdLinker`. **Design the `Linker` abstraction:** a `LinkRequest` (intent only — `startup_obj`, `module_objs`, `bundle_lib`, `force_include: Vec<Rlib>`, `entry_symbol`, `dead_strip`, `output`; NO toolchain flags) + a `Linker` trait (`link(&req)` + `describe(&req)->String`), impls `AppleLdLinker`/`GnuCcLinker` (+ Windows-ready), selected by the existing `cfg`-based `for_host()`. Each impl is the SOLE place its platform tokens appear (`force_include`→`-force_load` vs `--whole-archive`+`.o`-extraction; `dead_strip`→`-dead_strip` vs `-Wl,--gc-sections`). Rule on where it lives (int-internal `src/exe.rs`/new `src/link/` — no `cranelisp-types`/public-API). Extend `design/backend/executable-generation.md`. | pending |
| /dev int → /review int | src/ | ✅ **/dev DONE** — `src/link/{mod,apple,gnu}.rs`; `LinkRequest`+`Linker` (`link`/`describe` share `build_args`); `log_link_summary`+`LinkDriver`+`LinkerConfig` deleted; macOS byte-identical. **`grep force_load src/` → only `apple.rs:66`** ✓. Scoped run (post-`--workspace`): 12 output-equiv link + `platform_stdio_print_link` + `roundtrip_link` + hash-gates **GREEN** (only D3 red). 284/284 lib tests. GNU `dead_strip`→no-op (matches pre-refactor; `--gc-sections` broke platform-obj refs). **⚠️ surfaced: full `cargo nextest run` reds the link tests via test-vs-dev profile build-skew — reproduces on HEAD; the link tests never passed under plain `nextest` on Linux → Wave-3 build-skew hardening is now REQUIRED to claim green.** **/review int next.** | **✅ /dev done (/review pending)** | `link_executable_apple_ld`→`AppleLdLinker::link`; `link_executable_cc`+`extract_rlib_objects`+ordering→`GnuCcLinker`; `log_link_summary`→DELETE→`Linker::describe`; `link_executable`→`for_host()?.link(&req)`; delete `LinkDriver`/`LinkerConfig`; keep `host_entry_symbols`/`find_bundle_lib`/`find_platform_rlibs`. **This fixes D4** — no `-force_load` escapes `AppleLdLinker`. Verify (warm **full-workspace** build): ~13 `output_equivalence` link + `platform_stdio_print_link` GREEN, `roundtrip_link`/`hash_gate_link_refuses` stay green, macOS command byte-identical, `grep force_load src/` only in `AppleLdLinker`. | **in progress (/dev)** |

### Wave 3 — Convergence + close-prep

| Skill | Crate | Task | Status |
|---|---|---|---|
| /qa | tests/ | ✅ **GREEN — `/sprint`-verified vanilla `cargo nextest run`: 1224 passed / 0 failed / 9 skipped** (37s). All sprint reds cleared (both pillars + 8th red + D1/D1b/D2/D3/D4). 9 skips = 5 TCO (0141) + 2 backend (0221/0191) + 1 perf + 1 deferred 0289-item-5. | **✅ done** |
| /qa | tests/ (+ `.config/nextest.toml`) | ✅ **Wave 3a DONE — e2e test-infra (design + impl).** **Root cause CORRECTED:** not profile-skew — **artifact ABSENCE.** The 5 `--link` prereq crates (`cranelisp-exe-bundle` + 4 platform crates) are leaf workspace members nothing depends on; the compiler resolves them at runtime from `target/debug/`, but `nextest` never builds them → link can't find them (proven: hide them on HEAD → 5/14 link tests fail; restore → 14/14). **Fix:** nextest **setup script** (`.config/nextest.toml` + `tests/scripts/build-link-prereqs.sh`) builds the 5 once before the suite — `/qa`-owned, no `/arch`/`/dev`. Design doc `tests/plan/e2e-architecture.md` (root cause, mechanism+alternatives, isolation/grouping reserve levers, shared-harness consolidation, coverage contract). Deleted the `ensure_platform_cdylibs_built` band-aids. **`/sprint`-verified: vanilla `cargo nextest run` → 1222/2/9 (37s).** The previously-unreliable `--link`/output-equivalence surface all GREEN. | **✅ done (1222/2/9)** |
| /review | (touched crates) | Final change-set review across int/platform; Blocker/Important resolved-or-deferred. | pending |
| /spec | spec/ | Apply the 4 close-time annotation flips (→ `[Tested+Neg …]`) once the RED guard is green. | pending |

**Wave-gate reminder**: before each advance, scan `design/arch/fixmes/` for `target: /skill-in-wave` + `status: open`. The §7.2 pre-resolve (Wave 1 /dev int) and the `.so`-symlink provisioning (Wave 2 /examples) are carried in-plan, not as separate FIXMEs. *(Wave-0 gate note: 7 pre-existing out-of-scope open `/qa` FIXMEs — 0021, 0052, 0136, 0216, 0235, 0258, 0272 — are backlog, not S80 blockers; flagged for close/next-increment.)*

## Notes

- 2026-06-13 — Phase 1 scope drafted. Baseline from S79 close: 1196 passed / 7 failed (6 → 0323, 1 → 0317) / 8 skipped. User chose **"both pillars (platform + conformance)"** over the focused/rider/conformance-first shapes — full green by sprint end, 0323 + 0289 i4-5 + 0317. `/sprint` flagged the size risk (0317's sweep is plausibly a full sprint alone; dyld green-up tax compounds); user accepted. 0316 deferred to S81.
- 2026-06-13 — **Scope APPROVED** (user). Sequencing decision: **pillars run parallel, but green-ups serialize** — concurrent green-ups thrash the page cache under the dyld cold-load tax, so D/D/R work proceeds in parallel across both pillars while the integrating `cargo nextest` green-ups are run one-at-a-time (detached jobs). 0318's `08-modules.md:783` unconditional edit folds into Pillar B's `/spec` work (user confirmed). Advanced to Phase 2 — `/arch` review against this scope.
- 2026-06-13 — **Cut-line decided: EVERYTHING, no cuts** (user, informed by the /qa "not comfortably one sprint at floor" verdict + the ~200-main / Pillar-A-needs-real-int-change findings). Full scope retained; multi-wave, serialized green-ups. **Phase 4 waves locked** (Wave 0 QA-first → Wave 1 enabling int+DLLs → Wave 2 sweep+fixture+drift → Wave 3 convergence). Status → PHASE 5 READY, awaiting user go to begin execution.
- 2026-06-13 — **Phase 3 design collected** (`/spec` + `/qa` + `/platform`, parallel). Spec side design-complete (edits landed: 0318 unconditional ×3 sites + new §10.6.3 mode-output-equivalence; ruling = enforce main:IO). **Two findings materially enlarge the Phase-1 estimate**: (1) `/qa` enumerated the sweep at **~200 bare-Int mains / ~220 batch sites across 21 files** (not ~125/11 — a 60% undercount), and gave an explicit **"not comfortably one sprint at floor"** sizing verdict; (2) `/platform` source-corrected the Phase-2 "gap-retry auto-loads the platform type-module" claim — it does NOT (unresolved sig type-ref → `ModuleError`, not `ResolutionGap`), so Pillar A carries a **real `/dev` int §7.2 pre-resolve change** (FIXME incoming from /platform), not a possible no-op. Both pillars still terminal-shaped; the transform is mechanical (`(Pure EXPR)`, exit-codes preserved). **Sizing fork surfaced to user before Phase 4.**
- 2026-06-13 — **`/arch` Phase-2: SIGN-OFF.** No `cranelisp-types` / public-API change needed (no baseline regen); both pillars terminal-shaped (Principle 8 clean) + cleanly separable. **Two source-validated corrections** (full detail in the Arch-review section): (1) the 0317 `main:IO` enforcement seam is **`src/exe.rs::classify_main_return_type`** (int — a one-arm deletion of the `Type::Int` accept arm), **NOT typecheck** — re-owned to `/dev` int; (2) **0318's compiler-side `require_io_return` already landed S79** — 0318 residual is `/spec` text + the `shapes` `area`→`IO` fixture. Output-coverage reshape (0317 item 4) confirmed a `/qa` test-harness detail, no arch artefact moves. Crate-touch maps for both pillars recorded. Advanced to Phase 3 — design invocations: `/spec` (Pillar B semantics), `/qa` (test plan both pillars — the sweep needs a plan before mass authoring), `/platform` (Pillar A `shapes` fixture). `/arch` interface work complete (none needed); `/dev` int design refinement folds into Phase-5 Stage-2 (one-arm deletion + the load-ordering triage are implementation-discovered).
- 2026-06-13 — **Linux refresh of the still-unexecuted plan** (user: "review and refresh the plan"; testing now <1 min). A Linux aarch64 porting arc (`622d3d8`/`51a8dc3`/`74da7f1`/`4109c3e`) landed off-plan after `60901d7`. Re-baselined on the Linux VM: **1197 / 8 / 8** (was S79 macOS 1196/7). Findings: (1) the feared ~46 Linux failures never materialised — the porting arc made the ~38 macOS-only `--link` tests *pass* on Linux (not skip); the **8 skips are platform-independent** pre-existing `#[ignore]`s (5 TCO FIXME 0141, 2 backend FIXME 0221/0191, 1 perf), nothing skipped due to platform; (2) the 7 targeted reds reproduce identically — both pillars hold; (3) **one new (8th) red** — `examples::every_example_runs_with_documented_exit` (4 IO examples, `platform 'stdio'/'test-capture' not found`), root-caused to `src/platform.rs:61` `PLATFORM_EXT = "dylib"` (hardcoded macOS ext; Linux `--run` can't dlopen `.so`). **User: fold into Pillar A, supporting BOTH macOS and Linux** (not a Linux flip) → `cfg`-conditional ext, batched into the Wave-1 int D/D/R, `/qa` asserts the 4 IO examples (platform-agnostic), `/review` verifies cross-platform parity. (4) macOS-dyld "serialized green-up" sequencing **relaxed** — Linux suite runs clean in ~39s, parallel green-ups now safe. Target updated to **1205+/0/8**. **No re-arch needed** (the fix is pure `src/` int, same surface `/arch` already signed off). Status stays **PHASE 5 READY — awaiting user go**.
- 2026-06-13 — **8th-red root cause CORRECTED (Wave-0 finding).** The refresh entry above mis-root-caused the 8th red to `src/platform.rs:61` `PLATFORM_EXT = "dylib"` — that was a stale grep read (only the macOS `#[cfg]` arm surfaced). Source-verified: `src/platform.rs:59-65` is **already `cfg`-conditional** (`dylib`/`so`/`dll`) — the porting arc fixed it. The real cause: `examples/platforms/` holds only macOS `.dylib` symlinks and the examples test runs without `CRANELISP_PLATFORM_PATH`, so Linux discovery (`{root}/platforms/{name}.so`) finds nothing. **Re-owned `/dev int → /examples`**: provision `.so` symlinks (Wave 2). The planned Wave-1 `/dev` int `PLATFORM_EXT` change is **dropped (no-op)**. `/qa`'s Wave-0 narrow guard `platform_dll_resolves_on_current_platform` is the regression anchor. (Lesson: verify grep hits in context — a matched `const` line under a `#[cfg]` is not "hardcoded.")
- 2026-06-13 — **Wave 2 Pillar A revealed 3 deeper cross-skill defects (the "discovery-shaped" round-trip path, as Phase-1 predicted).** Fixture `:Int` fix + schema regen cleared `abi_mismatch`, 2 hash-gate, and `roundtrip_run`; the remaining 3 reds each root-cause to a distinct defect (each with a `/platform`-supplied repro — cross-skill-handoff-ready):
  - **D1 (`/arch` ruling → `/dev` int) — `hash_gate_run_refuses`:** `src/worker.rs:2851` `is_repl = ctx.introspection.is_some()` drives the hash-gate (REPL warns-and-loads; `--run`/`--link` refuse). Post-S78 `cluster::process_cluster` (`src/cluster.rs:222`) populates `introspection: Some` UNCONDITIONALLY, so `is_repl` is always true → `--run` wrongly warns-and-loads instead of refusing drift. **USER ARCHITECTURAL RULING (2026-06-13):** the unconditional population is the real bug — **introspection is REPL-command-only ("it's in the name"); compile-necessary data belongs in the symbol table.** The one compile-path read is `worker.rs:746` `resolve_macro_sexp_from`→`introspection[fq].sexp` (macro-clause recompile, FQ-autoload + cache-restore), which exists because **Decision 41 retired the per-entry `sexp` field and moved macro sexp into introspection**. Proper fix **reverses Decision 41**: macro sexp → symbol-table entry; introspection populated REPL-only; gate reads the real mode. **This is an `/arch`-grade data-model change, not a sprint-tail patch** (the mode-flag-only patch is the papering-over the user rejected). The gate logic (`layout_hash_gate`, unit-tested with explicit `is_repl`) is already correct — only the `is_repl` *source* + the introspection layering are wrong. See `memory/introspection-repl-only-principle.md`. Repro: `cranelisp --run` a drifted shapes program → exit 12 (should refuse).
  - **D2 (`/dev` backend/int) — `roundtrip_link`:** the DLL exports `__cranelisp_layout_hash_shapes` as a Rust `&'static str` (ptr+len, NO NUL; `crates/cranelisp-platform/src/lib.rs:1461`). `--run` reads it as `&str` (`src/platform.rs:298`) correctly, but the `--link` stub passes the address to `cranelisp_check_layout_hash` (`crates/cranelisp-intrinsics/src/layout.rs:48`) which does `CStr::from_ptr` → reads the 16 hex bytes + trailing `\n(schema…` rodata → corrupted `linked` ≠ baked `expected`. **Even the clean build aborts.** Fix: export the hash as a NUL-terminated cstring for `--link`, or have the stub read it as `&str`. *Real `--link` correctness bug: the layout-hash gate is broken under `--link`.* Repro: `--link` a clean shapes program → "layout hash mismatch" abort.
  - **D3 (`/qa` test-design, or `/int` to implement) — `roundtrip_cache_restore`:** the round-trip WORKS (exit 12 on cache restore); the failing assert checks `CRANELISP_MODULE_TRACE=1` stderr for `"hit"`, but `CRANELISP_MODULE_TRACE` is read by NO source code (only `tests/CLAUDE.md` docs + this test). Either fix the assertion to observe real cache-hit behaviour (`/qa`) or implement the trace env var (`/int`).
  Suite now ~**1221/3/9**. **DECIDED (user 2026-06-13): do the `/arch` increment NOW — extend S80 to fix all three** → new **Wave 2D** (below). D1 is the `/arch`-grade introspection/Decision-41 reversal; D2 the `/backend` `--link` hash-symbol fix; D3 the trivial `/qa` test fix. Genuinely completes Pillar A; S80 reaches full green.
- 2026-06-13 — **Wave 2 Pillar B sweep COMPLETE — suite 1217/8/8.** ~200 bare-Int batch mains `(Pure …)`-wrapped across 21 files (exit codes preserved); 3 test-design defects reshaped; rejection subjects (`batch_main_pure_int_return_is_rejected`, `batch_main_bool_return_is_rejected`, `link` String-main witness) intact. The only 8 reds are the Pillar-A `:Int` fixture set; zero `main:IO` reds. The sweep agent fanned out its own sub-agents per file-cluster (intra-sweep parallelism, disjoint files, build-lock-serialized — benign). **Stability finding (twice-observed → Wave-3 hardening):** a COLD/heavily-parallel first full run threw 16 spurious reds (platform-DLL-load / `Pure`-resolution under parallelism), all clear on warm reruns — the final green-up MUST be warm/cdylibs-pre-built to claim 0 honestly.
- 2026-06-13 — **D4 surfaced (pre-existing Linux `--link` dispatch bug; NOT a S80 regression).** A clean full-workspace rebuild + the new `output_equivalence` link permutations expose that `--link` calls `link_executable_apple_ld` (`src/exe.rs:723`, macOS `-force_load`) on Linux for platform-rlib linking → `aarch64-linux-gnu-ld.bfd` rejects `-force_load` → ~13 reds (12 `output_equivalence::*` link permutations + `platform_stdio_print_link`). `git blame` dates the `-force_load` code to **Sprint 23** (`ac8da04`); the GNU `--whole-archive` path exists (`src/exe.rs:814`) but the dispatch doesn't route this scenario through it. Reproduces warm + targeted + low-concurrency (NOT flakiness). Masked before by stale link artifacts; the clean rebuilds exposed it. **In-scope entanglement:** the `output_equivalence` link tests are S80's Mode-Output-Equivalence deliverable (spec §10.6.3), so D4 blocks verifying output-equivalence for `--link` on Linux. **Root: weak link-subsystem encapsulation** — there's a `LinkDriver` enum + `cfg`-selected dispatch (`exe.rs:600/629/671`), but the two driver fns (`link_executable_apple_ld`/`_cc`) are parallel implementations re-deriving the same intents in platform-specific syntax, and `log_link_summary` (`:1054`) hardcodes `-force_load` regardless of driver — platform tokens leak across ≥3 sites, which is HOW the Linux port missed one. **DECIDED (user 2026-06-13): encapsulate properly now (`/arch`)** — design a `LinkRequest` (intent: startup/module objs, bundle, `force_include` rlibs, entry, dead_strip, output — NO flags) + `Linker` trait (`link()` + `describe()`, impls `AppleLd`/`GnuCc`/future `Msvc`), refactor `exe.rs` onto it. Fixes D4 by construction (no token escapes a driver), readies Windows. → **Wave 2E**.
- 2026-06-13 — **D4 reconciliation (honest correction).** The Wave-3a `/qa` root-cause work found the `--link`/output-equivalence reds were primarily **artifact ABSENCE** (the 5 prereq crates unbuilt under `nextest`), NOT the `-force_load` code path `/sprint` diagnosed as D4. That diagnosis was made on an **inconsistent piecemeal build** — the same trap that misled the D2 verification — and over-attributed to code. On a consistent build the link tests pass on HEAD. **Wave 2E (the `Linker` abstraction) STANDS on design merits** — user-requested as a design question; it makes `-force_load`-on-Linux impossible by construction, kills the diagnostic-drift class, readies Windows. Net with Wave 2E + Wave 3a: **1222/2/9**. Lesson (memory): never diagnose a link failure from an inconsistent build.
- 2026-06-13 — **0289 item-5 (DispatchError e2e) DEFERRED (user).** Background established: `PlatformError::DispatchError{fn_name}` is defined + `Display`-handled but has **no live construction site** (only a `#[cfg(test)]` unit test builds one; the IO trampoline calls platform fn-ptrs with no fault guard). The Wave-0 e2e would only pass on the `/platform` fixture's self-authored stderr string — a fake-green. Producing it for real is a new fault-guarded-FFI-dispatch feature (out of S80 scope). User ruling: **defer**, mark `platform_dispatch_error_carries_fn_name` `#[ignore]` with a comment explaining it's unbuilt scope (NOT a defect → narrow sanctioned exception to failing-not-ignored), **no FIXME**; `/platform` removes the `shapes-dispatch-fail` fixture DLL. Item 4 (ABI mismatch — real, `check_abi_version`-wired) still lands. Target skip count 8→**9**.
- 2026-06-13 — **Wave 1E COMPLETE — `examples/` fully green.** `/examples`: root `justfile run-example` (cdylib build + `CRANELISP_PLATFORM_PATH=target/debug`), symlink doc-comments rewritten, 24 pure mains `(Pure …)`-wrapped (4 IO examples already-IO), `Pure` re-exported in the examples prelude. `/qa`: `run_example` wired to the search path + `Once`-guarded cdylib build (clean-tree-reproducible) + stdin null, ex-24 tightened to `[20]`, Wave-0 guard reworked onto the search path. **GREEN:** `every_example_runs_with_documented_exit` (all 27) + `platform_dll_resolves_on_current_platform`. **Two findings → Wave 2 / close:** (1) the 0289 i4-5 e2e (`platform_abi_version_mismatch_e2e`, `platform_dispatch_error_carries_fn_name`) currently red on the SAME `:Int` fixture issue as the 6 ADT reds (`unknown type Int (from module '')`) — folds into the Wave-2 `:Int`→`:primitives/Int` fixture fix. (2) **Latent reproducibility gap:** the existing `use_workspace_platforms()` tests (`spec_platforms.rs`, `spec_platforms_adt.rs`, `spec_10_io.rs`) assume the cdylibs are pre-built (no in-suite build step) — they pass because artifacts exist, but a truly clean tree would red them; candidate for a Wave-3 hardening (adopt `ensure_platform_cdylibs_built()`) or a CI `cargo build --workspace` pre-step. Out of Wave 1E scope.
- 2026-06-13 — **Wave 1E CONSOLIDATED into the full examples wave + discovery mechanism settled (user).** Source-traced the search-dir wiring: `CRANELISP_PLATFORM_PATH` (Tier-3) searches `target/debug` directly and resolves `libcranelisp_{name}.{ext}` — the only clean lever (the proven `use_workspace_platforms()` uses it); `Cranelisp.toml` only feeds `lib-dirs`→Tier-2 (`{lib_dir}/platforms/`), can't reach the bare artifacts → no committed-config option, user workflow gets a justfile recipe. **Coupling that forced consolidation:** with Wave-1 enforcement live, the examples' OWN bare-Int mains now fail the gate, so `every_example_runs_with_documented_exit` (all ~27) needs BOTH the discovery fix AND the example main→IO rewrap — user chose to fold the rewrap (was Wave 2) into Wave 1E so `examples/` greens in one coherent wave. `/examples` removed from Wave 2.
- 2026-06-13 — **Phase 5 Wave 1 (enabling) — /dev int + /platform + /review int ALL DONE (APPROVED).** Suite now **1093/132/8**: `/dev`-verified the `+120` reds are ALL the intended `main : IO _` enforcement collateral (every one cites the diagnostic; distribution matches Pillar B) — the planned forcing function, resolved by the Wave-2 sweep. **Premise correction:** `--run`/`trampoline` never called `validate_main` (only `--link` did) — `/dev` added it, so both batch modes now enforce. **§7.2 verified working** but the 6 ADT reds carry a **new fixture-side blocker → /qa Wave 2**: Wave-0's `shapes.cl` uses bare `:Int` with no prelude → switch to `:primitives/Int` (or load prelude); `/dev` confirmed exit 12 with the FQ shape. **Two handoffs for /qa Wave 2:** (1) the ADT-fixture `:Int`→`:primitives/Int` fix; (2) the `DispatchError{fn_name}` gap — no live construction site, `/platform` fixture self-describes via stderr; decide whether 0289-item-5 e2e asserts the structured carrier (needs a `/dev` funnel) or accepts the substring. **Wave 1E precedent:** `use_workspace_platforms()` already does the `CRANELISP_PLATFORM_PATH=target/debug` search-path trick — /qa mirrors it for the examples harness.
- 2026-06-13 — **8th-red fix approach SET + dedicated Wave 1E added (user).** After detailing the symlink problem (git-ignored `examples/platforms/` holding dangling macOS `.dylib` symlinks; real binaries are cargo's `libcranelisp_{name}.so` in `target/debug`), user chose **option #3 — drop the symlink convention, put `target/debug` on the platform search path** ("the symlink is an unnecessary step for the examples"). `resolve_platform_path`'s `check_dir` already tries the `libcranelisp_{name}.{ext}` naming, so discovery finds the `.so` directly, zero symlinks, `cfg`-correct on every platform. Scoped as **Wave 1E** (dedicated examples wave, parallel with Wave 1, depends only on Wave 0): `/qa` sets the search path in the `run_example` harness + reworks the Wave-0 guard + legacy `observability_io.rs`; `/examples` replaces symlink doc-comments with the committed search-path mechanism (config or justfile) for the user workflow. No `/dev` change. Build-ordering caveat (platform cdylibs built before the examples test) handed to `/qa`.
- 2026-06-13 — **Phase 5 Wave 0 COMPLETE.** `/qa` authored the sprint-wide failing tests: 6 ADT round-trip reds reshaped to the self-contained `.file("shapes.cl",…)` + `(import [shapes [Rectangle]])` fixture (RED on the §7.2 `unknown type Rectangle (from module shapes)` gap — confirms the Wave-1 §7.2 pre-resolve is the right fix); 2× 0289 i4-5 e2e (RED pending Wave-1 DLLs); `run_through_all_modes_output` harness + 12 output-floor tests (GREEN — §10.6.3 already holds for these classes); `batch_main_bool_return_is_rejected` neg (RED pending Wave-1 enforcement); `platform_dll_resolves_on_current_platform` narrow guard (RED, 8th red). Suite **1209/12/8** (8 baseline + 4 new RED; 12 floor GREEN; 1197 prior passes intact). Wave-gate scan: 7 pre-existing out-of-scope open `/qa` FIXMEs (0021/0052/0136/0216/0235/0258/0272) noted as backlog, not blockers. Ready for **Wave 1** (int D/D/R + /platform DLLs).

## Outcome (Phase 7)

### Delivered
- {what shipped}

### Deferred (with rationale)
- {item — why deferred, target sprint, escalation count}

### Findings (record in FIXMEs if not already)
- {unexpected observations, methodology lessons, skill feedback}
