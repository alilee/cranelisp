---
name: Ring 5 plans
description: User has new plans for Ring 5 beyond sketch parity — Ring 4 is the last ring matching the sketch
type: project
---

Ring 4 is the final ring that matches the sketch prototype. User has new (undisclosed) plans for Ring 5 that go beyond sketch parity. HKT and lazy sequences were moved from indefinite deferral into Sprint 24 to clear all Ring 3/4 spec debt before Ring 4 closes.

**Why:** "Ring 5+" was being used as a dumping ground for deferred features (HKT, lazy seq) — user flagged this as scope slip. All spec'd features must land in Ring 4 or be explicitly cut.

**How to apply:** Never label anything "Ring 5+" as a deferral target. All current spec features belong in Rings 0-4. Ring 5 content will come from the user's new plans, not from carried debt.
