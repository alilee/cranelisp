# Sprint 115: Stabilise — Clean & Green + the Assertion Ladder Complete

**Status**: PHASE 5 LANGUAGE (ACTIVE)

**Goal**: Flip all 11 attributed carry REDs to green with zero new unattributed REDs, and close the gap between the QA/arch instrumentation-and-assertion recommendations and what is actually in place — so future failures name their seam instead of flapping.

**Audit**: cranelisp-intrinsics (CONFIRMED at Phase 1 — last assessed in the S87-era pass, 2026-06-14; the Tier-5 diagnostic modes and RC/alloc seam assertions this sprint verifies live there)

## Scope

Context: S114 closed at 5164 run / 5153 passed / **11 attributed carries** / 1 skipped, with two standing findings this sprint answers directly: (1) heisenbug non-reproduction is a pattern (0604's 25/25 determinism evaporated; the 0694 flap; the ctor-crash shifted the same way) — the structural answer is synthesized triggers + seam assertions, not more quiet sweeps; (2) record-vs-source rot (the zombie 0590 five-agent chain) — the structural answer is the audit's process feedback landed as METHOD amendments.

### A. Clean & green — the 11 attributed REDs (anchor)

Fix every S114 carry RED at its attributed owner; each fix lands with its unit test per METHOD §2.2, flip-verified against the attribution in `tests/plan/s114-test-plan.md` §11/§12.

- **/dev(typecheck)**: MS-P7 chained `MayAliasOf` faces ×2 (family-grain invariant BINDING on the fix; design-first rule per the S114 adjudication; **0693 fence lands BEFORE/WITH** — its re-anchored trigger fires here); 0719 wrapper-indirection carrier-loss ×1 (axis named: free-var-through-bound-parameter distance; acceptance = the §5.1.2 equivalence-TWIN bar); 0709 §7.1 occurrence-rule enforcement ×2 (nullary non-dispatchable trait-method no-impl must not leak to codegen).
- **/dev(backend)**: ONE RC-release sweep covering the entry-payload leak + 0720 ADT-wrapped COW supersede ×2 (minimal repro: wrapped supersede tail loop leaks 2/iteration; discriminators recorded in plan §12); GOT-slot carrier-loss pair ×2 — fn-as-value + 0705 AutoCurry-over-local (the flagged collapse question: one mechanism or two — /design(backend) answers before the fix); **W-B5 patch collapse** as its own endorsed change-set (S114 deferral honored).
- **/dev(src)**: impl-redefinition hot-reload ×1 (user RULED at S114 close: hot-reload with defn's same-type constraint; /spec scribes 0714 first; the pin's hot-reload branch is the required behavior); 0707 restore-notice count from record (minor); **0604 early wave** (Track B — the structural gate).
- **/qa**: 0694 nullary-flap root-cause row (flapped 1-of-4 then 0-of-2 — root-cause under the standing counting convention; owner assigned by what the root cause names).
- **0708** (`:Type` not folded in macro-argument position): /spec user framing first (§1.4.5 vs §2.3.8 at the macro-arg seam); post-ruling pin rides /testing; fix lands this sprint only if attribution resolves to an in-scope surface — otherwise attributed carry with rationale.

Exit: suite green except REDs that are NEW, probe-discovered, and attributed this sprint; stable-REDs-exact + named-flap-set certification per the standing convention; ≥2 identical full-suite runs.

### B. Instrumentation & assertions — the recommendations actually in place (user directive)

The canonical recommendation set = `design/arch/safety-invariants.md` §4 register (R1–R13) + `tests/plan/memory-safety-coverage.md` (§1 oracle gate, §2 generative harness, §4.1 capability-fence lifecycle) + the s113 risk-assessment tiers (Tier-5 diagnostic modes, Tier-3 seam assertion density, standing `RC_DEC_CHECK` positives) + the 0604 observability rider (MODULE_TRACE + closure predicate).

- **Early wave /qa — instrumentation-completeness verification matrix**: every recommendation row → its named mechanism → VERIFIED-IN-PLACE (cite file:line + the test that exercises it) or OWED. No row closes on "it was scheduled"; the S108-R-4 lesson (multi-part acceptance silently half-executed) is the audit lens. Known-suspect rows to verify hard: §1.3's "enforced nextest gate, not a manual cert" (is the differential-oracle combinator an enforced gate today?); §4.1's mandatory unit-tier synthetic self-test per diagnostic mode; standing `RC_DEC_CHECK` positive assertions (4 test files reference it — is that the designed standing set or residue?); Tier-3 assertion density at RC/alloc seams (R8's W5 build — landed or partial?).
- **Owed items become dev change-sets THIS sprint** (wave-scheduled after the matrix lands): known-owed already from the register — **R4** mangle-family injectivity census beyond drop-glue (/design(backend) census → witness-or-disambiguator per family); **R6** persisted-index trust-boundary census → ONE validation seam in `deserialise_meta_with_build_id` (/dev(backend, cache)); **R1** §3b producer split if it slipped S114 (verify; typecheck); **R7 predicate correction rides 0604** (below).
- **0604 structural gate (/dev(src), early)** per the re-based plan in the FIXME: disposition the missed census row (`commit_staging_to_live`), land the **declared-export-closure** predicate as an unconditional diagnosed error at the chokepoint (deadlock hazard honored — precompute the closure), MODULE_TRACE emission at the seam; /testing lands the **synthesized-trigger unit test** (injected out-of-closure write → diagnosed error; interleaving-independent); the 25/25 recipe demotes to a bounded no-regression sweep; one time-boxed load-amplified re-induction attempt, abandoned without prejudice. /design(int) §2.2 correction + fixture-comment correction ride the wave. **The gate ships on its merits — writer identification is desired, not required, for 0604 to retire.**
- **R13** (fork-join error-slot ferry) — the one register row proposed to STAY parked: it is gated on the test-discovery implementation wave, not a stabilisation lever now. Explicit deferral recorded if the user concurs (see Phase-1 decisions).
- **/arch Phase 2**: standing register re-audit (§4 "re-audited every Phase 2") + rule on any matrix disagreements.

Exit: the matrix shows every row VERIFIED-IN-PLACE or carrying an explicit user-sanctioned deferral; no row rests on a scheduling claim.

### C. Record integrity — S114 audit disposal + METHOD amendment

Disposals proposed at Phase 1 (see Notes for the user decision record); on acceptance:

- **R-1 (accept)**: 0590 dispositioned against the S110 source evidence — /sprint deletes the FIXME (lifecycle exception: audit-verified zombie; the sole live-residual candidate — the rustdoc-inaccuracy sub-item — re-checked first) + strikes the phantom S115 slot; riders: /design(typecheck) corrects the two design-doc rows; /dev(typecheck) the CLAUDE.md paragraph (merged into R-4); /testing the `hkt_named_arm_probe.rs` comment (test KEPT as a born-green fence).
- **R-2 (accept)**: /design(typecheck) rewrites `typecheck.md` §§2–5 against the as-built crate (facade claims, dead file tree, retired roadmap, dead FIXME anchors, §10-vs-index disagreement).
- **R-3 (accept)**: /dev(typecheck) executes the designed `program/tests.rs` split (per-submodule sibling files per `program-decomposition.md` §3) + re-budgets `finalize.rs` at the §11.8.10 harvest-window seams (≤ ~1,200/submodule). This is the S108 R-4 done-criterion finally met; second silent drop is not an option — decline requires superseding the design's rejected-alternative box.
- **R-4 (accept)**: /dev(typecheck) crate-CLAUDE.md currency sweep (the falsified `build_concrete_codegen_view` contract is the safety-relevant fact).
- **METHOD amendment (/sprint, user approval)**: §3.3 — a FIXME disposition or carry decision verifies the claim against `refers_to` source as its FIRST act; §2.4/Phase-7 — the close checklist asserts FIXME-table-vs-§Delivered consistency. The audit's §2.9 class-cure, adopted as process.

### D. Ruling + diagnostics queue

- **/spec**: 0714 scribe (impl-redefinition = hot-reload, same-type constraint — ruled at S114 close); 0708 framing for user ruling; 0702 dotted-binder matrix framing for user ruling (spec-prose/table/design three-way disagreement rides it); 0704 §5.2.7 wording fix.
- **/dev(frontend)**: 0703 head-vocab mirrors residual; 0710 dangling-local-qualifier message parity; 0711 binder-reject wording at value-level positions. (+0708 fix if attributed here.)
- **/design(int)**: 0699 qualify-walk residual gaps. /design(backend): 0696/0697 ride the W-B5/backend design touch.
- **/qa + /testing**: 0702 cells post-ruling (polarity gated); examples 119→120 exit-code reconciliation rider (binding from S114).

### Out of scope

- Display-protocol §1.5 promotion (0050) — parked pending sprint slot; not a stabilisation lever. Target: next feature sprint.
- Effect-concurrency slice-2 reactor implementation + `concurrency_capacity` timing defect — effect-concurrency track (sanctioned deferral class).
- 0637 sibling-slot validation — parked to first consumer per the re-affirmed R5 ruling (S113 W5); the P8 co-landing rule IS the mechanism.
- 0052 (docs learn-system), 0463 (examples network-poll shape) — standing user-proxy deferrals, triggers unmet.
- R13 ferry — proposed parked (Phase-1 decision).

## FIXME debt

| FIXME | Target skill | Status | Notes |
|---|---|---|---|
| 0590 | — (zombie) | **DELETED at Phase 1** | Audit R-1 executed: residual rustdoc sub-item verified CURED against source; deleted vs S110 evidence (`5ed07d60`); no phantom slot in scope |
| 0721 | /design(typecheck) | open — ships | Audit R-2 + R-1 design-doc riders (typecheck.md rewrite + carrier-doc 0590 row) |
| 0722 | /dev(typecheck) | open — ships | Audit R-3: program/tests.rs split + finalize re-budget (2× escalation flagged) |
| 0723 | /dev(typecheck) | open — ships | Audit R-4 + R-1 CLAUDE.md rider (currency sweep; codegen_view contract is the safety-relevant fact) |
| 0724 | /testing | open — ships | Audit R-1 rider: hkt probe comment correction (test KEPT) |
| 0604 | /dev(src) | open — ships (Track B) | 3-sprint carry, escalation flag honored: structural gate on merits, early wave |
| 0693 | /dev(typecheck) | deferred → fires now | Re-anchored trigger = the MS-P7 chained fix (Track A); fence BEFORE/WITH |
| 0694 | /qa | open — ships | Flap root-cause row (Track A) |
| 0696 | /design(backend) | open — ships | Rides W-B5 change-set |
| 0697 | /design(backend) | open — ships | Rides backend design touch |
| 0699 | /design(int) | open — ships | Qualify-walk residual gaps |
| 0702 | /spec → /qa | open — ships | Dotted-binder ruling framing; cells post-ruling |
| 0703 | /dev(frontend) | open — ships | Head-vocab mirrors residual |
| 0704 | /spec | open — ships | §5.2.7 wording |
| 0705 | /dev(backend) | open — ships | AutoCurry GOT-slot pair (Track A; collapse question first) |
| 0707 | /dev(src) | open — ships | Restore-notice count from record (minor) |
| 0708 | /spec | open — ruling ships | User framing; fix in-sprint iff attribution in-scope |
| 0710 | /dev(frontend) | open — ships | Diagnostic message parity |
| 0711 | /dev(frontend) | open — ships | Binder-reject wording |
| 0714 | /spec | open — ships | Impl-redefinition hot-reload scribe (ruled S114 close) |
| 0050 | /repl-adjacent | deferred | Display-protocol promotion sprint |
| 0052 | /docs | deferred | Standing |
| 0463 | /examples | deferred | Standing; re-verified S114 |
| 0553 | /arch | deferred | Trigger unmet |
| 0637 | /design(backend) | deferred | Parked to first consumer (R5 ruling re-affirmed S113) |

## Architecture review (Phase 2)

**/arch, 2026-07-20. Verdict: SIGN-OFF WITH REVISIONS** (three revisions, listed at the end; none re-opens scope — the draft is coherent, debt-first, and P8-clean).

### 1. Scope coherence + interim-architecture risk (Principle 8)

The sprint is pure stabilisation debt with structural (not interim) mechanisms throughout: the 0604 gate is isolation-by-construction (S61→S93 precedent), the synthesized-trigger test is fail-on-revert by construction, the R6 seam is the ONE validation chokepoint, and the impl-redefinition fix reuses the existing redefinition path (below). No P8 half-measures found. The conditional 0708 fix ("only if attribution resolves in-scope") is a legitimate deferral-with-rationale, not an interim build.

### 2. MS-P7 chained-face family — the design-first rule IS sufficient; frame made binding now

No separate /arch ruling is needed before /design(typecheck) deploys. The family-grain invariant ("every may-alias link whose accounting includes a consumer-emitted release needs its protect") is the R1/R14 producer-truth obligation applied per-link — an instance of P25, already governed by the landed §16 frame (`ownership-inference.md`: §16.1 lattice + §16.2 rule table + §16.3 producer split, all verified in source at HEAD). Three constraints BINDING on the /design → /dev chain:

1. The fix lands as **§16.2 rule-table rows/corrections at the family grain** — the enumerated-table discipline — NEVER a 5th per-consumer/per-context arm (the W7 arm was the 4th; a 5th is the instance-patch anti-pattern the plan's §0 risk-2 names).
2. **Contingency route pre-authorized**: if the family rule requires carrier enrichment (a new `ResultMode` shape or advisory fact in `cranelisp-types/src/ownership.rs`), that is a `cranelisp-types` edit — FIXME `target: /arch` + my approval + ONE `CACHE_SCHEMA_VERSION` window (currently the sprint plans NO bump; see §7). Surface the need at Phase 3, not mid-wave.
3. 0693 fence before/with (already binding); the Conditional-container face is probe-first (pin only a demonstrated RED).

### 3. GOT-slot carrier-loss pair — ruling: ONE seam contract, presumptively TWO fixes; evidence step named

The two defects meet at the same backend seam (the fn-as-value/AutoCurry wrapper emitter's loud keyed miss) but sit on **opposite sides of the carrier contract**:

- **0705 (AutoCurry-over-local)** is a CONSUMER totality gap: typecheck is complete and correct (`VarRef::Local` + `ApplyRef::ViaCallee` recorded; no dispatch FQ — rightly, a local closure has no GOT slot); the backend wrapper emitter only supports slotted global targets and must gain the **curry-the-local-closure-value arm**.
- **fn-as-value `'='` (plan §11 item 5)** is provisionally a PRODUCER gap (typecheck mono_collect fn-value rewrite seam mints the wrapper with no carrier for a global target that HAS a slot).

The unifying architectural commitment (P24 corollary prong 3 / P20 exhaustiveness): **the wrapper-emission seam must be total over the closed carrier sum** — every legal carrier state {`Dispatch(FQ)`-slotted, `ViaCallee` + `VarRef::Local`} has an emission arm, and anything else is a located producer error. **Evidence /design(backend) must produce before the fix wave**: for each repro, dump the carrier state as read at the wrapper-emission seam (one keyed-read trace: `VarRef` verdict + `ApplyRef` + slot presence). If the `'='` face also arrives `ViaCallee`, both collapse into the ONE backend totality change-set; if it arrives `Global`/`Dispatch` with no slot carrier, its fix is **typecheck-side** and rides a typecheck wave. **Scope-text correction (revision 1)**: the draft's Track A line files the pair under /dev(backend) ×2; plan §11 item 5 attributes the fn-as-value face to typecheck. Phase 4 keeps a conditional typecheck slot; the evidence decides.

### 4. 0604 structural gate — shape CONFIRMED against R7 + the S61→S93 precedent; two /arch findings feed the wave

The declared-export-closure predicate at the chokepoint, closure PRECOMPUTED (deadlock hazard honored), MODULE_TRACE at the seam, synthesized injected-trigger unit test, recipe demoted to bounded sweep, writer-ID desired-not-required — all conform to R7's register row and the isolation-by-construction precedent. Verified at HEAD during the register re-audit:

- **Both landed predicates are provider-existence-shaped and BLIND to the live phantom by construction** (`bit-and` IS a bundled public primitive, `cranelisp-primitives/src/lib.rs:412` — so `write_is_closure_valid` AND `prelude_write_is_closure_valid` pass it); `commit_staging_to_live` (src/worker.rs:439/:513) routes through NO gate. The census gap and predicate correction are both real and both necessary.
- **Tier-3 sub-form ruling** (the CS-2 taxonomy nuance): the promotion is to an **unconditional diagnosed error** rather than an abort-tier `assert!` — acceptable at this seam PROVIDED the message self-identifies as an internal R7 invariant breach naming the seam (never mistakable for a user diagnostic; a session abort would kill a REPL on a defect the user cannot act on). This is now recorded on the R7 row.
- **Rider added to the wave**: the falsified premise comment inside `prelude_write_is_closure_valid` ("bit-and … absent from primitives", src/imports.rs) is corrected alongside the /testing fixture-comment correction already in the FIXME.

### 5. Impl-redefinition hot-reload (0714) — no NEW cross-crate/ABI implication; P11 constraint binding

The redefinition machinery the fix needs **already exists** at `commit_staging_to_live` → `commit_slotted_def` (slot policy + freeze, `RedefinitionOutcome`, the ABI-changing dependent-recompilation transaction). Under the user's same-type constraint the re-impl is ABI-preserving ⇒ the **GOT-patch path** (same slot re-pointed), not the recompile/epoch path — cross-module compiled callers stay valid by construction (D35: GOT is the single source of truth for callable addresses). The epoch/freeze machinery is not engaged because a type-changing re-impl REJECTS (per the ruling), same as defn. Binding constraints: (a) **P11/P7 — the impl seam routes through the SAME redefinition commit path as defn**, never an impl-specific parallel path; the impl-specific residue is only the `TraitImpl` shell overwrite at the trait's home (same `impl_module`, D45) + the mangled method Def re-staging; (b) the same-type check applies §18's defn rule at the impl registration seam against the existing method's scheme; (c) /spec 0714 scribes FIRST (already a Phase-4 constraint). Ownership-summary skew on redefinition is NOT widened by this fix (it inherits exactly defn's existing exposure; the §5 dependent-recompilation machinery remains the R12-sprint answer). No cache/schema impact (session-side); no types edit. Sequencing note: this fix and the 0604 gate touch the same src seams — land 0604 (early wave) first.

### 6. Standing register re-audit — DONE, rows updated in place (`design/arch/safety-invariants.md` §4)

The re-audited register is the /qa matrix's baseline. Rows changed:

- **R1**: the §3b producer split did NOT slip — §3a/§3b/§3c ALL landed S113 W5 (`3297adf8`); `Origin::{Unconditional,Conditional}` is walk-internal (`transfer.rs:150`, no serde ⇒ the anticipated schema bump was unnecessary); hard-claim arms match only `Unconditional` (`transfer.rs:263–283`). Open instance = the chained-face family (this sprint's fix, family grain).
- **R7**: status corrected to `asserted`-but-BLIND — the "names its seam on next firing" claim was FALSE for the live phantom (both predicates provider-existence-shaped; census missing `commit_staging_to_live`); remedy text rewritten to the re-based 0604 wave incl. the diagnosed-error sub-form ruling and the stale-comment rider.
- **R8**: the S113 W5 tier-5 + tier-3 build FULLY landed — M1 quarantine / M2 scrub / M3 alloc-parity in `cranelisp-intrinsics/src/diagnostics.rs`, each with unit-tier synthetic self-tests (`diagnostics/tests.rs`: quarantine ×2, scrub ×2, parity ×4 — the §4.1 mandate satisfied); A1–A4 seam checks release-gated on `CRANELISP_RC_DEC_CHECK` (intrinsics funnels + `backend/src/heap.rs` + `vec_codegen.rs`). Owed: nothing structural; the /qa matrix verifies the standing `RC_DEC_CHECK` positive set (designed-vs-residue) and self-test currency.
- **R4/R6**: marked SCHEDULED S115; scope shape confirmed. R6's routing straight to /dev(backend, cache) is ACCEPTABLE — the register row + CS-2 taxonomy + the named seam constitute the design pin — with the revision that the census table lands as a durable artifact in the cache-submodule rustdoc and /review verifies census completeness (recorded on the row).
- **R13**: user-sanctioned S115 deferral recorded (parked on the test-discovery implementation wave).
- **R14**: refreshed — B-2 LANDED (schema window 21→22), MS-P7 attribution RESOLVED + immediate face fixed; open residue = the chained-face family (points at R1).

Unchanged after spot-verification: R2/R3/R5/R9/R10/R11/R12. Archive-triage note: `backend-keyed-consumer.md`'s parked physical move to `archive/` stays parked — S115 actively cites it (0705, the GOT-slot seam error text).

### 7. Public-API assessment

**No planned `cranelisp-types` edits and no planned `CACHE_SCHEMA_VERSION` bump this sprint.** Named contingency (the only one): chained-face carrier enrichment (§2 above) — if it materializes it is a types edit + ONE schema window (22→23), /arch-approved via FIXME before any /dev work consumes it. Everything else is crate-internal: 0709's located error uses existing error machinery; 0604's diagnosed error is src-internal; the GOT-slot fixes work over the existing closed carrier sums; R6's `CacheStale` classes are backend-internal; W-B5 is the endorsed backend-internal change-set; impl-redefinition is src-internal. `public-api.txt` baselines: no expected diffs; any diff appearing in review is a finding requiring my approval per the standing discipline.

### 8. Required Phase-4 sequencing (additions to the constraints already listed under §Waves)

1. /design(backend) produces the **carrier-state evidence dump** (§3) BEFORE the GOT-slot pair fix wave; Phase 4 holds a **conditional typecheck slot** for the fn-as-value face.
2. The MS-P7 chained-face chain is /design(typecheck) rule-table design → /dev, with the **types/schema contingency surfaced at Phase 3** (so a window, if needed, is coordinated once).
3. **0604 early wave lands before the impl-redefinition fix** (same src seams, both /dev(src)).
4. The backend RC-release sweep (entry-payload both faces + 0720) must not weaken the general G2/item-26 protect (§2.1 both-polarity fence, already in plan) and runs the tier-4 safety lane + RC_STATS pins as its acceptance.
5. R6's /dev(backend, cache) change-set lands the census table in the cache-submodule rustdoc; /review verifies completeness (revision 3).

### 9. Verdict

**Sign-off with revisions**:

1. **Scope-text correction**: the Track A "GOT-slot carrier-loss pair ×2 — /dev(backend)" line becomes conditional on the §3 evidence — the fn-as-value face may be a typecheck fix (plan §11 item 5's provisional attribution); keep a conditional typecheck slot.
2. **0604 wave rider**: add the src/imports.rs falsified-premise comment correction (§4) to the wave's disposition list.
3. **R6 execution shape**: /dev routing accepted with the census-artifact-in-rustdoc + /review-completeness requirement (recorded on the register row).

No user-facing questions from this review — 0708/0702 are already correctly routed to /spec for user rulings; the diagnosed-error sub-form question at the 0604 chokepoint is architectural and ruled here (§4). Phase 3 may proceed.

## Skill plans (Phase 3)

{Collected at Phase 3.}

## Waves (Phase 4 — FIRMED 2026-07-21; all source-touching waves SERIAL; wave-gate FIXME scan before each advance)

Sequencing constraints honored: 0604 before impl-redefinition (same src seams); 0693 backend fence (W3) BEFORE the typecheck §17.2 escape-fact correction (W4); RC sweep before W-B5; generative harness after the Track-A fix waves; the `'='` producer fix rides W4 per the /design(backend) dump verdict. FIXME number bands pre-allocated per dispatch (numbering-race fix).

### W1 — QA-first delta battery (Stage 1) — /testing sprint-wide

The 11 carry REDs are already-committed pins; W1 authors the DELTAS per `tests/plan/s115-test-plan.md`: 0702 M3 dotted-binder reject cells (+ `Maybe.Some` §6.2.1 positive fence + the qualified-type-param located-reject cell); 0709 declaration-time-reject cells (polarity per the traits.md §2 design; `(zed [] self)` GREEN boundary control); 0719 equivalence-twin acceptance cells (if not already pinned); 0724 probe-comment correction (test KEPT); any RC-sweep face cells the plan owes beyond the existing pins. NO 0708 pin (awaits ruling). FIXME band: 0727–0739.

### W2 — src early: the 0604 structural gate — /dev(src) then /review(src)

Per `prelude-table-write-isolation.md` (as corrected): route `commit_staging_to_live` through `check_terminal_closure`; destination-keyed declared-export-closure predicate + `SharedState.declared_exports` map (precompute-before-guard); MODULE_TRACE at the seam; synthesized-trigger unit test (provides-name-but-outside-declared-exports — the /qa §3.1 binding shape); falsified-comment rider; ≥25× demoted no-regression sweep + ONE time-boxed load-amplified re-induction attempt; 0707 (form-granular, subtract `failed_forms`). Band: 0740–0744.

### W3 — backend — /dev(backend) then /review(backend)

Ordered change-sets: (1) 0693 consolidation + disagreement fence (MUST complete before W4); (2) RC-release sweep (entry-payload + 0720 ×2 per `s115-carrier-and-rc-sweep.md` §2; hazards honored); (3) 0705 curry-the-local-closure arm (totality table, no `_ =>`; ViaCallee+Global = located producer-contradiction error); (4) R4 `got_data_symbol_name` injectivity witness + census landing; R6 validation-seam extension in `deserialise_meta_with_build_id`; (5) W-B5 patch collapse (own change-set, incl. 0696 resolution; goldens certified). Band: 0745–0754.

### W4 — typecheck — /dev(typecheck) then /review(typecheck)

Gate: W3 change-set (1) landed. MS-P7 chained family fix (`ownership-inference.md` §17 rows; contingency tripwire = §17.6 — any schema bump outside it is a plan violation); `'='` producer fix at `mono_collect.rs::resolve_auto_curry` (re-resolve against concrete instance types; boundary: never transport a trait-method-decl FQ as dispatch carrier); 0719 window-3 extension (`monomorphisation.md` §11.8.11); 0709 declaration-time reject (`traits.md` §2); 0722 program/tests.rs split + finalize re-budget (re-survey stale anchors per the currency note); 0723 CLAUDE.md sweep. Band: 0755–0764.

### W5 — frontend — /dev(frontend) then /review(frontend)

Per `binder-head-reject.md` §2.2: the ONE-helper `.`-widening + `split_dotted_name` sibling + the type-param routing call + the 0711 message generalization; 0710 reader-seam parity one-liner; 0703 residual. /review greps per the design's structural acceptance. Flips the W1 0702 cells. Band: 0765–0769.

### W6 — int second — /dev(src) then /review(src)

Gate: W2 landed. Impl-redefinition hot-reload fix (`impl-redefinition-hot-reload.md`: enroll mangled method Defs into the FORCED loop; flips the S114 pin's hot-reload branch); 0718 qualify-walk fixes (quote shield + defn self-name + defmacro shield, §2.4–2.6). Band: 0770–0774.

### W7 — stabilisation close: harness + 0694 + certification

(1) /testing bounded generative-harness v1 dispatch (memory-safety-coverage §2; slip → explicit user sign-off for S116); (2) /qa 0694 nullary-flap root-cause row (suite-load experiments now safe — fix waves done); (3) certification: ≥2 identical full-suite runs, stable-REDs-exact + named-flap set, all 11 carries flip-verified against attribution, instrumentation matrix re-checked (every OWED row now VERIFIED). Band: 0775–0779.

| Wave | Skill | Surface | Status |
|---|---|---|---|
| W1 | /testing | sprint-wide | **done** (`77ea2b82`) |
| W2 | /dev → /review | src/ | **done** (`d9f2caea` + review `c9d1585e`) |
| W3 | /dev → /review | cranelisp-backend | pending |
| W4 | /dev → /review | cranelisp-typecheck | pending |
| W5 | /dev → /review | cranelisp-frontend | pending |
| W6 | /dev → /review | src/ | pending |
| W7 | /testing + /qa | suite-wide | pending |

## Dispatch log

| Wave | Agent | Surface | Model | Effort | Non-default reason |
|---|---|---|---|---|---|
| P2 | /arch | scope review + register re-audit | fable (shim) | xhigh | — |
| P3 | /qa | test plan + instrumentation matrix | fable (shim) | xhigh | — |
| P3 | /spec | 0714/0704 scribe + 0702/0708 framings | opus[1m] (shim) | high | — |
| P3 | /design | cranelisp-typecheck | opus[1m] (shim) | high | — |
| P3 | /design | cranelisp-backend | opus[1m] (shim) | high | — |
| P3 | /design | src/ (design/int/) | opus[1m] (shim) | high | — |
| P3 | /design | cranelisp-frontend (0702 chain) | opus[1m] (shim) | high | — |
| W1 | /testing | sprint-wide delta battery (+0708 pin addendum) | opus[1m] (shim) | high | — |
| W1∥ | /arch | annotated-Sexp contract (design-only, 0708) | fable (shim) | xhigh | — |

## Notes

- 2026-07-20: Phase 1 draft authored. Escalation items surfaced: 0604 (3-sprint carry — ships as structural gate); audit R-3 is the S108 R-4 acceptance completed at last (second carry of a half-executed acceptance — flagged). Audit disposal proposals R-1..R-4 all ACCEPT + METHOD amendment + R13-stays-parked presented to user.
- 2026-07-21: **W5a DONE (spec scribe + corpus repair), committed `c02abab4` — suite 5315/5291/24/1; the 24 REDs IDENTICAL name-for-name; NOTHING changed colour** (every repaired cell GREEN before and after, including those whose assertions were STRENGTHENED). **0702 scribed: 14 of 18 §5 binder-table rows changed + a `[S115]` ruling block (located reject, span on the name) + an explicit reference carve-out (`Maybe.Some`, `Type.field`, dotted type/trait refs, dotted import paths — "this bans `.` in the binder column ONLY") + the qualified-type-param rider; FIXME deleted. ★ /spec found the rule had FOUR COPIES — a MIRROR FARM IN THE SPEC: the categorical §5 prose, the per-row table, TEN per-subsection §5 restatements, and FOUR out-of-file restatements (04-expressions, 06-pattern-matching, 07-traits) that CITE "§5 Binder positions" and then restate it anyway. All reconciled; /spec recommends converting the restatements to pure citations (follow-up).** 0708 cascade scribed across §1.4.5 (+ §1.8 grammar: `annotated_form` added as a `form` alternative and `colon_prefix`/`colon_bare` REMOVED from `atom` — the standalone-atom invariant is now a GRAMMAR PROPERTY), §2.3.8/§2.4, §3.9, §9.1.2 (`SexpAnnotated` ADT row), §9.2.2 (the normative `(def x :Int 5)` example), §9.4.2, §7.1.1; FIXME 0708 annotated NOT deleted (retires with S116), recording **0785's corpus repair as a HARD PREREQUISITE of the S116 flip wave**. **★ /spec's mechanism note changes W5b's scope: under the read-time fold, an invalid return-position `:` is a TRAILING INTRODUCER before `)` — rejected MECHANICALLY by the reader, no trait-specific check needed. /sprint SCOPE DECISION (Principle 8, interim architecture avoided-not-deferred): do NOT build a separate return-position enforcement in W5b — it would be throwaway infrastructure the S116 fold replaces. The durable answers are the corpus repair (DONE) + the corpus-lint instrument (0785).** **Corpus repair: 23 sites across 7 files + 2 MORE found by re-grep beyond the enumerated list — and one was not cosmetic: `(req [a] :Int self)` was SILENTLY PARSING AS A DEFAULT METHOD (return `:Int`, body `self`) where a required method was plainly intended.** 2 kept as genuine negatives (verified assert rejection); final grep clean — zero positive fixtures retain the form; a "why `defn` differs from `method_sig`" note added at each repaired cluster. **0770's five cells re-decided, not preserved**: the `:a 7` cell now pins **`:primitives/Int 7`** (a genuinely stronger assertion); each cell's SUBJECT preserved (the annotated-named-param cell keeps `:a n` with bare `x` supplying the occurrence; the qualified-ref cell keeps the qualified ref in BOTH positions). One extra cell repaired alongside with a **semantic flag for /qa**: `arg_directed_dispatch_result_in_value_position_not_flagged`'s premise was "the recorded result-span type is the residual `:a`" — `self` preserves non-concreteness but NOT residual-type-var-ness, so the premise was rewritten honestly and the near-vacuous `contains(":a 7") || contains("7")` tightened to `contains(":primitives/Int 7")`; if /qa judges the residual-var premise load-bearing for the R16/R17 gate, that needs a differently-shaped fixture (small real coverage question). **7 GREEN e2e cells for the 0781 seams** through the MS-P1 SafetyMatrix combinator (modes × toggle × differential × RC balance), **including one beyond brief — `(vec-get (if b v v) 0)`, 0781's own minimal reduction, the cell where a partial re-break would hide** — plus 2 bare-`Var` discriminating controls proving the pins are a NARROWING of the release, not its deletion; the toggle-OFF leg is load-bearing (a syntax-keyed release is emitted regardless of the analysis, so all-Owned lowering is NOT a safe harbour for this class). 0781 `defect:` notation re-attributed typecheck→backend with the transferable lesson recorded in-comment. **Phase-6 routing owed to /repl: 11 demo-run dirs carry the invalid form in BOTH `user.cl` and `.cranelisp_history`; `(deftrait (Sizeable a) (size [:a x] :Int))` is TRIPLY wrong** (invalid return syntax + never-applied con-var head §7.2.1 + no implementing-type occurrence); one (`traits-probe`) may be an intentional malformed probe — /repl confirms before repairing. Zero FIXMEs filed (all findings route through open 0785/0781 — filing a duplicate for an issue with a record is the anti-pattern).
- 2026-07-21: **/spec 0770 ruling SCRIBED + §7.1.4 corrected.** §7.1.1 gains the dated `[S115]` ruling (rejection scoped by OCCURRENCE not parameter count — rejected at any arity; diagnostic reason "no occurrence of the implementing type") + a Rationale paragraph carrying the user's reasoning (no trait-level type variable; §7.1's parenthesized-head precedent; no `<Foo as Trait>::method` analogue ⇒ undispatchable by construction) + "method-level type variables are unaffected" + a marker-trait forward-pointer (zero-method traits inexpressible under §7.1, a SEPARATE capability question — no marker semantics scribed). **§7.1.4's `Convertible` example corrected to `(convert [:String s] self)`** — and the tell: `spec/02-grammar.md:721` ALREADY had the correct form, so §7.1.4 was the SOLE divergent copy (a mirror defect in the spec itself). **★ The root of the `:a` confusion is now NAMED in the spec**: `defn` legitimately takes `:Int` in the trailing slot (a `defn` has a BODY, and the `:` annotates that body in value position per §3.3.3), whereas a required `method_sig` has NO body — so its trailing element IS the return `type_expr` and takes no `:`. That contrast almost certainly produced the ~25 invalid fixtures, and it was previously left to inference; the return-position rule is now promoted to its own bolded paragraph. Spec-wide sweep: **no other spec example carries a return-position `:`** (the `] :Type` hits in 08-modules/03-types are all legal `defn` bodies). One UNRELATED malformed example found + fixed while sweeping (`spec/03-types.md:611` §3.7.1 — `fmap` params had neither `:` nor names, violating the `param` grammar in both slots; same class: a spec example contradicting the spec's own grammar). §7.2 gains an HKT-exemption cross-reference. §7.3.6's open sub-question deliberately NOT closed (the ruling narrows its candidate answer without confirming it — its own user gate). FIXME 0770 deleted. **/spec's flag for the wave plan: the shipped `registry.rs` scope (no-params AND no-`self`) is now spec-WRONG — /dev must widen to `!method_mentions_self(...)`, and the five cells encode a WRONG-ACCEPT to be re-decided, not preserved.**
- 2026-07-21: **W4c DONE, committed `56969823` — suite 5308/5283/24 stable/1 (exact: +12 new tests, −2 REDs).** The syntactic-shape class closed with **ONE derived answer**: `value_provenance`, a three-point lattice (`Fresh ⊑ OwnedTemporary ⊑ NotOwnedHere`, join = weakest arm, forwards through `Let`/`If`/`Match`, capped at `OwnedTemporary` through `Trace`/`ParBind`) — **`is_fresh_construction` is now literally its `== Fresh` face** (byte-identical on every kind). Principled two-threshold argument, pinned: the two consumers read the SAME lattice at two heights and differ at exactly one node kind (a general `Apply` — protect-elision needs "cannot alias a scope binding", release needs only "nothing else will release it", which the Decision-24 owned-return ABI satisfies); collapsing them would either leak every call result or strip every call result's protect. **FIVE gates converted, not one** — and **two of them were live `--link` 134 UAFs of the identical class found by the sweep** (`match_codegen` var-pattern alias + temp-scrutinee dec: `(match (if b v v) [xs (vec-get xs 0)])` 134→9, plus let-yielding and ADT twins). Scope call disclosed and correct per 0765 (each extra fix carries its repro at the unit tier /dev owns; **e2e cells REQUESTED IN-WAVE, not deferred** — routed to W5a). **Instrumentation answer (b) — the sharpest yet: `is_fresh_construction`'s exhaustive match IS a real instrument and works, but it guards ONE COPY of the answer; the class is "a second copy exists elsewhere phrased as a shape test", and an exhaustiveness obligation on the canonical predicate is STRUCTURALLY INCAPABLE of seeing the second copy. Every fix in this family has been "delete the second copy"; nothing prevented a third.** → landed a NEW KIND of instrument in-crate: `rc_ownership_fence_tests.rs`, a **structural fence** — no `matches!` in the RC-decision file set may test a `MonoExpr` kind ALONE (field-keeping tests are legitimate questions and are not fenced; `apply.rs` excluded with its reason recorded). **Detection proof measured**: reverting any one gate flips the fence RED naming `file:line`, one violation per reverted gate (verified ×3), plus a false-fire control. Architectural half → 0783 (/arch, likely folds into 0776's row). Class sweep: 9 `matches!` sites, 5 were the defect (fixed), 4 are genuine syntactic questions (unchanged). **NEW pre-existing defect found and NOT fixed — filed to the new standard**: 0782, var-pattern arm double-releases a genuinely-owned temporary scrutinee (`(defn f [] (match [7 8 9] [xs (vec-get xs 1)]))` → 134 at parent too), filed **with repro + discriminating control + seam observation + falsifiability clause** (the METHOD §2.2 amendment applied within an hour of landing), and its unit control deliberately asserts "at least one release" not a count, so it does not freeze the number 0782 must change. 0 warnings; clippy parity; backend 487/487; no public-api/types/schema/golden delta.
- 2026-07-21: **USER RULING — 0770 SETTLED (b)**: the broad §7.1.1 occurrence rule STANDS — a trait method that mentions the implementing type nowhere is rejected. Rationale confirmed against spec: a conventional trait carries NO trait-level type variable (the implementing type is `self`; §7.1 already rejects the parenthesized-head version of the same mistake, `(deftrait (Sizeable a) …)`), and Cranelisp has NO explicit-qualification syntax (Rust's `<Foo as T>::m` escape hatch), so a no-`self` method is undispatchable by construction. §7.1.4's `Convertible` example is itself the defect (an example violating §7.1.1's own MUST) → /spec corrects it. **Method-level type variables SURVIVE** where `self` also appears (`(map-val [:(Fn [a] b) f x] self)` stands). User raised Send/Sync/Sized — correctly identified as MARKER traits (zero methods): a separate, currently-inexpressible feature (§7.1 requires ≥1 method); if wanted, relax the one-method minimum as its own scoped decision, NOT via undispatchable methods. Routing: /spec scribes + corrects §7.1.4; /dev(frontend or typecheck) drops the nullary guard → `!method_mentions_self(...)` (predicate already written + unit-tested); /testing repairs the five fixtures (all are INCIDENTAL scaffolding, not tests of this rule — one-line each).
- 2026-07-21: **★ USER FINDING — `:Type` in RETURN position is INVALID SYNTAX and the corpus has normalised it.** §7.1.1 is explicit ("Return position is a bare `type_expr`; the leading `:` is parameter-annotation syntax only … never `(zed [] :self)` / `(zed [] :a)`"); grep confirms **~25 positive fixtures written in the invalid form** across `spec_07_traits.rs` (×12), `spec_05_definitions.rs:1594`, `spec_qualified_name_sweep.rs:90`, `w2_close_fences.rs:136/174`, `repl_persist.rs:1229/1268`, and committed `repl/demos/runs/*/user.cl`. (The two `(zed [] :a)` cells are legitimate NEGATIVE fixtures citing the spec's own malformed example.) **This is why it "comes up over and over": an unenforced MUST let the wrong shape become the house style, and the corpus then supplied FALSE EVIDENCE in later reasoning** — 0770's fork was framed on "five GREEN spec-traceable cells", one of which pins `(add2 3 4)` → `:a 7`, certifying an unresolved type variable as expected output. Same class as 0702 (unenforced §5 MUST). **Sequencing-critical: the S116 0708 implementation will light up EVERY one of these** (under Reading-A-structural, `:Int` with no following form is the trailing-introducer reader error) — corpus repair is a PREREQUISITE for that wave, not a follow-on. → **FIXME 0785 (/qa)**: attribute the enforcement gap (**opportunity: S115 W5 is the frontend wave already landing 0702's unenforced-MUST enforcement — same class, same crate, same one-predicate-at-the-seam shape**), draw the `{parameter, return} × {annotated, bare} × {deftrait, defn, deftype field}` matrix (the return column has never been tested for rejection), repair ~25 fixtures, and sweep for sibling positions. **Candidate standing instrument: a corpus lint parsing every `.cl` fixture against the spec's own grammar productions** — no instrument watches for "the corpus is written in a form the spec forbids", which is why the USER found this and no sweep did.
- 2026-07-21: **USER (methodology): "we're seeing a lot of misdiagnosis of issues. Do we need to strengthen our confirmation of repros?"** → **YES, and the gap is precisely named: a repro confirms a SYMPTOM; only a CONTROL confirms a MECHANISM.** S115's misdiagnosis tally (all with the symptom reproducing exactly as recorded, and the mechanism wrong): entry-payload's two design-named mechanisms BOTH falsified; 0749's root cause an unbalanced protect INC not the predicted missing dec; the RC sweep's named candidate `is_heap_type` falsified; 0753 predicted "benign artifact", was a real leak; 2 of 3 repro cells filed against typecheck were a BACKEND syntactic-shape test; `'='` and 0719 root causes both elsewhere than designed. **METHOD §2.2 amended (binding)**: an attribution now carries (1) a **discriminating control** — a sibling identical except for the claimed cause, behaving differently; every S115 diagnosis that HELD UP had one (the arm-swapped twin; variant-B supersede; the plain-lambda + global-target curry controls; the direct concrete `(= 3)`) — and **if no control can be constructed the mechanism is a HYPOTHESIS and the attribution is PROVISIONAL, said in those words** so the receiver probes rather than implements; (2) **the mechanism OBSERVED at the seam** (trace/carrier dump/CLIF diff/measured count) — "the symptom is consistent with X" is not evidence for X; (3) a **falsifiability clause**, with the receiving skill's FIRST act being to attempt the refutation — where this was written into a dispatch (the MS-P7 adjudication) it worked and cost nothing; where omitted, a wave was spent against a false premise. /review: mechanism claim with no control + no seam observation = **Important**; a FIX built on one = **Blocker** (an unguarded narrowing of the search space). Economics: a control costs minutes at diagnosis and saves a wave at fix time, and is cheapest exactly when the mechanism is freshly in hand — the same moment as the instrumentation question.
- 2026-07-21: **W4b DONE (repro-before-fix, first full application), committed `94dd3319` (repros) + `509cd9da` (fixes) — suite 5296/5270/26/1 exact.** /testing landed the repros FIRST as committed REDs, incl. **the passing arm-swapped twin pinned beside its failing sibling as the P24 acid-test evidence** (a fix curing one order cannot pass the pair) + 2 GREEN controls; all cells assert the SPEC-CORRECT contract (exit 1, abort-free), never the abort signature, so nothing inverts on the fix. **0772 FIXED — and it was TWO defects in one arm**, not one: the same-param/same-kind arm dropped the row-4 union AND published a hard `AliasOf` from a may-alias operand; now takes the ⊤-ward variant of BOTH operands and keeps the union regardless of order (both-`Unconditional` path byte-identical to pre-fix). **The instrument landed WITH its detection proof**: 4 seam-level property cells over the `Origin` lattice (no program, no `MonoExpr`; representative set = 9 origins × every ordered pair, both orders) — reverted, **3 of 4 go RED each naming the exact mechanism** ("dropped may-alias link Span{1,2}" / "published a HARD claim from a may-alias operand" / "join(U,C) ≠ join(C,U)"); the 4th is the documented row-8 exception, correctly insensitive. This is /testing's "property cells over the operand lattice, not example cells over one syntax tree" rule, landed and proven. **★ ONLY 1 OF 3 REDs FLIPPED — the other two are a BACKEND defect and 0772's recorded reason for them was WRONG.** With the fix, `OWNERSHIP_TRACE` shows the join producing the `Conditional` and row-6 firing — the walk is correct. **Reduction is devastatingly small: `(defn f [v b] (vec-get (if b v v) 0))` → `--link` 134. No `let`, no COW, no may-alias, BOTH ARMS IDENTICAL** (and a fresh-vector variant with no param reach also aborts); negative controls clean; aborts at HEAD *and* pre-0772, so not a regression. **Locus: `backend/compiler/vec_codegen.rs::emit_vec_drop_if_temporary` decides "owned temporary" SYNTACTICALLY (`matches!(vec_expr, MonoExpr::Var{..})`), so an `If`/`Match`/`Let` that merely YIELDS a borrowed value takes the release path — the un-converted member of the W3 "one predicate, not per-site syntax" family (`is_fresh_construction` already answers the right question and forwards through joins). FOURTH instance of the syntactic-shape-instead-of-derived-answer class this sprint.** → 0781. **0774: the comment described a REAL gap** — implemented (a); without it the self-call callee `Var` carries the RESULT type; pinned, revert→RED. 0719 template arm pinned (all three arms now pinned). **0775: discipline is now a required parameter at all six seams; and the instrumentation answer was MEASURED not asserted** — /dev flipped each seam to the opposite discipline and ran the tier: **only 1 of 6 reddens**, including its own new twin failing to detect `:441` — the cell says so in its own comment rather than posing as a pin → 0779 (/qa). 0 warnings; clippy parity; no public-api/types/schema delta; unit tier 808/808. FIXMEs 0772/0774/0775 resolved+deleted; 0779 + 0781 filed.
- 2026-07-21: **W4 DONE + REVIEWED, committed `d4efdf08` + `bd5628a8` — suite 5283/5258–5259/24 stable + flap/1; ALL SIX FLIPS CONFIRMED; every remaining RED traces 1:1 to a known open defect or pending wave; zero `#[ignore]` in the crate; no types/schema/public-api delta.** **REVIEW VERDICT: APPROVE-WITH-REQUIRED-FIXES. BLOCKER 0772 — `join_origin` computes the §17.2 cow link-set union and then DISCARDS it unless the FIRST operand is `Conditional`** (`other => other` arm); `MonoExpr::If` joins in source order, so `(vec-get (if b v (vec-set v 0 1)) 0)` aborts `--link` 134 while its **arm-swapped twin exits 1** — **the P24 acid test failing by name: the answer depends on incidental order** (the let-bound face aborts in BOTH orders). NOT a regression (RED at parent too; face 3 was §17.4 probe-first) but it **falsifies the wave's own §17.3 sufficiency claim** → design correction 0777, repros 0773 (/testing), fix 0772 (/dev). What the fix DID deliver, verified against a parent build: a three-link nested chain was 134 and is now clean; cross-function/nested-let/mixed chains clean; §17.3's STRUCTURAL claim holds by grep (no new consumer arm — the syntactic reach was deleted for a loop); no spurious retain (allocs==deallocs); §17.6 tripwire correctly did not fire. **FOURTH-WINDOW QUESTION ANSWERED INDEPENDENTLY: NO on the letter, YES on the pattern** — still exactly three `pass4_monomorphise` invocations (grep-verified); W4's addition is a re-drain of a DIFFERENT pass at an already-existing sequence point, so §11.8.10 is not breached and no /arch escalation was owed in-wave; but the wave grew "one operation at N seams" in a second pass with WEAKER discipline (six non-equivalent seams collapsed to two enum values, mapping in prose, dangerous polarity defaulted) → 0775 (/dev, P18: make the discipline a required parameter at all six sites). Drain verified idempotent (`mem::take` both vectors); `fq_is_trait_method_decl` verified genuinely carrier-keyed (P24). **0774: the 0719 self-call "arm" is 21 lines of comment describing a retype followed immediately by `return Ok(())`** — nothing to keep; must be implemented or rewritten as a reason. Template arm = real but unpinned → pin-or-revert (Suggestion). **0709 narrow scope RATIFIED as the right call** (collision verified directly: all five GREEN cells use parameterised no-`self` methods; shipped scope satisfies all four worked examples; fence makes widening deliberate) — /review expressly declined to touch the normative fork (correct). **0722 CLEAN: test-name set diffed old-vs-new — ZERO dropped, exactly 3 added (213→216); `test_support.rs`/`test_driver.rs` both `#[cfg(test)]`-gated fixtures only; finalize→ambiguity is a pure move.** Detection proof VERIFIED discriminating by structure + parent-binary evidence (caveat disclosed: the literal revert-rebuild exhausted tmpfs and was abandoned; repo untouched throughout). **Recurring-class ruling: /dev's "inline-vs-deferred ×3" is WRONG on count (0693 = name-vs-carrier, 0705 = consumer totality; only 0719 is clean) — but a BROADER class clears the bar with FOUR instances this sprint: "an operation performed at N non-equivalent seams where each seam's version can silently omit an obligation its siblings discharge" (3 mono windows; 6+1 auto-curry seams; inline-vs-deferred dispatch; §11.8.9 scan discipline). §11.8.10 fences instance 1 BY HAND and nothing generalises it → 0776 (/arch), framed as the seam-analogue of the standing coverage-by-variants category: the matrix is `seam × obligation`.** Process incident (agent-disclosed `git checkout <path>` on its own probe): reflog verified NO loss; noted as a real breach — same operation as the forbidden form. **Band reconciliation: /review used 0772–0777 per its dispatch; re-allocating W4b = 0778–0779, W5 = 0781–0784, W6 = 0785–0788, W7 = 0789–0794 (0780 taken by /arch's stdlib-unwrap FIXME).**
- 2026-07-21: **USER (methodology): "instrumentation needs unit tests that prove they catch the things we assume it does."** → **METHOD §2.2 amended: "An instrument is unverified until it is proven to detect" (binding).** Every assertion/gate/fence/validator/lane/oracle ships with a **capability test that plants the fault it claims to catch and observes detection**; until then its green is not evidence of health but evidence of NOTHING — and will be read as the former. Proof shapes match instrument shapes: **gate → fails on revert** (S115 0604); **variant-family validator → per-variant RED-then-GREEN + false-fire fence** (S115 R6); **lane/mode → planted SYNTHETIC fault, never a live defect** (planting on a real bug means the fence dies with the fix — the m1/m3 lesson, twice); **conditional fence → pinned in EVERY build configuration it ships in** (S115 0751's release-mode cell). **The decisive framing: we already had this rule, scoped one level too narrowly.** `memory-safety-coverage.md` §4.1 has long mandated a synthetic self-test per diagnostic MODE — and those fences exist and PASS (quarantine ×2, scrub ×2, parity ×4). The **lane composed of those modes** carried no fence of its own, so its differential signal sat green through five real leaks in the shared codepath. **The proofs of the parts do not compose into a proof of the composition.** Enforcement filed at both acceptance instruments: **0767 (/qa)** — a matrix row is VERIFIED only when it CITES its detection proof; "mechanism exists at file:line" and "a test exercises it" are both weaker and neither substitutes; rows without proof take the distinct status `asserted-but-unproven` (an open item, not a pass); re-audit the current 14 VERIFIED rows against the stronger bar (**movement is the finding, not a regression**); fold capability fences into 0761's lane from day one (planted leak + planted OVER-release — the polarity no residue-allowance ever catches — + planted constant-vs-scaling); generalise §4.1's scope from "diagnostic mode" to "instrument, including a composed one". **0768 (/arch)** — `asserted`/`gated`/`dynamic-lane` register statuses require a cited proof, else `asserted-but-unproven` ranked below `matrix-tested`; R7 is the worked counterexample (recorded `asserted`, predicate structurally blind, "no false fires" read as health when it was silence); re-audit R5/R10 (which look qualified — cite rather than assume, since assuming is what this stops); and consider recording per row **what the instrument is BLIND to**, since a blind spot is invisible precisely when the instrument is green. **Phase-7 principle candidate, sibling to "A fix carries its repro": *an instrument carries its proof of detection*.**
- 2026-07-21: **W3c DONE — suite 5271 run / 5241 passed / 30 failed / 1 skipped (exact: 5255 + 10 GREEN pins − 1 retired + 7 0760-family = 5271; REDs 28 − 1 + 3 intended = 30; 27 carried names byte-identical).** **0763 e2e tier CLOSED**: 9 cells (`tests/rc_escape_release_0763.rs`) asserting EXACT balance in BOTH toggles (+ `--link` on the escape rows), /dev's numbers reproduced verbatim; the 0720 pins UPGRADED from `residue ≤ 8`/non-scaling to exact equality at N ∈ {1,2,200,400} × both toggles. Harness subtlety recorded: read the LAST `[RC_STATS]` line — under `--link` the compiler process emits its own 0/0 first (taking the first would silently measure the linker). **ANSWERING THE USER'S EXEMPLAR QUESTION: the nested `solve-range` shape (ADT whose field is a Vec of ADTs) is GREEN** — 401/401 scalar leaf, 601/601 String leaf; the W3b type-directed release DOES recurse ADT→Vec→ADT→String. **NEW DEFECT MEASURED FOR THE FIRST TIME: the `MAX_DROP_GLUE_DEPTH = 4` cliff is exactly at nesting depth 5** — depths 1–4 balance (201/201…501/501), depth 5 = 601/501 (1/iter), depth 6 = 701/501 (2/iter), one further leaked object per level, toggle-independent, UNBOUNDED IN DEPTH. Found by walking the axis one step past anyone's list. This materially strengthens 0760's option (b) (named drop-glue fns collapse both faces). **0760's K/L now committed REDs** (401/201, 301/201) + 4 GREEN controls — the prose-only-defect gap is closed. 0746: **§4.1 tombstone fallback taken** (synthetic hook needs /dev(intrinsics) source — outside /testing's boundary); tombstone names both drained plants, the four unit-tier successors by exact path:line, the surviving wiring face, and the re-entry condition. NOT RED at certification. 0705 riders landed (present-tense framing stripped; born-green non-trait control added). **Flap family now THREE**: + `agent::y_short_flag_errors_on_non_agent_build` + `multi_sig_module_locality::imported_multi_sig_base_direct_call_repl` (RED run 1 / GREEN run 2, same tree; declared-GREEN fence, same should-be-green-under-load signature, same multi-sig seam family). Stable-REDs-exact = 30; named flap set = 3. One /dev claim did NOT reproduce (0720 bare-vec `reuse_hit=200` ON — actually 0/200 in BOTH toggles at HEAD; balance exact either way, no pin depends on it) → stale claim flagged to /design(backend). FIXMEs 0763+0746 resolved+deleted; 0694+0760 appended; **0766 filed (/qa — PLAN rows + spec-side `[Tested]` annotations for 16 new/2 upgraded/1 retired cells)**. **Instrumentation answer (b) BLIND, two independent reasons — the sharpest formulation yet: the lane asserts `imbalance(ON) == imbalance(OFF)`, a DIFFERENTIAL OVER TWO CONFIGURATIONS OF ONE CODEPATH, so it is structurally incapable of seeing a fault in the SHARED part — all five leaks lived in `is_fresh_construction` (not ownership-gated), so every cell was `0 == 0`: "the lane's pass was not weak evidence, it was NO evidence."** Second blindness: even for the toggle-asymmetric 0753 the corpus held no shape of that kind (shipped shapes differ from pinned ones in WHERE THE VALUE GOES, not what it is). Corollary for 0761, binding: **every weakened form is also blind** — our own `residue ≤ 8`/non-scaling pins could not have seen 0753's constant residue of 1, and no residue-allowance ever catches the opposite polarity (premature free); exact equality catches leak, under-count and constant faults with ONE assertion. Lane must be a GENERATED PRODUCT {owning type incl. depth 1..6} × {position: in-place, let-bound, returned, returned-through-N-lets, curried-partial, captured-escaping, Borrowed-arg, loop-carried} × {toggle} — hand-enumeration is exactly how C/D/E/F and both 0760 faces shipped. Affordability measured: 16 cells ≈ 22 subprocess runs < 3s; a ~60-cell lane is cheap.
- 2026-07-21: **/sprint PROCESS INCIDENT #2 (self-disclosed, SAME error one message after recording its correction): I committed `0f6db214` while W3c was in flight** — a `git add design/arch/fixmes/0764-*` glob matched the agent's in-flight file, and `git commit` took the agent's ALREADY-STAGED `git rm` deletions (agents stage as they work, so a pathspec-limited `add` does NOT bound what a bare `commit` takes). Re-split via `git reset --soft HEAD~1` into `9a186c19` (methodology) + the W3c commit; no content lost, nothing pushed. **Root cause was not the glob — it was committing at all during an active source-touching dispatch.** Hardened rule (stronger than the first attempt, which was advisory and failed within one message): **/sprint commits ONLY after a wave's completion notification has arrived, and ALWAYS inspects `git diff --cached --name-only` before committing.** Two instances = a recurring class; this is now a Phase-7 METHOD amendment candidate, not a note.
- 2026-07-21: **USER (methodology): reinforce that /qa+/testing do MINIMAL REPRODUCTIONS BEFORE the fix — to catch CATEGORIES, expose weaknesses in the testing method, and preserve REGRESSION tests; "where do we put this so it survives every issue? is it an instruction to /dev not to fix without a defect?"** → **Diagnosis: the rule already existed THREE times and still lapsed** — §2.3 covers cross-skill HANDOFF, root CLAUDE.md §Usability-Findings covers CLOSURE, METHOD §2.2 covers the fix's OWN tests; none reaches the moment a /dev agent mid-wave finds defects nobody dispatched it for (S115 W3b: 3 extra live leaks found + correctly fixed, e2e tier owed to a FIXME; 0760's two MEASURED leaks lived as prose with exact numbers and no failing test). A fourth restatement would have been the mirror/duplication anti-pattern this project fights. **Placement chosen: ONE normative statement + ONE enforcement gate + ONE forward-stated precondition.** (1) **METHOD §2.2 "Repro before fix" (binding)** — the normative home, stating the sequence (reduce → commit RED → fix, same change-set), the three deliverables reduction produces that a fix cannot (category-not-instance; the answerable "why didn't the suite have this"; the only artifact that survives the sprint), the anti-escape-hatch rule (**a tier you don't own is REQUESTED IN-WAVE; "FIXME next sprint" is not available for an already-measured defect** — /sprint schedules it), and the genuine-irreducibility carve-out (record the attempt + evidence). Root CLAUDE.md §Testing already cites METHOD §2.2, so it strengthens by reference — **no CLAUDE.md edit needed, no duplication created**. (2) **FIXME 0764 (/review): the Blocker gate** — a fix whose defect has no committed minimal repro in the same change-set is a Blocker, placed beside the existing Principle-25 narrowing check so the two read as one discipline at two altitudes; corollary: when a change-set fixes MORE than dispatched (the valuable case), each extra defect needs its own repro. (3) **FIXME 0765 (/dev): "no fix without a repro"** stated forward at the point of temptation, paired with the instrumentation question (both answered from the same freshly-held mechanism). **Phase-7 candidate: promote to an architectural Principle — "A fix carries its repro", the sibling of P25 "Narrowing carries its check"** (principles are auto-imported into /arch and cited project-wide = the most durable layer).
- 2026-07-21: **W3b DONE (in-wave fix batch) — all 5 review findings closed; suite 5255 run / 5225 passed / 28 stable REDs + 2 flaps / 1 skipped.** **0749 BLOCKER root cause was NOT "no dec" but an UNBALANCED PROTECT INC** (`mk` emits alloc + `protect_return_value` inc → rc=2; consumer decs once → rc never reaches 0 → env never freed → its drop glue never runs, which is why the TARGET closure leaked too). **Isolating it exposed THREE MORE LIVE LEAKS of the same mechanism with no curry involved**: `Lambda`/`StringLit` were recognised only by an ad-hoc `matches!` at the one call site (fresh at depth 0, NOT through a `let`) and `VecLit` nowhere — measured D 301/101→301/301, E 201/101→201/201, F 301/1→301/301. Fix: `is_fresh_construction` now covers every box-minting kind (carrier-keyed on `ResolvedCall::AutoCurry`, never callee shape), forwards through Let/If/Match, and the call site's duplicate list is DELETED (P7). Mechanism (b) confirmed exactly as /review predicted (bare `emit_rc_dec(..,None)` bypassing the embedded DROP_GLUE_PTR; visible only after (a) fixed: C2 301/201) → new `CaptureRelease::{Plain,ClosureBox}` shared by both capture-glue mirrors. Bar met: exact balance in ALL escaping shapes, both toggles, `--run` AND `--link`. **0753 was a REAL leak, not an artifact** — reduced to no-loop/no-TCO/no-`vec-set` (`(Pure (peek (Gr [5 5])))` ON 3/2, OFF 3/3); caller-side: `Borrowed` param → `emit_post_call_decs` bare-dec frees the box and strands its field → cured by ONE type-directed `emit_typed_rc_dec` (Vec→vec_drop+elem-dec, ADT→recursive glue, Fn→embedded glue), open-coded twin deleted; 0720 now EXACT both toggles at every N (5/5, 403/403, 803/803). 0751 polarity fixed + pinned in BOTH build modes (`#[cfg(not(debug_assertions))]` release cell). 0752: ONE carrier-keyed `cow_site_source` for all three consumers (+ the third expression folded); **the existing fixtures were themselves wrong** (built COW sites with `resolved_call: None` = asserting the spelling behaviour) — corrected; CLAUDE.md rewritten. 0750 backend half: exhaustive `result_mode_param_index` (a new variant is now a COMPILE ERROR, not a census escape). **Instrumentation answers (METHOD §2.2, first application): 0749 = (c) NONE — `is_fresh_construction` made exhaustive over `MonoExpr` in-crate so a minting kind can never again be swept into a catch-all; 0750/0751/0752 = (b) BLIND with corrections riding the fixes; 0753 = (b) BLIND and THE IMPORTANT ONE — `SafetyMatrix`'s RC face asserts `imbalance(ON) == imbalance(OFF)`, and every leak in this batch except 0753 is TOGGLE-INDEPENDENT, so the lane passed on both sides while five real leaks shipped.** → FIXME 0761 (/qa): an EXACT-balance lane (`allocs == deallocs` absolutely) over {owning type} × {position} — the two axes this wave proved independent; §2.3 states that bar in prose and nothing enforced it. **Goldens: 3 frames drifted (not 2), all the same certified shape (release work added inside an existing rc==0 branch; no dec/inc/fence removed) — including `f4_sudoku`, meaning the EXEMPLAR'S HOT PATH was leaking a Vec's elements+buffer per `eliminate-from-peers` call: plausibly a real slice of the ~11.8k-objects/solve residue 0720 was chasing — /port re-measure at Phase 6.** FIXMEs 0749–0753 resolved+deleted; filed 0760 (/design backend — last stranding site: capture glue still bare-decs Vec-of-heap/ADT-with-heap-field captures, K 401/201, L 301/201; needs a ruling that also retires the `MAX_DROP_GLUE_DEPTH=4` truncation whose own comment admits "fields leak"), 0761 (/qa), 0762 (/arch — 0750's cross-crate half, `ProjectionOf`'s raw `args[k]`), 0763 (/testing — e2e pins owed, spec'd with exact numbers; boundary respected). public-api UNCHANGED (all new items `pub(crate)`); 0 warnings; backend 477/477; clippy pre-existing only. **Baseline-arithmetic correction from /dev: the quoted 5238 triple was off by one — nextest's "N run" EXCLUDES the skipped test (5209+28=5237); my prior reconciliations inherited the slip.** New flap-family member: `agent::y_short_flag_errors_on_non_agent_build` (passes in isolation) — add to 0694's roster.
- 2026-07-21: **/sprint PROCESS INCIDENT (self-disclosed): my `git add -A` for the METHOD commit swept a mid-flight snapshot of the running W3b agent's backend source.** Root cause: committing while a source-touching agent is active. Reconciled by `git reset --soft HEAD~1` + re-split into `91a9080c` (METHOD alone) + the W3b change-set commit below — no content lost, no history rewritten beyond the un-pushed HEAD. **Standing correction: /sprint commits ONLY between waves, never while a source-touching dispatch is in flight; and scope `git add` to the paths the commit describes.** Candidate METHOD §2.x note at Phase 7.
- 2026-07-21: **USER (methodology): "any insights for instrumentation/assertions arising from isolating these defects? we should probably ask that question as a matter of course."** → **METHOD §2.2 amended: "The instrumentation question" is now BINDING on every defect isolation** — each report answers "what standing instrument would have caught this at its seam, and does it exist now?" with (a) exists-and-fired / (b) exists-but-BLIND + why / (c) none-exists + the named instrument, routed (register → /arch §4; coverage → /qa). "The suite caught it" is explicitly NOT an answer (a test is an example; an instrument is a standing mechanism). Recurring (c) = an architectural finding, not a testing gap. **S115 harvest (6 defects → 6 answer-classes, scribed into METHOD as priors)**: (1) **0749** — a balance/no-leak claim is SHAPE-LIMITED until parameterized by the **escape/reaching-context axis** (the sprint's recurring discriminator: MC-X1 reaching contexts, MS-P7 chained faces, 0705 escape — same axis three times); (2) **0604** — existence ≠ discrimination: no gate counts as verified without a **synthesized trigger that fails on revert** (the landed predicate was provider-existence and structurally blind for 3 sprints); (3) **0751** — a `debug_assert` fence's **release-mode fallback polarity IS the shipped behaviour** and must be explicitly chosen + pinned (leak-safe vs UAF-safe); (4) **0750** — a validation census over a **variant family** must be compile-enforced (exhaustive match), not prose, or it guards the safe variant and misses the dangerous one (coverage-by-variants class, broken inside the change-set stating the rule); (5) **0752/0693** — a name-vs-carrier decision wants **derive-and-fence** (producer records its verdict; consumer asserts agreement) rather than a second derivation — /dev's 0693 upgrade is the model, now generalized; (6) **0745** — a value produced at a **process/session boundary has no owner unless one is named**; process exit HIDES what a session accumulates (hence zero rc-dec sites in `src/` survived every prior sweep). **Routing: register-row candidates → /arch (W4 window, with the public-api sign-off); matrix rows → /qa at W7. All remaining dispatch briefs (W4/W5/W6 + reviews) now carry the question explicitly.**
- 2026-07-21: **W3 REVIEW: APPROVE-WITH-REQUIRED-FIXES (1 Blocker, 4 Important, 1 Suggestion), committed `1b122451`.** Core claims verified: item-26 join rule CORRECT both directions (adversarially probed; complementary to 0726 — the two predicates never both relax on one shape) though materially WIDER than reported (now fires at every `protect_return_value` site, incl. match-arm/let bodies — audited sound); `tco_owned_params` no double-inc, `is_borrowed` not-cleared confirmed in code, 0695 exemption undamaged (reuse_hit=200 ON), no UAF polarity either toggle; golden certification CONFIRMED instruction-by-instruction; R4 P7 forward correct + fence would catch divergence; R6 arms diagnose (never assert) + honest scope note ACCURATE. **BLOCKER 0749: the 0705 curry arm leaks unboundedly when the curried value ESCAPES its defining frame** — measured `(defn mk [] (let [g …] (g 1)))` + `((mk) 2)` = allocs 201/deallocs **1** (301/1 with a String capture); two controls isolate the new arm (plain returned lambda 201/201; global-target curry 101/101); `--run`≡`--link`. Not a regression (shape previously hard-errored) but violates P22 + the wave's balance bar; the `allocs==deallocs` claim was shape-limited. Candidate mechanisms named (curry env never dec'd; `AlwaysHeap` capture lowering bypasses the closure's embedded DROP_GLUE_PTR). **0750**: R6 row 3 validates the SAFE variant — the genuine panic-on-disk-content is the omitted `ProjectionOf` arm's raw index at `transfer.rs:731` (the R6 maintenance rule broken inside the change-set that states it; coverage-by-variants class). **0751**: release-mode disagreement fallback is NOT leak-safe (SYNTHETIC span shared, site B unrecorded → `recorded=true` → dec with no producer inc = UAF); the sibling ambiguity arm already takes `false` — one-line fix. **0752**: priority 2(c) answered NO — `arg_is_inplace_cow_on` + `return_cow_source_in_scope` still `matches!("vec-set"|"vec-push")`; survivor 2 FEEDS the consolidated predicate, so the user-fn-spelling channel persists one level upstream (backend CLAUDE.md overstates consolidation). **0753**: toggle-ON constant residual 1 at every N (toggle-OFF exact) — scaling defect genuinely fixed; residual is a toggle-divergence disposition item. Non-deliveries INDEPENDENTLY CONFIRMED: 0747 refusal correct (witness re-derived; a design contradiction /dev must not settle unilaterally — /design should first grep whether the drifting shape appears in ANY golden frame, which may decide the fork for free); 0745 attribution sound (probed a third "reorder" option — converges on the same owner; backend cannot cure without freeing the result before it is observed). /review's "route 0745 triage NOW" recommendation was already satisfied in parallel by the /qa batch. In-wave fix batch W3b dispatched (0749 first).
- 2026-07-21: **/qa W3 disposition batch DONE, committed `4d20cea1` (plan §8 = durable record).** **0745 ATTRIBUTED — falsification claims VERIFIED against source; owner moves to /design(int) → /dev(src) with a REQUIRED /arch mechanism consult; class `rc-miscount`; scheduled S116.** The finding is bigger than the carry: **NOBODY RELEASES THE PROGRAM RESULT VALUE, IN ANY MODE — verified by absence** (`src/` has zero rc-dec/value-release call sites; `io.rs:236` returns Pure's field0 without an inc; `drop.rs:303` deliberately no-ops). `--run` face is invisible except through M3/the oracle lane; **the REPL face is a real accumulating leak**. Only int knows the result type ⇒ int's call. Constraints: release strictly after consumption (0745's citation CORRECTED — the UAF is on the LIVE REPL path `pipeline.rs:149`→display, not the documented-unreachable `format.rs:598` — stronger conclusion); no type-erased release exists (`HeapHeader` = {alloc_size, rc}, no glue ptr) ⇒ the glue-lookup-vs-scoped choice is /arch's; the inc/dec-symmetry option is accounting-neutral and NOT the fix. **This RED does NOT flip in S115** — enters certification as an attributed carry with a NEW owner; §1.4's sweep row re-scoped to the two flipped 0720 faces. **0705 RETIRED+deleted** (`'='` sibling separately tracked by its committed RED — no FIXME needed). **0741 RATIFIED** + re-targeted /testing (re-shape: assert fields absent BY NAME + numeral-free tripwire name — kills the recurring bump cycle). **0746 RULED**: §4.1 prong-2 lifecycle case, re-plant must be SYNTHETIC (planting on the entry-payload leak = the anti-pattern a third time, REJECTED); needs a W7 slot + small /dev(intrinsics) hook, or the §4.1 tombstone fallback; **must not reach certification RED**. **0604 does NOT retire — waits on 0740** (census closure is acceptance item 4; mechanical at W6 close on two named checks). Matrix: O1 DELIVERED (R7 now asserted-and-discriminating), O2 DELIVERED (**/arch sign-off owed on public-api +15**), **O3 exits OWED-with-owner (cannot close as VERIFIED — 0748 gates it)**, O4 pending W7 + a new caveat (v1's generation space must exclude program-result-heap shapes or pre-register that face, else the first run reads as noise).
- 2026-07-21: **W3 dev DONE, committed `4ea5c758` — suite 5238/5209/28 stable + {0694 nullary} flap/1 (4 confirming runs: 29/29/28/29, the 28-name set IDENTICAL every run; zero unexplained delta).** (1) **0693 CONSOLIDATED + upgraded to DERIVE-and-fence** (consumer records its emitted retain decision span-keyed; shared predicate demoted to `debug_assert_eq!` disagreement fence; identity from `ResolvedCall::BuiltinFn` carrier, not the name; 7 cells incl. the latent-UAF row) — **W4 UNBLOCKED**; FIXME deleted. (2) RC sweep: **both 0720 faces FLIPPED**; the design's named candidate was FALSIFIED by probe (`is_heap_type(Gr)`=true) — true causes: toggle-OFF = `protect_return_value` over-inc on fresh-construction return in a NON-main fn (cured by the item-26 generalisation = 0696's ruling direction (b), FIXME deleted); toggle-ON = `Borrowed`-inferred param overwritten by the TCO back-edge (cured by `tco_owned_params` entry-block inc; `is_borrowed` NOT cleared so last-use transfer + in-place COW stay refused). Measured non-scaling (N=200 403/402, N=400 803/802); bare-vec twin + conj/assoc arms untouched. (3) **0705 FLIPPED** (totality enum, no `_ =>`, ViaCallee+Global = located producer-contradiction). (4) **R6 DELIVERED** (4 arms + 4 distinct `CacheStale` classes + census rustdoc with honest scope note; 6 cells incl. false-fire fence). **R4 BLOCKED with a finding → FIXME 0748 (/arch): `got_data_symbol_name` is DUPLICATED and the TYPES copy is the definer's** (backend-only change broke 40+ cross-module calls); backend body now a one-line forward (P7) + corpus-equality fence; injectivity fix belongs at the types home. (5) **W-B5 NOT executed — acceptance provably self-contradictory** (witness measured: `return_var_in_scope` Var-only vs `operand_live_binding_root` traces through let/match — collapse cannot be byte-identical-off) → FIXME 0747 (/design). **0745 (/design, cross-crate): entry-payload flip NOT delivered — both §2.1 mechanisms falsified; true owner = the driver of the program result value; needs /qa or /arch placement.** 0746 (/testing): `m3_parity_catches_planted_leak` RED is NOT a regression — its own FLIP-HAZARD note predicted it verbatim (its plant IS the over-inc that was fixed); re-plant owed. ONE golden frame drifted, certified line-by-line (3 removed instructions = the one protective inc; no dec/call/branch lost). public-api +15 lines (R6 CacheStale variants only) — **/arch sign-off owed**. 0 warnings; clippy parity; backend 460/460.
- 2026-07-21: **W2 REVIEW: APPROVE-WITH-REQUIRED-FIXES (0 Blockers), committed `c9d1585e`.** Self-alias arm **adversarially CLEARED**: both phantom mechanisms produce cross-module edges by construction (prelude has no prelude-fallback bit, so `X → prelude/X` into prelude cannot arise; a mis-targeted import carries a foreign source) — the arm is also NECESSARY (bootstrap + live `deftype` mint legitimate bare ctor self-aliases) and its no-verify tradeoff is correct (verifying = the exact deadlock the design avoids). Deadlock discipline HOLDS in code at all three routed seams; predicate ALIGNED with §8.4/§8.6.4; trigger DISCRIMINATES (RED on revert); 0707 correct; recurring-class check CLEAN (gate verified LIVE in production, not a no-op — `shared_state: Some(..)` at 4 call sites). **Important — FIXME 0740 (/design int): the census's CLOSURE CLAIM is materially false** — `src/bootstrap.rs:446` (cross-module PUBLIC Import edges into the live `macros` table — the exact phantom shape) + `src/platform.rs:407` (public own-def) are neither routed nor legal-skipped; both benign (unknown-D permit / own-def arm) so gate soundness is intact, but this is the S114 census lesson recurring — disposition required. **Scheduled: rides W6 (int window)** — /design(int) census disposition + /dev if any routing follows. 0741 (/qa, Suggestion): field-guard ratify + stale `_at_target_14` name. **Wave gate W3: PASS** (0740→/design int, 0741→/qa; neither targets a W3 skill). W3 dispatched.
- 2026-07-21: **W2 dev DONE, committed `d9f2caea` — suite 5208/5177/30 stable + 1 named flap/1 (exact: +10 GREEN units; the 30 unchanged; flap = 0694 nullary, out of scalar).** 0604 gate landed per design: `commit_staging_to_live` ROUTED (D(M) precomputed before the `get_mut` guard); predicate = own-def Ok-no-read / intra-module self-alias Ok-no-read (arm ADDED mid-wave — legitimate `ZedC → prelude/Zed.ZedC` self-aliases false-fired 6 tests; phantom is always cross-module — /review verify this arm cannot mask) / cross-module public re-export iff `name ∈ D(M)` / unknown-D permit; `SharedState.declared_exports` (int-internal, unserialized); MODULE_TRACE + R7-breach `Span::SYNTHETIC` diagnosed error; falsified comment fixed. **Synthesized trigger RED-on-revert VERIFIED** (3 reject-guards flip on predicate revert; provides-name-outside-declared-exports shape). Sweep 0/30; load-amplified attempt 0/496 (60s) — abandoned without prejudice. 0707 landed form-granular + 5 units, FIXME deleted. Boundary note: `tests/regression.rs` SharedState field-guard 16→17 (mechanical, designed-to-admit; /qa ratify). Zero new warnings; band 0740–0744 unused; 0604 FIXME appended NOT retired (/qa's call at W7). /review(src) dispatched.
- 2026-07-21: **W1 DELIVERED, committed `77ea2b82`** — suite **5198 run / 5168 passed / 30 failed / 1 skipped**; arithmetic EXACT: 11 stable carries + 19 intended new REDs (18× 0702 matrix incl. the `A.B → user/B` corruption face + qualified-type-param rider; 1× 0708 fold pin, flip trigger = S116), zero unattributed drift; 13+ born-green fences (`Maybe.Some` both positions, dotted import path, 0709 over-reach control `(size [x] Int)` must-stay-accepted). 0694 nullary flap manifested baseline-run-only (GREEN final) — reported per convention, outside the scalar. 0709 pins verified ALREADY declaration-time (no reshape; design polarity matched). 0719 + RC-sweep faces verified no-delta-owed (entry-payload toggle-ON pin rides W3 per plan). 0724 resolved+deleted (test kept). Band 0727–0739 unused. Findings: (1) /sprint addendum's "+2 REDs" parenthetical vs per-item labels — agent correctly authored +1 RED +1 GREEN (dispatcher arithmetic slip, no action); (2) dotted ctor cell uses uppercase-dotted `A.b` (lowercase masked by the uppercase gate — W5 review note: reject must fire independent of the case gate). **Wave gate W2: PASS** (0604+0707 are the wave's own work; 0718 targets W6 — scheduled in-sprint, noted). W2 dispatched.
- 2026-07-21: **/arch 0708 contract DELIVERED** (`design/arch/annotated-sexp-node.md`; pointer appended to 0708, file kept — retires with S116). Node = `Sexp::Annotated { annotation: Box<Sexp>, subject: Box<Sexp>, span }` (named fields; annotation half = raw colon-stripped Sexp, NEVER a TypeExpr — reader stays syntactic; RA-N5 type-validation stays at the AST builder, today's division one position earlier). Fold universal incl. quote (`'(:Int 5)` → quoted node; `~` in either half OK; `~@` as a half = located error; nesting = chained nodes; trailing introducer keeps today's exact reader-error wording; spaced `: Int x` parity). **Schema verdict: BUMP required S116** (Sexp cache-carried ×3: macro_sexp/ImplSexp/inline_body; ONE window, marshal tag 7 appends; public-api delta = types only). Census ~24 sites/5 surfaces; macro-observable form = the language-level `macros/Sexp` ADT gains ctor 8 `(SexpAnnotated [:Sexp :Sexp])` (dual seed bootstrap.rs:513 + builtins.rs:498); **splice-only macros incl. stdlib `def` need ZERO change**. Two extra mirrors found for retirement (`worker.rs::leading_annotation_len`; save.rs colon-suppression renderer). **Typecheck untouched CONFIRMED** (zero Sexp consumption in inference; consumes `Expr::Annotate` as today; sole edit = fixture ctor row). S20/S21 pins verified safe. S116 sketch = S114 dormant→flip template (W0 types → W1 frontend → W2 int/marshal → W3 FLIP + goldens → W4 matrix + /spec cascade rows listed in §8 + /stdlib 0780). FIXME 0780 filed (stdlib unwrap helpers).
- 2026-07-21: **USER RULING — 0708 SETTLED (Reading A-structural)**: `:Type <form>` parses to a NEW structural `Annotated` node at READ time — one fold rule, visible in the Sexp tree, macros receive the folded node (arity counts match what the user wrote); no metadata side-channel (silent annotation loss disqualified — annotations ASSERT). Typechecker impact assessed ≈none (it already consumes the structural AST `annotate_expr`; the pairing merely moves earlier; AST-builder scan-and-pair retires — one mirror deleted). Tax: frontend reader/printer + int expansion/qualification/quasiquote walk arms (exhaustive-match enforced) + likely serde/CACHE_SCHEMA bump (macro bodies persist as Sexp) — /arch contract dispatch NOW (design-only; band 0780–0784). **Disposition per the scope's pre-agreed conditional: behavior pin ships in-sprint (W1 rider: `(def x :Int 5)` → succeeds, RED) + /arch contract + /spec scribe; the multi-crate IMPLEMENTATION carries to S116** (attribution = frontend+int+types+schema, outside the stabilisation sprint) — user may override.
- 2026-07-21: **/design(typecheck) Phase 3 DELIVERED — PHASE 3 COMPLETE** (all 6 authority/design deliverables in). MS-P7 = §16.2 rule-table corrections at FAMILY grain (`ownership-inference.md` §17; root cause `transfer.rs:703-735` — escape-force reaches only the immediate syntactic `args[k]`; fix = Origin carries cow-alloc span-set, row 6 forces escape at every carried span; explicit no-5th-arm argument; internal-Origin only — no types/schema change; §17.6 precise carrier-enrichment tripwire NOT expected to fire); 0719 = window-3 EXTENSION not a 4th window (`monomorphisation.md` §11.8.11, derive-from-settled-subst, §11.3.2 precedent); 0709 = DECLARATION-time reject (`traits.md` §2 — occurrence rule is structural/P18; enforce-at-use applies to well-formed methods; `(zed [] self)` = GREEN boundary); **0721 RESOLVED+deleted** (typecheck.md §§2-5 rewritten vs as-built per R-2 criteria; both 0590-phantom rows corrected); program-decomposition §3 currency note added for 0722 (stale anchors; possible support/tests.rs sixth home). 0693 fence = cross-wave constraint (backend consolidation BEFORE the escape-fact correction). Phase-3 exit gate: public-API + interface set complete — ZERO types/schema/public-API changes across all four designs (consistent with the Phase-2 assessment; sole bounded contingency §17.6). **Phase 4 waves FIRMED (W1–W7 above); Status → PHASE 5 ACTIVE; baseline commit + W1 dispatch.**
- 2026-07-21: **/design(int) Phase 3 DELIVERED**. **0604 wave design complete** (`prelude-table-write-isolation.md` §2.2 correction LANDED in-phase): live commit path is `worker::process_cluster_once`→`commit_staging_to_live` (the `insert_cluster` per-entry loop is a normally-EMPTY Wave-3a-β scaffold) → ROUTE through `check_terminal_closure`; corrected predicate = destination-keyed **declared-export closure** (`name ∈ D(M)` from M's own `(export …)` specs; own-def arm = Ok with NO map read; unknown D(M) permits — never false-fires); deadlock hazard honored via a NEW int-internal session-side `SharedState.declared_exports` DashMap (unserialized, `prelude_fallback` model — **no types/schema/public-API impact**) + precompute-before-guard; verified `bit-and` genuinely outside prelude's declared exports (`stdlib/prelude.cl:52` curated list). **Impl-redefinition silent-ignore ROOT-CAUSED** (`design/int/impl-redefinition-hot-reload.md`): `derive_codegen_batch`'s TraitImpl arm enrolls the UNMANGLED name (dead lookup); mangled Defs reachable only via the `already_compiled`-gated sweep, which SKIPS them because AbiPreserving reuse carries prior code → new body never recompiles. Fix = enroll mangled method Defs into the FORCED loop (mirror of the multi-sig defn arm :971-989); same-type constraint inherited (trait-conformance check + AbiPreserving classification) — P11 no parallel path. 0707 sound + form-granularity caveat (subtract `failed_forms[module]`). 0699 RESOLVED+deleted (quote shield + defn self-name + defmacro shield ruled §2.4-2.6; doc banners refreshed); code fixes → **FIXME 0718** (/dev int). **Process fix adopted (numbering race, 2 collisions this phase): /sprint pre-allocates FIXME number bands in every dispatch prompt henceforth**; METHOD §3.3 amendment candidate at Phase 7.
- 2026-07-21: **/design(backend) Phase 3 DELIVERED** (`design/backend/s115-carrier-and-rc-sweep.md`; probe residue reverted, source clean). **GATING VERDICTS (carrier-state dumps at the wrapper-emission seam): 0705 = carrier CORRECT → backend consumer-totality fix (curry-the-local-closure arm; totality table over the closed sums, ViaCallee+Global = producer-contradiction located error). fn-as-value `'='` = carrier WRONG → TYPECHECK PRODUCER GAP; the pair does NOT collapse; the conditional /dev(typecheck) slot FIRES.** Producer locus named: `mono_collect.rs::resolve_auto_curry` fails to re-resolve trait_resolution against concrete instance types → transports the trait-method-DECL FQ (`prelude/=`, unslotted) as a dispatch carrier; decisive control: direct concrete `(= 3)` works (`Dispatch(primitives/eq-i64)`). Boundary rule: never transport a trait-method-decl FQ as a dispatch carrier. RC sweep designed (3 faces, toggle-independent discriminators; 0720 isolated decisively to `flush_superseded_heap_params_before_tail_jump` under-classifying single-ctor-product ADT loop params — variant-B evidence; hazards honored). R4 census: backend owed-witness = `got_data_symbol_name` (`.`→`_` flatten non-injective — constructible collision); LinkerSymbol/method-mangle `$`-join + platform names = cross-crate, routed. R6 seam = extend the ONE existing loop in `deserialise_meta_with_build_id`. W-B5 current (land RC sweep first). 0697 RESOLVED+deleted (+ /qa tripwire FIXME 0726); 0696 designed, rides W-B5 (/dev deletes). Note: 0725/0726 number collision avoided with /design(int).
- 2026-07-21: **/design(frontend) Phase 3 DELIVERED** (`binder-head-reject.md` §2.2 + revisions; `enforcement-matrices.md` §3.2; `frontend.md` §4.2): S113 premise FALSIFIED-and-retired (root cause: `reader.rs::read_dotted_name:762` joins a dotted run into ONE `Sexp::Symbol` that reaches every head slot). Widening design = ONE helper (`reject_qualified_binder_head`, ast_builder.rs:129) gains the `.` arm via ONE sibling `split_dotted_name` (consumed only inside the helper; `split_qualified_name` stays `/`-only — it serves REFERENCES, `Maybe.Some` stays legal); site census verified: ALL binder sites already route through the helper — zero per-site edits — EXCEPT the deftype type-param arm (`:714-739`, the one missing ROUTING, not a copy; insert at `:717` before the case gate = the qualified-type-param rider). The `A.B → user/B` corruption closes before ctor synthesis. 0711 FOLDED into the change-set (message → "a binder must be a bare (unqualified) name", no context param); 0710 = independent reader-seam one-liner (parity with `/bar` rich message); 0703 hard constraint honored (no added mirror; /review greps specified). No escalations, no public-API impact. Awaiting: /design typecheck, backend, src-int.
- 2026-07-20: **USER RULING — 0702 SETTLED (Ruling 1)**: dotted (`.`) binders REJECT in ALL binder positions, exactly like `/` — located error, span on the name; `.` is reserved for type/trait qualification only (reference positions like the `(Maybe.Some x)` ctor-pattern head stay legal). Rider: the qualified type-param cell's incidental `0..0`-span reject becomes a clean located binder-reject in the same work. Downstream unblocked: /spec scribes the §5 table `.` column + actions/deletes 0702 (next dispatch); /design(frontend) dispatched NOW (premise correction + widened predicate design); /testing lands the pre-authored reject-polarity M3 cells in W1; /dev(frontend) predicate widening joins the frontend wave. **0708 still OPEN** — user asked the macro-writer-impact question; /sprint supplied the three-shape analysis (A-structural: bounded author-side tax, one fold rule, no silent drop — recommended lean; A-metadata: disqualified by silent annotation loss (annotations ASSERT); B: per-macro pairing mirrors + user-side arity surprises); awaiting ruling. **O4 generative harness**: proceeding per /qa's recommendation (bounded /testing dispatch post-Track-A; slip to S116 requires explicit user sign-off).
- 2026-07-20: **/qa Phase 3 DELIVERED** (`tests/plan/s115-instrumentation-matrix.md` + `s115-test-plan.md`): matrix = **14 VERIFIED-IN-PLACE · 5 OWED (O1 R7/0604 wave; O2 R6 seam; O3 R4 census; O4 §2 generative harness — NOT BUILT, recommendation owed-bounded /testing dispatch post-Track-A with explicit-user-deferral fallback; O5 §1.3 doc amendment DONE) · 3 sanctioned parks (R13/R12/0637) · 1 registered-future (R3 P7)**. Hard rows: §1.3 oracle combinator IS an enforced nextest gate (8 cells; 3 as-built deviations recorded); RC_DEC_CHECK 4-file set = DESIGNED set not residue; §4.1 per-mode self-tests + e2e env-wiring fences all confirmed; MODULE_TRACE at 6 seams, `commit_staging_to_live` the one owed. **Binding wave finding: the existing 0604 chokepoint unit test CANNOT guard the corrected predicate** (injected source lacks the name — passes both predicates); the synthesized trigger must be provides-name-but-outside-declared-exports. Sanctioned suite run: **5164/5153/11/1 — 11 REDs map 1:1 to the carry set, zero drift**; named flaps {0694 nullary, agent-lane}, neither manifested. Examples 119→120 reconciliation ALREADY DISCHARGED (`tests/examples.rs:153`). 0694 disposition appended. User decision queued for Phase 4: O4 harness owed-vs-defer.
- 2026-07-20: **/spec Phase 3 DELIVERED**: 0714 scribed (spec/05-definitions.md §5.4.5 [S115] — re-`impl` replaces, dispatch uses new bodies, same-type constraint, silent-ignore = defect; §7.3 cross-ref; FIXME deleted) + 0704 fixed (§5.2.7 ctor-case sentence now matches §5.2.2 [S113] MUST; FIXME deleted). Two user framings appended to 0708 + 0702 (both OPEN — presented to user for ruling: 0708 = fold-in-macro-arg Reading A vs carve-out Reading B; 0702 = dotted-binder Ruling 1 reject-everywhere vs Ruling 2 park-unspecified; /spec consistency notes lean A-tension-noted and Ruling-1 respectively). Downstream gated: 0708 pin/fix; 0702 /design(frontend) premise + /testing M3 cells + /dev(frontend) predicate.
- 2026-07-20: **Phase 2 SIGN-OFF WITH REVISIONS (/arch; §Architecture review filled in place).** Revisions absorbed into scope: (1) GOT-slot pair = presumptively TWO fixes across the carrier contract (0705 backend consumer-totality; `'='` fn-as-value face presumptively typecheck producer gap) — /design(backend) produces per-repro carrier-state dumps at the wrapper-emission seam BEFORE the fix wave; Phase 4 holds a conditional /dev(typecheck) slot. (2) 0604 wave gains the `prelude_write_is_closure_valid` falsified-comment rider (verified: `bit-and` IS a bundled public primitive — both landed predicates blind to the phantom by construction). (3) R6 executes as /dev(backend, cache), census table in cache-submodule rustdoc, /review verifies completeness. Key rulings: MS-P7 needs NO pre-deployment /arch ruling (design-first binding: fix = §16.2 rule-table rows at family grain, never a 5th consumer arm; carrier-enrichment contingency pre-authorized via FIXME→/arch, schema 22→23 if needed); 0604 gate shape CONFIRMED vs R7 + S61→S93 precedent (diagnosed error self-identifying as R7 breach); impl-redefinition hot-reload = existing GOT-patch path (P11: same path as defn, no impl-specific parallel; land 0604 first — same src seams). Register re-audited in place: R1 §3b landed S113 (`3297adf8`); R7 → asserted-but-BLIND; R8 tier-5/tier-3 FULLY landed S113 (M1/M2/M3 + per-mode self-tests); R4/R6 SCHEDULED S115; R13 deferral recorded; R14 refreshed. No planned types/schema/public-API change; sole contingency = chained-face carrier enrichment. Advanced to Phase 3: /qa + /spec dispatched file-confined parallel (tests/plan/ vs spec/); /design ×3 follow.
- 2026-07-20: **Phase 1 APPROVED (user)** — scope stands as drafted; all four audit recommendations accepted; R13 stays parked; audit rotation = cranelisp-intrinsics. Disposal executed: 0590 residual verified CURED against source, file DELETED (audit-disposal exception); FIXMEs 0721–0724 filed (trail appended to `audits/cranelisp-typecheck-s114.md` §4); **METHOD amended** — §3.3 verify-against-source-first disposition rule + narrow Phase-1 audit-disposal deletion exception, §2.2 Phase-7 FIXME-vs-§Delivered close assertion. Advanced to Phase 2; /arch dispatched against the draft scope (standing register re-audit rides the dispatch).

## Outcome (Phase 7)

### Delivered
-

### Deferred (with rationale)
-

### Findings (record in FIXME's if not already)
-
