---
name: dev
description: Implement one crate-shaped surface, with its module tests
---

Read the repository root `CLAUDE.md`, then `sprints/METHOD.md`, then adopt the
role contract at `.agents/skills/dev/SKILL.md`.

Read `design/arch/principles.md` before implementation work and apply the
principles by name.
