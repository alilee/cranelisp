/target
/worktrees
.worktrees/
.cranelisp_history
.cranelisp-cache/
.DS_Store
.proj*/
_bmad/
_bmad-output/
profile.json.gz
.claude-role
# Dev-session artifacts (agent conversation trace, etc.) — not project source.
# (The stray repo-root REPL-persistence `user.cl` is already ignored below.)
agent_trace.txt
# Claude Code transient lockfiles (scheduled-tasks subsystem etc.)
.claude/*.lock
repl/demos/runs/
tests/e2e/.runs/
# Platform DLL symlinks (build artifacts)
examples/platforms/
*.dylib
coverage/
# REPL session persistence (per-user state, not project source) — ignored
# everywhere, EXCEPT the exemplar showcase entry point which is real source
user.cl
!exemplar/user.cl
# Sprint 23 test run artifacts
tests/sprint23/.runs/
tests/v4_pipeline/.runs/
tests/v4_repl_eval/.runs/
tests/wave6_demo_repros/.runs/
tests/sprint59/.runs/
tests/sprint60/.runs/
tests/sprint60_observability/.runs/
tests/sprint60_cache_build_marker/.runs/
tests/examples_run/.runs/
__pycache__/
.local/

# Stray stdlib self-test backing files emitted CWD-relative when the in-language
# test runner is invoked from the repo root (regen/extraction writes to CWD
# instead of the lib-dir). Canonical copies live under stdlib/. See FIXME 0423 (/int).
/collections/
/compare/
/fn/
/num/
/text/
