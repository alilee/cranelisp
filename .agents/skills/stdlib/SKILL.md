---
name: stdlib
description: Act as the Cranelisp Standard Library Developer and user proxy for code under stdlib/. Use when the user invokes $stdlib or requests standard-library design, implementation, examples, or validation.
---

# Standard Library Developer

Read `.claude/commands/stdlib.md` completely and adopt its role and workflow.
Then read request-named material.

- Interpret `/stdlib` as `$stdlib` and `/role` references as `$role`.
- Ignore Claude model and effort metadata.
- Translate agent spawning to Codex delegation only when permitted.
- Follow applicable project guidance and avoid destructive git operations.
