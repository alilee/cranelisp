# Sprint 94: Effect-Concurrency Slice 2 Completion — Real Effect-Node Await

**Status**: PHASE 6A ASSESSMENT

**Goal**: Finish the S93 reactor spill — route real poll-shape platform effect nodes through the `EffectPoll` await so production IO effects (not just the fixture leaf) suspend and overlap on the reactor — and drain the two carried near-term FIXMEs (0424 par-map stdlib, 0430 docstring regen).

## Scope

S93 landed the effect-concurrency slice-2 **spine**: a mio host-reactor + C-ABI waker + `block_on` executor + `EffectPoll` await boundary + strand sink, behind off-by-default `concurrency-runtime`. But the await is reachable **only by the fixture demo leaf** — `run_io_trampoline_inner_async` delegates straight to the synchronous stepper, so real platform IO effects do **not** suspend on the reactor yet (`design/int/reactor.md` §4, the as-built boundary note). The spine is proven; nothing real flows through it. **This is the load-bearing next step** and it gates every slice ≥ 3.

**1. Slice-2 completion — real effect-node await (the headline).** Wire real poll-shape platform effect *nodes* through the reactor. **Gated on R1: Phase 3 first ratifies the backend↔intrinsics poll-shape Effect-node seam** (node representation, state construction/arg-marshaling, result extraction) — joint /arch + /design backend + /design int — before any backend/intrinsics impl. Then the implementation:
   - `declare_platform!` emits poll-fns for async-capable leaves (`design/int/reactor.md` §5, §4 "Consequence");
   - a backend **GOT-indirect poll-dispatch arm** that passes `HostCtx`/`Waker` into the effect call — a **dual/gated path, byte-identical-when-off** (feature-off ⇒ today's v6 blocking thunk; R3);
   - the **real async `Effect` arm** in `run_io_trampoline_inner_async` (`io.rs:128`) — it grows a genuine `.await` body around the shared `TrampolineEnter`/`TrampolineExit` bookend (Principle 7 reuse) instead of delegating to the sync stepper;
   - a **real platform async leaf** demo (R2: a real `declare_platform!`-emitted leaf driven through the full macro→backend→loader path, **not** a fixture poll-fn; R6: an in-tree `DefKind::PlatformEffect`, no separate cdylib unless required), driven end-to-end through `cranelisp_run_io`.
   - **Acceptance**: two *real* platform IO effects overlap on one reactor thread through `cranelisp_run_io` (≈max not sum, no thread-per-read), strand trace visible; default build byte-identical-when-off; `--link` still links no executor. The S93 fixture-leaf overlap demo replays green as a regression guard.

**2. FIXME drain — the two carried actionables** (per `memory/feedback_close_fixmes_each_sprint`):
   - **0424** (par-map stdlib) — apply-arg sparking already ships and is general for the apply path (R5); the minimal remaining work is the **dependent-binding spark on the `let` path (limit #2)** — `/dev` (backend) sparks a dependent binding as an IVar and forces on demand via existing `ivar_force` (backend-only, no new runtime, no public-API impact). Then `/stdlib` layers `par-map`/`par-reduce`/`par-map-reduce` as ordinary `.cl` functions (no primitive — user ruling S93). Separate wave from the reactor.
   - **0430** (docstring-into-source regen) — `/design` ratifies the renderer-contract change (recommended candidate 1: docstring-aware `render_decl_sexp`); `/dev` (src/) re-lands the `set-doc` Document-mode write path so a docstring survives session restart.

### Out of scope (deferred — rationale + target)

- **Slice 3** (concurrency descriptor + token-cardinality `Semaphore` pool) — depends on real-node await existing (this sprint). Next slice once §1 lands.
- **Slices 4–8** (backpressure, supervisor, two-pool routing, cancellation/combinators, diagnostics) — sequenced later in the track.
- **Phase H openers** — 0050 (display protocol), 0052 (`/learn`), 0365 (`Type.member`).
- **0407** (host-callback Model B) — **reframed (R4): obviated as a concurrency mechanism by the reactor; narrowed to the synchronous-C-reentrancy escape hatch only (qsort comparator / GUI / signal handler); kept parked, not deleted.** /arch to update its reframing box.
- **0419** (shared HostCallbacks builder) — **kept open, decoupled from 0407 (R4)**, on its own divergence-proofing merit (DEF-6 hazard); not on S94 critical path, but /design int wires S94 host-construction so a later --link site shares one builder by construction.
- **0416** (bitwise intrinsics) — feature-gated, deferred.
- **0408** — **SPLIT (user correction, 2026-06-28):** the *parallel solver* (Sudoku search on the 0424/spark substrate) is **Phase 6 /port this sprint** — NOT blocked on `--release`; only the *absolute-perf showcase* (raw speed numbers) wants `--release` (Phase H). /port moves Sudoku to parallel search now, validating 0424 at scale. (expr-half shipped S92.)
- **0442** (unified CPU+IO budget abstraction) — a slice-4 backpressure design decision.
- **0447** (spec async-leaf/reactor user semantics) — spec'd at the slice it surfaces; slice 2/3 has minimal user surface.

## FIXME debt

| FIXME | Target skill | Status | Notes |
|---|---|---|---|
| 0457 | /arch | ✅ RESOLVED + DELETED | channel wired end-to-end; 5 reactor e2e rows green; App-B caveat → CLOSED |
| 0458 | /qa | open (Wave-3) | invert obsolete `lenient_vec_map_reduce_prior_binding_stays_serial` negative control — 0424 limit #2 now parallelizes the D&C shape |
| 0424 | /arch | ✅ RESOLVED + DELETED | substrate (apply-arg S92 + dependent-binding S94) + stdlib `par-*` shipped; 11/11 spark tests green; §7 → CLOSED |
| 0430 | /design (+ /dev src/) | deferred→S94 | docstring-into-source regen; candidate 1 = docstring-aware `render_decl_sexp` |
| 0050 | /int | DEFER (Phase H) | Display protocol |
| 0052 | /repl | DEFER (Phase H) | `/learn` tutorial |
| 0365 | /spec | DEFER (Phase H) | `Type.member` accessor qualification |
| 0407 | /arch | DEFER (narrowed, R4) | Reactor obviates the concurrency motivation; parked as synchronous-C-reentrancy escape hatch only |
| 0408 | /port | **Phase 6 (parallel solver) + DEFER perf-showcase** | parallel Sudoku search on the 0424 substrate ships this sprint; only raw-speed numbers wait on `--release` (Phase H) |
| 0416 | /arch | DEFER | Bitwise intrinsics — feature-gated |
| 0419 | /arch | open (R4, off critical path) | Decoupled from 0407; own divergence-proofing merit; /design int keeps S94 host-construction shareable |
| 0442 | /arch | DEFER (slice 4) | Unified CPU+IO budget — backpressure design |
| 0447 | /spec | **Phase 6 — re-evaluate (user, 2026-06-28)** | slice-2 real-node await surfaced reactor/auto-overlap behavior — /spec assesses what to spec NOW; defer only the combinator/supervisor half (slices 5/7). NOT a reflexive slip |

## Architecture review (Phase 2)

**Verdict: SIGN-OFF WITH REVISIONS** (`/arch`, 2026-06-27). Scope is coherent and correctly sequenced (real-node await gates slices ≥ 3; 0424/0430 are independent drains). Naming note: what S94 calls "slice-2 completion" pulls in work `reactor.md` §5 / App-B / `effect-concurrency.md` §13 label "slice 3 / deferred" (the `declare_platform!` poll-emission + backend dispatch arm) — relabel accepted, docs reconciled at land (R3). Six revisions folded into Scope (R1–R6):

- **R1 (blocking, Phase-3 gate):** The dormant ABI-v7 contract is **sufficient for the platform↔host seam but NOT for the backend↔intrinsics seam** — the runtime representation of a poll-shape Effect node is undefined: (a) state construction / arg-marshaling (poll takes only `(state,host,waker)`, so args must be in `state` before first poll), (b) result extraction (today's `ResultReader` is intrinsics-internal + fixture-specific), (c) `drop_state`/cancellation (no `drop_state` on `ConcurrentPlatformFn` — not needed S94 but coming). **Phase 3 must ratify this seam (node representation, state construction, result extraction) — joint /arch + /design backend + /design int — BEFORE any backend/intrinsics impl.** Decision rule: if any of (a)/(b)/(c) lands as a *platform-DLL* field, reserve it in the dormant `ConcurrentPlatformFn` **now** (same reserve-now discipline as `global_budget`) to avoid a 7→8 bump. If the closure-env model wins, (a)/(b) stay host-internal; only `drop_state` is a reserve candidate.
- **R2 (blocking, P8/P5):** The demo leaf MUST be a real `declare_platform!`-emitted async leaf driven through the full macro→backend→loader path — **not** a hand-written intrinsics fixture poll-fn (else the new code is unexercised scaffolding). S93 fixture-leaf overlap demo stays as substrate regression guard.
- **R3:** Backend is now a *touched* surface (App-B's "backend = NONE" was spine-only) with a **byte-identical-when-off** obligation (feature-off ⇒ today's v6 blocking thunk) — a dual/gated emission path. /arch reconciles App-B/§13/§6.8 at land; /design int refreshes `reactor.md` §4/§5 this sprint (same-skill same-sprint, no separate FIXME).
- **R4 (host-callback ruling):** /sprint read confirmed — reactor obviates 0407/0419 *as a concurrency mechanism* (continuation = trampoline's suspended state, not a host-held closure; the Model-B platform-owned-loop is exactly what A2 rejects). Split: **0407 → KEEP PARKED, narrowed** to the synchronous-C-reentrancy escape hatch only (qsort comparator / GUI / signal handler — nested sync re-entry, reactor-orthogonal); concurrent-server-loop motivation declared dead; do not delete. **0419 → KEEP OPEN, decoupled from 0407**, on its own divergence-proofing merit (DEF-6 heap corruption came from the two hand-mirrored host-construction sites; reactor freshly re-creates this hazard). Not on S94 critical path (only --run/REPL host-construction site active; --link concurrency is a later slice), but /design int wires S94 host construction so a later --link site can share one builder by construction.
- **R5 (0424 scope + sequencing):** Apply-arg sparking **already shipped and is general for the apply path** — the stdlib `par-map` Cons-recursion shape is buildable on the *current* substrate. The minimal remaining generalization is **limit #2 — dependent-binding sparks on the `let` path** (spark a dependent binding as an IVar, force on demand via existing `ivar_force`): **backend-only, no new runtime, no public-API impact**. Ordering: /dev backend lands the generalization (+ unit tests) → then /stdlib layers `par-map`/`par-reduce`/`par-map-reduce` as `.cl` defs. Separate wave from the reactor; /stdlib owns sprint placement.
- **R6 (Phase-3 check):** Confirm the real-leaf demo needs **no separate cdylib test-DLL fixture** — an in-tree async-capable `DefKind::PlatformEffect` satisfies R2 and keeps the wave self-contained. If a real cdylib is required, sequence the fixture first.

**Crates touched (reactor wave, single-agent-serial, gated on R1):** cranelisp-platform (`declare_platform!` poll-emission + manifest `ConcurrentPlatformFn`), cranelisp-backend (GOT-indirect poll-dispatch arm + arg-marshaling + result read, dual/gated byte-identical-off), cranelisp-intrinsics (real async Effect arm in `run_io_trampoline_inner_async` `io.rs:128`; generalize `EffectPoll`/result extraction beyond fixture), cranelisp-types (only if R1 adds cross-DLL fields), src/ (loader reads `ConcurrentPlatformFn`; host reactor construction; default + --link never enable `concurrency-runtime`). **Independent waves:** 0424 (backend → stdlib), 0430 (src/ only).

## Skill plans (Phase 3)

**Phase-3 structure.** Step 1 (serial gate): /arch ratifies the R1 poll-shape Effect-node seam (may touch `cranelisp-types`). Step 2 (markdown-only fan-out, after R1): /design backend, /design int, /qa. **/spec NOT invoked** — real-node await changes no observable language semantics (auto-IO-parallelism results provably identical to sequential; no new user construct); async-leaf user semantics stay deferred (0447). 

### /arch — R1 poll-shape Effect-node seam (the gate)

- **Task**: ratify the backend↔intrinsics poll-shape Effect-node seam — node representation, state construction/arg-marshaling, result extraction, drop_state/cancellation reserve decision; reserve any new platform-DLL field in dormant `ConcurrentPlatformFn` now. Update 0407 reframing box (R4).
- **Crate**: design/arch + cranelisp-types (+ cranelisp-platform interface types if needed)
- **Design refs**: SPRINT.md Phase 2 (R1), `design/int/reactor.md` §4/§5, `design/arch/effect-concurrency.md` §13/App-B, `design/arch/platform-interface.md` §6.8, `crates/cranelisp-platform/src/concurrency.rs`, `crates/cranelisp-types/src/scheduling.rs`, `crates/cranelisp-intrinsics/src/io.rs`
- **Acceptance**: ratified seam documented as the contract /design backend + /design int + /qa build against; public-API/interface set complete; `cargo check` clean if cranelisp-types edited.

### /arch — R1 ratified (done)

Closure-env model: new `IO_TAG_EFFECT_POLL` node → host-built state-closure in the existing heap-closure layout `[header | code_ptr=poll-fn | drop_glue_ptr | env = result-slot + i64 args + scratch]`. Backend-built state (no `make_state` export); GOT-indirect poll-fn load baked as `code_ptr`; `HostCtx`/`Waker` passed at poll time. Generic env-offset result read (no per-effect `ResultReader`). One reserved platform-DLL field `drop_state` on dormant `ConcurrentPlatformFn`, **no `ABI_VERSION` bump**. Blocking path untouched (byte-identical-off). Edited: `effect-concurrency.md` §13/App-B, `platform-interface.md` §6.8, `concurrency.rs` (reserved field), `0407` reframing box.

### /design (cranelisp-backend) — done

- **Poll-construction arm** (`design/backend/io-trampoline.md §12`): `IO_TAG_EFFECT_POLL=4` (32-byte node), construction is a GOT-slot **load** (factor shared `emit_got_slot_load` helper, P7) baking poll-fn as `code_ptr`; args marshaled as closure captures (reuse `compile_lambda` + `emit_capture_inc`); keyed on `ConcurrencyDescriptor.blocking==0`, **no cargo feature** (data-driven branch, P11); `drop_state` inert in S94.
- **0424 dependent-binding spark** (`design/backend/lenient-eval.md §2.6/§4.5`): admit a dependent binding iff every earlier var it references is itself sparked (single backward pass); thunks capture dependency **IVar pointers**, force via existing `cranelisp_ivar_force`; **backend-internal thunk-body force-prologue, NO new `MonoExpr` variant** (keeps R5's no-public-API-impact); one new RC rule (captured IVar `emit_capture_inc`'d, dec'd by thunk drop glue).
- **Unit-test seams**: byte-identical-off neg guard, poll-node CLIF shape, GOT load-not-call, arg-capture RC balance; 0424 sparkability admit/exclude, sequential-identity vs `CRANELISP_NO_LENIENT=1`, captured-IVar no-leak incl. panicking dependency, ferry first-error-wins.

### /design (src/ int) — done

- **Reactor refresh** (`design/int/reactor.md` §2.5/§2.7/§4/§5): await now reaches real `IO_TAG_EFFECT_POLL` through `cranelisp_run_io`; result read → generic env-offset; fixture leaf → real `declare_platform!` in-tree `DefKind::PlatformEffect` (R2/R6), fixture retained as guard; env-layout convention (result-slot first, then args in decl order, then scratch) is a v7 host↔platform contract.
- **0419 sharability** (`reactor.md §6`): key finding — reactor `HostCtx`/`Waker` is **already divergence-proof** (single-sited `make_host_ctx` in intrinsics, all modes via `cranelisp_run_io`); the live DEF-6 hazard is the platform-DLL `HostCallbacks` (mirrored `src/platform.rs:253` + `exe-bundle/src/lib.rs:131`). S94 int adds no parallel reactor builder; does not widen `HostCallbacks` for the reactor (seam passes `HostCtx`/`Waker` at poll time). 0419 stays narrowly the `HostCallbacks` consolidation, /arch-owned.
- **0430 renderer contract** (`design/int/session-persistence.md §11`, RATIFIED): `render_decl_sexp` gains `docstring: Option<&str>`; **live `Def.docstring` authoritative** (Some→emit+drop sexp docstring, no dup; None→emit sexp's own); stored sexp never mutated (P7). Unit seam: regen-reads-live-field, reconcile arm, round-trip. /dev re-land of `set-doc` + e2e is the remaining 0430 half (FIXME stays open until that lands).

### /qa — test plan done (`tests/plan/sprint-94.md`)

19 planned rows (9 e2e + 10 unit), grouped (a) feature-off byte-identical · (b) feature-on real-node await (suspend/resume + two-leaf overlap ≈max) · (c) result extraction · (d) --link no executor · (e) ABI guard (extend `concurrent_platform_fn_repr_c_field_order_v7` to pin `drop_state`) · (f) 0424 dependent-binding spark (sequential-identity floor + not-slower timing floor; inline, stdlib `par-*` not testable in `tests/`) · (g) 0430 docstring regen. Lanes: `nt`, `nt-concurrency`, `nt-concurrency-runtime`, `agent`, + proposed `reactor-e2e`. R6 confirmed (in-tree `PlatformEffect`, no cdylib).

### Phase-3 residual /arch confirmations — RULED (exit gate clear)

1. **`IO_TAG_EFFECT_POLL = 4`, homed in `cranelisp-platform`** (`IO_TAG_PAR=3` is current max; gated `concurrency`, off the default public-api edge). Landed dormant.
2. **0419 confirmed narrow** — reactor `HostCtx`/`Waker` is divergence-proof (single-sited `make_host_ctx`); 0419 = platform-DLL `HostCallbacks` consolidation only; reactor explicitly OUT. FIXME box added.
3. **`nt-reactor-e2e` lane ratified** — root `Cargo.toml` `concurrency-runtime = ["cranelisp-intrinsics/concurrency-runtime"]` passthrough + `nt-reactor-e2e = "nextest run -p cranelisp --features concurrency-runtime"` (additive; does NOT replace `nt`/`nt-concurrency`/`nt-concurrency-runtime`; default `nt` stays runtime-free). **Lands EARLY in the reactor wave** (else R2 degrades to unit-only). /sprint lands `.cargo/config.toml` in the wave.
4. **`render_decl_sexp` docstring-awareness is UNCONDITIONAL → default `nt` lane** (it's core persistence, not agent-gated; only the preamble helpers are `#[cfg(feature="agent")]`).

/arch edited: `cranelisp-platform/src/lib.rs` (IO_TAG_EFFECT_POLL), `effect-concurrency.md` (App-B constant pin), `0419` FIXME (scope box).

## Waves (Phase 4)

**Source-touching work runs serially** (worktree isolation broken — one editor at a time). Waves express dependency order; Waves 2/3/4 all touch source and execute **sequentially in calendar** even though logically independent. Priority order 2 → 3 → 4; spill order (if reactor runs long) sheds Wave 3 first (0424), then Wave 4. The R1 design gate is already cleared (seam ratified Phase 3).

### Wave 0 — Phase-5 setup (lane bootstrap) · unblocks the e2e tests' runnable home
| Skill | Surface | Task | Status |
|---|---|---|---|
| /dev | root `Cargo.toml` + `.cargo/config.toml` | `concurrency-runtime` passthrough feature + `nt-reactor-e2e` lane alias (arch-ratified shape) | ✅ done — single passthrough `concurrency-runtime = ["cranelisp-intrinsics/concurrency-runtime"]` (intrinsics feature is a superset); `nt-reactor-e2e` additive; default `nt` byte-identical; lane runs 1677/0/0 |

### Wave 1 — QA-first (Phase 5 Stage 1): all failing tests sprint-wide
| Skill | Surface | Task | Status |
|---|---|---|---|
| /qa | tests/ | Write all 19 rows per `tests/plan/sprint-94.md`, failing-not-ignored. | ✅ done — `tests/concurrency_reactor.rs` (a/d GREEN, b/c 3 RED `nt-reactor-e2e`), `tests/concurrency_spark.rs` (f, 3 GREEN floors), `tests/agent.rs` +2 (g, 2 RED). `nt` 1682/0/0 no regression. Ledger Stage-1 entry. **Carried to /dev:** (e) `drop_state` guard pin (in `crates/`, green-immediately); 10 per-crate unit rows (/dev-authored with each fix); (b)/(c) consts `async-demo`/`async-read` reconcile when the real leaf lands. |

### Wave 2 — Reactor real-node await (THE HEADLINE) · single coordinated D/D/R, source-serial
Internal order (one trampoline path, tested at the int seam): platform poll-emission → backend dispatch arm → intrinsics async Effect arm → int loader + host construction + **real in-tree `declare_platform!` leaf demo** (R2). Stage-2 /design is a light refine of the Phase-3 docs only if the impl surfaces a gap.
| Skill | Surface | Task | Status |
|---|---|---|---|
| /dev | cranelisp-platform | `declare_concurrent_platform!` (separate macro) emits poll-fns + `ConcurrentPlatformManifest` export; new in-tree `platforms/async-demo` `async-read` leaf | ✅ done |
| /dev | cranelisp-backend | `resolve_poll_effect_target`/`compile_poll_effect` poll-construction arm (`emit_got_slot_load` P7 helper; state-closure + drop glue; keyed `poll_shape`; byte-identical-off, no cfg) | ✅ done |
| /dev | cranelisp-intrinsics | Real async Effect arm (`IO_TAG_EFFECT_POLL`→`.await EffectPoll`; PAR→`join_all`); generic env-offset result read (dropped `ResultReader`); tag-4 drop glue; S93 fixtures reconciled | ✅ done |
| /dev | src/ | Probe `cranelisp_concurrent_manifest` first; cfg-split `poll_shape`; **routed `cranelisp_run_program`/`unwrap_io_inline` through `drive_io` (the load-bearing wiring — reactor was unreachable via sync stepper)**; binary feature wiring; no 2nd host-construction site (0419) | ✅ done |
| /review | (the change-set) | Review the reactor change-set — P6/P7/P8, unsafe-soundness (provenance/lost-wakeup, per S93 lessons), byte-identical-off | ✅ done — **ACCEPT-WITH-FIXES, no Blockers** |
| /dev | cranelisp-backend + concurrency.rs | Fix review Importants: 4 missing §12.7 backend codegen unit tests (the arm is only e2e-guarded today); SAFETY comment on poll-fn transmute; `drop_state` doc wording | ✅ done — `poll_codegen_tests.rs` (4 units, CLIF harness; sanity-checked tag 4→99 RED); SAFETY + doc fixed. Units land in `-p cranelisp-backend` lane (269/269; bare `nt` runs only the `cranelisp` pkg). `nt-reactor-e2e` 5/5 on this box; all lanes green |
| /design | design/backend | Fix io-trampoline.md §12 doc divergence (RC convention double-inc trap + keying mechanism says `blocking`, should be `poll_shape`) | ✅ done — §12.1/12.3/12.6/12.7 corrected to `poll_shape` keying + consuming RC convention (no double-inc); internally consistent with shipped code |

**✅ WAVE 2 COMPLETE — the reactor headline shipped, reviewed sound, all findings resolved.** Real poll-shape platform effect nodes flow through the reactor's `EffectPoll` await; two real leaves overlap ≈max-not-sum on one reactor thread through `cranelisp_run_io`; byte-identical-off; `--link` links no executor. **FIXME 0457 /dev+/design halves fully landed — ready for /arch to close (delete) at bookkeeping.**

**Wave 2 /review verdict: ACCEPT-WITH-FIXES (no Blockers).** Verified SOUND (read, not assumed): state-closure/node layout offsets match readers; generic env-offset result read; provenance (no S93 `&Reactor` shape; raw addrs from exposed-provenance heap model); waker lifetime/lost-wakeup (clone-before-drop + re-poll-every-turn); RC of args (consuming convention, balanced); tag-4 drop (free-once, no double-free/leak); byte-identical-off (data-keyed, no cfg); IO-forcing reroute (single-sited, all 3 modes). **Findings to resolve before close:** (Imp-1) backend codegen unit-test gap — `compile_poll_effect`/`build_poll_state_drop_glue`/`emit_got_slot_load` have zero backend-unit coverage; §12.7 mandates 5 seams, 4 absent; arm only reachable via `nt-reactor-e2e` → /dev. (Imp-2) `io-trampoline.md §12.3` RC guidance contradicts shipped code (would leak if followed) → /design. (Imp-3) `io-trampoline.md §12.1/3/6` keying says `blocking`, ratified+shipped is `poll_shape` → /design. (Sugg) `declare_concurrent_platform!` P7 mirror of `__declare_platform_body!` (~135 lines) — track convergence when v7 stabilizes; `drop_state` doc "appended" wording (inserted mid-struct); thin SAFETY comment on poll-fn transmute. **Env: disk was 100% full** (reclaimed 4G); re-run `nt-reactor-e2e` on this box at close to confirm green.

**Wave 2 /dev re-deploy COMPLETE (2026-06-28).** All 3 RED `nt-reactor-e2e` rows GREEN; two 60ms leaves overlap ~85ms (≈max, not 120ms sum). Lanes: `nt` 1682/0, `nt-concurrency` 333/0, `nt-concurrency-runtime` 173/0, `nt-reactor-e2e` 5/0. 4 unit tests added. Deviations (within the ratified contract): separate `declare_concurrent_platform!` macro (keeps v6 platforms byte-identical); routed int IO-forcing entries through `drive_io` (reactor reachability); demo leaf uses result-slot-as-scratch (avoids backend↔leaf scratch coupling). `cranelisp-platform/public-api.txt` +1 line (the new macro). **0457 /dev half fully landed** — ready for /arch to close after /review.

**Wave 2 BLOCKED on a Phase-2/3 design gap (Principle 8 stop).** /dev found the R1 seam ratified the poll-node *representation* (closure-env) but NOT the channel carrying `blocking==0` from a platform manifest → loader → backend: `DefKind::PlatformEffect` (cranelisp-types, /arch-owned) carries only `{scheduling_class, got_slot}` and `SchedulingClass` maps all classes to `blocking==1`; the host loader reads v6 `PlatformFn`, not `ConcurrentPlatformFn`. So the backend can't tell a leaf is poll-shape, the real in-tree leaf can't be built, and the 3 e2e rows stay RED. **Filed FIXME 0457 (/arch)** (renumbered from /dev's 0448 to avoid S93's deleted-0448 collision). /dev landed only the gap-independent (e) `drop_state` ABI-guard pin (`nt-concurrency` 330/330 green); deliberately withheld the intrinsics async arm (would break `EffectPoll`'s public API + S93 fixture guards, and without the channel would only be reached by hand-built unit nodes — R2's "unexercised scaffolding"). **Resolution: /arch ratifies + lands the `DefKind::PlatformEffect` descriptor field + manifest→loader channel (0457), then /dev re-deploys platform→backend→intrinsics→src/.**

**0457 /arch half LANDED (2026-06-28).** `DefKind::PlatformEffect` gains `poll_shape: bool` (`#[serde(default)]` ⇒ false = byte-identical v6; pre-S94 cache deserializes blocking; orthogonal axis beside `scheduling_class`; full `ConcurrencyDescriptor` deliberately kept off ungated `DefKind` to preserve the `_neg` frozen-edge guard). public-api +1 line; new back-compat guard; `nt` 1682/1682, `nt-concurrency` 331/331. **/dev channel wiring fully SPECIFIED in 0457** (separate `ConcurrentPlatformManifest` + `cranelisp_concurrent_manifest` export, dlsym-probe-first loader, gated `OwnedPlatformFnDescriptor.concurrency`, `nearest_scheduling_class`). FIXME stays open until /dev lands it. **Re-deploying /dev.**

### Wave 3 — 0424 dependent-binding spark + stdlib par-* · source-serial, independent
| Skill | Surface | Task | Status |
|---|---|---|---|
| /dev | cranelisp-backend | Dependent-binding spark (limit #2): admit dependent-on-sparked bindings as IVar; thunk force-prologue via `cranelisp_ivar_force`; RC rule for captured IVar | ✅ done — `sparkability.rs` admission carve-out + new `dependent_spark.rs` (`compile_dependent_thunk`, no new `MonoExpr`); IVar-aware drop glue (`cranelisp_ivar_dealloc`) frees ferried error String; 6 units; `-p cranelisp-backend` 273; result-identical across 3 regimes; no-leak incl. panicking dep. No public-api change. |
| /qa | tests/ | Invert obsolete negative control `lenient_vec_map_reduce_prior_binding_stays_serial` (pinned pre-S94 "stays serial"; D&C shape now parallelizes) — FIXME 0458 | ✅ done — inverted to positive result-identity floor `lenient_vec_map_reduce_prior_binding_result_identical_to_sequential` (§12.4.3); FIXME 0458 deleted; `nt` 1682/0 GREEN |
| /stdlib | stdlib/ | `par-map`/`par-reduce`/`par-map-reduce` as ordinary `.cl` functions over the sparking substrate (/stdlib owns sprint placement) | ✅ done — `collections.parallel` (D&C: independent `left`/`right` §2.1 sparks + dependent `joined` §2.6 IVar spark); 16 sequential-identity self-tests; import-on-demand (not prelude-promoted); `nt` 1682. 0424 left open w/ resolution note for wave-gate close |
| /review | (the change-set) | Review 0424 — sequential-identity floor, no-leak, ferry first-error-wins | ✅ done — **ACCEPT-WITH-FIXES, no Blockers** (RC exactly-once, ferry, admission all verified SOUND) |
| /stdlib | stdlib/ | I4: inline the combine into the `let` body (drop the inert `joined` spark that adds zero concurrency + starves the budget, P6) | ✅ done — all 3 D&C fns combine-in-body; left/right still spark (§2.1), inert spark gone (n=3→2); 16/16 self-tests; `nt` 1682. (Consequence: stdlib no longer uses limit #2 — it's now a general capability pinned by /qa's tests below) |
| /qa | tests/ | I1 three-regime equivalence (NO_LENIENT + SPARK_BUDGET=0) for the dependent shape; I2 dependent-panic ferry; I3 captured-IVar no-leak; + a genuine limit-#2-win partial-dependency acceptance test | ✅ done — 8 new guards in `concurrency_spark.rs` (3→11), all GREEN; limit-#2-win witnesses **~1.5× (102ms vs 155ms)**; I3 via `CRANELISP_RC_TRACE` alloc/free balance (+200× sustained no-abort). **Surfaced a pre-existing defect (below).** |

**✅ WAVE 3 COMPLETE — 0424 dependent-binding spark shipped, reviewed sound, all I1–I4 resolved.** Limit #2 pinned by dedicated tests (stdlib par-* combine-in-body don't use it); ~1.5× win witnessed; RC/ferry/no-leak guarded.

**USER DECISION (2026-06-28): fix the catch-leak NOW in S94 (server-workload relevance) + run Wave 4.** Added Wave 3b (leak fix) below.

**Pre-existing defect surfaced (pulled into S94 by user decision) — `catch-runtime-error` leaks one heap cell per caught `Err`** (linear, unbounded in a retry loop). Isolated by /qa as NOT spark/IVar/ferry-related — reproduces with no `let`/sparks/IVars (apply-arg/independent/dependent all leak exactly N). Likely `cranelisp-intrinsics` error-ferry or `cranelisp-backend` drop codegen for an unused `(Err m)` binding. **Pinned failing-not-ignored: `tests/spec_12_runtime.rs::catch_runtime_error_caught_leaks_one_heap_cell_per_catch_neg`** (61 alloc/41 free over 20 catches) + ledger entry. No FIXME (the failing test is the record+trigger per `memory/feedback_no_fixme_with_failing_test`). Owner /dev; **fix-vs-defer decision pending** (server-workload relevance — the effect-concurrency reference workload is web). Also noted: `project_config::scaffold_neg_not_created_on_bare_cwd_repl` is a load-saturation **flake** (passes isolated 0.022s; not a regression).

**Wave 3 /review verdict: ACCEPT-WITH-FIXES (no Blockers).** Verified SOUND: captured-IVar RC exactly-once on all paths (normal/panic/budget-declined); no double-free of the forced value (borrowed bind, single Phase-2 owner); ferried error String freed once + first-error-wins idempotent; admission rule topologically sound (single-source `find_free_vars`); no new unsafe, no public-api drift. **Findings:** I4 (stdlib combine is inert overhead — inline it; limit-#2 value undemonstrated/untested) → /stdlib + /qa; I1/I2/I3 (design-§9-mandated dependent-spark tests missing) → /qa. Suggestions S1 (IVar-dec mirror `dependent_spark.rs`↔`let_if.rs`), S2 (inner-fn skeleton now 3×), S3 (hoist `cranelisp_ivar_dealloc` declare) — deferred/tracked (non-blocking; flag a 4th site for extraction).

### Wave 3b — catch-runtime-error leak fix (pulled into scope by user decision)
| Skill | Surface | Task | Status |
|---|---|---|---|
| /dev | cranelisp-intrinsics | Root-cause + fix the per-caught-`Err` heap-cell leak; flip `catch_runtime_error_caught_leaks_one_heap_cell_per_catch_neg` GREEN | ✅ done — root cause was the **thunk closure** never consumed by `catch_runtime_error` (leaks Ok+Err paths; tests papered over with manual `dealloc`); fix = `consume_closure(thunk)` all paths (`call_continuation` fresh precedent). Unit test added; sustained 2000-catch 6001/6001 balanced. **Full `nt` 1691/0** |
| /review | (the change-set) | Review the leak fix — RC balance, no new leak/double-free, all catch shapes | ✅ done — **ACCEPT, zero findings.** Owned-by-move invariant verified on all 3 arg shapes; `consume_closure` last-ref-safe (shared closure only dec'd, never wrongly freed); no symmetric double-free; slot-based panic model ⇒ consume runs on Err path. Leak repro GREEN |

### Wave 4 — 0430 docstring-into-source regen · source-serial, independent (src/ only)
| Skill | Surface | Task | Status |
|---|---|---|---|
| /dev | src/ | Docstring-aware `render_decl_sexp` (`Option<&str>`, live-field-authoritative) + re-land Document-mode `set-doc` write path (S89-W3-removed) | ✅ done — `reconcile_docstring` (§11.3a authoritative-live, fresh sexp, stored sexp never mutated); `set-doc` re-landed in `agent/{pull,stub}.rs` (`#[cfg(agent)]`); 2 e2e GREEN; 9 renderer + 3 agent units; `nt` 1699, agent lane green. **0430 CLOSE-READY** (status note added; /design or /sprint deletes) |
| /review | (the change-set) | Review 0430 — round-trip, reconcile arm, no-dup; closes the FIXME | ✅ done — **ACCEPT.** Reconcile correct on all 4 arms incl. multi-sig; slot rule = parser byte-for-byte; stored sexp immutable (P7); round-trip stable; set-doc gated + refused by read-only allowlist + no-panic-on-miss. Suggestions S1/S2 (honesty) → small /dev fix |
| /dev | src/ | Fix S1 (`apply_docstring_edit` reports false success on a miss — return bool + "no such definition") + S2 (gate on `DefKind::UserFn` — non-UserFn docstrings don't persist, so refuse not silently-ephemeral) | ✅ done — `apply_docstring_edit` → `Result`; miss ⇒ "no such definition" + no regen; non-UserFn refused with clear message; 3 new guard tests + updated the gate test to UserFn; agent lane 62e2e+102lib green, `nt` 1699 |

**✅ WAVE 4 COMPLETE — 0430 shipped + reviewed (ACCEPT) + honesty refinements. set-doc is durable + honest.**

### Finding — "never slower than sequential" FLOOR VIOLATED for alloc/RC-heavy parallel workloads (pre-existing, newly quantified by /port)
Parallel Sudoku search is **~10× slower than serial** on debug backend (sys-dominated: ~21s sys; ~20s parallel vs ~1.9s serial). Repro ladder: penalty scales with **heap allocation + atomic-RC contention**, NOT compute (pure-compute parallelism holds the floor). Root: copy-per-guess of an 81-elem Vec of RC-managed `Cell` ADTs → allocator-lock + atomic-RC cache-line contention across workers. **The spark-budget create-gate bounds spark COUNT but not per-branch shared-resource contention.** Pre-existing (S92 substrate); S94 quantified it via the exemplar. **Strikes the central thesis floor** ("throughput is free; never slower than sequential") for the realistic alloc-heavy case incl. the server reference workload. Fix is architectural (non-copying structures / contention-aware gate / Phase-H non-atomic-RC + arena) — not a quick win. **RESOLVED (recorded, fix carried):** /qa pinned `tests/concurrency_spark.rs::alloc_rc_heavy_parallel_not_slower_than_sequential_floor` — **CPU-time** signal (wall-clock unusable on a 10-core box: contention hides behind parallelism; CPU shows the ~6.1× blowup load-independently), K=3 margin, **RED today** (failing-not-ignored, ledger). /arch ruled **(c) BOTH**: scope the floor claim now (unconditional for compute-bound; contention-bounded otherwise — `effect-concurrency.md` §3 + new §3.1) AND specify a contention-aware gate (backend cost-heuristic + create-gate, allocation/RC-density axis) as the in-track path back; **judgment: REINFORCES the concurrency→Phase-H sequencing** (non-atomic-RC cure is defined by which values cross threads — Principle 8, don't pull forward). Gate fix owned by backpressure slice 3/4 (static) + 0442 (dynamic) + Phase H (structural cure). Filed **FIXME 0459** (`target:/backend`, renumbered from /arch's 0448 to dodge S93's deleted-0448; /arch owes a 2-ref sweep of "0448"→"0459" in `effect-concurrency.md` + `design/arch/CLAUDE.md` at Phase 7).

### Expected gate at close (FINAL — floor guard demoted to deterministic form)
- **default `cargo nt`** = **1700 passed / 1 skipped** (the skipped = the `#[ignore]`'d CPU-floor benchmark). **Fully deterministic** (verified 3× idle, no flip). The floor finding's durable record is now: a GREEN deterministic correctness floor (`alloc_rc_heavy_parallel_result_equals_sequential` — parallel ≡ serial) + the on-demand `#[ignore]` benchmark (5.9× RED on an idle box) + ledger + `effect-concurrency.md §3.1`. (The CPU-ratio signal is scheduling-dependent — deterministic-RED-in-suite was infeasible; demote was correct.)
- **`cargo nt-reactor-e2e`** = **5 green reactor + 3 known-red** = the two-pool routing guards (slice-6) — deterministic feature-on failures, kept visible per user.
- `cargo nt-concurrency` 334/0 · `cargo nt-concurrency-runtime` 174/0 · agent lane green.
- A genuine regression is any RED **beyond** the 3 named two-pool guards (the only intentional REDs at close).

### Finding — two-pool routing gap (feature-on only; planned forward-slice)
`nt-reactor-e2e` (whole suite, `concurrency-runtime` ON) has 3 RED `spec_10_io` rows — blocking-effect auto-IO `Par` wall-clock witnesses. **Pass feature-off (production, default `nt` green); fail feature-on** because slice-2's reactor routes blocking effects through its single thread (`join_all`) instead of rayon — the deferred **two-pool routing** (roadmap slice 6). NOT a regression, NOT a 0457 defect. Recorded in `effect-concurrency.md` App-B. **USER DECISION (2026-06-28): keep as visible failing slice-6 guards** in the `nt-reactor-e2e` lane (failing-not-ignored, ledger-documented; flips green when two-pool routing lands). NOT narrowed/hidden — surface the symptom. Lane expected state = **5 green + 3 known-red (slice-6 two-pool)**. /qa records the classification in `tests/plan/ledger.md`.

## ✅ PHASE 5 COMPLETE — all three deliverables + the pulled-in leak fix shipped, reviewed, green.
- **Reactor real-node await** (headline): real poll-shape effect nodes flow through `EffectPoll`; 2 leaves overlap ≈max on one reactor thread; byte-identical-off; `--link` no executor.
- **0424** dependent-binding spark + stdlib `par-*` (~1.5× witnessed; limit-#2 pinned by dedicated tests).
- **0430** docstring regen + `set-doc` re-land (durable + honest).
- **catch-runtime-error leak** (pulled in by user): thunk-consume fix, unbounded-in-retry-loop leak closed.
- Suite: `nt` **1699/0**, `nt-concurrency` **334/0**, `nt-concurrency-runtime` **174/0**, `nt-reactor-e2e` **5/0**, agent lane green. Two `/review` rounds per concurrency wave; all Blocker+Important resolved.

## Phase 6 (user-facing) — reframed per user (2026-06-28)

| Skill | Task | Status |
|---|---|---|
| /qa | Classify the 3 feature-on blocking-Par tests as named slice-6 two-pool guards (ledger) | ✅ done — by-symptom ledger entry; `nt` 1699 green; `nt-reactor-e2e` 5-green-3-red (exactly the trio) |
| /qa (floor repro) | Pin /port's floor-violation as a guard | ✅ done — `alloc_rc_heavy_parallel_not_slower_than_sequential_floor` (CPU-time signal, K=3, RED today); ledger |
| /arch (floor ruling) | Rule on the §3.6.3 floor claim | ✅ done — (c) BOTH: scope claim (§3/§3.1) + contention-aware gate spec; reinforces concurrency→Phase-H sequencing; FIXME 0459 (/backend) |
| /spec | Re-evaluate 0447 (don't slip) — spec what slice-2 surfaced | ✅ done — judged NO new user-observable surface, BUT found + fixed a real gap: §10.12 "thread pool" → mechanism-neutral (slice 2 added the reactor mechanism); combinator/supervisor/token/backpressure kept deferred to slices 3/4/6; 0447 narrowed (stays open for later slices). Vindicates the user's push — reflexive defer would've missed the mechanism-neutrality fix |
| /port | Move Sudoku to a **parallel solver** on the 0424/spark substrate (validate par-* at scale; not blocked on --release) | ✅ done — solver guess-arm now `(par-map-reduce (fn [d] (solve …)) first-success Unsolvable digits)` (canonical stdlib, zero `spark`/`par` in source); **40/40 parallel ≡ serial correctness** (`CRANELISP_NO_LENIENT=1` equiv guard); same solution as baseline. **Surfaced a floor-violation finding (below).** 0408 parallel-solver half done; perf half carries (wants non-copying grid + release backend) |
| /stdlib | par-* Phase-6 assessment (shipped Wave 3; confirm + gaps) | pending |
| /stdlib | par-* Phase-6 assessment | ✅ satisfied by Wave 3 (par-* shipped + 16 self-tests + disposition left); no separate agent |
| /docs | Document par-* (+ set-doc if user-relevant) | ✅ done — new `user/guide/parallel-collections.md` (honest perf scoping: compute-bound floor holds, alloc-heavy can be slower — named the ~10× finding); **fixed currency over-promise in `getting-started.md`** ("never blows up… at worst as fast as serial" contradicted §3.1); set-doc judged out of user scope; env-knobs already cross-linked |
| /examples | par-* example currency (example 30) | ✅ done — example 30 current, runs (exit 56), free-standing; honesty-scoping edit (it implied a speedup; pure-compute is break-even per §3.1); correctly did NOT add a dependent-binding D&C demo (would teach the inert-spark anti-pattern I4 deleted) |

**✅ PHASE 6 COMPLETE.** User-facing surfaces assessed + actioned; honesty thread (don't over-promise par-* speedup) consistent across spec/docs/examples; prior demos replay green.
| /repl | set-doc demo + reactor env-knobs; replay prior demos green | ✅ done — **all 9 active + 26 archived demos replay green** (key regression guard); 1 pre-existing S86-era ring4i demo-rot flagged (NOT S94 regression); set-doc confirmed-covered-by-e2e (not duplicated); env-knobs §0.7 accurate; added `repl/spec.md §17.15.4` honesty contract; filed FIXME 0460 (/qa e2e coverage, gap-not-defect); no S94 defects |

## Notes

- 2026-06-27 — Phase 1 scope draft. S93 closed (1677/330/171 green; race gate structurally closed; reactor spine accepted, real-node wiring spilled). S94 proposed: complete slice 2 (real effect-node await) + drain 0424 + 0430.
- 2026-06-27 — **User approved scope (all three).** User raised: the new async trampoline likely obviates host-callback (0407/0419). /sprint read: obviates as concurrency mechanism; residual is synchronous-C-reentrancy escape hatch — routed to /arch for ruling. Status → PHASE 2 ARCH REVIEW.
- 2026-06-27 — **Phase 3 complete.** R1 seam ratified (closure-env model); /design backend + /design int + /qa plans landed (markdown-only fan-out); 4 residual /arch confirmations ruled (IO_TAG_EFFECT_POLL=4 in platform; 0419 narrow; nt-reactor-e2e lane; render_decl_sexp unconditional). Exit gate clear. **Phase 4 waves written** (Wave 0 lane bootstrap → Wave 1 QA-first → Wave 2 reactor headline → Wave 3 0424 → Wave 4 0430; Waves 2–4 source-serial, spill order 3→4). Awaiting user go to begin Phase 5 execution.
- 2026-06-27 — **Phase 2 complete: /arch SIGN-OFF WITH REVISIONS (R1–R6).** R1 (blocking): Phase-3 must ratify the backend↔intrinsics poll-shape Effect-node seam before impl; reserve new platform-DLL fields now. R2 (blocking): demo leaf must traverse the full declare_platform!→backend→loader path. R3: backend is a touched/byte-identical-off surface. R4: 0407 narrowed-parked, 0419 open-decoupled (host-callback ruling — user's instinct confirmed). R5: 0424 = backend dependent-binding spark → stdlib par-*. R6: in-tree PlatformEffect demo, no cdylib. Revisions folded into Scope + FIXME table. Awaiting user OK to advance to PHASE 3 DESIGN.

## Outcome (Phase 7)

**Pre-close gate:** default `cargo nt` **1700 passed / 1 skipped** (deterministic, verified 3× — the skipped is the demoted `#[ignore]` CPU-floor benchmark) · `nt-concurrency` **334/0** · `nt-concurrency-runtime` **174/0** · `nt-reactor-e2e` **5 green reactor + 3 known-red** (two-pool, deterministic feature-on) · agent lane green. The only intentional REDs are the 3 two-pool guards (slice-6; ledger-documented; flip green when two-pool routing lands). The floor finding is recorded via a green correctness floor + an on-demand benchmark + ledger + §3.1 (the CPU-ratio signal is scheduling-dependent, so a hard suite assert would be flaky — demoted). A genuine regression is any RED beyond the 3 two-pool guards.

### Delivered
- **Effect-concurrency Slice 2 completion — real effect-node await (the headline).** Real poll-shape platform effect nodes now suspend/resume on the host-owned reactor through `cranelisp_run_io`; two real `declare_concurrent_platform!`-emitted leaves overlap ≈max-not-sum on one reactor thread. Closure-env model (R1): poll-fn rides the existing heap-closure layout (RC + drop free), generic env-offset result read, reserved `drop_state` field (no ABI bump). The manifest→loader→backend `poll_shape` channel (FIXME 0457, mid-sprint design gap caught by /dev's Principle-8 stop) was ratified + landed end-to-end. Byte-identical-off; `--link` links no executor. Reviewed sound across 2 rounds (provenance, waker/lost-wakeup, RC, byte-identical-off all verified).
- **0424 — dependent-binding spark (limit #2) + stdlib `par-*`.** Backend admits a dependent-on-sparked `let` binding as an IVar forced on demand (no new `MonoExpr`, no public-API impact); `stdlib/collections/parallel.cl` provides `par-map`/`par-reduce`/`par-map-reduce` (combine-in-body after /review's I4 inert-spark fix). Limit-#2 win witnessed ~1.5×; pinned by dedicated tests (three-regime, dependent ferry, captured-IVar no-leak). FIXMEs 0424 + 0457 resolved + deleted.
- **0430 — docstring-into-source regen + `set-doc` re-land.** Docstring-aware `render_decl_sexp` (authoritative-live reconciliation, stored sexp never mutated); Document-mode `set-doc` re-landed, now durable across restart + honest (errors on miss, refuses non-`UserFn`). Reviewed ACCEPT. 0430 close-ready.
- **catch-runtime-error leak fix (pulled in by user — server relevance).** Root cause: the thunk closure was never consumed (leaked on BOTH Ok+Err paths; unit tests papered over it); fix = `consume_closure` all paths. Reviewed ACCEPT (owned-by-move invariant verified on all arg shapes). Unbounded-in-retry-loop leak closed.
- **Spec mechanism-neutrality fix (0447 re-eval, user-prompted).** Re-evaluating against the live reactor (not dormant contracts) surfaced that §10.12 named the concurrency mechanism as "thread pool" — factually incomplete once the reactor mechanism landed. /spec made §10.12.1/4/5 mechanism-neutral. No new user-observable surface this slice; combinator/supervisor/token/backpressure kept deferred to their slices.
- **/port parallel Sudoku solver** on the canonical `par-map-reduce` (40/40 parallel ≡ serial); **/docs** `parallel-collections` guide + currency over-promise fix; **/repl** prior demos replay green + §17.15.4 honesty contract; **/examples** example-30 currency + honesty scoping.

### Deferred (with rationale)
- **Two-pool routing** (blocking effects in a `Par` serialize through the single reactor thread when `concurrency-runtime` is on) → roadmap slice 6. Feature-on only; production (feature-off) unaffected. 3 named failing guards in `nt-reactor-e2e` (user: keep visible, don't narrow). 
- **"Never slower than sequential" floor scoped** (FIXME 0459 /backend): violated up to ~10× for alloc/RC-heavy parallel workloads (atomic-RC + allocator contention; create-gate bounds count not contention). /arch ruled (c): scope claim now + contention-aware gate as the in-track path; structural cure is Phase H (sequencing REINFORCED, not pulled forward). Recorded via a green correctness floor + an `#[ignore]` on-demand benchmark (CPU-ratio is scheduling-dependent → a hard suite assert would be flaky; durable record = ledger + §3.1). Fix owned by backpressure slice 3/4 + 0442 + Phase H.
- **Reactor real-effect-node wiring** is complete for the in-tree leaf; **0447 combinator/supervisor/cancellation half** → slices 4/6 (genuinely user-visible, spec'd with delivery).
- **Pre-existing**: ring4i archived demo-rot (S86-era) → future /repl demo-rot cleanup. FIXME 0460 (/qa set-doc honesty e2e coverage, gap-not-defect).
- **Phase-H openers** (0050/0052/0365), **0407/0416/0419** (host-callback narrowed/feature-gated), **0442** (slice-4 budget) — correctly parked.

### Findings (methodology / observations)
- **/dev's Principle-8 stop saved the headline.** The R1 seam ratified the poll-node *representation* but missed the manifest→backend `blocking` channel; /dev stopped rather than improvising cross-crate structure, /arch ratified the channel (0457), /dev re-deployed clean. The D/D/R cycle's mid-Phase-5 design iteration working as designed.
- **/review earns its keep on concurrency work again (S93 lesson holds).** Caught the inert-spark budget starvation (I4) and the docstring-tool honesty gaps (S1/S2) — neither a correctness defect in shipped seams, both real quality issues a green suite hid.
- **Rigorous measurement beats assumption — and surfaced a thesis-level finding.** /port's parallel Sudoku quantified that the central "never slower than sequential" floor is violated for the realistic alloc-heavy case (incl. the server reference workload). CPU-time, not wall-clock, was the only robust signal (wall-clock hides contention behind parallelism on a multi-core box).
- **The user's two corrections sharpened the sprint.** "Why slip 447?" → a real spec mechanism-neutrality fix the reflexive defer would have missed. "Port does the parallel solver, not --release" → the at-scale 0424 validation that surfaced the floor finding. Both improved the outcome.
- **Honesty thread.** The floor finding propagated consistently — /docs fixed a getting-started over-promise, /examples scoped its speedup claim, /spec scoped the thesis floor. No surface left promising unconditional par-* speedup.
- **FIXME numbering churn:** agents scan live files for max+1, but deletions drop the on-disk max below the historical max, so deleted numbers (0448) get reused. /sprint renumbered twice (0448→0457, 0448→0459). Methodology note for a future tweak: track a monotonic high-water mark, not on-disk max.

### Phase-7 /arch close items (pending)
- Candidate principle "confine mutable-state concurrency to the interpreter" (S93 arch R6, carried) — /arch to consider at close given the §3.1 atomic-RC-contention finding.
- 2-ref sweep "0448"→"0459" in `effect-concurrency.md` + `design/arch/CLAUDE.md` (the renumbered FIXME).
